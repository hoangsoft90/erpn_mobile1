# Handoff — 2026-09-15 02:00 (Phiên: result15 — LLM router real upstreams + dsh cơ chế thật)

> Đọc thêm: `result15.txt` (bằng chứng đầy đủ), `next.md`, `checklist.md`,
> `handoff_20260914-1023.md` (phiên trước), skill `.agents/skills/erpn-dsh-setup/`.

## 1. Mốc hiện tại

- **Commit đầu phiên `87fcbb1` đã push** (đã duyệt): security review fixes +
  LLM Router bản đơn giản + sign-off Phase 5 + LOCAL-TEST doc (result14).
- **Phase 5 tiến xa nhất từ trước tới nay** (result15):
  - Router `scripts/llm-router.mjs` nối **upstream thật**: endpoint chính thức
    zen `opencode.ai/zen/v1` · gemini `generativelanguage.googleapis.com/v1beta/openai`.
  - **Gemini verify generate thật 200** qua router ("250000 nhân 1000 bằng 250000000.").
  - **Zen bị chặn billing**: `CreditsError: No payment method` — glm-5.3-flash là
    PAID; big-pickle free nhưng chỉ chạy trong OpenCode client (MissingSessionID).
  - **2 bug router tự bắt khi chạy thật** (đã sửa + test): https upstream gọi bằng
    http.request → chọn module theo protocol; Gemini từ chối field OpenAI-only
    `store` → `stripFields` per-upstream. Thêm `LLM_ROUTER_DEBUG=1`.
  - **Cơ chế dsh thật** (đào source `dsh-base/cordis.patch.yml` — công thức
    settings.yaml của result6 ĐÃ LỖI THỜI): LLM config qua **cordis patch row
    override theo id** (`- id: llm-pi-ai` + `- id: agent-default-model`),
    `models` = **list object**. Mock qua router chạy thật 269.000đ.
    → skill mới `.agents/skills/erpn-dsh-setup/`.
  - **E2E dsh → router → Gemini flaky do free tier**: 1 session dsh tốn 2–3 LLM
    calls (session-title + agent), free tier = 20 req/phút → 429 quota + 503
    high demand là THIẾT KẾ free tier (nguyên văn result15 §6), không phải bug.
- Tests: router **7/7** · mcp-erpnext **49/49**. Audit JSONL không chép nội dung
  câu hỏi (có test bám).

## 2. Chưa commit — chờ user duyệt (vùng tiền/phân quyền)

```
M scripts/llm-router.mjs          (https transport + stripFields + LLM_ROUTER_DEBUG)
M scripts/llm-router.config.json  (endpoint thật + model + stripFields)
M result15/checklist/features/next/working/handoff/openspec tasks (docs)
```
Đề xuất: `feat: phase 5 — router real upstreams (gemini verified, zen
billing-blocked) + dsh cordis patch mechanism + debug logging`

## 3. Chờ user (mỗi việc 1 dòng)

1. **Duyệt commit** mục 2 (router fixes + docs).
2. **Upstream LLM**: Zen — nạp payment method hay bỏ upstream? Gemini — giữ free
   tier (20 req/phút, E2E flaky theo thiết kế) hay nâng paid?
3. Dán 3 giá trị GitHub Settings (Variables `COPILOT_BASE_URL`/`COPILOT_AUTH_USER`
   + Secret `COPILOT_AUTH_PASSWORD`) → CI build APK cài được (tunnel
   `erpn8788.loca.lt` đã verify E2E — result13; chỉ sống khi `lt` chạy trên Mac).
4. Chọn đường endpoint lâu dài (Tailscale / hosting mở port 8788 / tunnel).
5. Rotate key ERPNext (Hoàng, trên server) · thu audio 150 câu · test APK tại điểm bán.
6. Review `.project/ai-rules.md` (tạo phiên trước, chưa duyệt).

## 4. Lệnh verify cho phiên sau (chạy trước khi tin bất cứ ghi nhận nào)

```bash
git -C <repo> log --oneline -3 && git -C <repo> status --short
cd <repo> && timeout 60 node --test scripts/test/llm-router.test.mjs 2>&1 | tail -4
cd <repo>/mcp-erpnext && node --test 2>&1 | tail -4
# E2E dsh (công thức đã verify — skill erpn-dsh-setup):
#   nlp_service + llm-router cùng block, DSH_HOME MỚI SẠCH, patch row override
```

## 5. Bài học đã vào skill `erpn-verify-first` (result15, +8 mục)

settings.yaml-vs-patch-row (đọc source thay vì tin result cũ) · health ≠ generate
(zên models 200 nhưng generate 402) · CLI dsh tái dựng từ trí nhớ sai · pkill -f
tự kill cả khi bracket-trick (dùng `fuser -k <port>`) · node --test treo keep-alive
(`try/finally` + closeAllConnections) · https-by-http · `new URL` drop path ·
free-tier 429/503 là thiết kế.
