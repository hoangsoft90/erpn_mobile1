# handoff_20260923-1700.md — Bàn giao phiên sau M1 (customer.create)

## Đọc đầu phiên (theo thứ tự)

1. **`result64.txt`** — bảng trạng thái M1 đo được + số suite thật + 7 fail phân loại + **lỗ hổng thiết kế §3** (allow-list doctype ở `client.mjs`).
2. `.plan/next3/M1-customer-create-button.md` — spec gốc (§5 policy default, §9 exit criteria).
3. `checklist.md` mục "next3 / M1 (2026-09-23)" — bảng ✅/❌ từng mốc.
4. `LESSONS_LEARNED.md` mục "Đợt next3/M1" — 3 bài học vừa ghi (không tái phạm).

## ĐÃ XONG trong phiên 2026-09-23 (push + CI)

- **Đã commit + push** 2 commit + 1 merge: `3b354b4` (feat) · `25d1105` (docs) · `686b347` (merge, cây cloud shell thắng).
- **CI APK run [`35901343267`](https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35901343267) SUCCESS ~6 phút**, artifact `erpn-chat-debug-apk` **85.327.721 bytes** (head `686b347`).
- Suite đo trong phiên: **Node 691 (689 pass, 2 fail `dsh-gateway` môi trường từ B0)** · **Flutter 272/272** · analyze 0.
  ⇒ APK này **có** P9-A→F (5 loại phiếu nháp mới + hướng chi) · plan4 drawer tóm tắt ngày · OCR slots · **chưa có form tạo khách của M1** (backend + card parity đã xong).
- Ghi chú: nhánh remote từng có 5 commit P4/P5 từ shell khác ⇒ đã **merge `-s ours`** (giữ lịch sử, không force-push); `initp`/`faq1.md` bị bỏ khỏi cây theo AGENTS.md.

## Trạng thái một câu

**M1 backend có thật và chất lượng tốt (fail-closed đọc contract, pre-check 4 loại trùng, re-read + verify), NHƯNG phiên chưa xong: Flutter 0 hit, test riêng 0, falsify 0, M1-result 0 — và có 1 câu hỏi thiết kế phải hỏi user TRƯỚC khi sửa 7 test đỏ.**

## Việc phiên sau phải làm (thứ tự)

1. **~~HỎI USER~~ ĐÃ CHỐT (2026-09-23, `result64.txt` §3.1): user chọn (a) chấp nhận Customer creatable** — sửa 7 test đỏ kể trong `result64.txt` §2 theo ngữ nghĩa mới (giống DN/PR/SI/SR trước đây): 4 tripwire set thêm `create_customer` (b2:718, b3:606, b4:544, p9-delivery:368, p9-f:950) + 2 test "refuses undeclared doctype" đổi doctype ví dụ hoặc đổi kỳ vọng + test nguyên tắc B2:650 đổi thành ghim ngữ nghĩa MỚI: Customer creatable **nhưng chỉ qua capability `customer.create` qua gateway** (đưa doc "Customer must never become creatable" thành "Customer is creatable ONLY through the gateway-dispatched customer.create skill — raw callWriteTool with Customer doctype is reachable in-process only"). Ghi rationale đã duyệt vào từng test.
2. **Flutter M1** (spec §4.4): parse `offer_create_customer` trong `chat_models.dart` (`AskResult`) + form Tên*/SĐT/MST + nút **[Tạo khách mới]** (khác card thường — không nhét vào `_confirmableActions` theo nghĩa proposal, xem spec: tạo xong gắn `customer_id` vào context, **không** auto SO) + widget test + thêm `create_customer` vào `_confirmableActions` nếu theo đúng pattern card (KHỐI: confirm vẫn POST `/execute` với proposal đã có — kiểm kỹ copilot-server trả proposal action `create_customer` ở nhánh 642).
3. **Test riêng** `test/m1-customer-create.test.mjs`: builder từng mã từ chối (CC_NAME_MISSING, CC_DUPLICATE_NAME/MOBILE/TAX_ID, CC_FUZZY_MATCH), E2E `/ask "thêm khách …"` → card HIGH, `/execute` → đúng 1 Customer, chaos `MOCK_ERP_FAIL_AFTER_WRITE` retry không clone, `MOCK_ERP_CC_NO_CORRELATION_FIELD=1` ⇒ CC_CORRELATION_FIELD_MISSING, regress payment/SO.
4. **Falsify** `scripts/falsify/m1-customer-create.mjs` theo pattern `p9f-sales-return.mjs` (đột biến guard → RED, restore `sha256sum -c` byte-identical).
5. Full suite xanh + `.plan/next3/M1-result.md` + cập nhật `working.md`/`checklist.md`/`next.md` — **CHỜ user duyệt, KHÔNG tự commit** (vùng master data + tiền).

## Cảnh báo cho phiên sau

- **Không tin "đã xong"** — mọi mốc phải đo bằng `grep`/`ls`/suite; M1 đã từng bị báo "xong" trong lúc đứt kết nối.
- **Mock Customer KHÔNG dedupe là chủ đích** (luật ERPNext thật) — đừng "sửa" mock cho dedupe làm pre-check của skill trông đúng vì lý do sai.
- **Chỉnh nhánh `customer_create_write` đứng ở copilot-server ~642 VÀ offer ở ~1195** — sửa một chỗ quên chỗ kia là regression Offer.
- Route group `customer_create_write` phải đứng **trước** nhóm `customer` READ trong `capabilities.json` (keyword SUBSTRING "khách" nuốt mệnh lệnh) — đừng đổi thứ tự mảng khi chỉnh JSON.
- Suite dsh-env fail 2 ca (`dshGatewayHealth`, `verifyDshRuntime`) là **môi trường có từ B0**, không phải regression — đừng sửa dsh khi đang làm M1.

## Chưa đụng (giữ nguyên)

purchase return · Supplier/Item create (M2/M3) · XML/OCR auto-create · submit Customer (không tồn tại — master data) · Workstream A HĐĐT XML (`implementation.md` — chờ M1 xong).
