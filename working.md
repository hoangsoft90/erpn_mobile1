# working.md — Nhật ký đang làm

> Việc cụ thể gần đây, format ngày `YYYY-MM-DD`. Dọn mục đã xong quá 1–2 tuần
> (nội dung cũ nằm trong `result*.txt` + `.project/openspec.md` là pointer).

- [~] **[2026-09-18] P10 SLICE (phases2) — RATE LIMIT + CORRELATION KỸ THUẬT XONG, ⏳ CHỜ DUYỆT COMMIT (policy tiền — không tự commit)** (`result51.txt` + `.plan/phases2/p10-result.md`): ① **Phần A**: commit P7 `3e6240a` + push (7 file +821/−2, secret scan CLEAN) ② **Audit trước code** (luật P1): contract đã khai `operational_controls.rate_limit` từ P0 nhưng **không có code enforce**; kill switch đã có; `action_id`/`custom_ai_action_id` đã có ⇒ việc thật = enforce + cột §17 ③ **`rate-limit.mjs` (mới)**: `parseRateSpec` chỉ nhận `\d+/(second|minute|hour)`, spec hỏng → **fallback default THẬT** + cảnh báo stderr (không được tắt cổng lặng lẽ); `RateLimiter` fixed-window theo identity+bucket (clock injectable, guard clock lùi); `proposalBucketFor()` meter theo LOẠI contract; `rateLimitMessage()` copy TV ④ **`http-ask.mjs` +183/−5**: 429 **TRƯỚC** Safety Gateway (⇒ `command_id` KHÔNG bị đốt — đo thật `store.status(cid)=null`, 0 PE, mở lại cửa sổ ghi đúng 1 lần) + `Retry-After`; `/execute/cancel` KHÔNG throttle (nhả khoá, không ghi); `/ask` charge `read` trước pipeline, charge `write_proposal` chỉ khi proposal là WRITE ⑤ **Observability §17**: `logEvent()` + cột `request_id/user_id/command_id/action_id/erp_document_id/capability/risk/latency_ms` cho `/ask` + `/execute` + job runner (`phase:"execute"&#124;"job"`), taxonomy thêm `rate_limited` + 4 mã write (verify ∈ LEARNING_OUTCOMES) ⑥ **Review bắt 3 LỖI THẬT của chính code**: **F1** tự viết `capabilityForAction()` tra nhánh `operationalControls().capabilities` KHÔNG tồn tại ⇒ null mọi action ⇒ limit `payment.create 20/giờ` **tắt lặng lẽ** (contract đã có hàm đúng — bài học "đừng viết lại thứ contract có"); **F2** `export { x } from` KHÔNG tạo binding cục bộ ⇒ `ReferenceError` trong `try` của `/ask` ⇒ **mọi câu hỏi 500**; **F3** charge theo `proposal != null` ⇒ câu ĐỌC (route nào cũng có proposal `read_balance`) tiêu ngân sách GHI; **F4 harness** `NLP_PORT` là const cấp module + module cache ⇒ set env trong thân test vô hiệu ⇒ pipeline âm thầm NLP_UNAVAILABLE (200 nhưng proposal undefined) — pin port ở ĐẦU file test, TRƯỚC mọi import ⑦ **Falsify 4 guard** (script trên bản sao, restore IDENTICAL): gỡ throttle-trước-gateway → MONEY+correlation FAIL · cho READ proposal tiêu budget ghi → proposalBucketFor+E2E FAIL · đổi default rate limit sang off → fail 6 · nới regex spec → parseRateSpec FAIL ⑧ **Suite: Python 60/60 · Node 240/240 (228→240, file P10 12/12 chạy 2 lần) · Flutter 69/69 · analyze 0** ⑨ **4 bài học mới vào skill `erpn-verify-first` (+4 hàng) + `LESSONS_LEARNED.md`** ⑩ KHÔNG sửa `capabilities.json` (numstat 0) · không chạm ERPNext thật · gap P10 full ghi rõ trong `p10-result.md` (DR drill/dashboard/load test/kill-switch runbook = chưa làm).
- [x] **[2026-09-18] P7 (phases2) — JOB QUEUE: ✅ ĐÃ COMMIT `3e6240a` (đã push)**: xem `result50.txt` + `.plan/phases2/p7-result.md` (queue + runner thật trong `main()`, crash-recovery RUNNING→RETRYING, `/jobs` pending+completed, cancel `release()`, 5 finding review + 4 falsify; TTS skip có lý do).
- [x] **[2026-09-17] P0 (phases2) — CAPABILITY CONTRACT + SAFETY GATEWAY + GOLDEN DATASET + custom_ai_action_id (✅ ĐÃ COMMIT `b4acdb1` — đã push)**: theo `.plan/plan2_final.md` + `.plan/phases2/p0-*`, **không làm lại** Phase 0–3/7/9, giữ 8787/8788 + luồng payment NHÁP. ① **`capabilities.json`** = single source of truth (7 capability: 5 Reference READ + `payment.history` + `payment.create` HIGH + `document.delete` **forbidden**) + `capability-contract.mjs` validate **fail-closed** (schema/type/risk/errors/`scope.company`/route↔capability/execution flags) — router đọc trigger từ contract (bỏ hardcode) ② **`safety-gateway.mjs`** = CỬA DUY NHẤT cho WRITE; `/execute` trong http-ask chỉ parse HTTP rồi uỷ quyền (bỏ hết policy khỏi HTTP layer); thứ tự gate: contract → shape → amount policy → kill switch → freshness → idempotency → replay/resume-reconcile → execute → verify ③ **INVARIANT no-bypass có test TĨNH**: quét `src/**.mjs`, `executePaymentProposal(` chỉ được ở gateway (+ định nghĩa), `callWriteTool(` chỉ ở client + payment-write, http-ask KHÔNG được chứa cả hai ④ **kill switch** (`COPILOT_GLOBAL_READ_ONLY` hoặc `mcp-erpnext/control/read-only.flag`, thêm `COPILOT_DISABLE_CAPABILITIES`) ⇒ `503 SYSTEM_MAINTENANCE` **trước** idempotency gate ⇒ KHÔNG tiêu tốn `command_id` (cancel vẫn cho phép để không đóng băng ý định) ⑤ **`custom_ai_action_id` ĐÃ TẠO + VERIFY TRÊN ERPNEXT THẬT** (`Payment Entry-custom_ai_action_id`, Data, unique=1, search_index=1) bằng script operator idempotent `scripts/add-correlation-field.mjs` (REST API, không qua AI path; probe trước = chưa có; reconcile-theo-field trên site thật giờ `correlation_field_unavailable:false`) ⑥ **`action_id`** (`act_<uuid>`) sinh 1 lần ở proposal → ghi vào PE → `verifyWrittenPayment` kiểm **khi site có field** → `reconcilePaymentEntry(mcp, ref, {actionId})` tra thêm theo field; site chưa migrate thì KHÔNG vỡ (chỉ báo `correlation_field_missing`) ⑦ **untrusted-data** (`untrusted-data.mjs`): strip pattern chỉ dẫn + cap 500 + wrap delimiter; áp vào candidate lấy từ ERPNext + log violation ⑧ **Golden Dataset v1** 200 câu/6 bucket (`test/golden/golden-dataset.json` + runner spawn NLP, không LLM/credential): **read 50/50 · write 30/30 · kinship 39/40 · money 29/30 · ambiguous 30/30 · adversarial 20/20** — cả 6 bucket ĐẠT ngưỡng §24.5 ⑨ **3 gap THẬT đã xác minh nguyên nhân, không sửa ở P0**: `k18` "Con Linh" không strip vì `money.py:FILLERS` chứa "linh" (kinship dùng chính FILLERS làm tập không-phải-tên) · `m15` "một triệu hai" = null (shorthand chưa hỗ trợ) · `k36` "Bác sĩ Nam" bị strip "Bác" (strip chỉ nhìn token kế tiếp) — đều thuộc Phase 1 (money/kinship), cần falsify riêng ⑩ **command store** thêm 4 test khoá hành vi (không nằm `/tmp`, sống qua restart, atomic tmp+fsync+rename, file hỏng bị tách ra) ⑪ **Suite cuối: Python 60/60 · Node 152/152 (120→152) · Flutter 63/63 · analyze 0** ⑫ **✅ ĐÃ COMMIT `b4acdb1` + PUSH** (user duyệt 2026-09-17) — 15 file mới + 7 file sửa, secret scan **0 hit**, `.env` git-ignored; báo cáo đầy đủ + rủi ro còn lại ở `.plan/phases2/p0-result.md`; việc người thật: xác nhận 1 phiếu demo thật có `custom_ai_action_id` · diễn tập `global_read_only`.
- [x] **[2026-09-17] VÒNG TỰ REVIEW P0 — 3 LỖI CRASH/SAFETY THẬT (đều do chính refactor P0 tạo ra) ĐÃ SỬA + FALSIFY** (bằng chứng `result44.txt` §3–§9): ① **D1 (crash-class)** `openClient()` bị đặt TRƯỚC `try` (cả nhánh resume + execute) ⇒ `PARTIAL_ERPNEXT_CONFIG` từ `pickServerScript()` thoát khỏi `runExecute()` → thoát async handler `/execute` → **unhandled rejection ⇒ GIẾT cả process** (kéo theo `/ask` + `/health`); probe chứng minh `THREW_OUT_OF_GATEWAY`, falsify (gỡ guard + bỏ try ở http-ask) cho ra **test bị CANCELLED + file treo tới trần 60s**, không phải assertion đỏ; fix = `openClientOrRefuse()` trả `503 PAYMENT_CLIENT_UNAVAILABLE` (không throw) + guard tầng HTTP `EXECUTE_UNEXPECTED_ERROR` ② **D2 (zombie intent)** cùng đường để lại `PENDING` **không `reference_no`** ⇒ vì khoá ý định là `(customer|invoice)`, mọi command khác cho cùng khoản nợ bị `IDEMPOTENCY_INTENT_IN_FLIGHT` chặn, `/execute/cancel` cũng cần client nên cũng không mở được ⇒ **một lỗi đánh máy trong `.env` treo vĩnh viễn khoản nợ**; fix = `503 + retry_same_command_id: true`, **KHÔNG `store.fail`** (chắc chắn chưa ghi gì) ⇒ sửa config rồi gửi lại ĐÚNG `command_id`; test khẳng định `begin()` cùng cid ⇒ `resumed: true` ③ **D3 (rò rỉ)** `initialize()` ném ⇒ `close()` không bao giờ chạy ⇒ MCP server con ở lại giữ event loop sống; fix = catch quanh `initialize()` → `close()` → ném lại ④ **D4** test tĩnh no-bypass chỉ quét `src/` ⇒ `scripts/` (có tooling ghi schema thật) là chỗ ẩn hợp lệ cho đường ghi thứ hai → quét thêm `scripts/**` ⑤ gia cố: pin hành vi **`params.amount_vnd` thiếu ⇒ TỪ CHỐI** (dù contract khai `allow_full_balance: true` — người điền tiền là `buildPaymentProposal()`, request thiếu tiền là request HỎNG, không để tầng dưới tự clamp) ⑥ **sự cố falsify (đã thành bài học)**: lần falsify A+C đầu chạy quá 180s nên `cp` khôi phục KHÔNG kịp chạy ⇒ cây làm việc vẫn ở trạng thái **đã gỡ fix**, phát hiện bằng `grep -c` (=0) chứ không bằng trực giác; lần 2 chạy trên **bản sao** `/tmp/falsify-c` + `timeout` cứng ⑦ **4 bài học mới vào skill** `erpn-verify-first` (giờ 84 hàng, `TOTAL malformed rows: 0` — đồng thời sửa **1 hàng HỎNG** từ phiên trước thiếu ô "rule") ⑧ **Suite cuối: Python 60/60 · Node 156/156 (152→156) · Flutter 63/63 · analyze 0**.
- [~] **[2026-09-17] result43 — A) XÁC MINH HẠ TẦNG (URL tunnel đổi) · B) GIỚI HẠN LỊCH SỬ CHAT · C) MÀN HÌNH SETTINGS (CHỜ DUYỆT COMMIT — chưa tự commit)**: ① **A.1** `.env` đã đúng `ERPNEXT_URL=https://prevail-pantyhose-overvalue.ngrok-free.dev` (không cần sửa) — verify bằng ĐỌC THẬT qua pinned 3.0.4: `erpnext_customer_list` trả 3 khách thật (P1D-ACCEPT T5/T6/T7) ② **A.2** `mac-custom` config khớp URL cũ, tunnel SỐNG — curl `/v1/models` 502→**200** (flaky lần đầu là tính ĐÃ ghi trong `_note`, không phải lỗi mới) ③ **A.3** xác nhận `erpn8788.loca.lt` = tunnel Gateway (`http-ask.mjs` default port **8788**, `ASK_PORT`) bằng code + docs; ⚠️ **tunnel erpn8788 HIỆN TẮT** (408/408/502 — `lt` không chạy trên Mac), đây là tình trạng MÔI TRƯỜNG không phải lỗi config ④ **B** giới hạn lịch sử: `AppSettingsService` (MỚI, SharedPreferences, getter đồng bộ) + `ChatTurn.hasPendingProposal` + `ChatHistoryService.trimTurns/save(maxItems)` (pure) + controller áp CÙNG luật cho cả state LẪN storage; mặc định **20**, sàn **5** ⑤ **FALSIFY BẮT BUỘC ĐẠT**: gỡ nhánh an toàn → **3 test đỏ đúng chỗ** (Expected 21/Actual 20 ×2, 6/5 ×1) → khôi phục `markers=0` + 8/8 xanh ⑥ **C** màn Settings (URL + auth + max, validate, chặn <5) + route `/settings` + icon ⚙️; `CopilotApiClient` đọc settings MỖI request ⇒ đổi URL áp dụng NGAY (test: cùng 1 client instance, request 2 đi URL mới); rỗng → fallback dart-define (APK cũ không đổi) ⑦ **Suite: Python 60 · Node 120 · Router 19 · Flutter 62 (39→62, +23) · analyze 0** ⑧ **Hạn chế ghi rõ**: `/execute` đọc `dioProvider` trực tiếp (chỉ theo URL mới SAU khi có ≥1 lần /ask) — fail-CLOSED, không sửa phiên này; card đã ghi vẫn "pending" trong model (option b) nên không bao giờ bị cắt (an toàn > gọn) ⑨ **REVIEW vòng sau (tự tìm lỗi) — 2 LỖI THẬT ĐÃ SỬA**: 🔴 **F1** `attachRejection` gọi cùng luật `trimTurns` SAU khi đánh dấu card rejected ⇒ card thành "đã kết thúc" và (thường cũ nhất) bị CẮT NGAY — user bấm Xác nhận, bị từ chối, **banner lý do biến mất**; test chứng minh trước khi sửa: seed cap 5 + 6 turn (proposal #0) → `Expected: <6> Actual: <5>` ⇒ fix = BỎ trim khỏi `attachRejection` (B.2 cắt "sau mỗi lần thêm turn", từ chối KHÔNG thêm turn) → 61/61 xanh · 🟡 **F2** doc comment của provider mới "dính" sang `sharedPreferencesProvider` (analyzer không báo) — đã dán lại đúng chỗ · 2 bài học mới đã vào skill `erpn-verify-first` (grep trước 0 hit): luật miễn trừ theo TRẠNG THÁI X phải kiểm ĐIỂM CHUYỂN + liệt kê MỌI đường gọi; chèn code sát doc comment phải đọc lại vùng đó. ⑩ **F3 (phát hiện khi user hỏi "đã test chưa")** — HỞ TEST ở chính đường nối vừa thêm: route `/settings` + icon ⚙️ không test nào chạm (harness cũ pump `MaterialApp(home:)` không router / render thẳng màn đích) ⇒ suite XANH nhưng đường nối chưa từng chạy; fix = `test/settings_navigation_test.dart` (pump `MaterialApp.router` THẬT + tap ⚙️ + assert route resolve) ⇒ **Flutter 61 → 62** · 3 bài học mới vào skill. ⑫ **✅ ĐÃ COMMIT `c401b0f` + PUSH + BUILD APK XANH** (user duyệt 2026-09-17: "commit result43 + push cả hai đợt", docs commit riêng sau): 14 file code/test (1202+/12−), secret scan CLEAN (2 hit = false positive, chỉ tên biến) · push `9f496bf..c401b0f` (kèm `4546997` F1/F3/F2, branch hết ahead) · GH Actions run **`35173021349` SUCCESS 5m0s**, artifact `erpn-chat-debug-apk` ~80 MB (hết hạn 2026-12-16) · ⚠️ verify được repo **CHƯA set GitHub Variables** ⇒ APK có endpoint/auth RỖNG rơi về `127.0.0.1:8788`; bù lại dùng được **màn Settings mới** để nhập `https://erpn8788.loca.lt` + auth ngay trong app (không cần build lại) — điều kiện: `lt` đang chạy trên Mac · 🔎 finding nhỏ chưa sửa: câu báo 401 còn ghi "APK cần build lại với --dart-define..." đã lệch thực tế. ⑬ **USER REPORT "lưu URL thành công nhưng footer vẫn cũ" → 2 lỗi thật đã sửa + build lại (`f28869a`)**: footer đọc tĩnh dart-define + plain Provider không thông báo khi giá trị đổi (fix: footer đọc URL hiệu lực + `ref.invalidate` sau Save; falsify: gỡ invalidate → test đỏ đúng triệu chứng) · test hồi quy đi cả hành trình lưu→pop→assert footer · Flutter 63/63 · run **`35175220731` SUCCESS** artifact mới ~80 MB · 🟡 lỗi test tự bắt: `appRouter` toàn cục dùng chung giữa các test ⇒ setUp `go('/chat')` · 3 bài học mới vào skill. ⑭ KHÔNG đụng `.env` / router config / `mcp-erpnext/src/**` / ERPNext thật.
- [~] **[2026-09-16] result42 — F2 (option b) [commit `4546997`] `AutomaticKeepAliveClientMixin` XONG + FALSIFY (✅ ĐÃ COMMIT `4546997`, CHƯA PUSH)**: user chọn **(b)** và "commit để sau, đợi làm F2 rồi gộp" ⇒ ① Fix: `_ProposalCardState with AutomaticKeepAliveClientMixin` + `super.build(context)`; `wantKeepAlive` **CÓ ĐIỀU KIỆN** (`_confirming || _result != null || _error != null || _rejectionCode != null`) — không phải `=> true`, để card chưa từng bấm vẫn scroll đi bình thường (lịch sử dài không ghim mọi card trong RAM) ② Test hồi quy `result41 F2` dựng ListView 60 item (card ở đỉnh, filler cao 120) + drag −5000/+6000; dùng **MỘT** instance proposal (bài học probe result40) ③ **Falsify đạt**: gỡ mixin + `super.build` → FAIL đúng `Found 0 widgets ... Đã ghi phiếu thu: PE-M001` (line 653); khôi phục → marker 0, mixin dòng 59, super.build dòng 208 ④ Suite: Python 60 · Node 120 · Router 19 · **Flutter 39/39** (38→39) · analyze 0 ⑤ **Hạn chế ghi rõ**: option (b) giữ RAM ⇒ kết quả ghi mất khi restart app (option (a) mới persist = ĐỔI SCHEMA); an toàn tiền không phụ thuộc hiển thị — command_id ghim trong history + server replay (2 test đã có) ⑥ ✅ **ĐÃ COMMIT `4546997`** (user duyệt 2026-09-16: "commit nhưng KHÔNG push" ⇒ branch **ahead 1**): 4 file +272/−8, secret scan staged = 0 hit; lần commit đầu `d754487` bị git FOLD subject (dòng nối thụt đầu trong cùng đoạn ⇒ subject dài 1 dòng) → sửa bằng `git commit --amend` (chỉ MESSAGE; `git show --stat` vẫn đúng 4 file) ⑦ Saga §7: **vẫn chờ duyệt** — không code; F4 ghi nhận, không sửa; không đụng ERPNext thật ⑧ Docs/evidence (result37-42 · setup-test.md · handoff37/39 · checklist/next/working) **chưa commit — user chọn "commit docs riêng sau"**.
- [~] **[2026-09-16] result41 — ĐÓNG result40: STAGE 4 FILE F1+F3 (CHỜ USER DUYỆT COMMIT) · F2 CHỜ QUYẾT · SAGA CHƯA DUYỆT**: ① Xác nhận code đúng như result40: F1 = 4 guard `if (!mounted) return;` (card dòng 78/91/112 + trong `_handleRefusal` dòng 125), F3 = tolerant `is List` (card:131, model:246), 4 test hồi quy có mặt (proposal_card_test:514/545/570 · chat_data_test:194) ② **Staged đúng 4 file** (2 lib Dart + 2 test) — `179 insertions(+), 7 deletions(-)`; secret scan staged **CLEAN**; không lẫn `.env`/store/`.plan`/docs; **KHÔNG commit** (vùng tiền, chờ user) ③ Suite: Python 60 OK · Node 120/120 · Router 19/19 · **Flutter 38/38** · analyze 0 ④ F2 (mất kết quả "đã ghi" khi ListView recycle) — đã tóm tắt ưu/nhược 2 phương án (a) lưu kết quả ở model = **đổi schema** (đối xứng rejection, sống qua restart) vs (b) `AutomaticKeepAliveClientMixin` (nhỏ, nhưng chỉ giữ trong RAM + lệch cơ chế với rejection) → **dừng, chờ user chọn** ⑤ **Saga/compensation: KHÔNG code** — user chưa duyệt §7 ⇒ ghi "Saga chờ duyệt" ⑥ F4 ghi nhận, không sửa. Không mở Phase 4/8/10/11.
- [~] **[2026-09-16] result40 — REVIEW TOÀN BỘ CODE (UI/logic/crash), 2 LỖI THẬT ĐÃ SỬA · CHỜ DUYỆT COMMIT**: baseline Python 60 · Node 120 · Router 19 · Flutter 34 · analyze 0 → **🔴 F1 (crash)** `proposal_card._confirm()` cập nhật UI sau `await` ở 4 nhánh KHÔNG kiểm `mounted` (chỉ `finally` có) ⇒ `setState() called after dispose()` khi rời màn hình/xoá lịch sử lúc lệnh ghi đang bay; tệ hơn `catch (e)` chung nuốt lỗi đầu rồi ném lại ở dòng khác ⇒ stack luôn chỉ sai chỗ — chứng minh bằng probe thật (P1 409 + P1b 200, cả 2 ĐỎ), fix `if (!mounted) return;` ×4 + trong `_handleRefusal` (guard luôn `ref.read` trên scope đã dispose), **falsify**: vô hiệu 4 guard → ĐỎ lại, khôi phục (md5 khớp, 0 marker) → XANH · **🔴 F3 (fail-OPEN ở đường an toàn)** `body['problems'] as List<dynamic>?` ném TypeError khi field là String ⇒ banner KHÔNG hiện (`banner shown: 0`) ⇒ **nút [Xác nhận] vẫn còn trên đề xuất đã bị từ chối**; cùng cast ở `ActionProposal.fromJson` ⇒ `ChatHistoryService.load()` trả `[]` ⇒ 1 field lỗi xoá sạch lịch sử; fix parse tolerant `is List` (card + model), falsify đạt · giữ 4 test hồi quy (**Flutter 34 → 38**, analyze 0) · **🟡 F2 CHƯA SỬA (cần user chọn)** mất kết quả "đã ghi" khi ListView recycle card (probe P2: trước scroll success=1/button=0 → sau scroll success=0/**button=1**), AN TOÀN TIỀN đã đo: bấm lại gửi CÙNG command_id (P2b) ⇒ replay; 2 phương án (a) lưu kết quả ở model = ĐỔI SCHEMA (b) keep-alive mixin · **🟡 F4 latent** clone `attachRejection` đổi key khi seed null (P3 `same=false`, hiện không khai thác được) · LOW: F6 field parse nhưng không dùng · F7 `waitForNlpService` fetch thiếu AbortSignal · F8 Python `int(Content-Length)` non-số → traceback stderr. **Review sạch có bằng chứng**: http-ask (cap body/JSON try/deadline 120s+clearTimeout/fail-closed) · đo riêng: ghi response cho client đã abort KHÔNG crash Node (`uncaughtException: NONE`) ⇒ thiếu guard `destroyed` ở http-ask không phải bug (git log: chưa từng có; guard nằm ở router nơi có streaming) · copilot-server/client/idempotency/nlp_service đều vững. Lần chạy probe đầu báo "2 command_id khác nhau" là **LỖI PROBE của tôi** (tạo proposal mới mỗi build) — đã sửa probe + ghi skill
- [x] **[2026-09-16] result39 addendum — VIẾT `setup-test.md` (cài đặt · cấu hình · test)**: 8 mục, copy-paste được, chỉ nêu **TÊN** biến (không giá trị secret): yêu cầu môi trường (Python ≥3.10 · Node ≥20 · Flutter stable — bản đã verify) · cài đặt (npm ci · pub get + **BẮT BUỘC** `build_runner` vì `*.g.dart` gitignored · localtunnel) · cấu hình (`.env` theo nhóm + quy tắc chọn target fail-closed + `ASK_*` bind policy + 3 file config router + `--dart-define`/GitHub Settings) · chạy dev 3 terminal đúng thứ tự · test (unit 4 bộ · CLI NLP · E2E HTTP/execute/cancel · kịch bản replay-STALE-EXPIRED · E2E dsh · APK) · sự cố thường gặp · an toàn vùng tiền. 🔴 **Tự bắt 1 lệnh SAI trong chính doc**: `node --test scripts/test` → `MODULE_NOT_FOUND` (positional arg của `--test` không nhận directory trần) — lệnh đúng đã chạy thật `node --test scripts/test/*.test.mjs` (19/19) hoặc `cd scripts && node --test`; đã sửa cả 2 chỗ + ghi bài học vào skill (mục 1 item 6: lệnh trong DOC cũng phải chạy thật trước khi viết). Verify bằng cách chạy thật: CLI NLP 3 ví dụ (10500000 / 5000000 / 2500000) · Python OK · Node 120/120 · Router 19/19 · `NLP_SERVICE_PORT=8799` → ready port 8799
- [x] **[2026-09-16] result38 addendum — ĐÓNG GÓI BÀI HỌC LỖI VÀO SKILL `erpn-verify-first`** (user yêu cầu): 3 nội dung mới sau khi grep đối chiếu (tránh nhân bản) — ① mục 3 mở rộng thành 3 nhóm nghi phạm **CODE / TEST-PREDICATE / CHÍNH BỘ ĐO** với 3 ca thật (probe thiếu `Content-Type` → dio parse String → bug giả; regex đếm lẫn dòng định nghĩa hàm → đỏ trên code đúng; 2 bộ đếm lệch do **đơn vị đếm** / pipe đã escape) ② mục 6 thêm bullet shape tham số `read_files` (thiếu `path` — lỗi lặp lại nhiều lần trong phiên, mỗi lần 1 vòng) ③ bảng cuối thêm hàng **F6 thiết kế**: nhánh mới gọi LẠI hàm tốn kém mà guard ngoài đã gọi cùng input (bug chờ + round-trip thừa; cùng nhóm result31 F1). Sửa thêm 1 hàng bảng thật thiếu dấu phân cách (dòng 155) — tự phát hiện bộ đếm naive của mình báo động giả 5 hàng, kiểm lại bằng bộ đếm escaping-aware `re.split(r'(?<!\\)\|', l)` → **TOTAL malformed = 0**, 3 bảng `bad_rows=NONE`, 288→299 dòng
- [x] **[2026-09-16] result38 — REVIEW `2f7ec1f` + FIX F6 (VÙNG TIỀN) → ✅ ĐÃ COMMIT `9f496bf` (đã push)**: ① Scope `2f7ec1f` = docs-only đúng (10 file, grep `^(apps/|mcp-erpnext/src/|...)` = NONE) ② Đối chiếu docs↔code: TTL 10' (`proposal-freshness.mjs:19`) · STALE/EXPIRED (`http-ask.mjs:442/450`, `:77`) · cancel route (`:172`) — khớp runbook + device checklist ③ 🔴 **F6**: `answerQuestion()` resolve khách 2 LẦN trên cùng câu `thu tiền cho <khách> <số>` (guard ngoài dòng 291 + nhánh `payment_write`) ⇒ thừa 1 vòng `findCustomer("")` chạm ERPNext + nguy cơ 2 khối lệch nhau (cùng nhóm guard-before-branch của fix `notIf`) ④ Fix: nhánh write REUSE binding từ guard ⇒ `grep -c "await resolveCustomer(skills"` = 1; hành vi không đổi (E2E `copilot.test.mjs:251` xanh: `Nguyễn Thị Lan` / 500_000 / SINV-0001) ⑤ 🟡 **lỗi của chính tôi**: test hồi quy đầu tiên ĐỎ trên code ĐÚNG vì regex `/resolveCustomer\(skills/` khớp CẢ dòng định nghĩa hàm (đếm 2) — sửa thành `/await resolveCustomer\(skills/g`; falsify lại (chèn call thật → FAIL `found 2`, gỡ → marker 0) ⇒ đã vào skill `erpn-verify-first` ⑥ Suite: **Python 60/60 · Node 120/120 (+1) · Flutter 34/34 · analyze 0**. KHÔNG đụng ERPNext thật · KHÔNG tự commit (vùng tiền)
- [x] **[2026-09-16] result34 — ĐÓNG VÒNG (không mở phase mới)**: ① Quyết định commit: đợt UI STALE + F4 CHƯA được duyệt trong chat ⇒ chỉ report 12 file (4 Dart + 4 docs + result32/33 + 2 handoff; secret scan CLEAN) với message user soạn: `feat: phase 9 UI — stale/expired banner via real HTTP 409 (dio); params round-trip so confirm works from the app` ② Docs sync: next.md ghi rõ **MVP kỹ thuật Phase 7+9 an toàn ĐÃ XONG** — không Phase 4/8/11; việc tiếp = người thật hoặc saga khi duyệt §7 ③ **Runbook demo mới `docs/demo-payment-draft.md`** (1 trang, không code server): 3 terminal + luồng hỏi nợ → thu tiền → xác nhận → phiếu NHÁP + kịch bản STALE/EXPIRED/replay/cancel; secret scan CLEAN. Suite đầu + cuối phiên: **Python 60/60 · Node 119/119 · Flutter 34/34 · analyze 0** (lỗi Python lần chạy đầu = sai cwd, chạy từ apps/mobile — đúng cwd là repo root với PYTHONPATH=src). Không đụng ERPNext thật
- [x] **[2026-09-16] result35 — COMMIT `31d485c` ĐÃ PUSH (user duyệt)**: 15 files +1006/−22 (4 Dart UI STALE + wire 409 + F4 params round-trip; 4 docs; result32/33/34; 3 handoff; runbook `docs/demo-payment-draft.md`). Secret scan CLEAN trước stage; không stage .env/store/.plan/rác. Suite sau commit: Python 60/60 · Node 119/119 · Flutter 34/34 · analyze 0. Docs: xóa marker CHỜ DUYỆT, ghi hash vào checklist/next
- [x] **[2026-09-16] result33 — UI STALE review + fix wire 409 dio → ✅ ĐÃ COMMIT `31d485c` (đã push)**: ① Quyết định commit: user CHƯA duyệt đợt UI STALE trong chat ⇒ chỉ report 10 file (4 Dart + 4 docs + result32/33 + handoff) — message đề xuất giữ nguyên result32 §7 ② **Review UI STALE tìm 1 BUG THẬT**: dio mặc định THROW DioException với non-2xx (validateStatus chỉ nhận 2xx) ⇒ 409 PROPOSAL_STALE/EXPIRED của server không bao giờ tới `res.data` ⇒ banner Phase 9 vô hình trong production; 3 test cũ bơm rejection thẳng vào model (không qua wire) nên không bắt được ③ **Fix**: `_handleRefusal()` (stamp cục bộ + attachRejection persist) + nhánh `on DioException` đọc `err.response?.data` (connection drop giữ nguyên thông báo mạng) + `didUpdateWidget` xoá stamp khi proposal instance swap ④ **Test wire mới**: mock adapter trả 409 THẬT → banner + nút biến mất; **falsify**: gỡ nhánh → FAIL đúng assertion ("Found 0 widgets... đã lệch so với dữ liệu thật"), khôi phục grep 0 marker ⑤ Bài học mới vào skill (`erpn-verify-first`, 76 hàng): test qua wire với status thật cho mỗi nhánh lỗi, model-level chỉ cho render. **Suite: Python 60/60 · Node 119/119 · Flutter 34/34 · analyze 0**. Không đụng ERPNext thật · không code saga (chờ duyệt §7). **Review vòng 2 (F4, vùng tiền)**: Dart `ActionProposal` thiếu field `params` ⇒ `toJson()` không gửi ⇒ bấm [Xác nhận] từ app thật luôn 400 tại money-shape gate (`proposal.params.amount_vnd` — http-ask.mjs:309); phát hiện nhờ probe tự dựng pipeline model → jsonEncode → soi body gửi đi (Node test tự dựng JSON có sẵn params, Dart test chỉ assert response ⇒ không bắt được). Fix: model thêm `params` (unmodifiable) + toJson gửi ngược + attachRejection rebuild mang theo + test assert body GỬI ĐI qua mock adapter; falsify: gỡ khỏi toJson → FAIL `Expected: <500000> Actual: <null>`, khôi phục grep 0 marker. Bài học echo-back round-trip vào skill (77 hàng bảng + gộp bảng liền kề — 2 hàng result33 giờ nằm trong cùng 1 khối bảng, integrity check 75 rows OK)
- [x] **[2026-09-16] result31 — 5 việc đã duyệt XONG HẾT**: ① **Gộp 1 commit `eea0411` + push** (user quyết thay tách 3): 37 files +3794/−124 — Stage B + fix money/identity + Phase 9 safety + docs ② **Nối payment_write vào routeIntent**: nhóm ĐẦU ROUTES + anchor `startsWith` đầu câu (synonym "thu tiền"→"payment"; substring sẽ nuốt câu đọc "đã/chưa thanh toán"); E2E thật → proposal HIGH + widget test JSON verbatim → nút [Xác nhận] hiện thật; sửa chữ "Phase 2 chỉ đọc" (0 hit) ③ **Route `/execute/cancel`**: chỉ huỷ PENDING sau reconcile=0 chứng từ; COMPLETED 409; thiếu reference 409; ERPNext chết 503 — giải zombie PENDING ④ **Xoá 2 PE demo `ACC-PAY-2026-00114/00115`**: tool thật `erpnext_doc_delete` từ source (draft); gọi JSON-RPC thô 1 lần theo lệnh; verify: 2 GONE + `ACC-SINV-2026-00047` outstanding 457.875 Unpaid không đổi ⑤ faq đầu-file + §3.2/§3.3/§9/§8 + result31 + handoff. Suite: **Python 60/60 · Node 118/118 · Flutter 29/29 · analyze 0**; falsify từng fix (gỡ anchor 15/16 FAIL · disable nhánh cancel 13/14 FAIL · phá confirmable widget FAIL — khôi phục đủ xanh). ✅ commit `bda54cf` (kèm review vòng 2 — 18 files, đã push)
- [x] **[2026-09-16] result31 REVIEW VÒNG 2 (§11) — 2 lỗi thật + 1 nhỏ, đã vá + falsify**: **F1** anchor `startsWith` nuốt câu ĐỌC lịch sử (`thanh toán gần nhất của chị Lan...` → `payment gần nhất...` → payment_write SAI — probe answerQuestion thật) ⇒ fix `notIf` deny-list + 1 test round 2, falsify đạt (gỡ gate → FAIL đúng assertion); **F2** `store.cancel()` ngoài try/catch — race với /execute đồng thời ⇒ throw uncaught ⇒ Node ≥15 crash process (suite không bắt được — child-process cách ly; chứng minh bằng exit code + grep 0 global handler) ⇒ bọc → 409; **F3** reason dùng `rawText`. faq §3.1 viết lại (lệnh ghi vs câu hỏi lịch sử). Suite: **Node 119/119 · Python 60/60 · Flutter 29/29 · analyze 0**. Bài học mới vào skill: falsify-hụt (marker TAP vs spec reporter Node 24) + cancel-race phải bọc trong handler fail-fast. **✅ COMMIT `bda54cf` ĐÃ PUSH (18 files +978/−66; secret scan CLEAN)**
- [x] **[2026-09-16] PHASE 9 (phần an toàn) — ✅ ĐÃ COMMIT `eea0411` (vùng tiền, `result29.txt`)**: ① `proposal-freshness.mjs` — `created_at` ghim vào `buildProposal()` + Dart `toJson/fromJson`; `assertFresh` TTL 10 phút → 409 `PROPOSAL_EXPIRED` **trước** `store.begin()` (không chiếm command_id) ② `detectDrift()` so snapshot (outstanding/invoice/customer) với dữ liệu sống tại thời điểm execute — lệch ⇒ KHÔNG ghi, 409 `PROPOSAL_STALE` + `problems[]` (thay hành vi "kẹp số tiền im lặng" — đúng spec phase-09: không clamp im lặng) ③ intentKey `(customer|invoice)` — ý định trùng đang PENDING ⇒ 409 kèm `clash_command_id` (client resume đúng lệnh đang khoá); số tiền KHÔNG nằm trong intentKey — 2 đề xuất khác tiền cùng (customer, invoice) = CÙNG ý định khi PENDING (bản đầu từng ghi nhầm `(kind|customer|amount|invoice)` + "fingerprint khác tiền ⇒ intent riêng", đính chính qua review result29 §10-F1) · Test: +4 Node freshness/drift + 3 HTTP (expired không tạo store entry · stale · pending trùng kèm `clash_command_id`) + 1 Dart (`created_at` sống qua khôi phục history), falsify từng fix. **Review vòng sau khi viết (result29 §10)**: F1 — docs từng mô tả SAI intentKey (ghi nhầm gồm số tiền; code thật `(customer|invoice)`), đã đính chính docs + +1 hàng skill; F2 — zombie PENDING khoá intent vĩnh viễn: đã trả `clash_command_id` (falsify bằng rename tạm nhánh), route /execute/cancel ĐÃ CÓ (result31) — zombie PENDING có đường thoát. Suite: Python **60/60** · Node **112/112** · Flutter **28/28** · analyze 0. Còn thiếu: saga/undo-compensation, UI hiển thị lý do STALE chi tiết, route cancel cho PENDING (review result29 §10-F2 — zombie PENDING khoá intent: đã trả clash_command_id, chưa có đường huỷ, chờ quyết khi duyệt commit)
- [x] **[2026-09-16] Phase 7 GIAI ĐOẠN B — ghi THẬT vào ERPNext: ✅ ĐÃ COMMIT `eea0411` (vùng tiền, `result25.txt`)**: tool thật verify từ source (`erpnext_doc_create` — KHÔNG có `erpnext_create_payment_entry`; reconcile dùng `erpnext_doc_list` filter reference_no) · ghi thật thành công `ACC-PAY-2026-00114` (10.000đ) + verify độc lập (reference_no==command_id, paid_amount, 1 chứng từ, allocation đúng hóa đơn) · replay cùng command_id → 2ms, vẫn 1 chứng từ · 🔴 **tự tìm ra bug thật**: crash-recovery ghi PHIẾU THỨ HAI (`ACC-PAY-2026-00115`) vì `begin()` xoá mất `reference_no` → bỏ qua reconcile; đã fix fail-closed + test hồi quy có falsification + kiểm chứng lại trên ERPNext thật (reconciled:true, duplicate_documents:2, không thêm phiếu thứ 3). Node 86/86 · Python 58/58 · Flutter 25/25 · analyze 0. **Việc user**: ~~dọn 2 phiếu nháp demo~~ ✅ ĐÃ XOÁ trong result31 (tool `erpnext_doc_delete`, verify hóa đơn gốc không đổi) + quyết có SUBMIT không (chờ user)
- [x] **[2026-09-16] VIẾT `faq.md` ⇒ 2 BUG THẬT ĐÃ VÁ + 1 KHOẢNG TRỐNG LỚN (`result28.txt`) — ✅ ĐÃ COMMIT `eea0411` (khoảng trống: ĐÃ NỐI trong result31)UYỆT COMMIT (vùng tiền + định danh)**: ① `"bác Hai 500 ngàn"` từng ra **2.500.000đ**, `"chị Bảy 300 ngàn"` ra **7.300.000đ** (luật "số cách nhau" áp nhầm lên từ-chỉ-số) — fix cờ `current_from_digits` + 11 test, falsify được · ② `"thu tiền cho chị Lan 500 ngàn"` từng trả lời về khách **"Chị Tư — thầu nhỏ"** (mảnh danh xưng "chị" khớp duy nhất 1 khách ở site demo) — fix `KINSHIP_TITLES` + 3 test, falsify được · ③ **chưa sửa, chờ quyết**: `buildPaymentProposal()` chỉ được test gọi ⇒ không câu hỏi nào sinh đề xuất `create_payment_entry` ⇒ nút [Xác nhận] không bao giờ hiện trong luồng chat thật (câu trả lời còn ghi "Phase 2 chỉ đọc" — chữ cũ) · ④ `faq.md` 9 mục, ví dụ thật. Python 60/60 · Node 97/97 · Flutter 27/27
- [x] **[2026-09-16] RÀ 3a + VÁ KHOÁ IDEMPOTENCY KHI KHÔI PHỤC HISTORY + 2 CHAOS TEST + SKELETON PHASE 9 (`result27.txt`) — ✅ ĐÃ COMMIT `eea0411`lt27.txt`) — ✅ ĐÃ COMMIT `eea0411`**: ① `readonly-guard` vs `callWriteTool` không xung đột (submit/delete không có đường nào chạm tới) · store idempotency không ở /tmp (default `mcp-erpnext/idempotency-store`, gitignored) · ② 🔴 **lỗ hổng thật đã vá**: `ActionProposal.toJson()` không lưu `command_id` ⇒ **khôi phục history (app restart) cấp UUID mới cho cùng card** ⇒ bấm [Xác nhận] ghi phiếu nháp thứ hai; fix = `commandIdSeed` + ghim `command_id` vào JSON (khoá sống theo vòng đời CARD) + 2 test falsify được · ③ **2 chaos test**: “ERPNext ghi xong rồi mất response” (fault `MOCK_ERP_FAIL_AFTER_WRITE`) → retry reconcile về ĐÚNG document, đếm ledger thật = 1; “ERPNext chết lúc đối soát” (`MOCK_ERP_FAIL_LIST`) → 503 + không ghi + store vẫn PENDING; falsify 2 vòng (500-vs-503, 409-vs-200) · ④ **skeleton Phase 9** (plan-only): proposal chưa có `created_at`/snapshot, store chưa có TTL — Phase 9 sẽ đổi hành vi từ “kẹp số tiền im lặng” sang “lệch ⇒ 409 PROPOSAL_STALE”. Suite: Python 58/58 · Node **93/93** · Flutter **27/27** · analyze 0. **Không có lệnh write nào mới trên ERPNext thật**
- [x] **[2026-09-16] REVIEW VÒNG SAU STAGE B (vùng TIỀN) — 5 lỗi thật, đã sửa + falsify, ✅ ĐÃ COMMIT `eea0411`COMMIT (`result26.txt`)**: 🔴 (1) lỗi SAU khi đã đăng ký `reference_no` bị đánh FAILED ⇒ `begin()` từ chối ⇒ không reconcile được + đẩy user sang command_id mới = công thức ghi phiếu thứ hai — nay `503 {retry_same_command_id:true}` giữ PENDING; 🔴 (2) `amount_vnd: 0`/NaN bị thăng cấp thành **thu toàn bộ nợ** (`Number(x) || live`) ⇒ 400 ở boundary (không chiếm command_id) + throw `PAYMENT_AMOUNT_INVALID` trong executor; 🟡 (3) `?? modes[0]?.name` chọn đại phương thức ⇒ tiền vào sai tài khoản ⇒ nay `PAYMENT_MODE_UNRESOLVED`; 🟡 (4) `<= 0` để lọt NaN ⇒ `Number.isFinite`; 🟡 (5) comment/docs sai + 1 hàng bảng skill vỡ vì pipe chưa escape. **Falsification cả 5**: gỡ fix → test FAIL đúng chỗ (2 vòng thật), khôi phục → Node **91/91** (+5) · Python 58/58 · Flutter 25/25 · analyze 0. Skill `erpn-verify-first` +1 hàng (fallback trong đường tiền phải fail-closed; validate phải GIỐNG NHAU ở mọi đường đọc cùng field)
- [x] **[2026-09-16] Phase 7 Giai đoạn A — ĐÃ COMMIT `8ebfc0e`** (duyệt 2026-09-16, vùng TIỀN — bằng chứng `result23.txt`): idempotency store 3 trạng thái + `record_customer_payment` (dừng ở proposal) + route `/execute` (mock) + nút [Xác nhận thu tiền] trên Flutter. Test thật: Python 58/58 · Node 77/77 (+14 test idempotency: trùng command_id → 1 ghi, FAILED terminal, fingerprint mismatch, PENDING reconcile-required) · Flutter 23/23 · analyze 0. Guard Phase 2 KHÔNG bị nới (`callWriteTool` tường minh). ⚠️ **Giai đoạn B CHƯA CHẠY** — chưa có lệnh nào ghi ERPNext thật, chờ user quyết thời điểm
- [x] **[2026-09-16] Fix `command_id` theo quyết định user — XONG, ✅ ĐÃ COMMIT `eea0411` (vùng tiền, `result24.txt)`**: lỗi cũ = UUID mới mỗi lần bấm ⇒ retry sau lỗi mạng ghi phiếu thứ 2 (trái phase-07 spec). Fix: `ActionProposal.commandId` = Expando cache theo PROPOSAL INSTANCE (giữ `@immutable`, sống qua rebuild/recycle card; /ask mới = key mới); widget dùng `proposal.commandId`; KHÔNG sửa backend. Test: +2 (retry sau lỗi mạng → cùng key + replay "chống trùng"; proposal mới → key mới) + **falsification tiêm lại bug cũ → FAIL đúng chỗ**. Suite: Python 58/58 · Node 77/77 · **Flutter 25/25** · analyze 0 — diff 3 file +92/-13
- [x] **[2026-09-16] Phase 6 display-only XONG + ĐÃ COMMIT `553d962`**: Risk Level 4 mức code-enforced + Action Proposal v1 trên mọi nhánh trả lời + Flutter ProposalCard + test: Node 63/63 · Flutter 19/19 · Python 58/58 · analyze 0. ER ranking (SĐT/lịch sử) → Phase 6.5 theo user. Review vòng 1 tìm 3 lỗi thật (fail-open riskFor / freeze nông / Dart fromJson fail-open) — đã sửa fail-closed + test hồi quy
- [x] **[2026-09-16] Đối chiếu Phase 5 ship vs spec (chi tiết trong checklist.md)**: A/C/C.1/D ✅; B (scrub) chủ đích không làm theo sign-off — nguy cơ Nghị định 13 cần review lại trước khi có khách thật; exit criteria "mobile không giữ key" verify lần đầu bằng quét artifact APK thật (0 hit key). Còn lại: thought_signature live (chờ quota)
- [x] **[2026-09-16] Điều tra file audit biến mất — XÁC ĐỊNH ĐƯỢC NGUYÊN NHÂN (bỏ kết luận sai "cron bên ngoài")**: ① có phiên export `LLM_ROUTER_AUDIT_DIR=/tmp/audit17` (mỏ neo: result17.txt:40 ready line nguyên văn); ② `/tmp` trên host này = **overlayfs ephemeral**; ③ host là **container Google Cloud Shell** (PID 1 = `/google/scripts/onrun.sh`, boot 21:44 UTC 15/09) ⇒ restart container xoá sạch /tmp (giải thích luôn `/tmp/dsh-run` biến mất 2 lần). Đã loại trừ: crontab user (không có), systemd timer (không có), tmpfiles.d (chỉ /tmp 30d), router tự xoá (chỉ `appendFileSync`), bash history (sạch). Đã đính chính result20 + result22 §9C + runbook + skill (4 hàng mới: "đừng set AUDIT_DIR vào /tmp" · "kết luận mất dữ liệu phải có cơ chế từng bước" · "retract thì grep cả chỗ trích lại" · "xác định môi trường thật trước khi debug hệ thống — nhãn 'VPS' trong docs cũ là sai, đây là container"). Không có file nào trong `llm-router-audit/` của repo bị xoá
- [ ] **Chờ user dán 3 giá trị GitHub Settings** (token chỉ-đọc, PUT 404 — result11 §6): Variables `COPILOT_BASE_URL=https://erpn8788.loca.lt` (tunnel HTTPS verify E2E thật — result13; hoặc IP:8788 nếu hosting mở port/Tailscale) + `COPILOT_AUTH_USER` (=`ASK_USER` trong `.env`) + Secret `COPILOT_AUTH_PASSWORD` (=`ASK_PASSWORD` trong `.env`) → CI build APK cài được luôn. ⚠️ tunnel chỉ sống khi `lt` đang chạy trên Mac; lâu dài cần endpoint ổn định (Tailscale/hosting mở port)
- [x] ~~Chờ user chọn đường endpoint LÂU DÀI~~ → **user chốt 2026-09-16: HOÃN — không chọn bây giờ**. Bối cảnh chốt: Cloud Shell = workspace dev (ephemeral, đã hiểu), ERPNext + LLM Mac qua ngrok/localtunnel = **demo tạm để test lúc dev, KHÔNG phải kiến trúc production**; chuyển VPS thật làm SAU khi app xong; không tự thiết kế kịch bản production khi chưa được yêu cầu. Tunnel `erpn8788.loca.lt` + `llm9000.loca.lt` dùng nguyên trạng khi dev
- [x] **Sign-off Phase 5 ĐÃ KÝ (2026-09-15, Hoàng — xác nhận trực tiếp: không scrub, không 2-tier)** — gate MỞ; LLM Router bản đơn giản đã code + test (result14): proxy 127.0.0.1:8900, fallback chain JSON, audit JSONL
- [2026-09-15] Xong + commit **be57052** (đã duyệt, đã push): thought_signature gateway + harness E2E + docs (result17) — 10 files
- [2026-09-15] Xong + commit **8d9f04c**: **result18** — session E2E sau review **CHẶN bởi quota ngày** (6×429 `RESOURCE_EXHAUSTED`, `limit: 20`, `model: gemini-3.6-flash`; 429 bền sau >15 phút im lặng). ⚠️ Con số "20 lần 200/chạm trần" ở bản đầu SAI (đếm thô chưa lọc `attempts` có `gemini`, lại trộn 2 ngày quota — mốc reset ~07:00 UTC; đếm lại **verified: 17 lần 200 = 10 trước + 7 từ 07:00Z**, ngày quota hiện tại mới dùng **7**; audit cũng không ghi các probe curl trực tiếp) → đã sửa ở result18 §B/§E/§J + checklist. Router không lỗi (mọi 502 đều `attempts:[gemini-openai]`); turn replay tool chưa lần nào 200 ⇒ **logic thought_signature vẫn chưa verify live bằng code sau review** (mới hermetic 19/19). Bù lại: **E2E 0-quota XANH** (mock LLM wildcard + mock ERPNext) trả đúng `Nguyễn Thị Lan còn nợ 2.500.000đ`, audit 200×3 turn gồm cả turn replay tool. Thêm `scripts/llm-router.mock.json` (phát hiện: model filter loại mock — đúng thiết kế, cần wildcard để test)
- [2026-09-15] Xong + commit **be57052**: **E2E thật XANH end-to-end** (result17 §K/L) — trả đúng 457.875đ từ ERPNext thật qua dsh; fix root cause `thought_signature` của Gemini 3.x (`ThoughtSignatureCache` + capture stream + inject lại, config theo upstream) → router **17/17**; thêm harness durable + công tắc `E2E_TARGET`. Trước đó cùng ngày: 2 bug cooldown router (6/8 request bị từ chối không thử)
- [x] ~~Chờ user quyết định upstream LLM~~ → **user đã quyết 2026-09-15**: Zen để sau (giữ billing-blocked), Gemini free tier chấp nhận. (result17 đính chính: phần lớn "flaky" trước đây là bug cooldown của router, không phải free tier)
- [x] ~~ERPNext thật 500~~ → user đã khắc phục; **E2E THẬT ĐÃ XANH** (result17 §K, 2026-09-15 14:40): 1 session dsh → router → Gemini → MCP tool → NLP → **ERPNext thật** trả *"...còn nợ **457.875đ** (1 hóa đơn chưa thanh toán)"* — khớp chính xác ground truth tự lấy (ACC-SINV-2026-00047) — dsh exit 0, chịu được 7 spike 503 nhờ fix cooldown. Harness trong repo: `mcp-erpnext/dsh-e2e.patch.yml` (default MOCK · `E2E_TARGET=real` để chạm thật) + `scripts/llm-router.e2e.json`
- [ ] **Verify `thought_signature` LIVE — chạy theo runbook `docs/phase5-thought-signature-runbook.md`** (mốc reset ≥ 2026-09-16 07:00 UTC = 14:00 giờ VN; **1 session duy nhất**, không probe trước, không retry trong ngày). Runbook có: lệnh nguyên văn, định nghĩa XANH (3 điều kiện), bảng phân loại lỗi ĐỎ (429 vs 400-thiếu-signature = bug thật), và cảnh báo **`GEMINI_API_KEY` là key DÙNG CHUNG** — hôm nay audit chỉ ghi 7 request tới gemini mà request đầu đã 429 ⇒ quota tiêu bởi traffic không thấy trong audit, đừng giả định "ngay sau reset là chắc có quota". Luật đếm cũ vẫn giữ — ⚠️ luật đếm đã ĐỔI (result19): ① đếm REQUEST GỬI TỚI PROVIDER, không đếm `status 200` (đếm 200 ra 8<20 mà vẫn 7×429 ⇒ phép đếm cũ SAI); ② mỗi turn dsh ngốn **6–8 request** ⇒ ngân sách 20/ngày ≈ **1 session**; ③ **429 đầu tiên trong ngày quota = hết ngày**, dừng mọi request kể cả probe, chờ reset; ④ chạy việc verify quan trọng vào request ĐẦU TIÊN của ngày quota. Prompt + ground truth ở `result18.txt` §F: 2 khách → **457.875đ** và **171.800đ**, cần ≥2 tool-call (audit phải có turn `messages:7` nhận 200)
- [x] **Commit đợt result18** ✅ `8d9f04c` (đã duyệt + push)
- [2026-09-15] Xong + commit **a379a71** (cùng đợt result20): **result19** — E2E lần 2 sau review **chưa chạy được**: đếm đúng luật §G.1 (8<20, mốc 07:00Z đã được doc xác nhận) rồi chạy 1 session → **7×429**; phân tích thứ tự request (429 từ **#17**, sau đó mọi request đều 429) ⇒ **phép đếm `status 200` bị bác bỏ**, và mỗi turn dsh thực tế tốn 6–8 request provider (ngân sách 20/ngày ≈ 1 session). Đã viết lại luật đếm + luật "429 đầu tiên = hết ngày" vào skill. ERPNext thật đã sống lại (ping 200). `thought_signature` vẫn chưa verify live
- [2026-09-15] Xong + commit **a379a71**: **result20 — upstream `mac-custom` (LLM tự host trên Mac, KHÔNG quota) + E2E XANH 2 tool-call**. Endpoint verify: `https://llm9000.loca.lt/v1` model `oc/big-pickle` (tunnel flaky: 1–2 lần đầu 502/408, thử lại được — 3 lần probe: 502, 502, **200 "Hello! How can I help you today?"**). Thêm vào CẢ 2 config (mac-custom ĐẦU chain, giữ nguyên gemini-openai cho verify thought_signature riêng — `E2E_LLM_MODEL=real-gemini`, không sửa file). E2E thật: dsh exit 0, trả 457.875đ ✓ và 269.000đ cho khách smoke mới. ⚠️ **ĐÍNH CHÍNH (result22 §9B)**: claim "audit 5 req (408, 502, 200×3) + 2 turn replay đều 200" **không còn kiểm chứng được** (không còn file audit cho khung 08:20–08:45 UTC; file gần nhất là `attempts=['mock']`) — bằng chứng mac-custom hợp lệ duy nhất là result22. ⚠️ **PHÁT HIỆN BUG MỚI (vùng tiền — KHÔNG tự sửa)**: `copilot_ask` trả 269.000đ/3 hóa đơn cho khách có credit note, trong khi ERPNext thật = 171.800đ. Root cause traced đến tận dòng code: `listUnpaidInvoices` lọc `outstanding_amount > 0` **loại luôn credit note âm** (−97.200) — tool thô 3.0.4 trả ĐÚNG 5 hóa đơn, NLP resolve ĐÚNG khách, LLM chỉ đọc lại. Hướng xử lý chờ user duyệt: lọc `!== 0` thay vì `> 0` (kèm test)
- [x] **Đã fix credit-note (result21) + commit `6054458`**: `outstanding_amount > 0` → `!== 0` trong `mcp-erpnext/src/skills/customer.mjs` (helper dùng chung cho cả `getCustomerBalance` + sales) + `sales.mjs`; mock thêm credit note SINV-0004 (−320.000) để có test hồi quy; nhãn "hóa đơn chưa trả" → "**chứng từ** chưa thanh toán" + nhánh "hiện dư" khi outstanding âm. Verify THẬT (probe gọi thẳng `answerQuestion` với ERPNext thật, không LLM): **457.875đ/1 ✓** và **171.800đ/4 ✓** (trước fix: 269.000đ/3)
- [x] **E2E đầy đủ qua `mac-custom` — XANH (result22 §4)**: user bật lại `lt` (probe 3/3 HTTP 200) → dsh exit 0, trả **457.875đ/1** ✓ và **171.800đ/4** ✓ (lần đầu câu 171.800đ đi hết chuỗi thật). Audit **4/4 request 200, tất cả `attempts=['mac-custom']`**, gồm **2 turn replay tool** (2 tool-call) — chuỗi đầy đủ dsh → router → mac-custom → MCP → NLP → ERPNext THẬT.
- [x] **Thử `E2E_LLM_MODEL=real-gemini` (đúng 1 lần, không probe trước) — VẪN CHẶN BỞI QUOTA (result22 §5)**: dsh 502 `llm_router_all_failed (tried: gemini-openai)`; audit 7 request đều 502, **provider thật: 5×429 + 2×503** ⇒ trần ngày free tier vẫn cạn (429 vẫn còn ~6h sau lần 429 đầu → củng cố "trần NGÀY"). Router không lỗi (`attempts` không rỗng). ⇒ **`thought_signature` VẪN CHƯA verify live** (lần thứ 4 liên tiếp) — chỉ có bằng chứng hermetic router 19/19
- ℹ️ **`lt` phải đang chạy trên Mac mỗi lần E2E** (không phải việc còn treo — E2E đã xanh khi tunnel sống). Tunnel `llm9000.loca.lt` chết khi `lt` tắt; `/tmp` bị dọn giữa phiên còn xoá luôn dsh → phải cài lại (`npm i @deepseek-ai/dsh`, bản 0.1.5-rc.1) trước khi chạy
- [ ] **Chờ người thật thu audio 150 câu** — kịch bản sẵn: `docs/audio-collection-script.md` (blocker Phase 4)
- [ ] Sau khi APK chạy thật OK: sync + archive OpenSpec change `flutter-chat-mvp` (task 4.3), merge branch về master
- [ ] Khi AgentMemory sống lại: `/remember` pointer tới 2 skill `erpnext-mcp-connect` + `erpn-verify-first`

## Đã xong gần đây

- [2026-09-15] Xong + commit **6054458** (đã duyệt + push): fix credit-note net (`!== 0`) + nhãn "chứng từ" + test hồi quy + 2 batch runner guard + hermetic `ASK_*` (result21) — 18 files
- [2026-09-15] Xong + commit **8ebfc0e** (gộp vào đợt Phase 7 Stage A): **result22** — E2E `mac-custom` XANH: 4/4 request 200, `messages` 2→5→7→9 (≥2 turn mang tool result), 457.875đ/171.800đ khớp ground truth; real-gemini vẫn 429 (thought_signature chưa verify live). Số tool-call CHÍNH XÁC không chốt được từ transcript headless (nó không in khối tool result) — chỉ dám khẳng định "≥2 turn replay". Phát hiện: /tmp bị dọn giữa phiên ⇒ luôn kiểm tra `dsh/lib/bin.js` tồn tại trước E2E
- [2026-09-15] Xong + commit **6054458** (đã duyệt + push — vùng tiền): **result21 — fix credit note + review vòng 2 (3 lỗi thật)**. Fix `!== 0` (2 skill) + nhãn trung tính + nhánh dư tiền; verify thật: **457.875đ/1 và 171.800đ/4** khớp ground truth. Review đọc diff nguyên văn tìm thêm 3 lỗi trong chính đợt này: **(A)** `node --test` discover MỌI file trong `test/` nên 2 batch runner (18 câu + dump khách thật) bị chạy như unit test — trong shell đã `source .env` sẽ bắn vào ERPNext THẬT và in tên khách/số tiền thật; fix guard `NODE_TEST_CONTEXT` (probe xác định: `child-v8` vs unset — guard argv[1] một mình SAI vì runner spawn từng file); **(B)** `http-ask.test.mjs` treo cả suite >120s: shell leak `ASK_USER/ASK_PASSWORD` (test chỉ strip `ERPNEXT_*`) → throw trong setup → child Python rò → không exit; fix strip `ASK_*` + try/finally; **(C)** 5 kỳ vọng eval `"(N hóa đơn"` thành fail GIẢ sau khi đổi nhãn → đổi sang `DOC=/chứng từ|hóa đơn/` + chấm theo số tiền/số lượng. Test thật: Python 58/58 · mcp-erpnext 50/50 (9.9s, trước đó treo) · Flutter 14/14 · openspec valid · secret scan CLEAN
- [2026-09-15] Xong + commit **e2f5caf** (đã duyệt, đã push): result15 (router real upstreams + dsh cơ chế thật) + result16 (crash/hang review 7 lỗi + 3 regression test) — 16 files
- [2026-09-15] Xong + commit **49317a7** (đã duyệt, đã push): review vòng 2 trên fix của mình (result16 §6C) — 5 window-sau-await còn sót + stripFields validation; +2 regression test; router 10/10 · Node 49/49
- [2026-09-15] Xong + commit **be57052** (cùng đợt result17): **E2E 1 session sau reset quota (result17)** — quota reset ĐÚNG (agent turn 200 từ gemini-3.6-flash) nhưng session fail vì **2 bug thật của router mình viết**: cooldown làm chain RỖNG → 6/8 request bị từ chối không thử (audit `attempts: []`), + message sai "all upstreams failed (tried: none)". Đã fix (cooldown chỉ deprioritise + error type thật) + 3 test → **router 13/13**. Phát hiện an toàn: **dsh TỰ load `.env` từ cwd** → `unset ERPNEXT_*` vô hiệu, mock bị ghi đè thành REAL → fix literal `''`, verify `mock -> mock (in-memory)`. Harness durable: `mcp-erpnext/dsh-e2e.patch.yml` + `scripts/llm-router.e2e.json`. ERPNext thật đang 500 (user)
- [2026-09-15] Xong + commit **87fcbb1** (đã duyệt, đã push): security review fixes (CGNAT boundary, constant-time auth, 401 UX, CI env-injection) + LLM Router bản đơn giản + sign-off Phase 5 (không scrub) + LOCAL-TEST doc — result14
- [2026-09-15] Xong: **router nối upstream thật** (result15) — endpoint chính thức zen/gemini; tự bắt 2 bug router chạy thật (https transport, field `store` Gemini từ chối → stripFields) + LLM_ROUTER_DEBUG; **Gemini generate thật 200 qua router** · Zen chặn billing (CreditsError); cơ chế dsh thật = cordis patch row (result6 settings.yaml lỗi thời) → skill mới `erpn-dsh-setup`; E2E dsh→Gemini flaky do free tier 20 req/phút (429/503 nguyên văn result15 §6). Router tests 7/7 · mcp-erpnext 49/49

- [2026-09-14] Xong + commit **c9bc64d**: `result11.txt` + checklist — endpoint security verified (401→200→269.000đ thật), firewall finding, kịch bản thu âm
- [2026-09-14] Xong + commit **c3d74c3**: endpoint security (bind policy + basic auth, 8 test mới → Node 48/48) + Flutter client auth qua dart-define (Flutter 13/13, analyze 0) + CI APK từ repo Variables/Secret + `docs/audio-collection-script.md` (150 câu 3 miền có GT) — CI run #3 SUCCESS
- [2026-09-14] Xong: **APK nối được VPS** — bind policy tự từ chối unsafe config; verify thật trên 10.88.0.4; phát hiện firewall hosting chặn port từ ngoài + token GH chỉ-đọc (2 finding môi trường, không phải lỗi code)
- [2026-09-14] Xong: tạo `.project/ai-rules.md` (file KHÔNG tồn tại trước đó — verify-first bắt được; tổng hợp từ AGENTS.md + operating_rules + thực tế duyệt qua result1→11; CHỜ USER REVIEW) + cập nhật skill `erpn-verify-first` với 7 bài học result11
- [2026-09-14] Xong + commit **2946206**: `result10.txt` (CI run #2 SUCCESS + APK artifact về VPS) + `SIGNOFF-phase5-pii.md` (draft, gate ĐÓNG)
- [2026-09-14] Xong + commit **4c5bd26**: viết lại checklist/next 1 lần cho nhất quán + `.project/` + memory files
- [2026-09-14] Xong + commit **119edd4**: fix result9 — accuracy thật 27.8%→100% (5 nhóm lỗi) + CI codegen step; redact secret khỏi result9.txt trước commit
- [2026-09-14] Xong + commit **590b1b2**: Phase 3 Flutter chat MVP (64 files) — UI-checkpoint user duyệt; Node 36/36, Flutter 13/13; 4 bug thật (quan trọng nhất: ChatBubble không render answer)
- [2026-09-14] Xong + commit **0ac8e61**: Phase 2 thật — env-switch mock/real, resolver chống trùng tên, dsh smoke (mock LLM), `result6.txt`
- [2026-09-17] Review P3 tìm + fix 1 fail-open env-number trong classifier (commit 451cd8f)
- [2026-09-17] Device smoke test qua adb (Pixel 3a): 0 crash, Settings OK, footer fix f28869a xác nhận trên máy thật; E2E từ phone vào real ERPNext — "Lan" không có trong real catalog (75 khách test) ⇒ refuse đúng thiết kế; ground truth mới: P1F-ACCEPT T9 = 1.000.000đ
- [2026-09-14] Xong + commit **33f9dc0**: Phase 1 NLP pipeline (root commit 55 files)

- [2026-09-17] **P3 (phases2) ✅ ĐÃ COMMIT `c38e4ea` (đã push)**: `classifier.mjs` semantic-only qua LLM Router (không agent loop/DSH) · không bao giờ trả ERP id (regex ID_LIKE + allowlist 2 lớp) · intent ∈ contract (forbidden không offerable) · `routeByCapability` → cùng skill/Safety path · LLM down/timeout → rule-only UNKNOWN, low confidence → LOW_CONFIDENCE · golden classifier regression 9 case (mock LLM, CI gate) · falsify F1/F3/F4 trên /tmp · fix test flaky có sẵn (bucket P1 dedup). Suite: Python 60 · Node 195 · Flutter 69 · analyze 0. `.plan/phases2/p3-result.md` + `result47.txt`.
- [2026-09-17] **P2 (phases2) ✅ ĐÃ COMMIT `33ff725` (đã push)**: taxonomy 11 mã + copy tiếng Việt (uncertainty.mjs) · session context provenance+TTL (session-context.mjs) · WRITE fail-closed theo provenance · Flutter PipelineProgress 4 pha · falsify 3 luật trên /tmp. Lỗi tự sửa: stub check nuốt forbidden (đổi thứ tự + comment ORDER MATTERS). Suite: Python 60 · Node 181 · Flutter 69 · analyze 0. `.plan/phases2/p2-result.md`.
- [2026-09-17] **P1 COMMIT `ee93f13` (user duyệt, đã push)**: entity 4 trạng thái + picker + snapshot + codes tách + state machine + dedup + NLP-down block + test concurrent. `p1-result.md` 9/9 deliverable (saga optional bỏ qua có lý do). Suite: Python 60 · Node 173 · Flutter 67 · analyze 0. Bài học mới: shell giữ `ERPNEXT_*` ⇒ test mock lặng lẽ đi server thật (chạy `env -u ERPNEXT_URL … node --test`) + test concurrent phải pin hợp đồng quan sát được (409 fail-closed), không pin kỳ vọng tự suy.
- [2026-09-17] P1 kỹ thuật xong (trước commit): entity 4 trạng thái + picker + snapshot + codes tách + state machine + dedup + NLP-down block. Review vòng 2: fix harness Flutter (`_bodyOf` — dio đưa Map nguyên vào adapter), soi cancel/entity_id wiring (đã đúng). Suite: Python 60 · Node 172 · Flutter 67 · analyze 0. Bằng chứng `result45.txt`.
- [2026-09-17] P0 commit `b4acdb1` + docs/assets `0ce2893` (đã push); review P0 sửa 3 lỗi thật + falsify (result44); REDACT key cũ trong `result10.txt` (đã rotate từ trước).

## Việc kế tiếp khi user sẵn sàng (thứ tự)

1. User: dán 3 giá trị GitHub Settings + chọn đường endpoint (Tailscale/mở port/ngrok) → re-run CI → tải APK cài thiết bị thật
2. Người thật: thu audio 150 câu theo `docs/audio-collection-script.md` → mở khóa Phase 4 (STT)
3. P4 (Learning loop) theo `.plan/phases2/` — P3 đã commit `c38e4ea`, không còn gap chặn
- (Đã xong trước đó: sign-off Phase 5 ký 2026-09-15 · rotate key ERPNext)
- [2026-09-18] P5 dsh opt-in commit 6318eca (READ-only gate + D2/D8 proofs)
- [2026-09-18] P4 learning loop commit d7e9ba9 (JSONL log + cluster + workflow + vòng thử "doanh số")
- [2026-09-18] **P7 (phases2) queue job nền — kỹ thuật xong, CHỜ DUYỆT COMMIT (vùng tiền)**: `job-queue.mjs` (JSONL repo-local, enqueue chỉ khi verdict `retry_same_command_id`) + review vòng 2 tìm 5 finding thật: **F-A** job enqueue nhưng KHÔNG ai gọi `drain()` (exit criteria P7 gãy ngoài test) → thêm `startJobRunner()` timer trong main(); **F-B** job kẹt RUNNING sau crash → `_load()` chuyển RETRYING; **F-C** `/jobs` chỉ trả pending → thêm `completed()`; **F-D** cancel không nhả job → `release()`; **F-E** import chết. Falsify 4 guard trên /tmp (đều đỏ đúng chỗ). Suite: Python 60 · Node 228 · Flutter 69 · analyze 0. `result50.txt` + `.plan/phases2/p7-result.md`.
