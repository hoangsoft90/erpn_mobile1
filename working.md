# working.md — Nhật ký đang làm

> Việc cụ thể gần đây, format ngày `YYYY-MM-DD`. Dọn mục đã xong quá 1–2 tuần
> (nội dung cũ nằm trong `result*.txt` + handoff + LESSONS_LEARNED.md là bằng chứng).

- [ ] **[2026-09-19] TTS "ĐỌC CÂU TRẢ LỜI" (mobile client, theo `.plan/next2/tts-implementation-plan.md`) — ⏳ CHỜ DUYỆT COMMIT**: `flutter_tts 4.2.5` (on-device, `vi-VN`, rate 0.55) · setting `ttsEnabled` theo ĐÚNG khuôn `AppSettingsService` (`settings_tts_enabled_v1`, fail-safe mọi nhánh hỏng = OFF) + SwitchListTile "Đọc câu trả lời" trong Settings (**không dialog** — không nằm trong đường tiền) + ghi chú gói tiếng Việt · `<queries>` `TTS_SERVICE` trong `AndroidManifest.xml` (Android 11+ ẩn engine ⇒ `speak()` im lặng) · **`TtsService` interface (mới)** chỉ có `speak`/`stop` — KHÔNG method nào confirm/execute ⇒ không tồn tại đường TTS → ghi · hook **DUY NHẤT** tại `ChatController._append()` (không ở `build()`/`attachRejection()`); `pickEntity` đi qua `send()` nên vẫn được đọc (câu trả lời thật sự mới) · chỉ đọc `answer` + `proposal.summary`, sanitize gỡ markdown + **mọi token dạng UUID** · `speak()` no-op IM LẶNG khi máy không có gói tiếng Việt · try/catch quanh `speak` ⇒ TTS không bao giờ làm hỏng kết quả chat · **+21 test ⇒ Flutter 141** (120→141) · analyze 0 · Python 62 · Node 267 · **falsify A–F** (A ttsEnabled gate · B uuid stripper · C rò JSON máy · D try/catch · E auto-confirm nhét vào card · F nuốt proposal) — **C và E lần đầu XANH OAN ⇒ lộ 2 oracle RỖNG của chính tôi** (đã sửa, xem dưới) · **2 lỗi tự gây đã sửa**: (1) test "không có request /execute" gắn recorder vào Dio RIÊNG của `copilotApiClientProvider` trong khi card POST qua `dioProvider` ⇒ confirm thật không thể quan sát → override `dioProvider`; (2) khẳng định "không có UUID" bị chính sanitizer che ⇒ thêm khẳng định HÌNH DẠNG nguồn (`{`, `}`, `schema`, `params`, `risk_display`) · **[review vòng 2 — `result56.txt` §9, 2 lỗi THẬT đã sửa]** (F1) `await speak()` (đúng snippet plan §4) ⇒ engine TTS treo làm `send()` không bao giờ return ⇒ **stuck UI** (ô nhập không xoá); CHỨNG MINH bằng fake `Completer` không bao giờ complete ⇒ `TimeoutException` đỏ thật → sửa `unawaited(_maybeSpeak(turn))` + try/catch **bao trùm cả cổng** (lệch plan §4 CÓ Ý THỨC) · (F2) oracle DỄ VỠ: `textContaining('vẫn phải bấm Xác nhận khi thu tiền')` dùng chung đuôi câu với switch auto-send ⇒ đo thật khi bật cả 2 = **2 matches** → neo vào câu chỉ TTS có · (F3) 2 test bắt buộc của plan (#3 `build()` không đọc, #4 `attachRejection()` không đọc) **chưa từng falsify** → đã falsify G/H, đỏ đúng chỗ · **falsify A–H = 8/8** · **Flutter 142** (141→142) · analyze 0 · **[TRỞ NGẠI — xem `result56.txt` §10]** yêu cầu "commit TTS riêng" **KHÔNG tách được ở mức file**: `voiceAutoSend` (P6 UX) = 0 trong HEAD ⇒ 4 file trộn (nặng: `settings_screen.dart` 9/9, `settings_test.dart` 22/21), nghẽn ở `final ok = okGateway && okSubmit && okVoice && okTts;` (một hunk, TTS phụ thuộc `okVoice`) ⇒ commit chỉ-file-sạch sẽ **không build được**; **chưa stage/commit gì**, chờ user chọn (a) commit P6 UX trước rồi TTS lên trên, hay (b) 1 commit gộp có ghi rõ.
- [ ] **[2026-09-18] SETTINGS "TỰ GỬI SAU KHI NÓI XONG" (P6 UX, mobile client) — ⏳ CHỜ DUYỆT COMMIT**: switch mới trong Settings, **mặc định OFF** (giữ hành vi P6: đọc → xem/sửa → bấm Gửi). Bật ON ⇒ sau kết quả STT **final**, nếu ô nhập có text (trim ≠ rỗng) và không đang loading thì gọi **cùng** `onSend()` (POST `/ask`) — không path riêng, **không** auto-confirm WRITE, **không** đụng `/execute`; partial và transcript rỗng **không** trigger (rỗng còn **không** đụng ô nhập). Key versioned `settings_voice_auto_send_v1`, fail-safe mọi nhánh hỏng = OFF. Bật ON **không cần dialog** (khác F7-2 — không nằm trong đường tiền) nhưng copy ghi rõ "vẫn phải bấm Xác nhận khi thu tiền". **+11 test** · **falsify 3 guard** (B lần đầu KHÔNG đỏ vì có 2 lớp check trùng — đã gỡ lớp không thể chạm) · **Flutter 118** (107→118) · analyze 0 · 3 lỗi tự gây đã sửa (guard trùng · test cũ đỏ vì `SettingsScreen` là `ListView` LAZY không build phần tử dưới màn hình ⇒ cho viewport test cao 2400px · `textContaining` mơ hồ khi có 2 switch) · bằng chứng `result55.txt` §10 · **[review vòng 3 — `result55.txt` §11]**: gỡ **guard chết thứ 3** trong `_maybeAutoSend` (nay chỉ còn ĐÚNG 1 điều kiện `voiceAutoSend`; falsify C xanh toàn bộ ⇒ chứng minh không thể chạm; lớp chặn gửi chồng thật là `ChatController.send` khi `isLoading`) · sửa **2 doc sai** (R5 `_InputBar` còn ghi "no auto-send"; R6 `localeVerified` còn hứa "soft note") · sửa **báo sai kết quả lưu** (R8 `ok = okGateway && okSubmit && okVoice` — trước đó switch có thể hiện ON mà KHÔNG lưu được) · **+2 test ⇒ Flutter 120** · analyze 0 · ❓ **câu hỏi mở**: `pauseFor: 3s` (Android có thể ép 1–3s) ⇒ khi switch BẬT, ngập ngừng giữa câu = gửi câu nửa vời — đã ghi `human.md` §1, **chưa tự đổi hành vi**, chờ user chọn (a/b/c).
- [x] **[2026-09-18] BUGFIX P6 ĐÃ COMMIT + PUSH + APK CI ✅** — `77e2b57` (fix, 4 file code) · `bbca7ca` (docs) → `change/flutter-chat-mvp`; **APK run `35367603981` success** (artifact `erpn-chat-debug-apk` 84.169.705 bytes) và **xác minh binary**: `grep -c` trong `kernel_blob.bin` của APK = câu mới 2 lần, **câu SAI cũ 0 lần**, `vi_VN` 3 lần (⇒ APK không phải build cũ). Còn lại: cài máy thật + smoke mic (`human.md` §2). Chi tiết `result55.txt` §8.
- [x] **[2026-09-18] BUGFIX P6 — cảnh báo sai "Máy không có bộ nhận dạng tiếng Việt" (mobile client)**: user báo trên máy thật "Gboard hiểu tiếng Việt nhưng app báo máy không có bộ nhận dạng tiếng Việt". Nguyên nhân gốc (doc plugin `locales()`, nguyên văn): *danh sách này CHỈ phủ recognizer ON-DEVICE, có thể thiếu ngôn ngữ mà recognizer ONLINE vẫn nhận* — code cũ chỉ lấy `localeId` từ `locales()` ⇒ không thấy `vi` thì trả `null` ⇒ (a) UI kết luận "máy không có tiếng Việt" (sai bản chất), (b) `listen(localeId: null)` để OS tự chọn locale. Sửa 6 điểm: luôn xin `vi_VN` (`fallbackLocaleId`, kể cả list rỗng) · tách tín hiệu `localeVerified` (list ≠ năng lực) · platform từ chối locale đã xin ⇒ **retry 1 lần** với mặc định máy · `error_language_not_supported`/`_unavailable` bỏ locale ép nhưng **không** tắt service · UI **không hiện gì về locale** (UX follow-up cùng ngày: bỏ luôn gợi ý nhẹ — tín hiệu user không hành động được thì đừng hiển thị; cảnh báo THẬT denied/unavailable/error_language_* vẫn giữ, có test) · nới copy `unavailable`. **+6 test** (service 6→11: list thiếu `vi` · list rỗng · có `vi` ⇒ không retry · từ chối ⇒ `[vi_VN, null]` + `listening` · đã xác nhận ⇒ lỗi thật vẫn `unavailable` · refusal ngôn ngữ ⇒ service sống; widget 15→16: hint nhẹ + vẫn nghe + vẫn không gửi) · **falsify 4 guard A–D** (D = tái tạo ĐÚNG code cũ ⇒ đỏ đúng ca bug) · **Flutter 107** (101→107) · analyze 0 · Python 62 · Node 267 · **4 lỗi tự gây đã sửa** (falsify sai vị trí · trùng tên field/getter trong fake · `textContaining` phân biệt hoa/thường · `dart format` tall-style reformat cả file ngoài phạm vi → phải revert churn) · bằng chứng `result55.txt` + `.plan/phases2/p6-result.md` mục "Bugfix P6". Còn lại: cài APK + smoke mục 7 (máy thật).
- [x] **[2026-09-18] P6 (phases2) — VOICE / STT ✅ ĐÃ COMMIT `9b54d35` (đã push)**: gate 150 câu audio ĐÃ BỎ (user 2026-09-18) ⇒ dùng **STT của OS** (`speech_to_text` 7.5.0), không tự host model, không corpus riêng · luồng đúng `plan2_final` §20: mic → STT → text vào **CHÍNH ô nhập editable** → user sửa → Gửi → `POST /ask`; **không auto-Send**, interface `SpeechService` không có method nào gửi/execute ⇒ **không tồn tại đường voice → WRITE** · `AndroidManifest.xml`: `RECORD_AUDIO` + `<queries>` `android.speech.RecognitionService` (repo không có target iOS ⇒ keys `Info.plist` ghi lại trong result) · locale `vi_VN` + cảnh báo fallback; dictation giữa lúc đang gõ không xoá chữ đã viết; partial **thay** không nối; `dispose()` đóng mic · **21 test** (15 widget mock STT + 6 unit trên subclass plugin thật) · **+ falsify 8 guard** · **self-review tìm 4 lỗi thật đã sửa** (kết quả STT muộn ghi đè ô nhập đã gửi — plugin doc xác nhận "stopping a listen session will cause a final result to be sent" ⇒ guard UI + session token ở service · thông báo che "Đang nghe…" · double-tap mở 2 phiên · `cancel()` không thực thi hợp đồng) · **Flutter 101** (80→101) · analyze 0 · bằng chứng `.plan/phases2/p6-result.md` (bị gitignore). Còn lại: smoke mic máy thật (human) — **APK CI đã build + xác minh** trong mục bugfix ở trên.
- [x] **[2026-09-18] P8 (phases2) — MULTI-USER / RBAC / COMPANY SCOPE ✅ ĐÃ COMMIT `90401f0` (đã push, user duyệt trong chat)** (vùng phân quyền + tiền): **`src/authorization.mjs` (mới)** — boundary server-side đọc từ `capabilities.json`, không hardcode capability nào trong logic (grep = 0) · **2 chế độ**: `multi_user` (`COPILOT_USERS` JSON tường minh; user lạ degrade read-only, không crash; JSON hỏng ⇒ NÉM lỗi thay vì im lặng "mọi người được phép") và `single_tenant` mặc định (giữ hành vi cũ — **đo thật**: gỡ miễn trừ ⇒ **33 test đỏ**, 267→234) · company **server-first** (principal → `COPILOT_COMPANY` → request) + allow-list; multi-user thiếu company ⇒ `COMPANY_SCOPE_REQUIRED` (**map về copy P2** `AUTHORIZATION_DENIED` để user luôn có chữ) · **wire 4 cửa**: `/ask` chặn **trước** `route.factory()` (không proposal, 0 ERPNext read) · `/execute` bước 1b **trước** idempotency (403, **không tiêu `command_id`**) · `/jobs` lọc theo actor (+`hidden` counter) · `/execute/cancel` chỉ chủ lệnh hoặc người có quyền (record cũ không actor ⇒ fail-closed) · job replay mang `user_id` gốc ⇒ phân quyền **tính lại tại thời điểm ghi** · audit `user_id`+`company` vào record idempotency + job; `logEvent` `/execute`+`/cancel` dùng `principal.user_id` (**một khái niệm "ai" duy nhất** cho rate-limit/audit/job report) · **17 test mới** (`test/p8-authorization.test.mjs`) · **+ falsify 13 guard** (A–M, đỏ đúng chỗ, khôi phục diff sạch) · **self-review tìm 5 lỗi thật đã sửa**: rò `/jobs` chéo user · cancel không kiểm ai · `COMPANY_SCOPE_REQUIRED` không có copy · audit ghi sai người (credential ≠ principal) · allow-list là nhánh chưa từng được test (falsify 14/14 xanh) · **Node 267** (250→267) · Python 62 · Flutter 101 · analyze 0 · bằng chứng `.plan/phases2/p8-result.md` + `docs commit 2775d2f`. Còn lại: `COPILOT_USERS`/`COPILOT_COMPANY` thật cho deployment + user ERPNext thứ 2 = human gate.
- [x] **[2026-09-18] Phases2 P0–P8 ĐÓNG — toàn bộ đã commit + push**: P0 `b4acdb1` · P1 `ee93f13` · P2 `33ff725` · P3 `c38e4ea` · P4 `d7e9ba9` · P5 `6318eca` · P6 `9b54d35` · P7 `3e6240a` · P8 `90401f0` · P10-slice `7cb2798`/`21d77ff`/`d6295ab`. Suite tham chiếu: **Python 62 · Node 267 · Flutter 101 · analyze 0**.
- [x] **[2026-09-18] ĐỒNG BỘ `.project/` theo `ai-rules.md` (mục 1: kiến thức tĩnh cập nhật sau phase lớn)**: `architecture.md` (sơ đồ gateway 4 route + 7 lớp an toàn của lệnh ghi + module map P0–P8 + data flow có voice/picker/proposal) · `overview.md` (tóm tắt 1 câu mới + stack `speech_to_text`/tên app "Nghiệp Vụ AI" + trạng thái rút gọn P0–P8) · `patterns.md` (+8 pattern mới: Safety Gateway single door · contract-driven authz · fail-closed refusal có copy · human-approved learning · voice là input modality · snapshot frozen at ask-time · nhiều lớp độc lập · phases2 supersede) · `integrations.md` (CI trigger paths — backend/docs không trigger; LLM Router upstreams; authz env config) · `modules/chat.md` + `modules/copilot-backend.md` (endpoints `/execute` `/cancel` `/jobs`, files mới speech_service/proposal_card/entity_picker/settings, storage settings keys) · `state-routing.md` (route `/settings` + provider map `appSettingsService`/`speechService` + ChatState/proposal/command_id) · `design-system.md` (+4 widgets, luật banner/uncertainty copy) · `openspec.md` + `README.md` (trạng thái + định hướng). KHÔNG nhân bản progress vào `.project/` (luật ai-rules §1) — chỉ pointer + kiến thức tĩnh.
- [x] **[2026-09-18] Đợt trước (cùng ngày)**: F7 policy (a) `3b41313` (bảo trì không phải một lần thử) · F7-2 submit switch `eb4ba34` (setting "Cho phép nộp phiếu thu thật", frozen vào snapshot lúc hỏi; submit lỗi giữa chừng ⇒ PARTIAL không FAILED) · golden gaps k18/m15 `c5db7cc` ("một triệu hai" = 1.200.000; "Con Linh" resolve "Linh"; Golden runner 0 miss ⇒ **gate P9 mở**) · docs `46faf37` (result54 evidence). Chi tiết `result53.txt` + `result54.txt`.
- [x] **[2026-09-18] LESSONS_LEARNED.md + skill `erpn-verify-first` cập nhật sau P8** (5 bài học mới): một khái niệm "ai" duy nhất khi thêm identity · nhánh chết = test đi nhánh anh em (falsify từng nhánh, đếm pass/fail) · mã refusal mới phải đối chiếu bảng copy UI · "đo thật" = một lần chạy suite ghi con số (33 đỏ thay cho ước lượng) · đóng phase phân quyền phải duyệt từ danh sách ROUTE (`req.method ===`) chứ không chỉ call site của hàm gate.

## Việc kế tiếp khi user sẵn sàng (thứ tự)

1. **P9 (skill mới hàng loạt)** — gate ĐÃ MỞ (Golden 0 miss, P0–P3 acceptance đạt); CHỜ LỆNH user, không tự bắt đầu.
2. Human gates: `COPILOT_USERS`/`COPILOT_COMPANY` thật cho deployment + user ERPNext thứ 2 (P8) · build APK CI + smoke mic máy thật (P6) · smoke 2 bước trong `p8-result.md`.
3. Còn hoãn: saga §7 (chờ duyệt riêng) · P10 full (DR drill, dashboard, load test, rate-limit store phân tán) · thu audio → Phase 4 cũ (đã supersede bởi P6) · thought_signature live verify (cơ hội khi Gemini còn quota).
4. Sau khi APK chạy thật OK: sync + archive OpenSpec change `flutter-chat-mvp`, merge branch về master.
