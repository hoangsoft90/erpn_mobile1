# working.md — Nhật ký đang làm

> **Trạng thái mở — cập nhật 2026-09-26** (mục mới nhất nằm ngay dưới; dọn mục cũ hơn 1-2 tuần)
> - **next6/Prompt-6 — REGRESSION GATE TOÀN NEXT 6 — ✅ ĐẠT, CHƯA COMMIT (2026-09-26)**.
>   Đối chiếu `next6_final.md` §11: **PASS toàn bộ 26 dòng criterion** (Identity 3 · Session 3 · Isolation 4 · DSH 4 ·
>   WRITE 4 · REAL-only 3 · Quality 5). **Không sửa gì thêm** (chỉ Prompt-5 commit `0ef6334` là blocker đã vá) — không
>   mở scope, không tăng DSH concurrency, không thêm mock. **Focused**: session 19/19 · safety-gateway 10/10 ·
>   http-execute 18/18 · command-store 4/4 · payment-write 44/44 · correlation 4/4 → **99/99**. **Full**: Node
>   **847/845/2** (2 fail dsh env B0) · `flutter analyze` **0** · Flutter **344/347** (3 fail `_probe_review_test`
>   có sẵn, A/B proof). **REAL E2E trên site THẬT** (không `COPILOT_MOCK_OK`): `probe-p4-smoke-real.mjs --dry-run`
>   PASS (REAL MCP child spawned, đọc invoice `ACC-SINV-2026-01239`) + fresh REST read `ACC-PAY-2026-00749`
>   **docstatus=1** (khớp reference_no/action_id). Definition of Done **PASS** (6 test chứng minh cách ly
>   principal/conversation + `DSH_MAX_CONCURRENT` default=1). Báo cáo: `.plan/next6-final-report.md`. **Chưa push**.
> - **next6/Prompt-5 — REAL-only · API contract `/dsh/ask` · OBSERVABILITY — ✅ ĐẠT, CHƯA COMMIT (2026-09-26)**.
>   OpenSpec change `next6-real-contract-observability` (`openspec validate` **valid**). Audit code trước khi sửa
>   cho thấy phần lớn đã đạt từ Prompt-2→4; **2 gap thật đã vá**: (1) 3 `logEvent` phase `dsh_ask` trong
>   `dsh-gateway.mjs` (refused/failed/answered) thiếu correlation ⇒ thêm `request_id`/`user_id`(principal server-side)/
>   `conversation_id`/`dsh_in_flight`/`outcome`/`latency_ms` (không thêm log line mới, không đổi HTTP shape).
>   (2) Flutter **persist `conversation_id` từ response**: `DshAnswer.conversationId` (additive) +
>   `ConversationIdService.save()` + controller adopt id server cấp (cùng id ⇒ no-op); provider.dart vẫn chỉ
>   gián tiếp chạm 2 file. **API contract**: `user_id` body KHÔNG override principal server-side (thêm test chứng minh,
>   code vốn đã đúng); `conversation_id` malformed ⇒ 400; omitted ⇒ server mint `randomUUID()`. **REAL-only audit**:
>   grep `mock|fake|fallback|...` 324 match — KHÔNG runtime path nào tự fallback sang mock (mock chỉ opt-in
>   `COPILOT_MOCK_OK=1`/`OCR_PROVIDER=mock` hoặc test boundary; `OCR_PROVIDER` thiếu config ⇒ hard error).
>   **Số đo (thật)**: Node file Prompt-5 **19/19** · full Node **847/845/2** (2 fail `dshGatewayHealth`+`verifyDshRuntime`
>   môi trường từ B0) · `flutter analyze` **0** · Flutter vùng chạm **17/17** · full Flutter **344/3**
>   (3 fail `_probe_review_test.dart` RenderFlex — **CHỨNG MINH có sẵn** bằng A/B: gỡ hết 3 edit Prompt-5
>   probe vẫn fail y hệt, restore sha256 khớp). Không đổi auth model/Payment lifecycle/session isolation.
>   Bằng chứng: `.plan/next6-result5.md`. **Chưa commit** — chờ user duyệt.
> - **FIX P4-6 "Hôm qua" (phát hiện ngoài-scope D4) — ✅ SỬA XONG, CHƯA COMMIT (2026-09-26)**.
>   Chọn **Hôm qua** trước đây **đổi tiêu đề thẻ bán/thu nhưng số vẫn của hôm nay** ⇒ nay 2 thẻ
>   (Hóa đơn đã xuất, Tiền khách trả) hiện **đúng số của ngày đang xem** (`_yesterday`); đọc lỗi/chưa xong
>   ⇒ trạng thái *"Chưa đọc được ngày hôm qua"* + `Thử lại`, **KHÔNG hiện số hôm nay** (fail-closed);
>   drill mang `date = d.meta.date`. **Bỏ dòng delta** (user chốt hướng A + bỏ delta ⇒ xoá `_deltaLine` +
>   2 key `invoices-delta`/`receipts-delta`). **Số đo**: Flutter **333/333** · analyze **0** ·
>   falsify `p44-read-drill` **38/38 RED** (thay 3 ca delta cũ bằng 3 ca bảo vệ fix).
>   **Không loop thật trên máy** (sửa UI) — chờ user kiểm tay; docs cập nhật khớp (features/checklist/next/faq §10.3).
> - **next5/D5 — DOCS + ĐÓNG next5 — ✅ ĐẠT (2026-09-26)**.
>   (1) `.plan/next5/next5-drawer-done.md` (MỚI): đối chiếu **§8 exit criteria 10/10 ĐẠT** từng dòng + bằng chứng
>   từng phase; khai thẳng NO-GO row-level (D1.5) + dedupe D3 chỉ unit test.
>   (2) `features.md` mục Trụ D: đổi tiêu đề thành "Drawer READ (plan4 V1 → next5 hoàn thiện)" + block next5
>   (5 entry có drill id · REAL-only/provenance · **footnote công nợ verbatim** · polish D4 · verify thật · số đo).
>   (3) `docs/how-to-test.md` (MỚI): checklist tay drawer — mở từng mục + số khớp ERPNext · title ngày (D4) ·
>   refresh list ngắn · error/empty/config kho · đổi server/user · badge · ranh giới CẤM.
>   (4) README next5 trỏ **ĐÃ ĐÓNG** + bảng kết quả từng phase; `checklist.md` + `next.md` bổ sung D4 + D5.
>   **Không code feature mới** (đúng lệnh D5); không chạm aging/activity log. **Chưa commit** — chờ user duyệt.
> - **next5/D4 — POLISH (refresh · tiêu đề theo ngày · clear cache · badge · bound 7s) — ✅ ĐẠT, 6/6 mục (2026-09-26)**.
>   (1) **Bug thật do test bắt, không do đọc code**: `RefreshIndicator` **im lặng không chạy** trên list NGẮN —
>   `ListView` vừa khung không nhận drag ⇒ không có overscroll notification ⇒ drawer 0–2 dòng không
>   "kéo xuống để đọc lại" được. Sửa `physics: AlwaysScrollableScrollPhysics()`.
>   (2) **Tiêu đề drill nay nêu ĐÚNG ngày server đã đọc** (trước: contract "Hóa đơn hôm nay" gửi nguyên văn
>   ⇒ màn tiền tự nhận sai ngày khi Tóm tắt ngày đang ở "Hôm qua"): contract += `day_scoped: true` cho 3 drill
>   ngày + title **ngày-trung-tính**; validator **từ chối** title có sẵn chữ ngày khi `day_scoped`;
>   `drillDayWord(date, today)` ghép "hôm nay"/"hôm qua"/"ngày YYYY-MM-DD" từ **ngày thực đọc** + đồng hồ SERVER
>   (`today: vnToday()`), drill toàn công ty **giữ nguyên** title dù request mang date.
>   (3) **Clear cache khi đổi server/tài khoản**: Settings chụp URL+user cũ ⇒ đổi ⇒ `DailySummaryCache.clear()`
>   (đổi setting không liên quan thì KHÔNG xoá); màn Tóm tắt đang mở `ref.listen(copilotApiClientProvider)` ⇒
>   đọc lại khi client đổi identity.
>   (4) **Badge optional fail-safe**: số lấy từ summary **đã đọc** (drawer KHÔNG gọi gì ⇒ không thể block mở),
>   chỉ vẽ khi **> 0** (null/0 ⇒ không badge).
>   (5) **Bound TỔNG 7s** cho mọi read drawer bằng `Future.timeout` (KHÔNG dùng `receiveTimeout` — nó chỉ chặn
>   khoảng giữa các byte) ⇒ `CopilotTimeoutException` + [Thử lại].
>   **Verify thật** `.plan/next5/d4-verify-real.py` ĐẠT: **7/7 tiêu đề đúng** (3 drill ngày đổi theo ngày,
>   2 drill toàn công ty giữ nguyên) · dòng 2026-09-25 khớp REST **từng mã + từng số** (4 HĐ, tổng 1.125.000) ·
>   **độ trễ đọc thật 0,90–1,33s < 7s** · fresh read OK.
>   **Số đo**: Node **820/818/2** (2 fail dsh từ B0) · `p44` **23/23** · Flutter **333/333** · analyze **0** ·
>   falsify drill **37 ca ALL RED** (+6 ca D4).
>   **Phát hiện NGOÀI scope (số tiền) — ĐÃ SỬA 2026-09-26, xem mục FIX P4-6 đầu file**: thẻ Hóa đơn/Tiền khách trả trên Tóm tắt
>   ngày ở chế độ "Hôm qua" đổi **tiêu đề** sang hôm qua nhưng **con số vẫn của hôm nay** (`_body` luôn nhận
>   `_data`; `_yesterday` chỉ dùng cho delta + `date` của drill). Chi tiết + 2 hướng sửa: `D4-result.md` §8.
>   Bằng chứng: `.plan/next5/D4-result.md`. **Chưa commit**.
> - **next5/D3 — NHÁP HÔM NAY (aggregate) — ✅ ĐẠT, mục drawer ĐÃ BẬT ⇒ 5/5 mục drawer hết `soon` (2026-09-26)**.
>   `GET|POST /read/app-drafts` = **alias** của drill `app_drafts_today` (MỘT code path, id pin trong route ⇒ client
>   không đổi được; test so payload 2 đường bằng nhau). Nhánh skill viết lại theo §3.4: **per-section try/catch**
>   (doctype chết ⇒ `sections[dt].ok=false` + `partial:true`, các section khác vẫn trả — thiếu field ⇒ `FIELD_MISSING`,
>   không 500 cả màn) · **dedupe server** (cùng `custom_ai_action_id` ⇒ canonical = chứng từ SỚM NHẤT, trùng ngày ⇒
>   tên ASC; đếm `duplicates_dropped`; client không chọn) · client: banner `loadedPartial` (key `drill-partial-banner`)
>   + **empty actionable** (§5), xoá hẳn affordance `soon` của drawer (dead param sau khi 5/5 mục bật) ⇒ analyze 0.
>   **Bug thật bắt được lúc tunnel site đứt (~17:20-17:52, ERR_NGROK_3200)**: khi CẢ 4 section fail view vẫn trả
>   **200 + partial + rows rỗng** — "ERP sập" đội lốt "hôm nay không có nháp" (§2.2 cấm). Đã sửa: `okCount === 0` ⇒
>   **503 `ERP_UNAVAILABLE`**, `partial` chỉ còn nghĩa "một phần trả lời" — có test + 1 ca falsify.
>   **Verify thật** (tunnel sống lại): REST PO 4 nháp app-tagged (00006@09-24, 00007–00009@09-25);
>   `/read/app-drafts?date=2026-09-25` ⇒ **3 dòng KHỚP REST từng (mã, giá trị)** · sections 4/4 · fresh read OK;
>   mặc định (hôm nay 26/09) 0 dòng — đúng thực tế. **Dedupe chỉ có unit test** (site không có action id trùng
>   xuyên doctype) — không bịa là đã kiểm thật.
>   **Số đo**: Node **818/816/2** (2 fail dsh từ B0) · `p44` **22/22** · Flutter **323/323** · analyze **0** ·
>   falsify drill **31 ca ALL RED** (4 ca mới D3; 2 ca cũ đổi anchor vì D3 đổi shape/luật — harness báo "anchor
>   missing"/"NOT RED", sửa anchor chứ không nới luật). Số falsify ghi ở D1c/D2-result trước đó là **ước lượng sai**,
>   đã sửa lại thành con số đo trực tiếp.
>   Bằng chứng: `.plan/next5/D3-result.md` + `.plan/next5/d3-verify-real.py`. **Chưa commit**.
> - **next5/D2 — TỒN KHO NÓNG (`stock_low`) — ✅ ĐẠT, mục drawer ĐÃ BẬT (2026-09-25)**.
>   User chốt pin `COPILOT_DEFAULT_WAREHOUSE="Kho Cám - MP"` trong `.env` (D0 đo 3 settings mâu thuẫn, một cái trỏ
>   kho **SANLOAN** "Stores - S"; giá trị có dấu cách nên **phải quote**, lần đầu ghi trần làm `set -a; . .env`
>   tách chữ "Cám" thành lệnh — đã sửa). Nhánh drill `stock_low`: kho lấy từ **env của route** (không `process.env`
>   trực tiếp), thiếu pin ⇒ `STOCK_WAREHOUSE_UNPINNED` (mã riêng trong `SUMMARY_CLIENT_CODES`, câu hướng dẫn
>   `.env`/Desk, nói rõ cấm `warehouses[0]`) · 1 kho duy nhất qua `erpnext_stock_balance` (whitelist READ) · row
>   `kind=stock_low_item` với `amount_vnd` = **actual_qty copy** ⇒ client render **unit-less** (qty không phải tiền) ·
>   header "Tồn thấp — Kho: {tên}" + sort qty TĂNG. **Verify thật**: kho tồn tại/lá/đúng company + `Feed Dealer
>   Settings.default_warehouse` KHỚP pin; REST Bin 3 dòng–tổng 833 **khớp từng dòng**; fresh read OK.
>   **Số đo**: Node 815/813/2 · `p44` 18/18 · Flutter 321/321 · analyze 0 · falsify +2 ca (thiếu-pin-tự-đoán ⇒ RED,
>   sort qty giảm ⇒ RED). Bằng chứng: `.plan/next5/D2-result.md` + `.plan/next5/d2-verify-real.py`. **Chưa commit**.
> - **next5/D1.5 — NEGATIVE PERMISSION — ✅ ĐẠT (capability-level) + NO-GO môi trường (row-level) (2026-09-25)**.
>   Test E2E mới `mcp-erpnext/test/d15-drawer-negative-authz.test.mjs` (temp contract gán `Accounts User` cho
>   `customer.balance` + `invoice.lookup`, own process): denied ⇒ **403 `AUTHORIZATION_DENIED`, 0 rows/0 figures**
>   trên cả 3 view; cặp 403/503 chứng minh read chưa hề chạy; allowed nhận đủ list. Row-level ("user không được
>   Customer X") = **NO-GO môi trường**: site **không có User Permission** (probe `User Permission` = 0 rows) +
>   kiến trúc **1 API key chung** ⇒ không có danh tính end-user tới tầng ERP; đã ghi NO-GO + hướng test sau
>   (per-user credential) thay vì fake PASS. Bằng chứng: `.plan/next5/D1.5-result.md`. **Chưa commit**.
> - **next5/D1c — HĐ CHƯA TRẢ (`unpaid_invoices`) — ✅ ĐẠT, mục drawer ĐÃ BẬT (2026-09-25)**.
>   Contract += `unpaid_invoices` (capability `invoice.lookup`); nhánh drill dùng lại `companyDebtRows()` (đúng tập
>   block `receivables` ⇒ anti-drift), **mọi ngày** (không lọc `posting_date`), mỗi dòng = 1 hóa đơn
>   (`kind=unpaid_invoice`, note = khách), sort outstanding DESC + due_date ASC. **Verify thật**: 283 HĐ /
>   461.505.625đ khớp REST **từng dòng** (trang 10/283, `truncated=true`), 13 nháp PE không bị trừ, fresh read OK.
>   **Số đo**: Node 812/810/2 · `p44` 17/17 · Flutter 320/320 · analyze 0 · falsify +2 ca.
>   Bằng chứng: `.plan/next5/D1c-result.md` + `.plan/next5/d1c-verify-real.py`. **Chưa commit**.
> - **next5/D1 — CÔNG NỢ (lối tắt) + NỢ QUÁ HẠN / NỢ LÂU — ✅ ĐẠT, 2 mục drawer ĐÃ BẬT (2026-09-25)**.
>   Contract += `receivable_customers` + `overdue_top`; `ops-summary` += 2 nhánh drill dùng **cùng phép đọc**
>   block `receivables` (anti-drift); số = **GL raw KHÔNG trừ Draft PE** (§9), nháp đi dòng phụ optional
>   `draft_hint {count, amount}` (không khớp allocation — P9-D giữ effective trên chat; đọc lỗi ⇒ vắng mặt);
>   footnote verbatim §3.1 do server gửi; `overdue_top` sort **days_overdue DESC** (oldest due_date từng khách);
>   `DrillListScreen` tái dụng nguyên trạng + footer footnote/hint; `AppDrawer` 2 mục bật (drill id cố định,
>   không phrase), 3 mục `soon` đổi nhãn D1c/D2/D3.
>   **Verify thật**: drawer == ERPNext REST **từng khách + tổng** (461.505.625đ · 47 khách · 283 SI), quá hạn
>   17.764.500, site có 13 nháp PE (162.092.570đ) mà số KHÔNG giảm ⇒ GL-raw thật; fresh GET mỗi lần mở (2
>   `generated_at` khác nhau); truncated trung thực 10/47. 1 lỗi kỳ vọng của chính probe (đòi đủ 47 tên thay vì
>   so trang đầu đúng luật sort) — đã sửa, không phải lỗi sản phẩm.
>   **Số đo**: Node **810/808/2** (2 fail dsh từ B0) · `p44` **15/15** · Flutter **318/318** · analyze **0** ·
>   falsify drill **20/20 RED** (3 ca mới: trừ-nháp ⇒ RED, bỏ sort tuổi ⇒ RED, gỡ footnote ⇒ RED).
>   Bằng chứng: `.plan/next5/D1-result.md` + `.plan/next5/d1-verify-real.py`. **Chưa commit** (gộp A3+D0.5+D1 chờ duyệt).
> - **next5/D0.7 — index `custom_ai_action_id` (PE/SO/PO) — ✅ SKIP, KHÔNG thêm gì (2026-09-25)**. Đo **tươi** meta chỉ-đọc:
>   `search_index=1 · unique=1 · Data` trên cả 3 doctype + `custom_business_doc_key` trên PO (`DocField` = 0 dòng ⇒
>   Custom Field do migration tạo, khớp spec script). Idempotence **đo được**: `--plan-only` (10 cặp, exit 0) ·
>   `--dry-run` ("ĐÃ CÓ" cả 10, "không cần làm gì", exit 0) ⇒ không chạy chế độ ghi thật (không có gì thiếu).
>   Giới hạn: meta-level, chưa đo EXPLAIN; `unique=1` ⇒ dedupe D3 định nghĩa canonical theo doctype.
>   Bằng chứng: `.plan/next5/D0.7-result.md` + `.plan/next5/d07-index-probe.py` (chỉ GET, host không secret).
> - **next5/D0.5 — REAL-only audit (GATE trước D1) — ➡️ PASS sau khi đóng vi phạm §2.2 (2026-09-25)**.
>   Audit đầu: **1 vi phạm** đúng chữ §2.1/§2.2 — payload `MOCK`/không rõ nguồn **vẫn render số tiền như số thật**
>   (4 path: `daily-summary` chỉ dán nhãn rồi vẫn hiện đủ số; `null` bị coi như REAL; `/read/drill` + `/read/list`
>   **không mang provenance nào**). Kết luận đầu = **FAIL** ⇒ dừng, hỏi user. User chốt **(A) ẩn số**.
>   Đã sửa **11 file**: server mang `erp_target` tới 2 route còn thiếu (`drill-views`/`read-views`/`http-ask`,
>   cùng biểu thức `erpTargetLabel(process.env)` như `daily-summary`) · client thêm cổng dùng chung
>   `apps/mobile/lib/core/widgets/data_provenance.dart` (`isRealProvenance` + `InvalidProvenancePanel`) gắn vào
>   **3 màn tiền**: `MOCK`/`unknown` ⇒ panel **THAY** số, không dán nhãn trên số.
>   **Verify trên site THẬT** (gateway restart PID 139961, env từ `.env`): `/read/drill` → `erp_target "REAL"`
>   (6 khách quá hạn) · `/read/list` → `"REAL"` (42.692.500 · 5 dòng) · process **MOCK** → `"MOCK"` cho phần phục vụ được,
>   còn phần fixture không phục vụ được trả **503 `ERP_UNAVAILABLE` / 404 `CUSTOMER_NOT_FOUND`** ⇒ đúng thứ §2.2 cấm:
>   **"ERP fail → MOCK success" KHÔNG xảy ra**.
>   **Số đo**: Flutter **315/315** · analyze **0** · Node full **807/805/2** (2 fail `dshGateway*` từ B0).
>   Phát hiện phụ cho D1: fixture **chưa hỗ trợ toán tử `>`** (`mock supports only = (got >)`) ⇒ drill "Nợ quá hạn"
>   **không test được bằng mock** · `/read/drill` chưa hiện mốc thời gian · 403 chưa có câu riêng · authz chưa có
>   dữ liệu quyền để test negative. Chi tiết: `.plan/next5/D0.5-result.md`. **Chưa commit gì.**
> - **RESTART COPILOT + FILE XML MỚI ⇒ [XÁC NHẬN] TẠO NHÁP — ✅ ĐẠT (2026-09-25, `result73.txt`)**.
>   Restart stack thật: `python3 -m nlp_service.server --port 8787` + `node src/http-ask.mjs --port 8788
>   --host 10.88.0.4` với env từ `.env` (bind **IP private** vì loopback + `ASK_USER` bị code **throw**;
>   auth thật: `/health` không auth **401**, có auth **200**). Gửi **file XML MỚI** `C26TYY/0000128`
>   (`/input/einvoice` 200 → `/ask` 200 `create_purchase_order` → `/execute` **200**) ⇒ **NHÁP
>   `PUR-ORD-2026-00008`**, đếm PO **7 → 8 = +1** — **hết 500 `PO_COMPANY_UNRESOLVED`** sau khi pin company.
>   Kiểm chứng REST độc lập: docstatus **0**, company `Minh Phát Cám & VLXD`, supplier đúng, items
>   `CAM-GA-25KG×12 @270.000` = **3.240.000đ**, khoá `bdk_68055489…` **chỉ 1 đơn**. **KHÔNG submit, không tự huỷ nháp.**
>   Script `.plan/next4/loop-xml-confirm-draft.mjs` (nói chuyện với server ĐANG CHẠY qua HTTP thật).
>   ⚠️ **VẪN CÒN VIỆC VẬN HÀNH**: `erpn8788.loca.lt/health` → **HTTP 503**, `lt` **chưa cài** ở máy này ⇒
>   server phục vụ **app** là máy khác; **phải** thêm `COPILOT_COMPANY` + restart **ở máy đó** thì app mới hết 500.
> - **RETEST sau khi user restart server — 2 lần đo, 1 ĐẠT 1 KHÔNG (2026-09-25, `result73.txt` §6)**.
>   (a) **Máy này** (khởi động lại stack: nlp 129887 + ask 129893), file MỚI `C26TYY/0000129` ⇒
>   `/execute` **200** ⇒ nháp **`PUR-ORD-2026-00009`** (docstatus 0, `CAM-HEO-25KG ×8 @295.000`
>   = 2.360.000 — rate lấy từ ERPNext chứ **không** phải 265.000 trong file), PO **8 → 9**.
>   (b) **QUA TUNNEL `erpn8788.loca.lt` (đúng đường app) — ❌ VẪN 500**: tunnel đã **sống lại**
>   (không auth **401**, có auth **200**) nhưng file MỚI `C26TYY/0000130` ⇒ `/input/einvoice` 200 →
>   `/ask` 200 → **`/execute` 500 `PO_COMPANY_UNRESOLVED`** (*"có 3 company … và chưa pin company — đặt
>   COPILOT_COMPANY trước khi tạo đơn mua"*), PO **9 → 9** (fail-closed, không ghi gì) ⇒ **máy đó đã
>   restart NHƯNG môi trường VẪN thiếu `COPILOT_COMPANY`** (`.env` repo này có, máy đó không dùng nó).
>   (c) ⚠️ **Cảnh báo build APK**: repo hiện **KHÔNG có GitHub Variables/Secrets nào**; workflow luôn
>   truyền `--dart-define=COPILOT_BASE_URL="${VAR}"` kể cả khi trống, và **đo Dart** xác nhận
>   **define rỗng ⇒ `''`, KHÔNG rơi về default** ⇒ APK build ra sẽ không có endpoint cho tới khi nhập
>   ở ⚙️ Settings (`.plan/next4/dart-define-probe.dart`).
> - **VÒNG XML THẬT 2 LẦN CÙNG FILE — ✅ CHẠY ĐẠT (2026-09-25, user cho phép ghi)**. Script
>   `.plan/next4/loop-xml-dedupe-real.mjs` (NLP + gateway trong tiến trình + **đếm PO qua REST độc lập**),
>   file `tt78-basic.xml` (C26TYY/0000123). Lần 1 `/execute` **200 ⇒ NHÁP `PUR-ORD-2026-00007`**;
>   lần 2 (ĐÚNG file đó) **409 `PO_DUPLICATE_DOC` + `existing_doc` đúng tên đơn đó** + câu tiếng Việt
>   nêu tên đơn cũ; đếm PO **6 → 7 = +1**; kiểm chứng REST: docstatus **0**, company/supplier đúng,
>   khoá `bdk_ca5eaafe…` **chỉ 1 đơn** mang, items CAM-GA-25KG×10 + CAM-HEO-25KG×5, tổng **4.175.000 đ**
>   (rate lấy từ ERPNext, không lấy từ file). **KHÔNG submit, không tự huỷ nháp.**
>   ⚠️ **PHÁT HIỆN DEPLOYMENT (cần user xử lý)**: `/execute` lần đầu **500 `PO_COMPANY_UNRESOLVED`** —
>   site có **3 company** mà **chưa pin `COPILOT_COMPANY`**, scope company là server-first và **app KHÔNG
>   gửi company** ⇒ **phải thêm `COPILOT_COMPANY="Minh Phát Cám & VLXD"` vào `.env` + restart server**,
>   nếu không thì bấm [Xác nhận] trên app sẽ hỏng (vòng A2 trước đây tạo được 00006 vì khi đó chỉ có
>   1 company). Hai bẫy API khác đã ghi: `source_document` của `/input/einvoice` **không** dùng trực tiếp
>   cho `/ask` (thiếu `kind` ⇒ 400), và `slots.text` là **mô tả**, câu hỏi do client ghép theo template
>   `'đặt mua {goods} từ {party}'`. Giới hạn: `PUR-ORD-2026-00006` (cùng tờ, tạo TRƯỚC migration) có
>   key `null` ⇒ không được dedupe; race 2 phiên chưa đo. Bằng chứng: `result72.txt` +
>   `.plan/next4/B-dedupe-result.md` §10.
> - **A3 — L1 + L3 ĐÃ SỬA** (cùng phiên, user yêu cầu "sửa nốt"): **L1** cap số dòng nay chạy **KHI DỰNG**
>   trong `flush()` (mã riêng `EINVOICE_PDF_TOO_MANY_LINES` ⇒ **413**, trần `max_lines × 100` = 20.000
>   dòng chữ — suy ra từ policy, không thêm knob mới; là TỪ CHỐI chứ không cắt ngắn) · **L3** guard
>   sync/pure trong `a1-einvoice-parser.test.mjs` nay áp cho **MỌI** module `src/einvoice` (trước gọi
>   tên riêng `einvoice-xml.mjs` ⇒ `einvoice-pdf.mjs` không được canh dù hôm nay nó sync).
>   **Số đo**: `a3` **29/29** · `a1`+`a2`+`a3` **54/54** · Node full **807/805/2** (2 fail dsh-env từ B0) ·
>   falsify `a3-einvoice-pdf.mjs` **12/12 RED** (+2 ca: L cho L1, M cho L3 — ca M tiêm `async` vào module
>   PDF và test tĩnh ĐỎ). Vẫn **CHƯA COMMIT** (vùng định danh + trần tài nguyên).
> - **A3 PDF — SỬA H1/M1/M2 (user duyệt "sửa đi") ✅ CODE+TEST+FALSIFY XONG (2026-09-25),
>   ⏳ CHỜ USER DUYỆT COMMIT** (định danh tài liệu + trần tài nguyên ⇒ AI không tự commit).
>   Ba lỗ đã ĐO ở `.plan/next4/A3-result.md` §9.5: `invoice_no` rác (`"thu"`/`"b"` từ nhánh `so` trần,
>   mà `complete=true` ⇒ khoá chống trùng của B cũng rác) · `inflateSync` KHÔNG trần OUTPUT · `readDate`
>   lấy ngày đầu tiên không nhãn. **Sau khi sửa**: `"0000049"` đúng ở layout MST-trên-Số và ở footer
>   "In số bản"; inflate bomb **57 ms/+49,1 MB ⇒ 19 ms/+8,5 MB** và có mã riêng 413; ngày theo NHÃN
>   (`Ngày đặt hàng`/`In ngày` không còn thắng). **Probe còn tự tìm thêm 3 ca RÁC NỮA trên chính bản
>   fix đầu tiên** (`"M"` từ dòng MST, `"S"` từ dòng số lượng, `"1"` từ dòng BẢNG) ⇒ luật cuối: dòng
>   dưới nhãn phải LÀ giá trị (`wholeLineToken`). Thêm 13 test (`a3` 15→**28/28**) + harness MỚI
>   `scripts/falsify/a3-einvoice-pdf.mjs` (**10/10 RED**) · **gỡ 1 case falsify vì nó chạy GREEN**
>   (luật chữ số đã phủ — giữ case xanh là để harness nói dối). Node full **806/804/2** (2 fail dsh-env
>   từ B0) · Flutter không đụng. Chi tiết + số đo: `result71.txt` + A3-result.md **§9.6**.
> - **P9-D (P9-C-followup) — có NHÁP phiếu thu/chi rồi thì `/ask` sau không đề xuất lại đúng số tiền đó
>   ✅ ĐÃ COMMIT `f82f656`** (2026-09-25, user duyệt RIÊNG vì vùng SỐ TIỀN, KHÔNG push;
>   `next.md:508-519` gọi nó là P9-C-followup — "P9-D" ở `.plan/next1/p9_prompts.md` là việc khác, đã xong).
>   **ĐO trước khi sửa**: seed 1 nháp `PE-M901` (CUST-00001, 1.000.000, phân bổ SINV-0001) rồi hỏi 2 lần
>   "thu tiền cho Nguyễn Thị Lan 2 triệu" ⇒ **cả hai lần đều 2.500.000** (ERPNext chỉ giảm nợ khi SUBMIT
>   ⇒ nháp là **tiền vô hình**, bấm xác nhận lần hai = 2 nháp cùng trả 1 khoản nợ). **Sửa**: builder đọc
>   `listOpenDraftPaymentEntries` (docstatus 0, limit 10) + `getPaymentEntryDoc` (→ `references[]`) rồi trừ
>   phần đã phủ ⇒ `effective_outstanding` (vẫn giữ GL RAW bên cạnh: `raw_outstanding_vnd`/`draft_cover_vnd`),
>   chọn đích trên danh sách hiệu dụng, amount mặc định + kẹp theo HIỆU DỤNG, cảnh báo **nêu cả tên nháp
>   lẫn tên hóa đơn** và nêu GL khi hai số khác nhau; phủ hết/phủ quá ⇒ `PAYMENT_DRAFT_COVERED` (nêu tên nháp).
>   **Executor đọc lại ĐÚNG code đó** lúc xác nhận ⇒ nháp bị submit/hủy ở giữa ⇒ `PROPOSAL_STALE` (không ghi
>   im lặng); drift được so với **hiệu dụng**, và còn 1 chốt giá trị CHÍNH XÁC cho số tiền lẻ mà `Math.round`
>   của drift bỏ qua. Chỉ thêm **2 đường ĐỌC** (qua `assertReadOnly` sẵn có); đọc lỗi ⇒ TỪ CHỐI; bag thiếu read
>   ⇒ `PAYMENT_SKILLS_INCOMPLETE` (không đội lốt "ERP chết"); **không** thêm đường ghi nào, không auto-submit.
>   **Số thật: Node full 793/791/2 (2 fail dsh-env từ B0) · p9 68/68 · p1+b4 33/33 · p8+p10 30/30 · falsify
>   12/12 RED (2 ca MỚI K–L cho chính phần trừ nháp) · probe §0 chạy lại: 2.500.000 → **1.500.000**, ổn định
>   qua 2 lần hỏi**. **1 lỗi của CHÍNH TÔI do suite bắt**: guard "phủ hết" viết `<= 0` ⇒ nuốt luôn ca credit
>   note số âm (không liên quan nháp) ⇒ đã siết thành `<= 0 && drawnForTarget > 0` + ghim ca hồi quy.
>   **4 test cũ phải sửa vì đúng hành vi mới** (p8 ×2, p10 ×2: chúng gửi lại CÙNG proposal cho CÙNG khoản nợ
>   — trong sản phẩm không luồng nào làm vậy, vì trùng `command_id` là gateway replay) ⇒ mỗi test một khoản
>   nợ riêng, khẳng định cũ giữ nguyên. Chi tiết + giới hạn: **`.plan/next4/P9D-result.md`**.
> - **A3 — HĐĐT PDF có LỚP CHỮ → cùng slots → cùng pipeline nháp ✅ CODE+TEST XONG, ĐO TRÊN SITE THẬT (CHỈ ĐỌC),
>   CHỜ USER DUYỆT COMMIT** (chạm đường tài liệu ⇒ AI không tự ký duyệt). Một PDF có lớp chữ đi vào **CÙNG**
>   `/input/einvoice` → cùng schema `slots` (untrusted) → form → câu đã sửa → `/ask` → capability **từ contract**
>   → proposal → confirm → Safety Gateway → **NHÁP** ⇒ **KHÔNG mở đường ghi mới, không auto-create master,
>   không auto-submit, không làm OCR trong luồng này, không chạm B/M1**. Nguyên tắc chi phối: **một con số
>   KHÔNG đọc được thì KHÔNG được điền vào** — dòng mà `qty × rate ≠ amount` là **problem** ⇒ request TỪ CHỐI,
>   không thành dòng "trông-hợp-lý". Bộ đọc `src/einvoice/einvoice-pdf.mjs` (~1148 dòng, thuần & đồng bộ,
>   0 dependency): quét object KHÔNG tin xref · **FlateDecode bằng stdlib `zlib`** · **ToUnicode CMap** (đường
>   duy nhất ra dấu tiếng Việt của font CID/Type0) · ngắt dòng theo **VỊ TRÍ y** (`PDF_SPACE_GAP=-250`) ·
>   `pdfNumberReadings` trả **MỌI cách đọc** (`.` nghìn / `,` thập phân — **KHÁC A1**, nên KHÔNG tái dùng
>   `strictNumber`) và **số học của chính dòng phân xử** mơ hồ. Từ chối THEO TÊN: `NOT_A_PDF · TOO_LARGE ·
>   ENCRYPTED · NO_TEXT · COMPRESSED_OBJECTS · TEXT_UNMAPPED · UNSUPPORTED_FILTER · BROKEN` + warning
>   `MST_UNLABELLED · UNIT_GUESSED_FROM_TEXT` + request `EINVOICE_INPUT_AMBIGUOUS` (gửi cả xml lẫn pdf_base64).
>   Contract: `max_pdf_bytes 650000` · `pdf_reader "text_layer_only"` · `comment_pdf`. Route: nhánh XOR
>   `hasXml`/`hasPdf` (cả hai ⇒ 400, **không** chạy pipeline) · map 413/422/400 · `provenance.source`.
>   **ĐO TRÊN SITE THẬT (chỉ ĐỌC)**: `hd-pdf-tounicode.pdf` → **200** (1270 ms), `source=einvoice_pdf`,
>   party resolve **theo MST** ra NCC THẬT `Đại lý Cám Bình Dương` (`ambiguous=false`), 2 dòng khớp item THẬT
>   kho (`CAM-HEO-25KG` 10 / `CAM-GA-25KG` 5 Bao), `warnings=[]`, **không lộ bề mặt ghi** (không command id/
>   proposal); `hd-pdf-no-textlayer.pdf` → **422 `EINVOICE_PDF_NO_TEXT`** "dùng nút camera…" — **không bịa số**.
>   **Số thật: Node full 782/780/2 (2 fail dsh-env từ B0) · A3 15/15 · Flutter 304/304 · analyze 0.**
>   **6 bug THẬT do chính spike bắt được** (đã sửa, có test): `foldVietnamese` thiếu `lowercase` ⇒ MỌI regex
>   nhãn trượt · độ sâu `readLiteralString` bắt đầu 0 ⇒ nuốt stream · số trong `[ ]` không vào array ⇒ `TJ` vỡ ·
>   kerning −150 cắt đôi từ (⇒ −250) · `/Contents` trỏ sai object + `dictOf` trả text dict thay vì stream CMap
>   (⇒ thêm `refTarget`) · `pdfNumberReadings` trả NaN. **Giới hạn ghi rõ**: không có PDF THẬT của NCC (fixture
>   tự dựng) · **ObjStm chưa giải nén** (từ chối theo tên) · scan ⇒ đẩy sang camera C · **Flutter CHƯA có nút
>   chọn `.pdf`** (⇒ A3 hôm nay đo qua route, chưa với tới được từ UI) · chưa có falsify riêng cho A3.
>   Chi tiết: **`.plan/next4/A3-result.md`** · spike format **`.plan/next4/A3-pdf-spike.md`** · bằng chứng **`result69.txt`**.
> - **B DEDUPE — cùng MỘT tờ HĐ gửi 2 lần ⇒ MỘT nháp ✅ CODE+TEST+FALSIFY XONG (2026-09-25), CHỜ USER**
>   (chạm đường GHI tài liệu ⇒ AI không tự commit). Khóa `bdk_<sha256>` = **loại chứng từ (lấy từ CONTRACT,
>   không lấy nhãn client) + party (MST ưu tiên, fallback id ERPNext) + số HĐ + ngày**; MST thì bỏ separator
>   (định dạng), **số HĐ thì so chính xác** (bỏ separator = gộp 2 số khác nhau thành 1 định danh ⇒ lần từ chối
>   sau nêu SAI tài liệu); **không** có tiền trong khóa (tiền đọc từ file luôn chỉ để hiển thị); **không** gồm
>   series (OCR sau này có thể không đọc được ⇒ khóa lệch giữa 2 kênh = trượt trùng = nháp thứ hai).
>   Định danh đi **NGOÀI câu nói** (`source_document` trên body `/ask` — vì A-result đã đo NLP đọc `00049`
>   thành **số tiền 49**), validate **fail-closed ở CẢ 2 đầu** (client: thiếu số HĐ/ngày/MST ⇒ gửi KHÔNG có,
>   không gửi nửa vời; server: 400 `SOURCE_DOCUMENT_INVALID` + `reason`). Executor probe Purchase Order theo
>   `custom_business_doc_key`: **có** ⇒ `409 PO_DUPLICATE_DOC` + `existing_doc`; **site thiếu cột** ⇒
>   `PO_DOC_KEY_FIELD_MISSING` **KHÔNG ghi** (500 — site misconfig, cùng họ `*_CORRELATION_FIELD_MISSING`);
>   đọc lại sai khóa ⇒ `PO_WRITE_UNVERIFIED`. Câu nói thường (không định danh) ⇒ **2 nháp vẫn OK** (mua nhiều
>   lần là bình thường); cùng HĐ nhưng resolve ra NCC khác ⇒ **vẫn TỪ CHỐI** (khóa theo tờ giấy).
>   **Số thật: Node mới 10/10 · Node full 765/763/2 (2 fail dsh-env từ B0) · falsify 8/8 RED (A–H, gồm 2 ca
>   DART) · falsify M1 vẫn 16/16 (hồi quy harness) · Flutter 304/304 (+5) · analyze 0** · migration mở rộng
>   họ thứ hai, plan-only in đúng 2 dòng cho Purchase Order. **2 lỗi của CHÍNH TÔI do falsify bắt**: neo đột
>   biến gãy khi refactor (báo `anchor missing` — đúng cơ chế) và **harness Dart bỏ sót cả lớp lỗi** (`expect`
>   đồng bộ trong `group()` bị báo "no test ran" ⇒ ca ĐỎ thật bị quy cho "pattern sai") → đã thêm nguồn
>   `Failing tests:` + chạy lại harness M1. **Review tự làm sửa 5 điểm** (nhánh if rỗng · payload chết
>   `existing_docstatus` · echo `source_document` không UI nào đọc · `invoiceLabel` bọc giả · doc comment Dart
>   nói sai điều kiện). Còn lại: **migration trên site + commit + vòng XML thật 2 lần** đều CHỜ USER;
>   giới hạn ghi rõ: đường BÁN chưa có lớp này · kênh ẢNH chưa phát định danh · đơn đã hủy vẫn giữ khóa
>   (`unique=1`) · race thật chưa đo. Chi tiết: **`.plan/next4/B-dedupe-result.md`** · bằng chứng **`result68.txt`**.
> - **A2 FLUTTER — upload HĐĐT XML trong app ✅ CODE+TEST XONG (2026-09-25), CHỜ USER DUYỆT** (thêm
>   dependency + chạm UI đường GHI ⇒ AI không tự commit). Nút **"HĐĐT XML" cạnh camera ở CẢ 2 layout** →
>   `file_selector` chọn `.xml` → `POST /input/einvoice` (JSON `{xml, kind:"purchase"}` — đúng shape route,
>   **không** multipart) → **form slots SẴN CÓ của camera** (`OcrSlots` + `showOcrSlotsForm`, chỉ đổi wording
>   bằng enum `OcrSlotsSource`) → user sửa → `OcrCompose` → `/ask` → proposal → [Xác nhận] → **NHÁP** ⇒
>   **KHÔNG có đường ghi mới, không auto-execute, không auto-create master, không parser XML thứ hai**
>   (có **tripwire tĩnh** quét `lib/**.dart`, đã kiểm SỐNG). Lỗi tiếng Việt: lời server hiện **nguyên văn**
>   (XML_NOT_INVOICE / MALFORMED / EMPTY / TOO_LARGE / EINVOICE_INCOMPLETE / KIND_UNKNOWN) + câu riêng của app
>   (rỗng / vượt trần 950.000 byte / không UTF-8 / không mở được picker) + warning thiếu NCC **nêu MST**.
>   **Chỉ 1 kind `purchase`** (đúng `einvoice_policy.document_kinds` của server).
>   **Số thật: Flutter 299/299 (+18, 0 regression) · analyze 0** · `flutter pub get` chỉ +4 package
>   (`file_selector` chính chủ flutter.dev + android/ios/web; linux/macos/windows + platform_interface đã có sẵn
>   từ `image_picker`). **Review tự làm (2 vòng) tìm 6 điểm, đã sửa**: `byteLength`/`kiloBytes` chết ⇒ xoá;
>   guard parse thiếu `kind` (payload thiếu `kind` sẽ ra TypeError thô) ⇒ thêm vào; **lời hứa "nút có ở CẢ 2
>   layout" KHÔNG được test ghim** ⇒ thêm test + đột biến 2 chiều đều RED (bỏ nút khỏi row voice-first lúc
>   đó mới làm test đỏ); `xmlTypeGroup` public ⇒ private; hàm thuần `decodeEinvoiceXml` đọc trần qua class gắn
>   plugin ⇒ tách hằng `einvoiceMaxXmlBytes`; tham số `kind` không ai truyền ⇒ bỏ. **3 quan sát không sửa**
>   (trần byte-thô vs body JSON escape · tên file là dữ liệu không tin cậy · nút vẫn bật ở chế độ AI nhưng
>   đường đó chỉ `/dsh/ask` **chỉ ĐỌC**, y hệt nút camera sẵn có) — ghi ở deliverable §5.2. Sau mọi fix ⇒
>   **chạy lại analyze + TOÀN BỘ suite**.
>   **Bước mới đáng ghi:** dựng gateway MOCK + POST fixture thật rồi parse bytes thật bằng model Dart ⇒ **bắt
>   ngay 1 fixture tự chế sai** (tôi ghi `CAM-GA-25KG`/"Cám gà thịt 25kg", route thật trả `CAM-GA-10KG`/
>   "Cám gà thịt 10kg") — bài học: fixture tự chế có thể đồng ý với client mà không đồng ý với server.
>   Chi tiết + cách test tay trên máy: **`.plan/next4/flutter-xml-upload-result.md`** · bằng chứng **`result67.txt`**.
>   **Chưa làm**: E2E ERPNext thật cho kênh này (server đã cover ở `A-result.md` §3; chạy lại sẽ tạo thêm
>   1 nháp thật ⇒ cần user cho phép).
> - **A2 HĐĐT XML → NHÁP THẬT ✅ (2026-09-24)**: Exit A2 đạt trên ERPNext THẬT — tạo được
>   **`PUR-ORD-2026-00006`** (docstatus **0**, KHÔNG submit) từ `tt78-basic.xml` qua
>   `/input/einvoice` → câu compose → `/ask` → `/execute`, đọc lại verified (MST `0300000002` →
>   `Đại lý Cám Bình Dương`; 2 item thật `CAM-GA-25KG` 10 Bao + `CAM-HEO-25KG` 5 Bao, cả hai kho
>   `Kho Cám - MP`; `custom_ai_action_id` đã có sẵn ⇒ **không** cần migration). Cấu hình site tối thiểu:
>   `Item Default.default_warehouse` cho đúng 2 item đó (script idempotent, kho lấy từ **lịch sử**
>   PO-00005, không bịa). Site sau đó: 6 PO / **đúng 1 nháp**.
> - **2 bug THẬT sửa được nhờ vòng chạy thật (2026-09-24)**: **R5** UOM `Nos` (tên đơn vị của CHÍNH
>   ERPNext — alias **đồng nhất**, không thêm hệ số quy đổi nào) và **R6** đường ĐẶT HÀNG nhận item
>   "ma" từ **1 từ đầu trùng TÊN NCC** (`Đại lý **Cám** Bình Dương` → mọi item `Cám …`) — sửa bằng
>   `maskPartyMention` (blank tên đối tác, giữ nguyên độ dài ⇒ offset span vẫn đúng), nối **cả 7 đường
>   đặt hàng** (PO/QT/SO/DN/SI/PR/SR). **Cả hai đều kiểm chứng trên site THẬT** (R6: `/ask` chỉ đọc nay
>   trả proposal đúng **1 dòng** `PHI-VAN-CHUYEN` 3 `Nos`, giá **500.000** từ `Standard Buying`).
> - **Commits (CHƯA push)**: `74eb467` (code workstream A) · `a229ddf` (docs §3) · `5e6071d` (R5) ·
>   `fd19ebe` (R6). Tất cả theo từng lệnh duyệt của user; vùng TIỀN/KHO không tự commit.
> - **Suite đo lại (2026-09-24)**: Node **743** (741 pass, 2 fail `dsh-gateway` do môi trường DSH từ B0)
>   · Python NLP **63 OK** · cohort ảnh hưởng 13 file **221/221** · falsify R5/R6 đều **RED đúng test**
>   rồi restore byte-identical.
> - **Còn MỞ** (cập nhật 2026-09-25): M1-site ✅ xong (chờ duyệt commit) · B dedupe tờ HĐ ✅ CODE+TEST+FALSIFY
>   xong (chờ migration + duyệt commit) · A3 PDF ✅ server + đo site thật, **còn nút chọn `.pdf` phía Flutter
>   chưa làm** (thay đổi UI ⇒ cần UI-checkpoint + user đồng ý) · Flutter upload XML (A2) ✅ code xong, chờ duyệt.
> - **PUSH + CI APK ✅ (2026-09-23, theo lệnh user)**: 2 commit `3b354b4` (feat: P9-A→F + M1 +
>   plan4 + OCR slots + 7 tripwire sửa theo quyết định (a)) và `25d1105` (docs) + **merge `686b347`**
>   (nhánh remote đã có 5 commit P4/P5 từ shell khác ⇒ merge `-s ours` để **cây cloud shell thắng**
>   nhưng **giữ lịch sử remote** — không force-push; bỏ `initp`/`faq1.md` theo AGENTS.md, giữ lại
>   `bin/`+`syncode`). **CI run [`35901343267`](https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35901343267)
>   head `686b347` SUCCESS ~6 phút** — artifact `erpn-chat-debug-apk` **85.327.721 bytes**
>   (run #22 cũ 84.222.402 ⇒ +1,1 MB do lượng code mới: P9-A→F + M1 backend + OCR slots + ops drawer).
>   Suite đo lại trong phiên: **Node 691 (689 pass, 2 fail `dsh-gateway` môi trường từ B0)** ·
>   **Flutter 272/272** · analyze 0.
> - **M1 (2026-09-23): backend + card parity XONG, còn Flutter form/test riêng/falsify.** Sau quyết định
>   user (a): 7 regression M1 đã sửa (tripwire set + test ngữ nghĩa Customer) ⇒ suite đếm lại thật:
>   **Node 691 (689 pass, 2 fail `dsh-gateway` môi trường có từ B0)** · **Flutter 272/272** · analyze 0.
>   Thêm: `create_customer` vào `_confirmableActions` (tripwire Dart↔server khớp) + label "Xác nhận tạo
>   khách hàng (record thật)" + success text riêng — **vá luôn gap P9-E** phát hiện khi làm M1 (card
>   `create_stock_adjustment` không có nhánh success ⇒ rơi vào nhánh payment, in "Đã ghi phiếu thu … ?đ":
>   đã thêm `_stockSuccessText` + 2 widget test). Còn thiếu: form Tên*/SĐT/MST, test suite riêng M1,
>   falsify M1, `M1-result.md` — xem `result64.txt`.
> - **DUYỆT**: **P9-C ĐÃ ĐƯỢC USER DUYỆT 2026-09-23 (chưa push)** — giới hạn "nháp chưa trừ vào nợ" user chốt **để lại, làm sau**
>   (đã ghi thành **P9-C-followup (nhỏ, CHƯA LÀM)** ở `next.md`; mục này trước ghi là "P9-D" — đã **đổi tên** vì **P9-D** là
>   `sales_invoice.create`). **P9-D ✅ KỸ THUẬT XONG 2026-09-23 — ĐANG CHỜ DUYỆT COMMIT** (vùng SỐ TIỀN).
>   **Còn CHỜ DUYỆT COMMIT** (vùng loại trừ Ponytail): đợt
>   2026-09-20→22 (phases3 A1→C2 · plan4 P4-0→P4-7 · plan5 P5-0→P5-4 · **P9-A1/A2/B** — WRITE #5/#6).
>   Máy hiện **không có git** (`git rev-parse` → not a repository) nên không có diff/commit tại chỗ.
> - **Suite đang xanh (số thật cuối)**: Node **691** (689 pass, 2 fail `dsh-gateway` do môi trường
>   DSH từ B0) · Flutter **271/271** + analyze **0** · falsify in-repo
>   (`scripts/falsify/p9c-payment-pay.mjs` — **9/9 RED** · `scripts/falsify/p9d-sales-invoice.mjs` — **11/11 RED** ·
>   `scripts/falsify/p9e-stock.mjs` — **17/17 RED** · `scripts/falsify/p9f-sales-return.mjs` — **30/30 RED**) **ALL GUARDS ALIVE**.
> - **Việc cần trước khi dùng ERPNext thật cho WRITE mới**: migration `custom_ai_action_id` — ✅ **XONG 2026-09-23** trên cả 7 doctype (SI/DN/PR vừa tạo, PE/SO/QT/PO đã có; verify dry-run lần 2 + probe READ-ONLY 2 call của executor → HTTP 200, 0 Error Log); còn lại: xác nhận mapper
>   `make_delivery_note`/`make_purchase_receipt`; xác nhận Mode of Payment + tài khoản `2110` (phải trả NCC) trên site thật.
> - **Việc của user (human)**: device smoke (mic · chế độ AI · camera) · revoke token GitHub cũ ·
>   bật lại tunnel Mac (`llm9000.loca.lt` 503) trước khi `openspec archive` → merge master.
> - **Nguồn sự thật**: `checklist.md` (đã/chưa/chờ) · `next.md` (roadmap) · `features.md` (tính năng) ·
>   `result*.txt` (bằng chứng) · `.project/` (kiến thức tĩnh) · `.plan/result-p9-*.md` (chi tiết phiên)

- [x] **[2026-09-23] P9-F — `sales_return.create` (WRITE #9, KHÁCH TRẢ HÀNG = Sales Invoice NHÁP `is_return=1`) → ✅ KỸ THUẬT XONG, **CHỜ DUYỆT COMMIT** (vùng TIỀN: phiếu trả là chiều NGƯỢC của hoá đơn ⇒ hoàn tiền/credit note; AI không tự ký duyệt)**: deliverable **`.plan/result-p9-F.md`**. **USER CHỐT TRƯỚC KHI CODE** (hỏi vì chạm tiền): `return_against` = **Sales Invoice ĐÃ SUBMIT** (không phải DN) · trần **trừ cả phiếu trả NHÁP đang treo** (giống delivery) · phiếu = SI NHÁP `is_return=1` + `update_stock=1` + qty **ÂM** + `rate` **copy từ HĐ gốc**. **ĐO TRƯỚC (site thật, READ-ONLY)**: site có sẵn `ACC-SINV-2026-00052` ← `ACC-SINV-2026-00049` (qty `-1`, rate 90000, `update_stock=1`) ⇒ dấu âm/giá gốc/chiều kho là **shape thật**, không suy đoán; `custom_ai_action_id` **đã có** trên SI từ P9-D ⇒ **không cần migration mới**. **Đã ship**: (1) contract `sales_return.create` WRITE `risk HIGH`/`requires_confirmation`, `entities.required: [invoice]` (customer optional để đối chiếu), `line_policy: {entry_type: credit_note, qty_negative: true, update_stock: 1, rate_source: erpnext, no_price: from_invoice}` + **23 mã `SR_*`**, `execution: {write_doctype: Sales Invoice, draft_only, allow_submit false, verify, reconcile}`; nhóm route `sales_return_write` đứng **ĐẦU** mảng (trước `payment_write`/`payment` — nơi keyword SUBSTRING `trả` từng nuốt mệnh lệnh trả hàng); **validator LOAD-TIME mới**: `qty_negative ⟹ bắt buộc update_stock=1` + `qty_positive` unset, `update_stock ∈ {0,1}`, `entry_type` khai thì không được rỗng; (2) `src/skills/sales-return-write.mjs` (mới, 12 export): builder 0 write (HĐ gốc phải **đã submit** + **không phải phiếu trả**, mặt hàng phải **thuộc** HĐ, đơn vị phải **của HĐ**, trần = `qty − đã trả(submitted) − NHÁP đang chiếm`, **vượt trần ⇒ TỪ CHỐI, không kẹp**) · 2 guard fail-closed `returnNature()`/`stockDirection()` đọc **hợp đồng**, thiếu khai báo ⇒ từ chối (**không default**) · executor (đọc LẠI HĐ + phiếu trả; giá đổi/hàng đã về thêm ⇒ `PROPOSAL_STALE` **trước mọi write**; thiếu correlation ⇒ KHÔNG ghi; ghi **1 NHÁP**; **đọc lại** mới tin, kể cả **dấu qty** và `update_stock`) + `buildSalesReturnData` (âm hoá qty tại **MỘT** chỗ) + `reconcileSalesReturn`; (3) wire `router` (factory + **`findCustomer`** vì khối party dùng chung vẫn cần) · `copilot-server` **nhánh SỚM** cạnh nhánh stock · `safety-gateway` (`"sales_return.create"` → executor); (4) mock `createSalesReturn` **tái hiện LUẬT đo được** (buộc `return_against` trỏ HĐ **đã submit** + **không phải phiếu trả**; qty **ÂM**; rate **bằng đúng** dòng HĐ; `update_stock=1`; chặn trả vượt số đã bán; tạo NHÁP; knob `MOCK_ERP_SR_FORCE_NOSTOCK` để thấy giá trị của đọc-lại) + `MOCK_ERP_INV_FIXTURE` cho hoá đơn "site đã submit"; (5) Flutter: `create_sales_return` vào `_confirmableActions` (tripwire so với server), nút **"Xác nhận trả hàng (NHÁP)"**, dòng hàng + tạm tính **theo giá HĐ**, câu kết quả riêng nói **"CHƯA nhận lại kho / CHƯA ghi công nợ"**, badge NHÁP + **1 widget test**; (6) harness đột biến **trong repo** `scripts/falsify/p9f-sales-return.mjs` — **30/30 RED**. **REVIEW TỰ LÀM TÌM 7 ĐIỂM — đã xử lý**: (a) **guard party chặn cả câu** — câu hợp lệ bị `ENTITY_PICK_REQUIRED` vì CẢ CÂU bị coi là tên khách (đo được) ⇒ chuyển thành **nhánh sớm** (entity là HOÁ ĐƠN; khách đọc TỪ hoá đơn), số HĐ lấy **nguyên văn** từ câu vì tầng NLP đọc `00049` thành **amount 49**; (b) router bag thiếu `findCustomer` ⇒ `/ask` nổ `skills.findCustomer is not a function`; (c) **BUG THẬT trong mock** — nhánh `doc_create` cho Sales Invoice đọc `data` **trước khi khai báo** (TDZ `Cannot access 'data' before initialization`) ⇒ sửa đọc `args?.data?.is_return`; (d) **tham số CHẾT** `listSubmittedReturns(..., field)` không dùng ⇒ gỡ khỏi skill+router; (e) **hàm CHẾT** `getSalesOrderDoc` (đường trả neo vào HĐ, không theo DN) ⇒ gỡ; (f) test "2 lớp fail-closed" phải tách đúng lớp vì validator đã ghép cặp `qty_negative ⟹ update_stock=1` ⇒ sửa test theo từng lớp (falsify H/G/I pin riêng); (g) **full suite bắt 1 test P9-E ghim SỐ HỌC SAI** (`cf=25` nhưng kỳ vọng phủ `1×25 = 500 Kg` — di chứng lỗ hổng quy đổi đã vá trong review P9-E) ⇒ sửa test theo số học đúng (draft 20 Bao × cf 25 = 500 Kg ⇒ `SE_NOTHING_TO_ISSUE`), hồi quy P9-E **29/29**. **Tripwire sửa có chủ ý** (WRITE #9 làm đổi bộ action/đổi danh sách write skill): `safety-gateway.test` (danh sách write SKILL) · `b3-quotation` · `b4-purchase-order` · `p9-e-stock` (`executableWriteActions`) · `capability-contract` · `correlation-field-migration`. **Số thật**: `p9-f-sales-return.test.mjs` **31/31** (gồm **E2E** `/ask` ra card HIGH + câu trả lời nói NHÁP, `/execute` ghi **1 NHÁP** + replay idempotent) · **Node 691 — 689 pass, 2 fail** (cả 2 `dsh-gateway` lỗi môi trường DSH từ B0; so baseline P9-E 656 ⇒ **+35 test, 0 regression**) · Flutter analyze **0** · Flutter **271/271** (+1) · `proposal_card_test` **39/39** · **30/30 đột biến RED** + restore byte-identical · không còn dấu vết đột biến trên đĩa (grep 0). **KHÔNG đụng**: **purchase return** (trả NCC) · **DN-return** (user chốt `return_against` = SI) · submit phiếu trả từ chat (`allow_submit: false`, client chặn, có test) · mọi skill khác. **OCR không khả dụng** trong phiên (không có công cụ) ⇒ **review mặc định** theo Graceful Degradation của `AGENTS.md` (đọc diff + soi null/validate/injection/secret/race thủ công); `detect_changes`/`search_graph` cũng không dùng được ⇒ dùng `grep -rn` cho dead code/impact. **AgentMemory/Simplenote: tool không có trong phiên này** ⇒ KHÔNG ghi memory qua MCP (báo user, không tự cài).
- [x] **[2026-09-23] P9-D — `sales_invoice.create` (WRITE #7, Sales Invoice NHÁP theo đơn bán) → ✅ KỸ THUẬT XONG, **CHỜ DUYỆT COMMIT** (vùng SỐ TIỀN: hoá đơn submit là ghi doanh thu + công nợ ⇒ AI không tự ký duyệt)**: deliverable **`.plan/result-p9-D.md`**. **§0 ĐO ĐƯỢC misroute thật** (Phase 1 + `routeIntent` thật): `xuất hóa đơn cho Lan` · `lập hóa đơn cho Nguyễn Thị Lan` · `xuất hóa đơn cho đơn SAL-ORD-…` đều rơi vào nhóm **ĐỌC `sales` (`invoice.lookup`)** — mệnh lệnh bị trả lời bằng danh sách hoá đơn (đúng lớp lỗi B4/A1); `xuất hoá đơn` (chữ `oá`) = `UNKNOWN_INTENT`. **Đã ship**: (1) contract `sales_invoice.create` WRITE/`risk HIGH`/`requires_confirmation`, `entities.required: [order]` (customer optional), `line_policy.rate_source: erpnext` + `no_price: ask`, `execution.draft_only true` + **`allow_submit false`** + verify/idempotent/reconcile + correlation `custom_ai_action_id`, **29 mã lỗi**, nhóm route `sales_invoice_write` đứng **TRƯỚC** nhóm `sales`; (2) `src/skills/sales-invoice-write.mjs` (mới): builder (0 write) + executor + verify + reconcile; (3) router/copilot-server/safety-gateway nối card HIGH + `/execute`; (4) mock `createSalesInvoice` **tái hiện validation thật** (buộc `sales_order`+`so_detail`, `rate` PHẢI bằng rate của đơn, chặn over-billing) và **KHÔNG đụng `billed_amt`** (chỉ submit mới nhích); (5) Flutter: `create_sales_invoice` vào `_confirmableActions` (có tripwire so `executableWriteActions()`), card hiện dòng hàng + "Tạm tính" theo giá **của đơn**, nút "Xác nhận tạo hoá đơn (NHÁP)", câu kết quả nói rõ **chưa ghi doanh thu/công nợ, KHÔNG đụng kho"; (6) harness đột biến trong repo `scripts/falsify/p9d-sales-invoice.mjs` — **11/11 RED**. **LẦN CHẠY TEST ĐẦU: 19/25 — 6 đỏ, đã truy nguyên nhân**: **(a) 1 BUG THẬT trong mock** — `erpnext_doc_get` có **HAI nhánh `Sales Invoice`**, nhánh cũ (`INVOICES`) `throw` trước ⇒ nhánh mới (đọc lại NHÁP vừa ghi cho verify + draft-cover) **không bao giờ chạy** (triệu chứng `Sales Invoice SI-M001 not found` trông y hệt bug của executor) ⇒ gộp thành MỘT nhánh, chỉ fixture mới được tiêm `company`/`debit_to`; **(b) 1 GAP THẬT phát hiện qua chuẩn bị harness** — ca J `NOT RED` với anchor sống: guard "executor trừ hoá đơn NHÁP" **có code nhưng chưa có test** (bản DN có, bản SI mirror thiếu — **mirror drift lần 2** như P9-B) ⇒ bổ sung test rồi RED; **(c) 4 kỳ vọng SAI của test, KHÔNG sửa code**: `SI_NOTHING_TO_BILL` (đường duyệt nhiều đơn khi câu không nêu đơn) khác `SI_ALREADY_BILLED` (mã theo **từng** đơn) · `PROPOSAL_VERSION_STALE` (P1 đã tách; `PROPOSAL_STALE` chỉ còn `legacy_code`) · câu HỎI trả **card ĐỌC A1** (`read_open_invoices`, `risk READ`) — không phải card GHI, nên assert **loại** card chứ không assert `null` · 2 knob drift bật cùng lúc thì nhánh số-dòng `else` chặn so-giá ⇒ **tách 2 test riêng** (còn dùng 2 knob `MOCK_ERP_SI_{DROP_LINE,DRIFT_RATE}` vốn chưa được test nào dùng). Đổi tên key `params.net_total_vnd` → **`estimated_total_vnd`** để card dùng chung renderer 6 chứng từ (nếu không, dòng "Tạm tính" in `?đ`). **Số thật**: `p9-d-sales-invoice.test.mjs` **28/28** · **Node 631 — 629 pass, 2 fail** (cả 2 `dsh-gateway` lỗi môi trường DSH từ B0; baseline 603/601 ⇒ **+28 test, 0 regression**) · Flutter analyze **0** · Flutter **269/269** (+1 widget test) · **11/11 đột biến RED** + restore byte-identical (`sha256sum -c` cho cả skill lẫn `capabilities.json`) · secret scan 7 file: 0 khớp. Tripwire cũ sửa **có chủ ý** (Sales Invoice từ STUB nay là WRITE thật): `capability-contract`, `correlation-field-migration`, `safety-gateway`, `b2`, `b3`, `b4`. **KHÔNG đụng**: submit từ chat · hoá đơn không-theo-đơn · DN/PR/payment · Journal Entry. **OCR không chạy được** (máy không có git ⇒ không có diff) ⇒ review thủ công có bằng chứng + mutation proof.
- [x] **[2026-09-23] P9-C — `payment.create` HỌC HƯỚNG CHI (chi tiền NCC), KHÔNG capability mới → ✅ ĐÃ DUYỆT (user chốt 2026-09-23), CHƯA PUSH (vùng SỐ TIỀN/HƯỚNG TIỀN — AI không tự ký duyệt)**: deliverable **`.plan/result-p9-C.md`**. **§0 ĐO ĐƯỢC 1 MISROUTE THẬT (probe Phase 1 + `routeIntent` thật)**: `src/vietnamese_nlp/synonyms.py` gộp **cả hai chiều tiền** vào MỘT nhãn `payment` (`trả tiền`/`thu tiền` → `payment`), và `payment-write.mjs` hardcode `Receive`/`Customer`/`Sales Invoice` ⇒ **lệnh CHI đi vào đường THU** (NCC trùng tên khách ⇒ tạo **phiếu THU** cho ý định chi); thêm nữa `chi xăng 200 nghìn`/`chi tiền mặt 500 nghìn` = **UNKNOWN_INTENT**, `chi 2 triệu trả NCC Hà Tiên` rơi vào `payment.history` (đọc), `đã chi cho Hà Tiên bao nhiêu` = UNKNOWN. ⛔ KHÔNG sửa `synonyms.py` (đổi Phase 1 dùng chung mọi đường) — **hướng tiền suy từ DỮ LIỆU** (sổ nào giữ tên). **USER CHỐT 2026-09-23**: mở rộng `payment.create` (không capability mới) + chi-không-NCC ⇒ **TỪ CHỐI kèm giải thích**. **Đã ship**: (1) contract `direction_policy` (rule + verb_check + **không nhận `direction` từ request**), slot `customer`→**`party`**, errors `PAYMENT_DIRECTION_AMBIGUOUS/CONFLICT/PARTY_NOT_SUPPLIER/SUPPLIER_NO_OPEN_INVOICE`, routing `payment_write` thêm mốc `chi ` (kèm `notIf` giữ câu HỎI + `chi tiết`/`chi nhánh`); (2) skill 2 hướng bằng **một bảng `DIRECTION_SPEC`** (payment_type/party_type/anchor doctype+account field/money side — hai phía không thể lệch nhau) · Pay đọc **Purchase Invoice** mở của NCC · executor re-verify; (3) resolve tên với **CẢ HAI sổ** trên text đã bỏ số tiền nói ra, câu nói chỉ là **kiểm tra MÂU THUẪN không bao giờ đảo hướng**; (4) READ lịch sử 2 hướng (`partyType`); (5) mock: sổ phải trả `2110`, PURCHASE_INVOICES (1 dòng đã tất toán), đối tác trùng tên cả hai sổ; (6) Flutter nhãn theo hướng (badge P5-3 giữ nguyên, KHÔNG đổi storage key); (7) **harness đột biến TRONG REPO** `scripts/falsify/p9c-payment-pay.mjs` — **9/9 RED**, restore byte-identical (thay script `/tmp` của P9-B). **Golden**: 6 lệnh → `payment.create` (`trả tiền NCC Hà Tiên 2 triệu` · `thanh toán hóa đơn mua cho Hà Tiên 2 triệu` · `chi xăng 200 nghìn` · `chi tiền mặt 500 nghìn` · `chi 2 triệu trả NCC Hà Tiên` · `thu tiền của Lan 2 triệu`), **3 câu HỎI lịch sử chi → `payment.history` KHÔNG mở card**, 2 câu `chi tiết/chi nhánh` không bị nuốt, 7 câu E2E `/ask`, mốc động từ chỉ để phát hiện mâu thuẫn (`trả tiền`/`thanh toán` = **null**, tiếng Việt dùng cả hai chiều). **REVIEW TÌM 4 LỖ HỔNG — đã xử lý**: (1) **NẶNG NHẤT: `verifyWrittenPayment` KHÔNG chứng minh hướng tiền** — nó chỉ so `party` với id mình gửi, mà id là **cùng một chuỗi** ở cả hai phía ⇒ một phiếu Receive cho lệnh chi vẫn "verify sạch" nếu số tiền khớp (trước P9-C vô hại vì chỉ một hướng; hướng thành biến số thì đây là chỗ hở) ⇒ thêm `direction` (tuỳ chọn, tương thích ngược) so `payment_type`+`party_type`, kèm test "cùng tài liệu hỏi hướng ngược ⇒ `PAYMENT_WRITE_UNVERIFIED`" + ca đột biến **I**; (2) **ca đột biến G của tôi trỏ SAI tên test** (lần đầu báo `NOT RED`) — `anchor_doctype` **có** được khẳng định, nhưng ở tầng E2E `/execute` (đọc lại phiếu), không phải test builder ⇒ sửa `only` + ghi lý do **trong harness** (đây là bước CONTROL của harness bắt lỗi **người viết ca**, không phải guard chết); (3) hai **thông báo lỗi nói SAI sổ** cho hướng chi ("không phải khoản **phải thu** dương" / "số tiền **thu** không hợp lệ") ⇒ đổi thành phải trả/phải thu, chi/thu (**chỉ chữ**; mã `PAYMENT_INVOICE_NOT_RECEIVABLE` giữ nguyên vì đã map trong bảng uncertainty P2 + test cũ khẳng định); (4) `PAYMENT_PARTY_NOT_SUPPLIER` **gộp hai nguyên nhân** ("không phải NCC" vs "đọc bị từ chối") ⇒ giữ `readError` và in ra trong thông báo. Xoá 3 file tạm của phiên (`.plan/probe-p9c-*.mjs`, `.plan/mut-p9c.sh`). **Số thật**: `p9-pay.test.mjs` **19/19** · p9-pay+payment-write+correlation-field **60/60** · **Node 603 — 601 pass, 2 fail** (cả 2 `dsh-gateway` lỗi môi trường DSH từ B0; baseline 584/582 ⇒ **+19 test, 0 regression**) · Flutter analyze **0** · Flutter **268/268** (+3 widget test) · **9/9 đột biến RED** + restore byte-identical · quét secret 9 file chỉ khớp **TÊN** biến. **GIỚI HẠN ghi rõ (không tự sửa)**: (a) **nháp chưa trừ vào nợ khi đề xuất lại** — `outstanding_amount` tính từ GL (đã submit) nên nháp không giảm nợ; khác delivery (P9-A1 có `openDraftCover`) nhưng đây là hành vi **có từ trước** ở đường THU ⇒ cần user quyết (đề xuất P9-D nhỏ); lưới an toàn hiện có: mỗi `/execute` đọc lại nợ + kẹp trần, `detectDrift` ⇒ `PROPOSAL_STALE`, ERPNext validate lúc submit; (b) **chưa có picker NHÀ CUNG CẤP** (picker Flutter chỉ mang id khách ⇒ tên NCC mờ bị TỪ CHỐI, không tự chọn); (c) chi phí cửa hàng không có đường ghi (cố ý — JE ngoài phạm vi); (d) `reconcilePaymentEntry` không khẳng định hướng (tra theo `reference_no = command_id`, duy nhất theo action — ghi lại để biết là lựa chọn). **OCR KHÔNG chạy được** (repo chưa có git ⇒ không có diff cho OCR) ⇒ review thủ công có bằng chứng (đọc source + grep + mutation proof), KHÔNG tự cài/cấu hình OCR. **KHÔNG đụng**: skill DN/PR/SI · mặc định auto-submit · Journal Entry · capability mới. **DUYỆT 2026-09-23**: user duyệt P9-C (chưa push — máy không có git; push để riêng khi cần build APK) · giới hạn "nháp chưa trừ vào nợ khi đề xuất lại" user chốt **ĐỂ LẠI làm sau** ⇒ đã ghi thành **P9-D (nhỏ, CHƯA LÀM)** ở `next.md` §"Sắp tới" (không tự mở lại — vùng SỐ TIỀN).

- [x] **[2026-09-22] P9-B — `purchase_receipt.create` (WRITE #6) + SỬA 4 LỖ HỔNG CỦA `delivery` → ✅ KỸ THUẬT XONG, CHỜ DUYỆT COMMIT (vùng SỐ LƯỢNG/KHO + CHỨNG TỪ MUA — AI không tự ký duyệt)**: deliverable **`.plan/result-p9-B.md`**. **(A) Rà A1/A2 tìm được 4 lỗ hổng THẬT trong `delivery-write.mjs`, đã sửa**: (1) **UOM mismatch** — `pairLinesPure` không so **đơn vị nói** với **đơn vị dòng** ⇒ "giao 1 tấn" cho dòng tính bằng bao = số sai; (2) **dead clause full-page** (heuristic limit không bao giờ chạy); (3) **draft-DN accumulation** — nháp DN trước đó **đã chiếm** phần chờ giao nhưng builder đọc đơn không trừ ⇒ **đề xuất giao lại phần đã có nháp** (nay `listOpenDraftDeliveryNotes` + `openDraftCover`, executor cũng trừ draft khi drift-check); (4) header cũ. **(B) P9-B**: contract `purchase_receipt.create` **bỏ stub** (draft-only, `write_doctype: Purchase Receipt`, `rate_source: erpnext`/`no_price: n/a`) · **builder 2 đường** (theo **PO đã submit** — pending thật = `qty − received_qty − returned_qty`, `purchase_order_item`/`po_detail`; hoặc **direct supplier**) · executor + reconcile + verify `docstatus === 0` (**0 submit**) · route `"nhập hàng"`/`"nhận hàng"` + **notIf giữ câu HỎI tồn** ⇒ vẫn về READ `stock.balance` (đúng comment B4) · mock `PURCHASE_RECEIPTS` + PO **submitted** fixture (`MOCK_ERP_PO_FIXTURE`) + `createPurchaseReceipt` (bắt buộc `purchase_order`+`purchase_order_item` khi theo PO, chặn vượt pending, draft **không** đụng `received_qty`) + doc_list/get + knobs chaos/DRIFT · `safety-gateway` executor + allowlist doctype · Flutter nhãn **"Xác nhận tạo phiếu nhận hàng (NHÁP)"** + `_receiptSuccessText` ("CHƯA cộng kho") + 1 widget test. **LỖI CỦA CHÍNH TÔI**: (1) **lặp lại y hệt lỗi tham số của A1** — đặt `lines` vào túi `resolved` (tham số 2) thay vì `opts` (tham số 3) ⇒ 1 ca đỏ *trông như guard sai*; (2) **script đột biến quên bước RESTORE** ⇒ file còn **4 đột biến chồng nhau** (M4∘M3∘M2), phải đảo ngược từng replace + `sha256sum -c` mới về byte-identical; (3) **M3 làm test KHÔNG đỏ ⇒ phát hiện thật: guard khấu trừ draft ở **executor** có code nhưng **chưa có test nào** (bản DN có, bản PR mirror thiếu) — **mirror drift**; đã bổ sung test rồi M3 RED; (4) `partyKind` chưa nhận `purchase_receipt_write` là **supplier** ⇒ E2E đỏ (sửa ở `copilot-server.mjs`); (5) expectation **409 vs 500** cho `CORRELATION_FIELD_MISSING` — convention project là **500 "site misconfigured"** (B4 đã pin), sửa expectation chứ không sửa code. **Số thật**: `p9-receipt.test.mjs` **22/22** · **Node 584 — 582 pass, 2 fail** (cả 2 `dsh-gateway` lỗi môi trường DSH từ B0; baseline 537/535/2 ⇒ **+47 test, 0 regression**, trong đó `p9-delivery` nay **25/25**) · Flutter analyze **0** · Flutter **265/265** · **6/6 đột biến guard PR đều RED** + restore `sha256sum -c` **byte-identical**. **Tripwire cũ sửa có chủ ý**: `b2`/`b3`/`b4`/`capability-contract`/`correlation-field-migration`/`c2-ocr-proposal`/`safety-gateway` (danh sách write tường minh + scope migration + `EXECUTOR_NOT_REGISTERED`/`CAPABILITY_NOT_FOUND` đúng mã thật) + `chat_models.dart` `_confirmableActions`. **KHÔNG đụng**: submit PR/DN · ERPNext thật · MCP thô cho DSH · stock entry generic. **Máy vẫn KHÔNG có git** ⇒ chưa commit/diff tại chỗ.

- [x] **[2026-09-22] P9-A2 — `delivery.create` WIRE UX + EXECUTE NHÁP (`.plan/next1/p9_guide.md` §3, WRITE #5) → ✅ KỸ THUẬT XONG, CHỜ DUYỆT COMMIT (vùng SỐ LƯỢNG/KHO — AI không tự ký duyệt)**: deliverable **`.plan/result-p9-A2.md`** · tiền đề **không dùng ERPNext thật** trong phiên (prompt không cho phép) — mọi số là mock/suite thật. **§0 ĐO ĐƯỢC 1 MISROUTE THẬT ĐÃ ĐÓNG**: `"giao 5 bao cám gà thịt 10kg cho Lan"` (dạng mệnh lệnh tự nhiên nhất) rơi vào **`inventory/stock.balance`** = MỆNH LỆNH bị trả lời bằng một con SỐ TỒN KHO (đúng lớp lỗi B4 đã đóng cho "nhập kho") ⇒ thêm keyword `"giao "` + thêm `"dịch"` vào notIf ("giao dịch …" giữ nguyên hành vi cũ). **§0 điểm 2**: `resolveCapability` **chỉ áp notIf khi route có `startsWith`** ⇒ test routing ghim `startsWith === true` để notIf không thành dead code. **Không bỏ cờ stub trước khi wire** (bài học A1): A2 wire **đủ** nhánh `isDeliveryWrite` + factory + executor rồi mới bỏ ⇒ hết nguy cơ "giao hàng cho Lan" trả SỐ NỢ. **Routing golden (test thật)**: **7/7 lệnh** (`giao hàng …`, `giao 5 bao … cho …`, `giao cho …`, `xuất hàng cho …`, `giao đơn hàng …`, `giao hàng đơn SAL-…`) → `delivery.create`; **9/9 CÂU HỎI** (`đã giao chưa`, `giao bao nhiêu`, `còn giao không`, `xem tình trạng giao hàng`, `hôm qua tôi giao hàng cho Lan`, `giao dịch …`) → **KHÔNG** mở card; `đặt hàng`/`báo giá` vẫn về đúng đường cũ. **`/ask` (E2E thật stdio copilot + NLP + mock)**: proposal `create_delivery_note` HIGH `need_confirm`, `params.against_sales_order = SAL-ORD-2026-00001`, **`lines` lấy từ chính ĐƠN** (dòng 1 `qty = 6` = 10 − 4 của ERPNext, có `so_detail`), `submit_now: false`, answer VN nói rõ **NHÁP + KHÔNG tự submit + CHƯA trừ kho**, và **không có `erpnext_doc`** (chưa ghi gì); khách không có đơn đã submit ⇒ **refusal có lý do** (ASK). **`/execute` (HTTP → Safety Gateway → executor → mock)**: confirm ⇒ 200 `DN-M###` **`docstatus 0`** + verify đọc lại; **trùng `command_id` ⇒ `replay: true` cùng document, state 1 phiếu**; **CHAOS lost-response** (`MOCK_ERP_FAIL_AFTER_WRITE`: ghi xong rồi mất phản hồi) ⇒ lần 1 **503 `retry_same_command_id`** (command giữ PENDING, phiếu đã nằm trong state), lần 2 **200 `reconciled: true, duplicate_documents: 0`** — **KHÔNG giao hai lần**; **không confirm ⇒ 0 phiếu**; card **STALE** (11' > TTL 10') ⇒ **409 `PROPOSAL_EXPIRED`** 0 phiếu **không burn id**; card **DRIFT** (số lệch so với đơn thật) ⇒ **409 `PROPOSAL_VERSION_STALE` + `legacy_code: PROPOSAL_STALE`** 0 phiếu (**không kẹp số**). **Submit vẫn chặn ở code**: `writeDoctypes()["Delivery Note"] = {create: true, submit: false}` ⇒ `erpnext_doc_submit` trả **WRITE_REFUSED**, executor verify còn đòi `docstatus === 0`. **Mock bổ sung (G-MOCK của A1 đã trả)**: fixture env `MOCK_ERP_SO_FIXTURE` (đơn **đã submit**, cùng convention `MOCK_ERP_P4_FIXTURE`) + `createDeliveryNote` **tái hiện đúng luật ERPNext** (bắt buộc `against_sales_order`, đơn phải `docstatus 1` + đúng khách, `so_detail` phải là dòng thật, chặn vượt `qty − delivered_qty`, **draft không đụng `delivered_qty`**) + doc_list/get + knob chaos/DROP_LINE/DRIFT_QTY; `DELIVERY_NOTES` khai **trước** khối restore state (bài học TDZ B2). **Flutter (type tối thiểu, không redesign)**: `_confirmableActions` += `create_delivery_note` (test cross-check ghim **bằng nhau** với `executableWriteActions()`), nhãn nút **"Xác nhận tạo phiếu giao (NHÁP)"**, **bỏ dòng "Tạm tính"** cho phiếu giao (không có giá riêng), `_deliverySuccessText` = "Đã tạo phiếu giao hàng NHÁP: DN-… — phiếu NHÁP theo đơn, **CHƯA trừ kho**", badge vẫn theo P5-3 (không submit step ⇒ NHÁP) + **1 widget test mới**. **4 TRIPWIRE CŨ PHẢI SỬA (có chủ ý)**: `b2-sales-order` (cross-write cũ ghim `KNOWN_INTENT_UNIMPLEMENTED` ⇒ nay khẳng định **bất biến** "không bao giờ là đơn bán" + `proposal = null` + refusal có lý do), `b3-quotation` + `b4-purchase-order` (danh sách `executableWriteActions()` viết tường minh ⇒ thêm `create_delivery_note`), `correlation-field-migration` (scope derive từ contract ⇒ **Delivery Note** vào danh sách; ca "stub không bịa doctype" chuyển sang `purchase_receipt.create`). **BUG CỦA CHÍNH TÔI khi làm đột biến M2**: lần đầu `if (qty - line.pending > EPS)` xuất hiện **2 chỗ** (builder + executor) — tôi sửa **chỗ đầu** ⇒ đột biến **không bắt được gì** (suýt kết luận oan "guard không sống"); phải neo bằng **đoạn văn cả 3 dòng** đặc trưng của executor rồi mới RED. **Số thật**: `p9-delivery.test.mjs` **21/21** (13 A1 giữ nguyên + **8 mới**) · **Node 558 — 556 pass, 2 fail** (cả 2 `dsh-gateway` từ B0; baseline A1 550/548/2 ⇒ **+8 test, 0 regression**) · Flutter analyze **0** · Flutter **264/264** (+1) · `proposal_card_test` **32/32** · **đột biến M1 (bỏ keyword "giao ") / M2 (vô hiệu kiểm drift qty trong executor) / M3 (`allow_submit: true`) đều RED**, restore `sha256sum -c` **byte-identical** (capabilities.json khớp hash gốc `fce27945…`). **An toàn chế độ AI (dsh) — ĐO chứ không suy diễn**: `blockedInDshContext()` lọc mọi nhóm WRITE **derive từ contract** ⇒ `delivery_write` tự động bị chặn, **đã thêm test** (`COPILOT_DSH_CONTEXT=1` + câu giao hàng ⇒ `DSH_WRITE_BLOCKED`, proposal null; câu đọc vẫn trả lời). **QUAN SÁT (không sửa, có từ B2)**: `dsh-gateway.mjs:435` `dshQuestionGate` vẫn hardcode `payment_write` ⇒ không chặn sớm delivery/SO/QT/PO ở **lớp ngoài**, nhưng **không tạo được đường ghi** vì lớp contract-derive là lớp thật và `dshGatewayHealth` fail nếu patch thiếu `COPILOT_DSH_CONTEXT=1` — để riêng một việc dọn. **Gap bàn giao**: `posting_date` cố ý không gửi (bài học P5-2) · mapper `make_delivery_note` nên là đường chạy thật khi bật ERPNext thật · `warehouse` chỉ truyền khi đơn có · cần **migration `custom_ai_action_id` trên Delivery Note** trước khi dùng thật (script đã phủ DN) · **chưa submit DN bao giờ** (Go/No-Go riêng). **KHÔNG đụng**: submit DN · skill khác · MCP ERPNext thô cho DSH · ERPNext thật. **Máy vẫn KHÔNG có git** (`git rev-parse` → not a repository) ⇒ chưa commit/diff tại chỗ.

- [x] **[2026-09-22] P9-A1 — `delivery.create` CHỈ PROPOSAL PATH (`.plan/next1/p9_guide.md` §3) → ✅ KỸ THUẬT XONG, CHỜ DUYỆT COMMIT (vùng SỐ LƯỢNG/KHO — AI không tự ký duyệt)**: deliverable **`.plan/result-p9-A1.md`** · file mới **`src/skills/delivery-write.mjs`** (`buildDeliveryProposal` — **KHÔNG có executor**). **§0 đo trước**: `writeDoctypes()` derive từ `execution.write_doctype` ⇒ **không** khai `write_doctype` ⇒ write gate vẫn từ chối "Delivery Note"; `executorFor()` fail-closed độc lập cờ stub. **⚠️ QUYẾT ĐỊNH CỐT LÕI — giữ `status: "stub"`**: đo được `copilot-server.mjs:928-932` **không có nhánh `delivery_write`** và `SKILL_FACTORIES` không có factory cho group đó, nên bỏ cờ stub trước khi wire ⇒ "giao hàng cho Lan" resolve khách xong **rơi xuống nhánh cuối `customer` (đọc công nợ)** = trả SỐ NỢ cho MỆNH LỆNH giao hàng (regression tệ hơn "chưa làm"). Vì vậy A1 chỉ ship builder + policy + test, cờ stub giữ nguyên ⇒ `b2-sales-order.test.mjs:818` (E2E refusal) vẫn xanh; lý do ghi ngay tại contract + đầu file skill. **Builder**: entity bắt buộc = **Sales Order** (không tự chọn đơn), đọc lại đơn từ ERPNext, đơn phải **đã submit** (ERPNext không nhận `against_sales_order` trên đơn nháp), **khách do đơn quyết định** (mismatch ⇒ từ chối), pending = `qty − delivered_qty`, số lượng tường minh phải thuộc chính đơn đó + ≤ pending (**từ chối, không kẹp**) + **không lặp cùng mặt hàng**; proposal HIGH `create_delivery_note`, `submit_now: false`. **VERIFY-FIRST ăn tiền**: đọc skill đã verify của chính project — `erpnext-rest-api-core:205` xác nhận **DN dùng `against_sales_order`** (SI mới dùng `sales_order`+`so_detail`) ⇒ payload đúng; `erpnext-rest-api-recipes:974` dùng `delivered_qty` trên output mapper thật ⇒ **field thật** (không chỉ mình tôi đoán); `recipes:976-980` cho biết đường ĐÃ CHẠY THẬT là gọi mapper `make_delivery_note` ("không cần tự cộng trừ") ⇒ **A2 nên dùng mapper ở execute** (ghi gap). **REVIEW sau khi code — 1 finding CAO do chính A1 tạo ra, đã sửa**: cùng mặt hàng nêu 2 lần ("2 bao và 3 bao") ⇒ 2 dòng cùng item, mỗi dòng ≤ pending ⇒ **double-deliver**; nay chặn bằng `DN_QTY_AMBIGUOUS` (không cộng/merge, đúng tiền lệ SO coi "2 bao và 3 bao" là CÂU HỎI) + khai contract + test + **đột biến M3 ⇒ RED**. **2 test cũ phải sửa vì là tripwire có chủ ý**: `capability-contract.test.mjs:200` (`stub.skill === null` → nay khẳng định skill path **+** vẫn stub/không executable) và `safety-gateway.test.mjs:171` INVARIANT (derive write-skills từ contract → lọc `!isStub(id)`, có ngữ nghĩa: builder của stub KHÔNG ở trên đường ghi, và giữ nó NGOÀI allowlist là điều kiện mạnh hơn). **1 bẫy nữa**: INVARIANT static quét **raw text** nên comment tự viết "execute…Proposal(" bị tính là ở trên write path ⇒ sửa bằng cách **không viết ra chữ đó**, không whitelist file. **BUG CỦA CHÍNH TÔI trong test**: đặt `lines` vào túi `resolved` (tham số 2) thay vì `opts` (tham số 3) ⇒ `opts.lines` undefined ⇒ builder dựng full pending ⇒ 3 test đỏ **trông như builder sai**; đã thêm comment cảnh báo tại chỗ. **Số thật**: `p9-delivery.test.mjs` **13/13** · **Node 550 — 548 pass, 2 fail** (cả 2 `dsh-gateway` từ B0; baseline 537/535/2 ⇒ **+13 test, 0 regression**) · Flutter analyze **0** · test **263/263** · đột biến **M1/M2/M3 đều RED**, restore `sha256sum -c` **OK**. **Gap cho A2** (đo được): G-WIRE (nhánh + factory, **rồi mới** bỏ cờ stub) · G-ROUTE (G1 `startsWith`+`notIf`) · G-EXEC (executor/reconcile + dùng mapper + bật idempotent kèm correlation fields) · G-MOCK (mock hiện tạo SO `docstatus: 0`, thiếu `delivered_qty`/`customer_name` ⇒ cần fixture SO đã submit) · G-VERIFY (`so_detail` trên dòng DN chưa verify cho DN). **Không đụng** Flutter · không `/execute` · không submit DN · không gọi ERPNext thật.

- [x] **[2026-09-22] P9-0 — GATE BACKLOG P9 (`delivery.create`) → ✅ GO (kèm 5 gap là việc A1/A2, KHÔNG viết code)**: deliverable **`.plan/result-p9-0.md`**. **Đo từ contract + suite thật, không đọc guide rồi tin**: 4 Reference WRITE đã ship (`payment.create`/`sales_order.create`/`quotation.create`/`purchase_order.create`) **skill path ≠ null** + wiring thật ở `router.mjs` `SKILL_FACTORIES` + `copilot-server.mjs:1021` + `safety-gateway.mjs` `WRITE_EXECUTORS`; 2 STUB (`delivery.create`, `purchase_receipt.create`) `skill=null`/`status="stub"`/`write_doctype=null` ⇒ `copilot-server.mjs:528` trả `KNOWN_INTENT_UNIMPLEMENTED`, `proposal=null` (ghim bởi `b2-sales-order.test.mjs:818`, `b4-purchase-order.test.mjs:725`, `copilot.test.mjs:417` — đều PASS). **10 file skill** hiện có (6 READ + 4 WRITE), **chưa** có `delivery-write.mjs`. **GO/NO-GO: GO** — không gap nào chặn A1; 5 gap liệt kê file cụ thể: (G1) `delivery_write` group **thiếu `startsWith`+`notIf`** — `capability-contract.mjs:761` chỉ áp deny-list khi có `startsWith`, nên khi stub thành skill thật thì câu HỎI "đã giao chưa" sẽ mở card HIGH (đúng lỗi B4) ⇒ A2 phải thêm + test; (G2) contract `delivery.create.execution` thiếu `draft_only`/`write_doctype`/`correlation_field`/`reference_field` + `confirmation.ttl="0m"`, `errors[]` cần `DN_*`; (G3) chưa có nhánh `isDeliveryWrite` + factory, và **DN cần resolve Sales Order** (máy móc mới — hiện `partyKind` chỉ resolve customer/supplier); (G4) chưa có executor/reconcile; (G5) chưa có file skill. **An toàn nền đủ GO**: `allow_submit:false` + DN draft (docstatus 0) **không** dịch chuyển tồn kho. **Suite thật**: Node `537 — 535 pass, 2 fail` (cả 2 là `dsh-gateway.test.mjs` `dshGatewayHealth`+`verifyDshRuntime`, lỗi môi trường DSH từ B0, KHÔNG liên quan P9) · Flutter analyze **0 issue** · Flutter test **263/263 All passed**. **Không đụng gì** ngoài đọc + chạy suite (không skill mới, không sửa behavior WRITE, không ERPNext thật).

- [x] **[2026-09-22] PUSH LẦN 2 lên GitHub + CI BUILD APK XANH → ✅ XONG (kèm 3 rò rỉ bị chặn)**: commit **`53e1851`** (merge `--allow-unrelated-histories -X theirs`, **không force**, giữ nguyên 12 file remote-only: workflow + 11 `.project/`, **không xoá file nào của remote** — kiểm bằng `comm -23`). **CI: `android-debug-apk` → `success`** (run **`35688782230`**, artifact **`erpn-chat-debug-apk` 81.4 MB**): https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35688782230 · chỉ **1 file mới** (`p52-payment-snapshot.mjs`) vì toàn bộ P5 là sửa file đã có. **🗑️ 3 rò rỉ bị chặn trước khi push** (nếu lọt là nằm vĩnh viễn trong history): (1) `.chats/sess1.md` chứa **token GitHub THẬT**; (2) `.review/chat1..7.md` + `dsh_test.md` = 8 transcript chat thô; (3) `.gemini/**` (192K config CLI + skills) + `initp` (script tạo .gitignore — AGENTS cấm commit). Cả 4 **chưa từng** lên remote (kiểm `ls-tree` remote = 0). Đã chặn vĩnh viễn bằng `.gitignore` (`.chats .review .gemini .opencode` + `initp`) — **lỗi của tôi ở lượt trước**: lần push đầu đã dùng `git add -A` mà không soi danh sách thư mục; lần này bắt được nhờ `git grep -l <token thật>` + liệt kê top-level dir. **🔐 `.env` đang là `664` (ai cũng đọc được: ERPNext key/secret, GH_TOKEN, GEMINI/ZEN/MAC LLM key, ASK_PASSWORD) trong khi rule dự án bắt 600 ⇒ đã `chmod 600`.** `.env` đã sẵn có `GH_TOKEN=` + `GH_REPO_URL=` (đúng chỗ rule cho phép) nên tôi **xoá** bản token trùng mình vừa tạo ở `~/.config/erpn-gh/token` (giảm bề mặt lộ, secrets chỉ 1 bản). **Skill mới `.agents/skills/erpn-deploy-github/`**: repo URL + branch + workflow + artifact + quy trình push đã verify 2 lần + cách đọc token từ `.env` — **KHÔNG chứa token** (rule cấm secret trong repo/docs). **3 bẫy git đã ghi LESSONS**: `git rm -r --cached a b c` **abort toàn bộ** khi 1 pathspec không khớp (và im lặng nếu nuốt stderr) · `git merge FETCH_HEAD` trả **"Already up to date" SAI** cho commit không liên quan (dùng **tên ref** thì đúng) · `.gitignore` **không** áp dụng cho file đã tracked. **⚠️ VIỆC CỦA USER**: **revoke token `ghp_…7aO`** (đã qua chat 2 lần + nằm trong `.chats/sess1.md`) rồi tạo token mới dán vào `.env`; hoặc bật **deploy key WRITE** cho repo để bỏ hẳn PAT.

- [x] **[2026-09-22] P5-4 — VOICE-FIRST LAYOUT (2 CHẾ ĐỘ) + HUỶ + CAP 25s (`.plan/plan5_final.md` §6 + §7 mục 3/4) → ✅ XONG, CHỜ DUYỆT UI**: deliverable **`.plan/result-p5-4.md`** (mô tả 2 layout để user duyệt — không có tool chụp màn hình). **§0 đo trước**: "VAD" thật là `pauseFor: 3s` + `listenFor: 30s` **hardcode** trong `SystemSpeechService` (không phải 1.5s như review đoán) · `AppSettingsService` **không reactive** nhưng ChatScreen **đã tự re-render khi quay về từ /settings** (comment base-URL footer) ⇒ đọc setting trong `build` là đủ, không phát minh cơ chế lắng nghe mới. **Đã làm**: (1) `AppConstants.voiceMaxRecordDuration = 25s` + `voiceSilenceTimeout = 2200ms` — cả Timer trong `_InputBar` LẪN `listenFor`/`pauseFor` của recognizer đọc cùng nguồn, hết literal rải rác; (2) 2 layout đúng 2: OFF = `_buildDefaultRow` **giữ nguyên từng pixel** (test cũ tìm `Icons.mic_none` vẫn xanh), ON = `_buildVoiceFirstRow` **cùng vùng** input bar (mic CTA `filled` key `voice-primary-mic`, icon 28, đỏ khi nghe; TextField **vẫn enabled** — có test gõ tay; camera `filledTonal`; AnimatedSwitcher 200ms + KeyedSubtree key riêng); (3) **Huỷ** (key `cancel-dictation` + icon trong row, nút to đặt NGOÀI AnimatedSwitcher): cancel() + **khôi phục field về `_baseText`** — hợp đồng "không đưa transcript vào input" phải là khôi phục, vì partial **đã stream vào field trong khi nghe** (có precondition test chứng minh); khác tap-to-stop (giữ transcript) — có test đối xứng cho cả 2 phía; (4) **cap 25s** arm cho mọi session cả 2 layout, bắn = `_toggleMic()` đúng hình thái tap-to-stop: giữ transcript, **không Huỷ, KHÔNG auto-send** (quyết định ghi ở result §3: timer tự gửi = gửi chữ user chưa kịp đọc; auto-send chỉ thuộc final engine tự bắn; test của TÔI kỳ vọng /ask đã ĐỎ ⇒ soi guard teardown 2026-09-18 rồi sửa EXPECTATION, không thêm auto-send). Duration đọc qua `_maxRecordDuration` có dart-define override (`P5-4-TEST-MAX-RECORD-OVERRIDE`) nhưng test **pump 26s vượt cap thật** — chứng minh đúng hằng production. **Bất biến §3**: auto-send vẫn chỉ `onSend()` → `/ask`, không auto-confirm — test pin cho layout mới (`rec.paths == ['/ask']`, body đúng câu, field rỗng sau gửi). **3 LỖI TỰ MẮC TỰ SỬA**: (1) lỡ xoá comment quyết định P6 khi chèn state (oldString nuốt 15 dòng) — block setState vỡ, analyze bắt, khôi phục nguyên văn; (2) tạo `main()` thứ 2 rồi xoá nhầm main gốc + thiếu `}` đóng `_FakeSpeech` trong `voice_input_test.dart` — thoát bằng đếm `main` + đọc EOF + analyze từng bước; (3) expectation sai của chính tôi ở test cap (§3). **Số thật**: `voice_input_test` **34/34** (26 cũ + 8 mới) · **Flutter full 263/263** · analyze **0** · đột biến **M1** Huỷ dùng stop() ⇒ RED, **M2** bỏ khôi phục field ⇒ RED, restore `sha256sum -c` **OK**. **Không làm (P5-5)**: push-to-talk · noise meter · VAD dynamic · preview 500ms · draft aging. **Không đụng** `_maybeAutoSend` · storage key · server.

- [x] **[2026-09-22] P5-3 — BADGE NHÁP/ĐÃ NỘP + GỢI Ý THEO LOẠI LỖI + COPY SETTING (`.plan/plan5_final.md` §4.1/§4.2) → ✅ XONG**: deliverable **`.plan/result-p5-3.md`**. **Badge** cạnh dòng kết quả (`ValueKey('submit-badge')`): `submit_ok == true` ⇒ **ĐÃ NỘP** tone `scheme.primaryContainer`; **mọi thứ khác** (false/`null`/server cũ) ⇒ **NHÁP** tone `scheme.tertiaryContainer` — **không hardcode hex** (house-rule đầu file), NHÁP mượn đúng tone banner chống trùng. Đọc field **có kiểm kiểu** (`is bool` / `is String`) nên "không có báo cáo submit" ≠ "đã nộp". **Gợi ý** (`ValueKey('submit-hint')`, chỉ khi `_submitOk == false`): `permission` → liên hệ quản trị viên ERPNext để được cấp quyền Submit · `period_locked` → liên hệ kế toán, kỳ đã khoá · `workflow` → chứng từ cần được duyệt trước khi nộp · `other`/thiếu field → **không thêm gì**. Message gốc ERPNext **giữ nguyên văn** (hint cộng thêm, không thay thế — có assert riêng). **Copy setting** đổi đúng §4.1 ("Cho phép nộp (submit) Payment Entry trên ERPNext", mô tả nêu rõ OFF = chỉ nháp / ON = tạo + submit nếu đủ quyền) — **`AppConstants.allowSubmitPaymentStorageKey` KHÔNG đổi** (setting đã lưu của user còn nguyên). **2 LỖI TỰ MẮC RỒI TỰ SỬA** (bằng chứng: lần chạy đỏ + `flutter analyze`): (1) helper chèn ngay trước `}` cuối file mà `}` đó đóng **`main()`** ⇒ thành **local** ⇒ trip `no_leading_underscores_for_local_identifiers` ⇒ chuyển 3 helper ra **top-level**; (2) test vòng lặp 4 kind đỏ `tap('Xác nhận thu tiền')` = *"Found 0 widgets"* — **không phải finder sai**: `pumpWidget` dựng **cùng hình dạng cây** nên Flutter **tái dùng Element/State** của card ⇒ vòng 2 đọc kết quả vòng 1 và nút xác nhận đã biến mất ⇒ bọc `KeyedSubtree(key: UniqueKey())` trong `_confirmWith` (comment tại chỗ ghi rõ key là load-bearing). **Số thật**: `proposal_card_test` **31/31** · `settings_test` **31/31** · **Flutter full 255/255** · analyze **0**. **REVIEW sau khi xong (cùng phiên)**: (a) chứng minh UI guard sống bằng **2 đột biến tay** (đảo điều kiện tone ⇒ test "absent submit_ok" RED; phá chữ map `permission` ⇒ test advice RED) + restore `sha256sum -c` **byte-identical** — vì `scripts/falsify/` **không có harness cho card**; (b) **1 giả thuyết SAI tự bắt**: tôi tuyên bố "test không ghim mô tả OFF/ON" vì grep **cả câu** ra rỗng — thật ra test ghim bằng tiền tố ngắn `textContaining('TẮT: xác nhận')` / `('ĐANG BẬT: xác nhận')` (`settings_test.dart:376/417`) ⇒ suýt báo finding sai + thêm test trùng (đã ghi LESSONS + skill); (c) **+1 test mới**: card **BÁO GIÁ** không bao giờ đội badge "ĐÃ NỘP" (B3: báo giá không phải đã bán) — đường này trước đó không ai ghim; (d) **FINDING THẬT → USER CHỐT "đổi theo trạng thái" → ĐÃ SỬA**: dòng kết quả render `'✅ $_result'` **vô điều kiện** ⇒ ca submit bị từ chối đọc thành *"✅ Đã tạo phiếu NHÁP … NHƯNG submit lỗi"* + badge vàng NHÁP = **hai tín hiệu ngược nhau trên cùng dòng**; nay `'${_submitOk == false ? '⚠️' : '✅'} $_result'` (`false` ⇒ ⚠️ · `true`/`null` ⇒ ✅) + **4 assert mới** ghim cả 3 trạng thái (và báo giá giữ ✅; **không** assert ⚠️ findsNothing ở card dòng-hàng vì `params.lines` rỗng có ⚠️ riêng) + **đột biến M3** (bỏ ternary ⇒ test refused RED, restore byte-identical). **Còn lại mức quan sát**: chữ dòng kết quả vẫn màu `scheme.primary` kể cả khi ⚠️ (nền tảng cũ, chưa đổi — để user quyết). **Không đụng** logic server-side · không đổi storage key · không gọi ERPNext thật.

- [x] **[2026-09-22] P5-2 — posting_date từ snapshot + phân loại lỗi submit → ✅ XONG, QUYẾT ĐỊNH ĐÃ ĐƯỢC USER CHỐT**: deliverable **`.plan/result-p5-2.md`**. **✅ USER CHỐT 2026-09-22: giữ NGÀY CỦA TIỆM (VN)** cho `posting_date` (không dùng UTC host) — quyết định nghiệp vụ lâu dài, ghi ở `result-p5-2.md` §1.2 để phiên sau KHÔNG mở lại; đổi ngược về UTC = 1 dòng nhưng phải kèm lý do (test neo vào `shopDay()` nên vẫn xanh kể cả khi hành vi sai). *(AgentMemory không chạy — `http=000`; `codebase-memory-mcp`/ADR không có trong tool của phiên ⇒ không lưu ADR được, đã ghi vào docs repo thay thế.)* **Việc 1 (§4.4)**: `buildPaymentProposal()` freeze `params.posting_date` lúc PROPOSE; `executePaymentProposal()` đọc `proposal.params?.posting_date ?? shopDay()` — **không còn đọc đồng hồ**. Card cũ (không có field) vẫn chạy (fallback). Có `opts.now` làm seam nội bộ (server build opts theo tên, `copilot-server.mjs:1081`). **⚠️ Đổi hành vi**: code cũ dùng **ngày UTC của host**, code mới dùng **ngày TIỆM (Asia/Ho_Chi_Minh)** — không phải sở thích mà đúng hợp đồng đã ghi (`http-ask.mjs:236-241` "which calendar day is it at the shop" + `capabilities.json user_timezone`) và đúng giá trị ERPNext tự chọn; khác biệt thật trong khoảng **00:00–07:00 VN** (phiếu thu bị ghi vào ngày hôm trước ⇒ sai ngày trong báo cáo tiền). `shopDay()` viết cục bộ vì import `vnToday` sẽ tạo **vòng import** + làm **trôi anchor falsify p42** ⇒ bịt rủi ro bằng test `shopDay == vnToday` tại 4 mốc VN/UTC. **Việc 2 (§4.2)**: `submit_error_kind` (`permission|period_locked|workflow|other`) ở **cả 2** nhánh submit lỗi; **string-match** vì P5-0 đã xác nhận `client.mjs` throw `Error` phẳng (`TOOL_ERROR: <text>`) — regex bám tên exception + `HTTP 403` (hình thái thật của client 3.0.4); `submit_error` gốc giữ **nguyên văn**. **Việc 3**: KHÔNG xây permission pre-check 3 cấp (đúng §4.2). **Xác nhận**: TTL 10 phút nên 23:59→00:01 hợp lệ, còn >10 phút thì `assertFresh` (`safety-gateway.mjs:377`) từ chối **trước** executor (dòng 488); `/execute` trả nguyên `result` (`http-ask.mjs:1914`) nên field mới tự tới client cho P5-3. **BUG THẬT tìm được**: full Node suite `530 pass / 3 fail` ⇒ cái thứ 3 là **bom ngày tháng lần 2 trong ngày** — `p44-read-drill.test.mjs` hard-code `D = "2026-09-21"` nhưng request **không kèm `date`** ⇒ server dùng `vnToday()` = 2026-09-22 ⇒ `0 !== 4`; đã sửa bằng `D = vnToday()` + `dayOffset()` (kiểm trước: assert không phụ thuộc chuỗi ngày) ⇒ 12/12; rà p42/p4-ops/p43 chỉ p44 dính. **Số thật**: `payment-write` **33/33** (25 cũ vẫn xanh + 8 mới) · Node **533 — 531 pass, 2 fail (dsh từ B0)** · **falsify p52 7/7 RED, 0 PROBLEM** · p44 harness **ALL GUARDS ALIVE** · mutation sót **0**. **Không đụng** Flutter (P5-3), không bump `PROPOSAL_VERSION` (sẽ phá card cũ), **không gọi ERPNext thật** (prompt không có "cho phép ERPNext thật" + vùng SỐ TIỀN). **REVIEW sau khi xong (cùng phiên)**: tìm ra **1 lỗ THẬT do chính P5-2 tạo** — `params.posting_date` là dữ liệu **client gửi lên** ở `/execute` (`http-ask.mjs:846`) và **không ai validate**; probe chứng minh `posting_date: "2026-01-02"` (lùi 5 tháng) được **ghi không exception**, và `""` cũng được ghi (`??` chỉ bắt null/undefined). Trước P5-2 ngày tính server-side nên client không chọn được. **USER CHỐT "sửa ngay"** ⇒ thêm `readPostingDate()` + `POSTING_DATE_MAX_SKEW_DAYS = 1` (chuỗi YYYY-MM-DD · round-trip ngày có thật · cửa sổ ±1 ngày · thiếu field thì `shopDay()`) đặt **trước `setReference()`** cùng chỗ các re-check khác; mã `PAYMENT_POSTING_DATE_INVALID` (tự map `BUSINESS_VALIDATION_FAILED` qua `uncertainty.mjs:103`, đã khai vào `capabilities.json`). Probe **trước/sau**: ghi âm thầm → `BỊ TỪ CHỐI (fail-closed)`. Harness p52 **9/9** (+H bỏ cửa sổ, +I bỏ round-trip) — và **anchor ca A/C của chính harness đã trôi** khi đổi code (đúng bài học P4-7), đã sửa. **Số cuối**: `payment-write` **37/37** · Node **537 — 535 pass, 2 fail (dsh)** · mutation sót **0**.

**Chờ bạn**: commit hàng chờ (P4-1→P4-7, P5-0/P5-1/P5-2) — máy vẫn chưa có git.

- [x] **[2026-09-22] P5-1 — KHÔNG THỰC THI (tiền đề sai) + SỬA 1 BUG THẬT TÌM ĐƯỢC → ✅ XONG**: kết quả ở **`.plan/result-p5-1.md`**. **Tiền đề không thoả**: prompt ghi *"result-p5-0.md kết luận bug #2 CÒN TÁI HIỆN"*, file thật ghi **"Bug #2 … ĐÃ ĐÓNG"** — và chính bộ plan đã chốt trước (`plan5_final.md` §5 *"P5-1 (chỉ nếu P5-0 xác nhận còn bug thật)"* · §7.1 *"coi bug #2 là đã đóng … chứ không sửa logic"* · `plan5-prompts.md` *"CHỈ chạy nếu result-p5-0.md kết luận cần"*) ⇒ **không sửa logic**, không đổi UI voice-first, không đổi VAD (đúng `CẤM`). **3/4 yêu cầu của P5-1 đã đúng sẵn và có test pin** (mặc định OFF `app_settings_service.dart:68` + test dòng 521; chỉ `/ask` test dòng 605; 3 test P5-0a/b/c đã thêm ở P5-0). **BUG THẬT tìm được khi verify**: full suite đỏ **`+246 -5`** — 5 test P4-6 hard-code `'2026-09-20'` làm "hôm qua" trong khi màn tính từ `now` (`daily_summary_screen.dart:150-155`) ⇒ hôm nay là 2026-09-22 thì client hỏi đúng `2026-09-21` ⇒ đỏ dù **không có code nào đổi**. Không phải flaky: `chat_controller_test` riêng = 18/18 pass, `daily_summary_test` riêng = đỏ đúng 5 test đó. Sửa: helper `_yesterdayVn()` dùng **cùng thuật toán** (VN midnight − 1) thay 7 chỗ hard-code ⇒ **251/251 xanh, analyze 0**. **Còn lại là việc của USER** (không làm được bằng agent): test auto-send trên máy Android thật, phân biệt "chữ không hiện" (tầng engine/quyền mic) với "chữ hiện mà không tự gửi" (= ca P5-0a, hạn chế OEM, muốn tự gửi thì làm fallback ở **P5-4** sau khi user chốt đánh đổi). File sửa duy nhất: `test/daily_summary_test.dart` (test-only).

- [x] **[2026-09-21] P5-0 — ĐIỀU TRA 3 BUG (chỉ đọc + test, KHÔNG sửa logic) → ✅ XONG**: kết quả ở **`.plan/result-p5-0.md`**. **Bug #2 (auto-send): ĐÃ ĐÓNG** — 26/26 test `voice_input_test.dart` xanh, thêm 3 test mới P5-0a/b/c cho 3 case biên (STT không bao giờ final / double-tap / gửi khi đang in-flight) — hành vi hiện tại ĐÚNG cả 3. **Bug #3 (draft dù nộp thật): ĐÃ ĐÓNG tầng server** — `payment-write.test.mjs` 25/25 xanh, cả 2 nhánh submit_ok true/false có test; khi submit lỗi draft GIỮ NGUYÊN + không báo success (test F7-2 mid-way, dòng 528); `client.mjs` trả `Error` phẳng `TOOL_ERROR: <text>` (exc_type chỉ nằm trong chuỗi) ⇒ **input P5-2: phân loại lỗi phải match chuỗi submit_error, không expect field cấu trúc**. **Q3 /execute timeout: KHÔNG tự reconcile** — chủ ý trả 503 `retry_same_command_id: true`, an toàn nhờ 4 lớp (idempotency record · intent lock · reconcile-trước-khi-ghi · PENDING-không-FAILED) ⇒ KHÔNG phải P0. **Kết luận: P5-1 không cần chạy (bug #2 đóng); P5-2 đáng chạy nhưng mục tiêu mới = phân loại submit_error cho UI, không phải fix logic.** File sửa duy nhất: `voice_input_test.dart` (+3 test, không đổi test cũ).

- [x] **[2026-09-21] PUSH LÊN GITHUB + CI BUILD APK XANH → ✅ XONG**: push thành công lên `hoangsoft90/erpn_mobile1` branch `change/flutter-chat-mvp` (commit `367625a` — merge cây P4 vào lịch sử remote, **không force**, **giữ nguyên 320 file remote** gồm `.github/workflows/android-debug-apk.yml` + 11 file `.project/`; +98 file mới: Trụ D + docs). **CI: `android-debug-apk` → `success`** (run `35624280991`, artifact `erpn-chat-debug-apk` ~85MB). **Đường đi thật**: máy KHÔNG có git + deploy key **read-only** trên repo đó ⇒ thử PAT do user cung cấp (qua chat — **user cần revoke token ghp_…7aO ngay**) ⇒ push 2 bước: (1) commit verify-key, (2) merge `--allow-unrelated-histories` với **54 conflict add/add** ⇒ resolve lấy **bản P4** (kiểm trước: bản P4 có P4-4→P4-7 + drill, bản remote không) ⇒ verify 0 file mất / 0 .env / workflow còn ⇒ push ⇒ dõi CI đến `success`. **Bài học (đã ghi LESSONS + skill)**: PAT **qua chat = phải revoke ngay** (không lưu vào file/remote/config); trước khi push cây không chung lịch sử phải **đếm 2 phía** (file remote không có ở local, file local không có ở remote) và merge bằng `--allow-unrelated-histories` thay vì force.

> Nhật ký cũ nằm dưới.

> Việc cụ thể gần đây, format ngày `YYYY-MM-DD`. Dọn mục đã xong quá 1–2 tuần
> (nội dung cũ nằm trong `result*.txt` + handoff + LESSONS_LEARNED.md là bằng chứng).

- [x] **[2026-09-21] RÀ HARNESS TOÀN REPO (sau P4-7) — 10/10 ALL ALIVE, bắt 1 harness CHẾT-IM → ✅ XONG**: chạy đủ **10 harness** (c0/c1/c2 · execute-id-types · issue1×2 · p4-ops/p42/p43/p44). **Phát hiện chính: `c2-ocr` chết-im** — spawn `node --test` **không truyền `COPILOT_MOCK_OK=1`** (mà `npm test` đặt) ⇒ 3 test route fail ngay **baseline** ⇒ exit **trước mọi case** ⇒ **0/10 ca được kiểm** mà không ai hay; nếu nó chỉ nhìn `status !== 0` thay vì baseline-check thì **10 guard C2 "được chứng minh" trên nền đỏ sẵn**. Sửa `env: {...env, COPILOT_MOCK_OK:"1"}` ⇒ **ALL C2 GUARDS FALSIFIED (10/10)**. Diễn biến phụ: `timeout 240` của tôi kill c2 lúc baseline (không sót mutation vì chưa áp — kiểm `.falsify-bak` rỗng + baseline xanh sau). **Đóng câu hỏi review treo (sentinel 403)**: grep node_modules 3.0.4 — client **throw `FrappeAPIError`** khi `!response.ok`, message `[FrappeClient] … failed: … (HTTP 403)` ⇒ hình thái throw của sentinel ĐÚNG; thêm test dùng **đúng text thật** để regex UI không chỉ khớp wording mock ⇒ p4-ops **25/25**. **Số thật**: Node **525 (523 pass, 2 fail dsh)** · Flutter **248/248** · analyze **0** · Python **62** · falsify **10/10 ALL ALIVE, 0 PROBLEM** · mutation sót **0**. **Bài học → LESSONS đợt rà-harness (5 bullet: env harness · baseline là guard · audit tĩnh 0-match · sentinel bám contract thật · timeout bọc harness Dart) + skill `erpn-verify-first` (+2 hàng)**. **Docs**: phụ lục `result-p4-done.txt` · checklist/next/features · faq (+1 treo APK) · **openspec `flutter-chat-mvp/STATUS-2026-09-21.md`** (2 task chờ USER: GitHub Settings + APK máy thật — không archive để "dọn file") · handoff `20260921-1549`.

- [x] **[2026-09-21] P4-7 — ĐÓNG plan4 V1: ĐỐI CHIẾU §7 12/12 + DOCS → ✅ XONG**: deliverable **`result-p4-done.txt`**. **Ma trận §7**: đi từng case (12) tìm test thật ⇒ **11/12 có, THIẾU §7.6 "thiếu quyền một doctype"** — case này rất dễ bị coi là "đã bao phủ bởi test failure chung", nhưng khác bản chất: **trang rỗng = "hôm nay không có" ≠ bị từ chối = "không được xem"**, gộp hai thứ là bịa 0 về tiền. **Đã bịt bằng 3 phần**: (1) sentinel `__FORBIDDEN__` trong `src/mock-server.mjs` mô phỏng Frappe `PermissionError`/403 (trạng thái site thật mà fixture **không thể** tái hiện); (2) test `"P4 §7.6: a doctype the user may NOT read is a NULL block — never an empty 0-day"` — assert `partial=true` + `sales_invoices === null` **và `!== 0`** + `receivables === null` (cùng doctype ⇒ từ chối cùng lúc) + lý do từ chối **sống tới UI** (`/permitted|permission/i`) + không cascade; (3) ca falsify **O** (wrapper nuốt PermissionError thành `return []`) ⇒ RED. **Hai phát hiện hạ tầng test (quan trọng hơn cả §7.6)**: **(a) 2 anchor falsify bị TRÔI** — ca **L** (`appDrafts`, trôi ở P4-5 khi thêm `company` vào field list) và ca **F** (`http-ask`, trôi ở P4-4 khi guard công ty chuyển vào `resolveReadCompany()`) ⇒ harness in **`anchor missing` + tính PROBLEM** (nhờ vậy mới thấy; **ca bị bỏ qua trông y hệt ca đã pass** trong dòng tóm tắt) ⇒ sửa cả hai + ghi chú tại ca; **(b) lệnh Python trong handoff P4-6 SAI** — tôi ghi `python3 -m pytest` mà máy **không cài pytest**; lệnh đúng là `PYTHONPATH=src python3 -m unittest discover -s tests` ⇒ sửa cả 2 handoff + LESSONS (phiên sau chép nguyên sẽ báo "suite Python đỏ" oan). **Case §7.12 "đúng 20 chứng từ" phải ĐẾM**: đếm từ fixture = **20** (không tin comment); filter "advance" đã verify với site thật ở P4-4/P4-6. **Số thật**: Node **524 (522 pass, 2 fail dsh từ B0)** · Flutter **248/248** · analyze **0** · Python **62** · falsify p4-ops **15/15** (+O) · p42 **15/15** · p44 **20/20** · p43 **11/11** — **0 PROBLEM** · mutation sót **0**. **Docs cho người dùng**: `faq.md` **§10 — Màn `Tóm tắt ngày` đọc thế nào** (bảng nhãn thật · 3 thứ không được đọc sai: **không có chữ "Doanh thu"** là chủ ý / `Lỗi · chưa đọc được mục này (MÃ)` **không phải 0** / `DỮ LIỆU CŨ` = số lần đọc trước · `Hôm qua` chỉ đổi **bán/thu** còn **công nợ luôn hiện tại** · delta **tuyệt đối không %** · drill ≤10 cũng chỉ đọc · footer `REAL｜MOCK`) + **sửa 2 chỗ đã lỗi thời** (§4.3 & §7 đang nói "tổng hợp theo ngày chưa có" ⇒ nay **đã có ở drawer**, chat thì vẫn chưa) · `features.md` (Trụ D vào Hoàn thành + ranh giới còn lại) · `checklist.md` (ma trận §7) · `next.md` · `LESSONS_LEARNED.md` (đợt P4-7) · skill `erpn-verify-first` (+2 hàng). **KHÔNG làm (đúng lệnh dừng)**: không skill WRITE mới (P9) · không Redis · **không WRITE vào két** (giữ READ §3.6). **Trạng thái**: **plan4 V1 KHÉP** (P4-0→P4-6 code xong, P4-7 đóng bằng chứng); việc còn lại **không phải code** — commit (máy chưa có git, **AI không tự ký duyệt** vì vùng SỐ TIỀN) · B2/B3/B4 idempotency site thật · APK (user chốt "để sau").

- [x] **[2026-09-21] P4-6 — HÔM NAY | HÔM QUA + DELTA TUYỆT ĐỐI (`.plan/plan4_final.md` §6 + §3.4) → ✅ KỸ THUẬT XONG, CHỜ MÁY CÓ GIT + DUYỆT COMMIT (vùng đọc SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-6.txt`**. **§0 verify-first**: route `/read/daily-summary` **đã nhận `date` từ P4-2**, client cũng đã có tham số ⇒ **KHÔNG sửa server** (xác nhận bằng đọc code, không đoán); khoảng trống thật là **Flutter** (`DrillIntent` chưa có `date`, `DrillListScreen` không truyền). **Flutter**: `SegmentedButton` Hôm nay (mặc định) / Hôm qua · delta **chỉ 2 chỉ số** (`sales_invoices.amount` + `receipts.total`) · **hiệu TUYỆT ĐỐI `±Xđ` + định tính, KHÔNG %** (§6) · chỉ hiện khi **đủ 2 ngày VÀ cả hai `partial=false`** (partial + số suy ra = 2 lần nói dối) · **công nợ LUÔN hiện tại** + note UI khi xem Hôm qua (§3.4) · drill truyền ngày đang xem nhưng giữ `date: null` cho Hôm nay ⇒ **server** quyết định ngày tiệm. **Đọc Hôm qua LƯỜI** (chỉ khi user chọn) — sửa bug re-mount làm delta biến mất im lặng, đồng thời giữ **1 lượt ERP** mỗi lần mở màn (không +1). **4 phát hiện thật, đã sửa**: (1) **kill harness giữa chừng ⇒ mutation P4-5 sót trên đĩa** (`_RowTile` quay lại hiện `0đ`) ⇒ khôi phục file + **thêm restore-on-`SIGINT/SIGTERM`** vào `scripts/falsify/lib/harness.mjs`; (2) 3 ca falsify P4-6 khai **sai Dart suite** (`drill` thay vì `summary`) ⇒ harness báo **PROBLEM "no test ran"** (không cho RED giả — tự tố giác), sửa thành **20/20 RED, 0 PROBLEM**; (3) **test yếu**: ca drill-theo-ngày vẫn **PASS khi bỏ `date`** (chỉ assert "có request") ⇒ siết assert đúng ngày cụ thể; (4) **bug thật**: `DrillIntent.date` có nhưng `_load()` **không truyền** ⇒ drill từ Hôm qua đọc hôm nay. **Probe CỦA TÔI sai trước code**: lần chạy đầu lệch (receipts hôm qua 5.000.320.000 vs 320.000) — probe thiếu lọc company nên gom 10 phiếu thu của `DEMO POC CO` (site 3 công ty); **route đã scope đúng**; đúng cái lỗi company-scope vừa vá ở P4-5 — nếu không vá thì 5 tỷ đó đã vào màn thật. **Bằng chứng**: Flutter **248/248** (+6) · analyze **0** · Node **523 (521 pass, 2 fail dsh từ B0)** — không đổi (không đụng server) · falsify **20/20 RED, 0 PROBLEM** (+3 ca) · **vòng lặp THẬT (read-only)**: 2 ngày đều **200 / `partial=false`** · **receivables 2 NGÀY GIỐNG HỆT NHAU** (488.935.625 / 4.700.000) ✅ đúng §3.4 (công nợ là số hiện tại) · delta ± tuyệt đối tính đúng · cross-check từng ngày **ALL MATCH** (sau khi probe thêm company). **Không làm**: %/chart (§6 khóa) · "công nợ hôm qua" (§3.4 — phạm vi mới) · `/execute`/nút ghi.

- [x] **[2026-09-21] P4-5 — BLOCK NHÁP APP HÔM NAY (`.plan/plan4_final.md` §4.4 + §5.3) → ✅ KỸ THUẬT XONG, CHỜ MÁY CÓ GIT + DUYỆT COMMIT (vùng đọc SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-5.txt`**. **§0 đo meta thật**: `grand_total` có trên SO/QT/PO nhưng **KHÔNG có trên Payment Entry** (DocField rows=0; hỏi ⇒ 417 `Trường không được phép trong truy vấn`) ⇒ drill nháp field list **theo doctype** (`paid_amount` + `posting_date` cho PE) — bản drill đầu của phiên hỏi `grand_total` cho cả 4 doctype ⇒ **vòng thật bộc lộ 503 ERP_UNAVAILABLE trước khi hoàn tất**, sửa + đo lại **200**. **Server**: row nháp COPY giá trị riêng chứng từ (PO-APP 99.000.000 từ `grand_total`; PE-DRAFT-APP 3.000.000 từ `paid_amount`) — không tự tạo 0; giữ filter `custom_ai_action_id is set` + docstatus 0 + trong ngày (`transaction_date`/`posting_date` của chính chứng từ) + company scoping. **"Không id không vào"**: 4 dạng (không field / null / chuỗi rỗng / không thuộc block) đều VẮNG ở cả block lẫn drill. **Flutter**: row nháp dẫn bằng CHỨNG TỪ, money side **"—"** (không 0đ); **CẤM ghi/xoá/submit** — assert 7 cụm hành động (`Submit/Xác nhận/Gửi/Xoá/Xóa/Hủy phiếu/Ghi sổ`) findsNothing + không TextField + không FilledButton + log request không 'execute' (§5.3). **Không làm** "command store trong ngày" (không tạo nguồn sự thật thứ hai cạnh correlator chính thức B2). **Bằng chứng**: server drill test **12/12** (3 test P4-5) · Flutter drill **9/9** (+1) · falsify **17/17 RED** (+3 ca: bỏ filter id / bỏ điều kiện ngày / Flutter render 0đ lại — restore byte-identical) · Node **523 (521 pass, 2 fail dsh từ B0)** · Flutter **242/242** · analyze **0** · vòng thật: drill nháp **HTTP 200 `total=0`**, drill=block=0 (site chưa có nháp trong ngày — 0 ĐÚNG vì server đọc xong mới nói, partial=false) · ALL MATCH 9/9. **Bài học (LESSONS bổ sung đợt P4-4 + skill +2 hàng)**: field list cho NHIỀU doctype phải đo meta từng doctype ("cùng nhóm chứng từ" ≠ "cùng schema"; mock fixture không biết schema thật ⇒ vòng thật là phép kiểm duy nhất) · "không 0 giả" ở TỪNG ROW (server copy, client chọn nhân cách hiển thị).

- [x] **[2026-09-21] P4-4 — DRILL-DOWN STRUCTURED (`.plan/plan4_final.md` §4.4) → ✅ KỸ THUẬT XONG, CHỜ MÁY CÓ GIT + DUYỆT COMMIT (vùng đọc SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-4.txt`**. **Review vòng 2 cùng ngày bắt thêm 2 bug thật (đã sửa + test + falsify 14/14 RED, Node 520)**: (1) `clampUiLimit` — `Number(null)=0` ⇒ **KHÔNG gửi limit bị clamp về min (5) thay vì default (10)** — trang co lại im lặng so với hợp đồng; (2) **nháp app không lọc company** (cả block `appDrafts` lẫn drill) — nháp công ty khác bị đếm oan; docs trước đây ghi sai tên helper (đã sửa thành `resolveReadCompany()`). **Server**: block `drill_screens` trong contract — **5 id cố định** (sales_orders_today · invoices_today · receipts_today · overdue_customers · app_drafts_today), mỗi id gán **capability READ đúng dữ liệu nó đọc** (authorize theo nó TRƯỚC khi chạm ERPNext) · `readDayDrill` (ops-summary.mjs) **DÙNG LẠI đúng primitive của aggregate** — không extractor thứ hai ⇒ số dòng luôn khớp block · route `GET|POST /read/drill`: auth non-loopback như /ask · bucket READ · **KHÔNG classifier** (id, không text — §4.4 cấm inject free-text) · id lạ ⇒ 404 · company resolve server-side qua helper dùng chung `resolveReadCompany()` tách từ P4-2 (ranh giới tin cậy 1 nhà) · partial đi thẳng. **Flutter**: `drill_models.dart` + `dailySummaryDrill()` + `DrillListScreen` (Back · retry · truncated hiển thị trung thực · **không affordance ghi**) · route `/drill` **fail-closed** khi thiếu intent · tap chỉ số trên màn tóm tắt qua `context.go` (GoRouter). **Bằng chứng**: server test **9/9** + authz tách file · Flutter **8/8** (tap drill **không gọi /execute** — assert log request · không classifier · truncated · Back · fail-closed) · **guard chống drift**: tổng dòng drill = ĐÚNG số block aggregate trên cùng fixture (bắt thật: drift đầu tiên do SQL COUNT gộp dòng null ⇒ đếm bằng list đã lọc) · Node **519 (517 pass, 2 fail dsh từ B0)** (+9) · Flutter **241/241** (+8) · analyze **0** · falsify **12/12 RED** qua harness lib dùng chung `scripts/falsify/lib/harness.mjs` (không bản sao runner thứ 3) · Python **62 OK**. **Vòng lặp THẬT (read-only)**: 5 drill ↔ block summary trên site thật 2026-09-21 — **ALL MATCH** (SO sub/draft · HĐ 575.000 · thu 11.430.000 tách HĐ/ứng trước · quá hạn 4.700.000 · nháp 0). **Lỗi tự gây đã sửa + falsify lại** (§5): credit-note fixture phải âm (số dương ⇒ guard đỏ oan) · authz đoán sai helper khuôn P4-2 · `Navigator.pushNamed` không qua GoRouter ⇒ đổi `context.go` · **test chaos `http-execute` duy nhất không truyền `idemStore`** ⇒ để record PENDING sót trong store dùng chung ⇒ độc hại lần chạy sau (fail ổn định) — đã cô lập · SQL COUNT gộp null. **Chờ user**: duyệt commit · P4-5 nháp block chi tiết.

- [x] **[2026-09-21] GỠ CHỐT P4 (user duyệt) — tạo correlation field THẬT + chốt APK để sau → ✅ XONG**: (1) **`custom_ai_action_id` đã tạo trên site THẬT** cho `Purchase Order` · `Quotation` · `Sales Order` (đọc script trước khi chạy: target **dẫn xuất từ `capabilities.json`**, idempotent, verify bằng read-back, không in secret) ⇒ **4/4 doctype** `Data unique=1 search_index=1`; chạy **lần 2 = no-op** ✓. **Verify ĐỘC LẬP 3 lớp** (không dùng lại read-back của script — script chỉ chứng minh nó đồng ý với chính nó): META `Custom Field` 4/4 · **đọc DOC có field** (PE `ACC-PAY-2026-00708`, SO `SAL-ORD-2026-00328`, PO `PUR-ORD-2026-00005` trả `null`; **`Quotation` không có chứng từ nào ⇒ check này VACUOUS, nói rõ không tính là bằng chứng**) · **bịt lỗ vacuous bằng `WHERE` trên chính cột** (thiếu cột ⇒ Frappe 417 `Unknown column`, không phải 0 row) ⇒ 4/4 **HTTP 200 rows=0** ⇒ cột có thật **cả trên Quotation** · **control âm** (field không tồn tại ⇒ 0 row) ✓. (2) **Hệ quả đo được**: chạy lại **vòng lặp thật (read-only)** ⇒ **`partial=false`** (trước migration có `partial=true` + `app_drafts=null`) · `GET==POST` · `2026-02-30`→400 · claim `SANLOAN`→403 · **cross-check REST độc lập cùng lúc**: `receivables 488.040.625 / 281 HĐ` **khớp chính xác**. ⚠️ Số cũ `493.440.625 / 286 HĐ` **không sai lúc ghi** — site **đã đổi dữ liệu giữa 2 lần đo** (−5.400.000 · thu trong ngày 11.430.000 ⇒ 5 HĐ đã thanh toán); điều được chứng minh là **route khớp REST TẠI THỜI ĐIỂM ĐO**, không hardcode số. (3) **APK**: user chốt **"để sau"** ⇒ **không push, không `git init`, không đụng repo**; đã ghi lại khảo sát (checkout **không có `.git`**/**`.github/workflows`** ⇒ CI chỉ build được remote = không có P4-3; thiếu Android SDK; không phone/emulator) + **cảnh báo đừng push thô** (history không chung gốc + bản local thiếu file ⇒ push cả cây sẽ **XÓA file trên branch duy nhất**, phải `--force`) để phiên sau không tự làm. **Bài học mới (đã ghi LESSONS nhóm 26 + skill)**: probe tự viết cũng phải **grep skill API trước** — gotcha `order_by` **không** json-encode đã có sẵn ở `erpnext-rest-api-recipes` mà tôi vẫn mắc (417 `Định dạng trường không hợp lệ trong Order By`), và đừng vội đổ cho server.

- [x] **[2026-09-21] P4-3 — DRAWER + MÀN `Tóm tắt ngày` (`.plan/plan4_final.md` §2, §6) → ✅ KỸ THUẬT XONG, CHỜ MÁY CÓ GIT + DUYỆT COMMIT (vùng đọc SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-3.txt`**. **Flutter** (25 test mới) + **1 mảnh server nhỏ bắt buộc** vì §2 đòi footer **REAL|MOCK** mà **không có tín hiệu thật nào tới client**: `ops-summary.mjs` hardcode `source: "real"` (cả khi chạy fixture) ⇒ thêm `meta.erp_target` = `erpTargetLabel(env)` ở `copilot-server.mjs`, dùng **đúng biểu thức** của dòng log khởi động và `main()` cũng gọi helper đó ⇒ **log và footer không thể lệch** (ca falsify chứng minh helper bám `pickServerScript` cả 2 nhánh); không ai nói ⇒ `null` ("không rõ nguồn"), **không** đoán REAL. **Drawer (§2)**: `Tóm tắt ngày` **đầu tiên + "Mặc định"**, section THEO DÕI/PHIẾU TỪ APP/LỐI TẮC READ/CÀI ĐẶT, mục chưa làm ghi **"Sắp có (bước P4-4/P4-5)"** + disable (không nút đoán), `Server / đăng nhập` → `/settings`, footer REAL|MOCK + URL + giờ; dùng ở cả chat (chat vẫn là màn mặc định của app) và màn tóm tắt. **Screen**: SO **submitted \| draft TÁCH BẠCH** (§10.1) · HĐ **giá trị sau VAT** · thu **TM/CK/theo HĐ/ứng trước** + dòng **"Chưa phân loại được"** (§3.2 — không gộp để phần chia vẫn cộng ra tổng) · chi + footnote **"Chưa gồm chi qua Journal Entry"** (§10.2) · **két dự kiến** (Đầu + Thu TM − Chi TM) · **nợ hiện tại** + quá hạn · nháp app (block null ⇒ thẻ lỗi riêng). **Trạng thái §6**: skeleton · OK + stamp `generated_at` · **partial theo TỪNG block** ("Lỗi · chưa đọc được mục này (MÃ)" + `Thử lại`) · offline/401/503 ⇒ giữ số cũ + nhãn **DỮ LIỆU CŨ** · **empty day** chỉ khi `partial=false` + câu nói rõ "không phải lỗi". **Cache §2**: **45s** (giữa 30–60s) — trong cửa sổ **không gọi HTTP**, quá cửa sổ ⇒ hiện số cũ + nhãn cũ **ngay** rồi revalidate, cache hỏng ⇒ bỏ qua + đọc live (fail-safe như `ChatHistoryService`). **Nhãn (§3.1)**: **không** dùng chữ "Doanh thu" ở bất kỳ đâu (có test canh). **Review tự soi bắt 4 phát hiện THẬT**: (1) "không 0 giả" chỉ ở tầng render là chưa đủ — parser biến `{}`/chuỗi thành **block rỗng 0** ⇒ vá ở **tầng parse** (`_block()` trả `null` cho cả object rỗng) + test + falsify; (2) fixture `payload({receipts})` dùng `?? ` ⇒ **`null` không biểu diễn được**, 3 test "block lỗi" chạy trên dữ liệu lành ⇒ cờ tường minh `noReceipts/noAppDrafts/noCashDrawer`; (3) ca falsify **"xanh oan" 2 lần** vì nhánh khác cũng chặn (guard `partial` che bởi guard `receipts==null`; guard `_stale=!fresh` che bởi nhánh lỗi tự set cờ) ⇒ đổi fixture + thêm test quan sát **lúc request đang bay** (server bị gate, `settle:false`); (4) **probe P2b cũ đỏ sẵn không phải regression của P4-3** — quy trách nhiệm bằng bằng chứng (chạy cô lập + comment fix F2 trong `proposal_card.dart` **trích số đo của chính probe đó** + không file nào tôi sửa nằm trong cây import + probe đối chứng). **Bằng chứng**: analyze **0 issue** (sau khi đổi helper test sang private) · `daily_summary_test` **25/25** · Flutter **233/233 PASS** (đã **xoá probe P2b `_probe_review_test.dart`** theo quyết định user ⇒ 236→233; trước P4-3 suite là **212 test / 211 pass + 1 fail**) · falsify P4-3 **11/11 RED** (7 Dart + 3 Node, mỗi ca: xanh-trước + áp được mutation + đỏ **đúng tên test** + restore byte-identical) · Node **508 (506 pass, 2 fail dsh-gateway từ B0)** (+3) · mutation sót **0**. **Vòng lặp THẬT (read-only, service thật + ERPNext thật)**: `meta.erp_target = REAL` **và** log khởi động nói `REAL -> host` (**khớp nhau**) · `partial=true` với `app_drafts` null (đúng: site thiếu `custom_ai_action_id`) · **cross-check REST độc lập**: receivables **493.440.625** (286 HĐ) + invoices **575.000** (1 HĐ) **khớp chính xác**. **Chờ user**: (a) ~~xoá probe P2b~~ **ĐÃ XOÁ** (user chốt); (b) tạo `custom_ai_action_id` trên SO/QT/PO; (c) **commit** (env này **không có git**); (d) **APK debug — user chốt "ĐỂ SAU"** (không push/không `git init`/không đụng repo trong phiên này);: checkout **không có `.git`** + **không có `.github/workflows`** ⇒ CI chỉ build được trạng thái **trên remote = không có P4-3**; build local thiếu **`platforms/`+`build-tools/`** và `/home` còn **~1.5G** (cài SDK = tool mới ⇒ xin phép); `flutter devices` chỉ có `Linux (desktop)` ⇒ **không cài lên máy được từ đây**. **Giới hạn**: không có emulator/device ⇒ UI chứng minh bằng 25 widget test chạy trên **router thật** + 1 vòng HTTP thật, **chưa có ảnh màn hình trên máy thật**; AgentMemory/Simplenote không có trong tool phiên này.

- [x] **[2026-09-21] P4-2 — route `GET|POST /read/daily-summary` (`.plan/plan4_final.md` §4.2/§4.3/§5/§7) → ✅ KỸ THUẬT XONG, CHỜ MÁY CÓ GIT + DUYỆT COMMIT (vùng đọc SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-2.txt`**. CHỈ route HTTP + auth + partial + rate-limit + test (không Flutter, không WRITE, không drill, không `/execute`). **Route** trong `src/http-ask.mjs`: `date` optional ở body **và** query (body thắng), thiếu/rỗng = **hôm nay tại tiệm** (`Asia/Ho_Chi_Minh` — không phải ngày của host), 400 `INVALID_DATE` (regex **không đủ**: `2026-02-30` chặn bằng round-trip lịch), **không** qua NLP/classifier (capability id cho sẵn). **Company server-side tuyệt đối**: pin config → `Global Defaults.default_company` (đo: **Single** ⇒ `doc_list` **500**, `doc_get` **OK**; `User.default_company` **417**; `Company` list **3 rows**) · không có ⇒ **503 `COMPANY_UNRESOLVED`** · client gửi `company` khác ⇒ **403 `COMPANY_SCOPE_MISMATCH`**. **Auth/limit**: 401 basic-auth non-loopback như `/ask` · `authorize` **trước mọi lần đọc ERP** · bucket **READ** charge sau parse, trước authz. **Partial đi thẳng response**: 200 + `meta.partial` + block `null` + `errors`. **Mock**: nhánh `erpnext_doc_get` `Global Defaults` + 2 knob (`MOCK_ERP_DEFAULT_COMPANY` `=""` = không có default; `MOCK_ERP_FAIL_GLOBAL_DEFAULTS`). **Review tự bắt 3 lỗi THẬT rồi sửa** (vòng 2 theo yêu cầu user "review code, đã test chưa?"): (1) `pickServerScript()`/`createMcpClient` **ngoài `try`** ⇒ ERP chưa cấu hình làm **async listener reject ⇒ process chết** (kéo cả `/ask`) → đưa vào `try` + `finally` đóng an toàn + test "can never take the service down"; (2) mã `TOOL_ERROR` của tầng MCP lộ ra client → whitelist theo spec §4.3 (`ERP_UNAVAILABLE`), **log giữ mã thật**; (3) **`client.close()` TREO VĨNH VIỄN khi child đã chết trước đó** (`once("exit")` không bao giờ bắn lại) — ĐO được: child chết lúc khởi động ⇒ `initialize()` reject sạch sau **77ms** nhưng `close()` → **`TIMEOUT` sau 3000ms**; hậu quả: mọi route mở client mà gọi `close()` trong `finally` (kể cả `/read/list`, `/ocr/slots`) **không bao giờ return** ⇒ rò 1 socket + 1 child **mỗi request lỗi** → sửa `client.mjs#close()` **idempotent** (`child.exitCode/signalCode !== null ⇒ return`, child sống thì y nguyên) + test `close() is IDEMPOTENT` + falsify ca **N**. Đã kiểm **không phải lỗ hổng**: `new URL(req.url, base)` throw với request-target tuyệt đối (`http://[`) nhưng nằm **sau** phép khớp `path` nên không tới được; 12 dạng tương đối xấu (percent-encoding hỏng, control char, 200k ký tự) đều parse OK. **Bằng chứng**: Node **505 (503 pass, 2 fail dsh-gateway từ B0)** (+20) · P4-2 **19/19** (18 route + 1 authz contract tạm) + `client.test.mjs` **9/9** · falsify **14/14 RED** (mỗi ca: **xanh trước khi mutate** + đỏ **đúng tên test** + restore byte-identical) · Python **62 OK** · không mutation sót (`grep "if (false)"` = 0) · chạy lại **vòng lặp THẬT** sau khi sửa `client.mjs`: 200/400/403 y nguyên. **Vòng lặp THẬT (read-only, service thật + ERPNext thật)**: GET (không date) 200 + `date=2026-09-21` = today VN + `company` **lấy từ session** · GET == POST cùng ngày · `partial=true` với `app_drafts` null + lỗi Frappe thật (site thiếu `custom_ai_action_id`) · `2026-02-30` → **400** · claim `SANLOAN` → **403** · **đối chiếu chéo REST độc lập**: `receivables 493.440.625` (286 HĐ) **khớp chính xác**, SI ngày 575.000/1 khớp. **Giới hạn**: OCR không chạy (checkout **không có git** ⇒ không có diff) ⇒ review mặc định; **AgentMemory DOWN** (`/health` → http=000) + MCP Simplenote **không có trong tool phiên này** ⇒ không tra/lưu memory ngoài repo; ❓ chờ user: duyệt commit + tạo `custom_ai_action_id` trên SO/QT/PO. **Tiếp theo**: P4-3 Flutter drawer → **ĐÃ XONG cùng ngày, xem mục trên**.

- [x] **[2026-09-21] P4-1 review vòng 2 (user: "review code những gì vừa làm") — 2 lỗ hổng của CHÍNH guard vừa viết + 1 phát hiện mới ⇒ ĐÃ SỬA HẾT**: (a) tripwire mới **lọt** dạng array `["docstatus","in",[0,1]]` (nhóm value của regex dừng ở `]` đầu tiên) → vá tách phần tử + nhận `+1`; (b) ca falsify K/M đỏ mà **không chứng minh đúng tripwire là thứ bắt** → thêm tuỳ chọn `only`, ca **K/M/N** chỉ chạy file tripwire → **14/14 RED**; đã xác nhận tripwire **thật sự được `npm test` chạy**. (c) **Phát hiện MỚI (vùng GHI)**: `action_id`/`command_id` do **client** gửi (`http-ask.mjs` — `/execute` KHÔNG dùng `isValidCommandId` trong khi `/execute/cancel` có) → nếu là SỐ thì tool thật từ chối ⇒ `reconcile*` báo `correlation_field_unavailable` ⇒ executor **từ chối ghi OAN** + chẩn đoán SAI `SO_CORRELATION_FIELD_MISSING` ("ERPNext chưa có field — chạy migration"); **fail-closed ⇒ không có nguy cơ ghi trùng**. User duyệt "Sửa ngay (validate ở biên HTTP)" ⇒ thêm `correlationIdProblem()` (400 `INVALID_CORRELATION_ID`, gọi sau parse body và **TRƯỚC rate-limiter** để không đốt command_id) + test (số ở `command_id`/`action_id`, chuỗi rỗng, control, `store.status(12345) === null`) + harness mới `scripts/falsify/execute-id-types.mjs` **3/3 RED**. **Bài học phụ**: ca C **không đỏ** vì test chỉ assert **status 400** (bỏ guard vẫn 400 từ chỗ khác) → siết thành assert **MÃ lỗi** — "assert mã, đừng assert status". Suite sau cùng: Node **485 (483 pass, 2 fail dsh-env từ B0)** · Python **62 OK**.
- [x] **[2026-09-21] P4-1 follow-up — user duyệt 3 việc ⇒ ĐÃ XỬ LÝ**: (1) **fix filter số cùng lớp ở B2/B3** (`sales-order-write.mjs` + `quotation-write.mjs`: `[["selling","=",1]]`) — đọc Item Price của CẢ HAI đường ghi **chết trên site thật** mà 41 test mock vẫn xanh; sửa `1`→`"1"` + **tripwire TOÀN REPO** `test/filter-literal-types.test.mjs` (quét mọi `.mjs` trong `src/`, strip comment, control `>=12` chống "scan hỏng ⇒ xanh giả") + falsify ca **K**/**M** (harness P4 nay chạy cả 2 file test); **bằng chứng site thật**: `selling=1` → `TOOL_ERROR … must be string`, `selling="1"` → **27 dòng Item Price**; B2+B3 **41/41 pass** · (2) **`add-correlation-field.mjs --dry-run`** (chỉ đọc) → **Payment Entry ĐÃ CÓ** field, **THIẾU: Purchase Order · Quotation · Sales Order** (khớp độc lập probe `Custom Field` meta) ⇒ `app_drafts` P4-1 phải null cho tới khi tạo field (**tự hết, không cần sửa code**) và **correlation B2/B3/B4 hiện chỉ đứng với mock** — tạo field là GHI vào ERPNext ⇒ chờ user · (3) **duyệt P4-1** ⇒ marker chuyển "CHỜ DUYỆT COMMIT" → "ĐÃ DUYỆT, chờ máy có git". Suite sau cùng: Node **484 (482 pass, 2 fail dsh-env từ B0)** · Python **62 OK** · falsify P4 **13/13 RED**.
- [x] **[2026-09-21] P4-1 — `ops.daily_summary` (`.plan/plan4_final.md` §3/§4.3/§7/§10) → ✅ KỸ THUẬT XONG, CHỜ DUYỆT COMMIT (vùng SỐ TIỀN — AI không tự ký duyệt)**: deliverable **`result-p4-1.txt`** · CHỈ backend (không Flutter, không route HTTP, không `/execute`, không ghi ERPNext). **Contract** `ops.daily_summary` (READ, `route_group: ops`, no confirm, `triggers: []` ⇒ đường chat KHÔNG đổi) · **skill** `src/skills/ops-summary.mjs` aggregate server-side 8 block đúng schema §4.3 (SO tách submitted/draft, SI/PE chỉ submitted + `grand_total`, chi chỉ PE Pay + footnote JE, két = GL trước 00:00 VN trên `default_cash_account`, Cash|Bank theo **`Account.account_type`** đúng khuyến nghị P4-0) · **partial** nhánh lỗi ⇒ block `null` + `errors`, KHÔNG bao giờ 0 giả · company **từ session**. **§0 đo trước (site thật)**: filter value **phải là STRING** (số ⇒ `Property /filters/0/2 must be string`) · `count` = **số dòng trả về** (limit 3 → count 3) ⇒ thử `limit+1` mới phát hiện cắt trang · `custom_ai_action_id` **CHỈ có trên Payment Entry** (Custom Field meta 0 row cho SO/SI/QT/PO) · `>` với chuỗi + `GL Entry` đọc được. **Review tự bắt 5 lỗi THẬT rồi sửa**: (1) `receivables` filter **số** ⇒ cả nhánh chết trên site (**551.910.625** không bao giờ hiện) → `"1"` + **tripwire tĩnh** khoá lớp lỗi; (2) `app_drafts` `catch {}` ⇒ **"0 nháp" giả** vi phạm §4.3 → bỏ catch, block `null` + ghi đúng message Frappe; (3) có thể **cộng thiếu im lặng** khi trang bị cắt → `docList` xin `limit+1`, vượt ⇒ `ERP_TRUNCATED` (số tiền sai tệ hơn số tiền thiếu); (4) `cashDrawer` trả `cash_in_today/out_closing = 0 giả` → chỉ trả `{opening, account}`; (5) **guard không falsifiable** = trang trí (`if (!so.ok…){}` rỗng + guard kiểu filter runtime) → xoá + đổi thành tripwire **strip comment** (tripwire từng bắt chính comment của nó). Dọn thêm: import `markUntrusted` thừa · `const today = date` · ternary lồng · `against_invoice` tính bằng trừ · indirection `rowOfCompanyStrict`. **Bằng chứng**: Node **484 (482 pass, 2 fail dsh-env từ B0)** · P4-1 **23/23** · **falsify 12/12 RED** (restore byte-identical) · Python **62 OK** · Flutter không đụng. **Vòng lặp THẬT read-only**: 21/09 `receivables 551.910.625` **khớp chính xác** cộng độc lập qua REST (295 HĐ) · 20/09 thu 320.000 (ứng trước) closing **243.622.620 = opening 21/09** · 19/09 thu 21.127.120 (`cash 19.627.120`/`bank 1.500.000`) chi 250.000 closing **243.302.620 = opening 20/09** ⇒ chuỗi 3 ngày khớp liên tiếp. **Chờ duyệt**: (a) fix filter số **cùng lớp ở B2/B3** (`Item Price` `selling = 1` ⇒ đọc Item Price chết trên site thật, 2 ký tự/2 file); (b) `custom_ai_action_id` chưa có trên SO/QT/PO thật ⇒ correlation B2/B3/B4 chỉ đúng với mock, cần chạy `add-correlation-field.mjs`. **Giới hạn**: OCR không chạy được (checkout không có git) ⇒ review mặc định; codebase-memory-mcp/AgentMemory không có trong tool phiên này; `ccc` chưa init (không tự cài).
- [x] **[2026-09-21] ISSUE1 — TẮT FALLBACK-MOCK NGẦM (`.plan/issue1_fix1.md`) → ✅ KỸ THUẬT XONG, CHỜ DUYỆT COMMIT (vùng loại trừ an toàn)**: deliverable **`result63.txt`**. **Nguyên nhân gốc**: `mock-server.mjs` hard-code `CUST-00001="Nguyễn Thị Lan"`; `pickServerScript()` coi "thiếu 3 biến ERPNEXT_*" là tín hiệu chọn mock ⇒ launcher `dsh` quên export ⇒ **fail-OPEN duy nhất còn sót** ⇒ dsh trả khách không tồn tại như thật. **Fix (mục 2)**: cổng xin phép tường minh `COPILOT_MOCK_OK === "1"` (đúng chuỗi, không truthy) ⇒ thiếu cấu hình **throw `ERPNEXT_NOT_CONFIGURED`**; 1 điểm chốt duy nhất vì mọi đường sống đều qua `pickServerScript()` (`answerQuestion` cả `/ask`+`copilotAsk`, `http-ask` ×3, `safety-gateway`) · `main()` in WARNING khi phục vụ fixture · comment `client.mjs`/`index.mjs`/`dsh-gateway.mjs`/`dsh-e2e.patch.yml` sửa cho đúng sự thật. **mục 3**: `package.json` `"test": "COPILOT_MOCK_OK=1 node --test"` (1 dòng; CI workflows **không có trong checkout**). **Audit (mục 6+6b)**: không patch nào chứa `COPILOT_MOCK_OK`; grep toàn repo xác nhận **không còn đường im lặng nào khác** (và khoá bằng tripwire tĩnh mới); phân biệt thiếu-cấu-hình vs không-kết-nối-được (host chết vẫn resolve REAL — ma trận 3 host × có/không opt-in); rà secret: **không rò** (chỉ tên biến, không giá trị). **Bằng chứng**: Node **449 (447 pass, 2 fail dsh-env từ B0)** · Python **62 OK** · Flutter không đụng · **falsify in-repo 5/5 RED** (`scripts/falsify/issue1-mock-optin.mjs`) · 5.4 tái hiện bug gốc thành công (exit 2 + stdout RỖNG, kèm control chứng minh cổng là thứ chặn) · 5.6 thiếu opt-in ⇒ in `ERPNEXT_NOT_CONFIGURED` rồi exit 124 (không âm thầm dùng fixture). **2 lỗi tự gây**: harness treo làm mutation `if (false)` **sót lại trên đĩa** (sửa: bound 15s + bắt SIGINT/SIGTERM để khôi phục) · tripwire bắt chính harness (sửa: ghép chuỗi `"MOCK"+"_SERVER"`). **🔴 BUG MỚI (verify ERPNext THẬT, chưa sửa — chờ user)**: site thật **KHÔNG có "Nguyễn Thị Lan"** nhưng **có "Nguyễn Thị B"** ⇒ `resolvePartyFromList` pha substring lấy fragment `"Nguyễn Thị"` (user gõ dài hơn) khớp unique `"Nguyễn Thị B"` ⇒ **ĐỌC trả lời tự tin về SAI khách** (`error_code: none`); **đường GHI an toàn** (`ENTITY_PICK_REQUIRED`). ⇒ đã hỏi user. **User chốt: duyệt issue1 + THÊM validate lúc BOOT cho `http-ask`, VÀ fix bug resolver ngay.** **ĐÃ LÀM (đều đã test + falsify)**: (1) **boot validation** — `http-ask.main()` gọi `pickServerScript()` TRƯỚC `listen()` ⇒ thiếu cấu hình exit 1, in target rõ ràng; 2 test mới (thiếu cấu hình ⇒ exit 1 + không `ready:true`; có opt-in ⇒ boot). Cố ý KHÔNG probe mạng lúc boot (tunnel lên xuống thất thường). (2) **rule 2b** trong `resolvePartyFromList` — bỏ hit duy nhất khi cả 3: (a) tên ERPNext CÒN token phía sau vị trí khớp, (b) token kế tiếp là name-like (không số + không thuộc từ vựng ý định của router — lấy từ `listRouting()` của contract, không hardcode), (c) token đó không có trong tên row; bỏ thì **chặn cả vị trí bắt đầu** để fragment ngắn hơn không re-pick. **3 hồi quy do bản đầu của 2b, CHỈ full suite bắt được, đã sửa**: `"…chị Lan 500 ngàn"` bị chặn oan vì token kế tiếp là SỐ TIỀN; và `"…năm trăm ngàn"` (chữ) ⇒ điều kiện "row còn token phía sau" mới đúng, tính theo TỪNG field (nối tên+id làm id bị đọc thành tên còn dài — test tự viết bắt được). **Bằng chứng**: Node **461 (459 pass, 2 fail dsh-env từ B0)** · Python 62 OK · `entity-name-collision` **10 test** · falsify **rule-2b 5/5 RED** + **mock-optin 6/6 RED** (đã thêm ca boot) · **đo ERPNext THẬT 11 câu: chỉ 1 dòng đổi — đúng ca bug** (`Nguyễn Thị B` ⇒ `null | AMBIGUOUS_ENTITY`), 10 ca còn lại y nguyên ⇒ zero hồi quy trên dữ liệu thật. **Ghi nhận không sửa** (§9.3): nhiều tên nhiều từ thật vẫn MISSING/AMBIGUOUS (một phần do Phase 1 stripper ăn mất tên) — gap phủ sóng, không phải lỗ hổng an toàn, đề nghị task riêng. **Re-verify 2026-09-21 (user báo site bật muộn 10 phút — nghi probe "thật" giả)**: 3 lớp kiểm READ-ONLY — (1) dữ liệu thật nguyên vẹn khớp bản ghi (122 Customer, `%Lan%`=[], `Nguyễn Thị B` duy nhất) ⇒ đối chiếu before/after hợp lệ; (2) chạy lại cả 2 probe khi site BẬT — từng dòng khớp bảng §9.2 (ca bug → `AMBIGUOUS_ENTITY`, 10 ca còn lại y nguyên); (3) thử nghịch host chết (`127.0.0.1:1`, không mock) ⇒ **fail-loud**: `isError:true` + `TOOL_ERROR … HTTP 0`, không thể lẫn fixture ⇒ **không có số liệu giả nào trong result63** (ghi §10).

- [x] **[2026-09-20] C2 — CAMERA → PROPOSAL (`.plan/phases3/C2-camera-to-proposal.md`) → ✅ KỸ THUẬT XONG, CHỜ DUYỆT (vùng ghi — AI không tự ký duyệt)**: deliverable `.plan/phases3/C2-result.md`. **§0 đo trước**: ảnh không có động từ mệnh lệnh (HÓA ĐƠN → `invoice.lookup`, PHIẾU THU → `payment.history`, mock C1 → `invoice.lookup`) ⇒ kind do USER chọn trên sheet; câu do app tổng hợp từ slots đo route ĐÚNG `sales_order.create`/`purchase_order.create` ⇒ **không có đường ghi thứ hai** (đi qua `/ask` + Safety Gateway y như câu đánh máy). **Code**: `ocr_policy.document_kinds` trong contract (validate fail-closed: kind trỏ stub/submit-capable/unknown ⇒ contract invalid) · `src/ocr/ocr-slots.mjs` (`kindSpec`/`assertSlotsProvenance` fail-closed 4 lớp: status lạ/mock (mặc định!)/confidence thiếu/dưới sàn 0.6 + `resolvePartyFromPhoto` chặt hơn chat: tên cắt cụt → candidates, KHÔNG auto-pick; word-boundary chống "Hà Tiên 20 Bao" nhặt "Hà Tiên 2") · `src/line-parse.mjs` nhà chung PURE (builder + OCR dùng chung parser, C0 static guard `src/ocr/**` không reference skills/ vẫn xanh) · route `POST /ocr/slots` (throttle → kind → provenance → authz → đọc master data; trả slots KHÔNG có proposal/price/command_id) · Flutter `OcrSlotsForm` (mọi field sửa được) → câu text gửi `ChatController.send()` = user's own turn → card confirm CŨ · outcome log `slots_served`. **Review lần 2 (phiên này) bắt 1 lỗ THẬT + vá**: `/ocr/slots` nhận `body.text` TRỰC TIẾP qua client (comment khẳng định sai "đã sanitize ở C0 seam") ⇒ client đã auth đẩy ~1 MB text vào NLP + resolver O(n²) → vá `assertSlotsText()` tại route (400 `OCR_READ_TOO_LONG`, trần `ocr_policy.max_text_length`) + test 5001 ký tự + falsify guard. **Bằng chứng**: Node **440 (438 pass, 2 fail dsh-env từ B0)** · C2 **15/15** · golden 7/7 · Python **62 OK** · analyze **0** · Flutter **211 pass, 1 fail = probe P2b cũ** · **falsify C2 10/10 RED** (in-repo, cả Node + Dart), C0/C1 9/9 vẫn đỏ sau refactor.

- [x] **[2026-09-20] B3 — QUOTATION.CREATE (`.plan/phases3/B3-quotation-create.md`) → ✅ **ĐÃ DUYỆT** (user: "Đạt — xếp hàng chờ commit B1→B2→B3"), CHỜ MÁY CÓ REPO**: deliverable `.plan/phases3/B3-result.md`. **§0 probe read-only ERPNext thật**: `Quotation` tồn tại nhưng site có **0 dòng** · `custom_ai_action_id` → **HTTP 417** (cần migration, cùng field với Sales Order) · Phase 1 **không** viết lại "báo giá/xin báo giá/gửi báo giá" nhưng **"hỏi giá" → `unit_price`** (keyword "hỏi giá" chỉ sống với text thô; câu hỏi giá rơi vào `stock.balance` — gap UX, không phải lỗ hổng ghi). **Code**: `quotation.create` từ stub → thật (execution + line_policy + taxonomy **`QT_*`** riêng + skill) · `sales-order-write.mjs` thành toolkit dùng chung (export `refuse/rowsOf/docOf`, `pairLines({codePrefix})`, `resolveSalesCompany({code, noun})` — hành vi SO không đổi) · `src/skills/quotation-write.mjs` (MỚI: build + execute NHÁP + reconcile + verify, party shape `quotation_to`/`party_name`) · gateway registry + `QT_DUPLICATE_ACTION: 409` (áp bài học B2 ngay) · router factory + nhóm `quotation_write` (`startsWith` + deny-list câu HỎI) · copilot-server gộp 1 nhánh cho 2 line-document (khác builder + câu trả lời "BÁO GIÁ NHÁP … ĐỀ NGHỊ") · mock `createQuotation` + `QUOTATIONS` khai TRƯỚC khối restore + inject `MOCK_ERP_QT_*` · Flutter card nhánh `create_quotation` + `_confirmableActions` thêm `create_quotation` · golden **8 ca báo giá `q01–q08`** (write bucket 38→46). **Bằng chứng**: Node **382 (380 pass, 2 fail dsh-env từ B0)** · `b3-quotation` **17/17** · B2 **24/24** (đã cập nhật 4 kỳ vọng cũ do stub thành thật) · golden 7/7 · Python **62 OK** · analyze **0** · Flutter **184 pass, 1 fail = probe P2b cũ** · `proposal_card_qt_test` 4/4 · **falsify 9/9** (A–I, mỗi lần đỏ đúng chỗ, restore byte-identical). **2 bug thật**: (1) **gộp nhánh làm mất SHAPE tham số** — truyền thẳng object khách thay vì `{ customer: … }` ⇒ **mọi câu đặt hàng bị `SO_CUSTOMER_UNRESOLVED`**, do **E2E B2 bắt** (unit test B3 gọi builder trực tiếp nên không thấy) → đã sửa + thêm **E2E B3 qua copilot**; (2) **`_confirmableActions` (Dart) thiếu `create_quotation`** ⇒ card báo giá render không có nút xác nhận → đã thêm (tripwire cross-check Dart↔contract giờ 3 = 3). **2 bài học mới vào skill** (giữ SHAPE khi gộp call site + "falsify không đỏ ⇒ kiểm đã gỡ 1 hay toàn bộ chuỗi guard dự phòng" — ca F chỉ xanh vì `setReference` đã phủ).
- [x] **[2026-09-20] DUYỆT B3 + MIGRATION CORRELATION FIELD (user: "Đạt — xếp vào hàng chờ commit B1→B2→B3" · "đồng ý: câu hỏi không tạo chứng từ" · "soạn migration cho cả SO + Quotation")**: `B3-result.md` §6 đã ghi 3 quyết định — trong đó **quyết định #2 là CỐ Ý khác plan3_review3 §528** (câu hỏi giá không mở card HIGH; ghi rõ để không bị coi là thiếu sót). **Migration**: `mcp-erpnext/scripts/add-correlation-field.mjs` từ hardcode "Payment Entry" → **derive từ contract** (`execution.write_doctype` + `correlation_field`) + `--plan-only` (không creds/mạng) + `--dry-run` (chỉ đọc), vẫn idempotent + verify đọc-ngược. **Đo thật (chỉ đọc)**: plan = 3 target (Payment Entry / Quotation / Sales Order); `--dry-run` trên ERPNext thật → **Payment Entry ĐÃ CÓ** (unique=1, search_index=1) · **Sales Order + Quotation THIẾU** field → cần migration trước khi ghi thật; **KHÔNG tự tạo field**. Test canh scope: `test/correlation-field-migration.test.mjs` 2/2 (derive khớp contract, thu hẹp scope không mở rộng được, doctype lạ ⇒ exit 1). Suite Node cuối: **384 (382 pass, 2 fail dsh-env từ B0)**.
- [x] **[2026-09-20] DUYỆT B1 + B2 (user: "Đạt — commit B1 rồi B2" · "đồng ý cả 2" câu B1 · "cho phép probe read-only")**: `B1-result.md` §7 + `B2-result.md` §7 đã ghi kết quả. **Probe read-only ERPNext thật (Sales Order)**: `custom_ai_action_id` → **HTTP 417 Trường không được phép** (control không field → OK, 3 đơn `SAL-ORD-2026-…`) ⇒ **site thật chưa có field correlation trên Sales Order** ⇒ G1 của B2-go-live = migration thêm field TRƯỚC lần ghi SO thật; `SO_CORRELATION_FIELD_MISSING` là hành vi đúng trên site thật tới khi đó. Không ghi gì trong probe. **Commit B1→B2: đã duyệt, CHỜ MÁY CÓ REPO** (env này `git rev-parse` → not a git repository — không tự `git init`).
- [x] **[2026-09-20] B2 — SALES_ORDER.CREATE (`.plan/phases3/B2-sales-order-create.md`) → ✅ KỸ THUẬT XONG, **CHỜ DUYỆT COMMIT** (WRITE #2: executor thứ hai trong firewall + write-gate client + bảng refusal status)**: deliverable `.plan/phases3/B2-result.md`. **Mock-only** (chưa "cho phép ERPNext thật"). SO đi qua CÙNG cửa ghi payment (kill-switch→authz→amount→freshness→snapshot→executor registry literal `payment.create`+`sales_order.create`, không entry ⇒ `EXECUTOR_NOT_REGISTERED` fail-closed); contract thêm `sales_order.create` + stub `delivery.create`/`quotation.create` + deny-list `notIf` (đo trước: "giao hàng/báo giá/câu hỏi về đơn" misroute vào stock.balance) + `line_policy` + `rate_source` refuse `utterance` (giá 999 từ client ⇒ `PROPOSAL_VERSION_STALE`); skill `sales-order-write.mjs`: probe correlation field TRƯỚC khi ghi (thiếu ⇒ 500 `SO_CORRELATION_FIELD_MISSING`), action-id có đơn ⇒ `SO_DUPLICATE_ACTION` **409 + existing_doc**, create `docstatus: 0`, verify đọc-ngược lệch ⇒ 503 + **PENDING** + `retry_same_command_id`; mock: `PRICE_BOOK` per (item,uom) + fault injections `MOCK_ERP_SO_NO_CORRELATION_FIELD`/`MOCK_ERP_SO_DROP_LINE`/`MOCK_ERP_FAIL_AFTER_WRITE` + state persist `MOCK_ERP_STATE` qua nhiều process; golden **8 ca SO o01–o08** đúng schema; Flutter card "Xác nhận tạo đơn (NHÁP)" + dòng quy đổi `(=1000 Kg)` + confirm POST dòng đóng băng không flag submit. **3 bug thật bắt được và fix**: (1) **TDZ nuốt bởi try/catch trong mock** — `const SALES_ORDERS` khai SAU khối restore ⇒ state file đúng nhưng không bao giờ đọc lại; solo probe 2 process lộ ra (P1 tạo → P2 thấy **0**); gây lại ⇒ duplicate test đỏ `200 ≠ 409` tức **đơn thứ hai được ghi thật**; (2) `SO_DUPLICATE_ACTION` 500 thay vì 409 + mất `existing_doc` ⇒ bảng `SKILL_REFUSAL_STATUSES` trong firewall, falsify xoá rỗng bảng ⇒ `500 ≠ 409`; (3) **formatter TIỀN render SỐ LƯỢNG** ⇒ "1 Tấn (=1.000 Kg)" ⇒ tách `_qty()` khỏi `_vnd()`. **Bằng chứng**: Node **365 (363 pass, 2 fail dsh-env từ B0)** · `b2-sales-order` **24/24** · `proposal_card_so_test` **6/6** · Python 62 OK · analyze **0** · Flutter còn đúng probe P2b cũ · **falsify 10/10** (A–H harness + I TDZ + J bảng status) đỏ đúng chỗ, `grep FALSIFY` rỗng · fingerprint payment assert không đổi (không regress). Gap: probe read-only `custom_ai_action_id` trên Sales Order thật → **HTTP 417 (site chưa có field) ⇒ cần migration trước khi ghi SO thật** · thuế/chiết khấu thật chưa mô phỏng · update/cancel draft = phase sau · **3/4 cách nói `lên/tạo/chốt đơn` không tạo được đơn trên ĐƯỜNG THẬT** (synonyms.py Phase 1 viết thành `sale` TRƯỚC router ⇒ rơi về `invoice.lookup`; đo lại sau khi B2 xong; chỉ `đặt hàng` tới `sales_order.create` — sửa synonym là việc của Phase 1, chờ user).
- [x] **[2026-09-20] B1 — ITEM / SUPPLIER / UOM RESOLVERS (`.plan/phases3/B1-item-supplier-uom.md`) → ✅ KỸ THUẬT XONG, **CHỜ DUYỆT COMMIT** (vùng xác thực/kiểm soát truy cập: mở thêm 1 tool đọc vào whitelist + 1 route/capability READ mới)**: deliverable `.plan/phases3/B1-result.md`. **§0 đo TRƯỚC bằng 3 probe read-only trên ERPNext thật, đảo ngược 3 giả định của plan3_review3 §B**: (1) `Item.uom` **HTTP 417** — field không truy vấn được ⇒ default UOM chỉ có `stock_uom`; (2) `UOM Conversion Factor.item_code` **HTTP 417** ⇒ hệ số là **GLOBAL, không per-item** (schema `uom_alternatives` per item của plan không có nguồn dữ liệu); (3) site có `Bao/Tấn/Kg/Xe/Thung(!)/Viên/Mét/m3` nhưng **KHÔNG có Cây/Tạ/Bó/Cuộn/Tấm/Pallet**, và hệ số chỉ có `Tấn→Kg=1000`, `Bao→Kg=25` — **không có Kg→Bao/Tấn→Bao**. **Code**: `src/uom.mjs` (MỚI) 6 mã quyết định (`NO_UOM/UOM_UNKNOWN/UOM_AMBIGUOUS/UOM_FACTOR_MISSING/NO_CONVERSION_NEEDED/CONVERTED`) — tên đơn vị từ UOM doctype, hệ số từ UOM Conversion Factor, **chỉ hệ số TRỰC TIẾP**, mọi CONVERT bắt buộc có `display`, `ask` là mặc định fail-closed; policy vào contract `defaults.uom_policy` + loader `validateUomPolicy` fail-closed (8 biến thể JSON nới rule ⇒ throw). Item **4 states** dùng `classifyEntityResolution` (generic hoá từ P1, giữ nguyên matcher đã đo ở batch b13–b15) + picker candidates; **Supplier API** = `skills/purchasing.mjs` + capability READ `supplier.lookup` + route group `supplier` (thêm `erpnext_supplier_list` vào `READ_ONLY_TOOLS`, có test canh cả 2 chiều); **dùng lại parser Python** (`nlp.quantities`, `quantity.py` đòi số đứng trước đơn vị) làm nguồn CHÍNH, extractor Node chỉ bù đơn vị Python không biết, một bảng đơn vị cho cả hai đường. **Bằng chứng**: Node **339 (337 pass, 2 fail dsh-env có từ B0)** · `b1-item-supplier-uom` **16/16** · Python 62 OK · analyze **0** · Flutter **175 (174 pass, 1 fail = probe cũ)** · **falsify A–D** (deny-bigram `bao nhiêu` · tự nghịch đảo hệ số · item ambiguous tự lấy phần tử đầu · bỏ longest-name-wins) → đỏ đúng chỗ, `diff` khôi phục sạch. **Loop THẬT (ERPNext thật + NLP thật)** bắt **2 bug mà 16 test xanh không thấy**: (1) `Number(null) === 0` ⇒ `quantity=null` cho ra **"quy đổi: 1 Tấn = 0 Kg"**; (2) "**25kg trong TÊN mặt hàng**" bị parser nhận là đơn vị của user ⇒ câu hỏi tồn kho bị hỏi ngược về đơn vị + ASK oan. Đã sửa + khoá bằng test mới + **3 dòng bài học vào `.agents/skills/erpn-verify-first/SKILL.md`** (2 từ loop thật + 1 từ vòng review: `supplier.lookup` khai `ERP_UNAVAILABLE` nhưng nhánh đọc NCC không có try/catch ⇒ lỗi ERPNext bay ra 500 trần, mã đó vĩnh viễn không phát ra được — nay có guard + fault injection `MOCK_ERP_FAIL_SUPPLIER_LIST` + test E2E, falsify F đỏ đúng chỗ). Gap: `Tấn→Bao` chưa có (gap DỮ LIỆU) · hệ số global có thể sai với bao 50kg · đơn vị không có trên site ⇒ luôn ASK · multi-line chưa xử lý (B2).
- [x] **[2026-09-20] A1 — UX-READ DRILL-DOWN (`.plan/phases3/A1-ux-read-drilldown.md`) → ✅ KỸ THUẬT XONG, **CHỜ DUYỆT COMMIT** (thêm 1 ranh giới tin cậy: endpoint đọc mới có authorize + re-validate entity)**: deliverable `.plan/phases3/A1-result.md`. **Contract là nguồn duy nhất**: `capabilities.json` + khối `ui_screens` (`customer_account`, `offered_by` = `customer.balance`+`invoice.lookup`, `limit 5–10`) + loader validate **fail-closed** (màn phải do capability READ compose, `offered_by` toàn READ, `min ≤ default ≤ max`). **Server**: `/ask` gắn `ui` có cấu trúc cho nhóm ĐỌC (customer/sales → màn; inventory/payment → `ui:null`; card WRITE không có khoá `ui`); `src/read-views.mjs` (MỚI) + route `POST /read/list` theo thứ tự throttle → screen∈contract → `authorize()` → **entity_id re-validate trên dữ liệu vừa đọc** → đọc tươi (`listUnpaidInvoices` + `getCustomerBalance`) → `{summary, rows bounded, total_documents, truncated}`; đọc tươi thật, giới hạn 5–10 do contract (1→5, 99→10), mã từ chối `UNKNOWN_READ_SCREEN/MISSING_ENTITY_ID/CUSTOMER_NOT_FOUND`; audit outcome mới `read_served`. **Flutter**: `ReadUiIntent` + `ChatTurn.readUi` (lưu lịch sử, lượt cũ giữ JSON cũ) · `readList()` gộp 1 hàm map lỗi với `ask()` · nút `ReadDrillDownButton` chỉ khi server nói · `ReadListScreen` (loading/lỗi+Thử lại/kéo-làm-mới, chú thích "chỉ để xem") · route `/read` **fail-closed khi thiếu intent**. **Bằng chứng**: Node **323 (321 pass, 2 fail dsh-env có từ B0)** · `a1-read-drilldown` 9/9 · `a1-read-authz` 1/1 (403 thiếu quyền / 200 control) · Python 62 OK · analyze **0** · Flutter **175 (174 pass, 1 fail = probe cũ)** · `read_drilldown_test` 6/6 (tap = 1 `/read/list`, **0 `/execute`**). **Falsify F1/F2/F3** (guard nút · bỏ re-validate entity · bỏ clamp limit) → đỏ đúng chỗ (4/4/2), khôi phục sạch. **ERPNext THẬT (read-only)**: khách `P1A-ACCEPT Customer` → nút/màn `96.399.500đ / 95 chứng từ`, `truncated true`, rows `ACC-SINV-2026-01250…`; và **MATCH answer==screen: true** (câu trả lời chat và màn cùng con số). Không regress payment (25/25 · 17/17 · 13/13 · 10/10).
- [x] **[2026-09-20] B0 — P9 READINESS GATE (`.plan/phases3/B0-p9-readiness-gate.md`) → ✅ **GO** (không sửa 1 dòng code sản phẩm nào; deliverable `.plan/phases3/B0-result.md`)**: đo LẠI toàn bộ suite trong phiên — **Python 62 OK · Node 313 (311 pass/2 fail) · Router 19/19 · Flutter analyze 0 · Flutter 169 (168 pass/1 fail)** · golden 202 case **100% mọi bucket** (read 52 · write 30 · kinship 40 · money 30 · ambiguous 30 · adversarial 20). **2 fail Node** = 2 test `dsh-gateway` cần runtime dsh chạy thật; đo trực tiếp `verifyDshRuntime` → `ran:false`, `--version timed out after 8000ms` (registry `PONG 215ms` nhưng npx phải resolve ~250 package >150s) ⇒ **gap môi trường, không phải regression**; đóng bằng `npm i` ở root (pin đã có) — KHÔNG tự cài. **1 fail Flutter** = `test/_probe_review_test.dart` P2b (harness tạm result40, kỳ vọng hành vi CŨ) ⇒ cần user chọn xoá/chuyển thành test thật. **Checklist B0 10/10 xanh** (contract+forbidden 7+1 · authz P8 17 · entity P1 10 · immutable+TTL 12 · idempotency 25+4 · `custom_ai_action_id`+reconcile 4 · false-write≈0 · duplicate→1 doc 25+17 · golden 7 · kill-switch/rate-limit không đốt command_id 13+10). **Bằng chứng sống (read-only, ERPNext thật)**: `custom_ai_action_id` trên Payment Entry → `unique=1, search_index=1` ✓ · nhưng **query theo fieldname CHO THẤY CHỈ Payment Entry có field** ⇒ **G1 (chặn cứng B2 go-live)**: doctype WRITE mới chưa có field correlation (script migrate cũng hardcode `DT = "Payment Entry"`) · Payment Entry mang action id hiện = **0** ⇒ G4 human gate · command store thật: 78 command / 76 COMPLETED / 2 FAILED (**cả 2 FAILED đều không có `erpnext_doc`** ⇒ fail-closed), 75 record là chạy mock `PE-M002`, 1 doc thật `ACC-PAY-2026-00115` `reconciled:true`.
- [x] **[2026-09-19] ĐỒNG BỘ DOCS SAU PUSH (theo yêu cầu user: cập nhật `.project/` + root docs)**: bằng chứng **`result60.txt`** (đo LẠI thật trong phiên: `sha256sum` APK, `gh run view` #22, label qua `iconv -f UTF-16LE`, marker trong `kernel_blob.bin`). **`.project/`** (kiến thức tĩnh, không chứa progress): `architecture.md` (+route `/dsh/ask` `/dsh/health`, module `dsh-gateway.mjs`, nhánh AI trong data flow) · `patterns.md` (+3 hàng: resolver in "nguồn thắng", một chỗ quyết định spawn, guard `ref.mounted`) · `integrations.md` (run #22 + dispatch tay vì `paths` filter + cách verify APK + `DSH_MODE` + drift model) · `modules/chat.md` (chế độ AI + 2 endpoint + số test) · `modules/copilot-backend.md` (dsh-gateway + endpoint + `dsh:check`) · `state-routing.md` (mode selector = state cục bộ; sửa fence ```dart bị lặp) · `design-system.md` (**sửa đoạn bị lặp 2 lần** + quy ước SegmentedButton) · `openspec.md`/`overview.md`/`README.md` (HEAD `7476051`, suite mới). **Root**: gỡ marker lỗi thời "CHỜ DUYỆT COMMIT" của đợt DSH runtime ở `checklist.md` + `next.md` (đã commit từ trước — đúng dạng lỗi marker mục nát) · `faq.md` **§6.12** (push-backend/docs không build APK) + **§6.13** (chế độ AI cần gì, không cần cài gì trên từng máy) + 1 dòng §9 · `openspec/changes/flutter-chat-mvp/tasks.md` **§6.17–6.20** · `handoff_20260919-1005.md`.
- [x] **[2026-09-19] PUSH + CI BUILD #22 ✅ (theo lệnh user "push code lên gh để build apk")**: 2 commit đã push `2edafa4..03338ae` — `dc60a4e` **fix(dsh)** resolver 6 mức (vùng an toàn: trust boundary + ràng buộc cứng, user đã duyệt sau khi agent trình bày file list + message) · `03338ae` docs (result59 + setup-test + faq + checklist/next/working/features/LESSONS + .project + runbook). **LƯU Ý đã phát hiện trước khi push**: workflow `android-debug-apk.yml` lọc `paths: apps/mobile/**` ⇒ đợt backend+docs này **KHÔNG tự trigger**, phải kích tay `gh workflow run android-debug-apk.yml --ref change/flutter-chat-mvp`. **CI run #22** [`35435259669`](https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35435259669) head **`03338ae`** · **success** ~315s · artifact `erpn-chat-debug-apk` **84.222.402 bytes** / `app-debug.apk` **165.117.508 bytes** · `sha256=9da9a05f63b360b34ca0b161cf3ebb9d79407b5f282cc97f5d886cef48c0574f` (**đúng bằng cỡ APK run #21** — không có file Flutter nào đổi, nên đây là bản build lại y hệt về nội dung app). **Verify binary thật**: label `Nghiệp Vụ AI` tìm thấy trong `AndroidManifest.xml` (string pool **UTF-16LE** — `strings` thường không thấy; bài học §10) · marker code trong `kernel_blob.bin` trùng run #21: `Phân tích bằng AI` ×3 · `Chat thường` ×2 · `/dsh/ask` ×4 · `dshAsk` ×3 · `ttsEnabled` ×14 · `voiceAutoSend` ×16 · endpoint `erpn8788.loca.lt` ×3. Suite trước commit: **Python 62 · Node 313 · Router 19 · Flutter 150 · analyze 0**; review vùng an toàn: log_tail đều qua `scrubDiagnostics`/`redactToken` (bound 300) ✓ · child env giữ `PATH` và xoá đủ 5 key secret (probe sống: `verifyDshRuntime(child)` → `ran:true, 0.1.5-rc.1`) ✓ · spawn `shell:false` ✓.
- [x] **[2026-09-19] DSH RUNTIME DISCOVERY FIX (`.plan/dsh_prompt_fix1.md`) — ✅ ĐÃ COMMIT `dc60a4e` + PUSH (user duyệt; vùng an toàn)**: bằng chứng đầy đủ **`result59.txt`**. **Gốc lỗi**: resolver hardcode `/tmp/dsh-run/...` — máy Mac của chủ dự án KHÔNG cài dsh trong repo mà chạy `npx @deepseek-ai/dsh web` ⇒ `dsh:check` báo "entry missing"/health UNAVAILABLE. **Fix**: một resolver `resolveDshRuntime()` 6 mức — `DSH_ENTRY` → `DSH_COMMAND` → package local → **`npx --yes @deepseek-ai/dsh@<pin đọc từ package.json>`** → `/tmp/dsh-run` (**chỉ khi file tồn tại thật**, không còn là mặc định) → unavailable; `dshSpawnPlan()` là **chỗ duy nhất** quyết định spawn (argv-shaped, không shell string) và được **dùng chung** với `dsh-remote-runner.mjs`. **Bằng chứng**: `npm run dsh:check` → PASS, in `source=npx-pinned` + `--version -> 0.1.5-rc.1`; `/dsh/health` trả thêm `source`; WRITE block 31ms (`DSH_WRITE_BLOCKED`); `/ask` không dính dsh (171.800đ/4 khớp ground truth). **Vá thêm 3 lỗi thật tìm thấy khi chạy**: health phải chạy `--version` thật thay vì suy luận · response lỗi `/dsh/ask` nay mang `log_tail` = lý do thật (`llm-router: all upstreams failed (tried: mac-custom)`) · check script WARN khi `source=legacy-tmp`. **Falsify 4 lớp** (gỡ npx / đảo ưu tiên legacy-trước-npx / health không chạy thật / gỡ nhánh `DSH_COMMAND`) → đều ĐỎ đúng chỗ, khôi phục `diff` IDENTICAL. **BLOCKED_EXTERNAL**: DSH READ chạm ERPNext thật lần cuối không chạy được vì `llm9000.loca.lt` trả **503 Tunnel Unavailable** (đo trực tiếp; cùng code đã PASS thật lúc 09:19 trong phiên — 171.800đ/4 + 7 call router + 1 dòng `dsh_ask` runtime=local chứng minh dsh ĐÃ spawn). Suite: **Python 62 · Node 313** (306→313) **· Router 19 · Flutter 150 · analyze 0**.
- [x] **[2026-09-19] COMMIT + PUSH + CI APK ✅ — 3 commit tách theo phạm vi (user duyệt cả 3 nhóm, gồm vùng an toàn)**: `b61df0a` feat(dsh) (17 file: gateway + route + patch + 6 script + pin) · `868be04` feat(chat) client AI mode (5 file) · `f2dfddb` docs (9 file). Push `94daa29..f2dfddb`. **CI run #21** [`35426407766`](https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35426407766) head `f2dfddb` **success** ~3,5 phút, artifact `erpn-chat-debug-apk` **84.222.399 bytes** (+9.386 bytes so với run #20 — nhất quán với lượng code thêm). **BẰNG CHỨNG BINARY — lần đầu tiên grep được APK thật** (bài học result56 §11 "APK chưa grep vì shell không có token" — **nay `GH_TOKEN` có sẵn trong `.env`**, không in giá trị): tải artifact → giải nén → đọc `assets/flutter_assets/kernel_blob.bin` (build debug: Dart nằm ở kernel, **không** phải `libapp.so` — grep sai file sẽ ra 0 hit oan) ⇒ `Phân tích bằng AI` ×3 · `Chat thường` ×2 · `/dsh/ask` ×4 · `dshAsk` ×3 · `ttsEnabled` ×14 · `voiceAutoSend` ×16 · endpoint CI nhúng `https://erpn8788.loca.lt` · label `Nghiệp Vụ AI`. Suite trước khi commit: **Python 62 · Node 307 · Router 19 · Flutter 150 · analyze 0** (đo lại thật trong phiên, không chép số cũ). Secret scan 35 file → CLEAN (1 hit duy nhất là placeholder `<giá trị từ .env>` trong `setup-test.md`). Không stage: `.env` · `idempotency-store/` · `job-queue/` · `learning-log/` · `llm-router-audit/` · `.gemini/` · `.opencode/` · `.review/` · `initp`.
- [x] **[2026-09-19] DSH FINAL MINI-SPRINT (`.plan/dsh_prompt_check.md`) — ✅ ĐÃ COMMIT `b61df0a` (đã push)**: toàn bộ bằng chứng **`result58.txt`**. **§3 PIN**: `package.json` root pin `@deepseek-ai/dsh@0.1.5-rc.1` (trước đó KHÔNG pin ở đâu cả) + `resolveDshEntry()` resolve từ package đã cài thay vì hardcode `/tmp/dsh-run/...` của một máy + `dshRuntimeInfo()` + `scripts/check-dsh-runtime.sh` (exit code = nguồn sự thật, in cả version + marker). **§2 TOPOLOGY**: thêm `DSH_MODE=local|remote` — remote gọi runner thật trên Mac qua tunnel (`scripts/dsh-remote-runner.mjs`, token bắt buộc ≥16 ký tự, TỪ CHỐI khởi động nếu thiếu token/entry/patch/marker cổng chặn ghi, so token bằng `timingSafeEqual`, dùng lại `buildDshChildEnv` nên luật che secret chỉ có 1 bản); remote **KHÔNG BAO GIỜ** hạ cấp về local (có test + falsify #1); mọi response — kể cả THẤT BẠI — mang `runtime: local|remote` (bài học L6). **§4 REAL GEMINI PASS**: 1 session duy nhất qua `gemini-openai`, KHÔNG probe trước ⇒ **171.800đ/4 chứng từ** khớp ground truth live, router audit `upstream=gemini-openai attempts=[gemini-openai] status=200 messages=7` ⇒ ≥2 vòng tool-call (đây cũng là verify sống đường đa-lượt mà `thought_signature` từng làm gãy). **§1 SIX DEFECT đã sửa + falsify**: F1 parser giữ NGUYÊN VĂN câu trả lời nhiều dòng (trước chỉ lấy dòng cuối ⇒ **mất số tiền**) · F2 store bounded · F3 validate `conversation_id` tại biên (400) · F4 **gỡ marker CHẾT** `COPILOT_GATEWAY_DSH` (đặt nhưng không ai đọc, doc lại tuyên bố "both must be present") + đổi test xanh-vô-nghĩa · F5 `/dsh/health` nói thật · **F6 LỖI THẬT do test HTTP bắt được: route map TỪ CHỐI an toàn thành 502** (giám sát/retry hiểu nhầm là lỗi hạ tầng, không phân biệt được với tunnel chết) → nay 200 + `refused:true` · F7 `redactToken()` (secret mình ĐANG GIỮ thì che theo giá trị, generic scrub không nhận ra token runner). **E2E remote THẬT (loopback stand-in cho Mac)**: 200 + `runtime:"remote"` + ERPNext THẬT 171.800đ/4, log runner `run ok in 4390ms`; WRITE block qua topology remote 83ms, 0 proposal/execute. ⚠️ **BLOCKED_EXTERNAL**: hop thật backend → Mac qua tunnel không verify được vì `llm9000.loca.lt` trả **503 Tunnel Unavailable** (việc người thật: bật lại `lt` trên Mac) — dán nguyên nhân kèm bằng chứng, KHÔNG báo xanh thay. **6 script E2E** (`dsh:check` · `check:topology` · `dsh:e2e:read` · `dsh:e2e:write-block` · `check:ask-normal`) + **falsify 4 guard** (dispatch remote · map refusal 200 · `redactToken` · marker trong health → đều ĐỎ đúng chỗ, khôi phục diff IDENTICAL) + audit bypass 6 invariant. Suite: **Python 62 · Node 306** (290→306, +16 test) **· Flutter 150 · analyze 0**.
- [x] **[2026-09-24] M1-SITE — `customer.create` chạy THẬT trên ERPNext ✅ E2E ĐẠT, CHỜ DUYỆT COMMIT** (vùng master data + ghi record thật — AI không tự ký duyệt): (1) **bỏ hardcode "Múa"** — phân loại `customer_group/territory/customer_type` resolve theo contract `field_policy.profile.fields`: user/app → env `COPILOT_DEFAULT_*` → site tự khai (Select default / leaf duy nhất) → reqd ⇒ REFUSE / optional ⇒ trống + warning; value không có trên site ⇒ `CC_PROFILE_INVALID` (typo to, không fallback âm thầm); mock giờ mirror ERPNext (Link lạ ⇒ LinkValidationError — bug gốc giờ thấy được trong test). (2) **migration `custom_ai_action_id` trên Customer ĐÃ CHẠY trên site thật** (user duyệt, VERIFIED). (3) **pre-check đọc TOÀN BỘ master** (`limit:100`→`limit:0` — đo thật: 0 = all 123; tool pinned không có offset; trước fix card dựng cho khách row 122 đã tồn tại "Khách lẻ Minh Phát" ⇒ `CC_DUPLICATE_NAME` đúng sau fix). (4) **BUG THẬT thứ 2 tìm bằng vòng thật — fuzzy match chết cả feature**: site có customer tên **"A"**, luật cũ `name.includes(have)` ⇒ "khách test app m1" chứa chữ a ⇒ MỌI create bị `CC_FUZZY_MATCH`; fix so khớp theo TỪ nguyên vẹn (token run) + test mới + falsify ca P. (5) **E2E THẬT đo được**: `/ask "thêm khách Khách Test App M1"` → card HIGH profile `Individual/Vietnam/Individual` (env) → `/execute` → **Customer THẬT "Khách Test App M1"** (`replay:false`, verify đọc lại PASS) → `/ask "công nợ Khách Test App M1"` ⇒ "không còn nợ gì" (resolve ngay) → đếm site **123→124**, newest `Individual/Vietnam/Individual/disabled=0`. Suite: Node M1 **30/30** · Node full **755/753/2** (2 fail dsh-env từ B0) · falsify **16/16 RED** (A–P, restore byte-identical) · Flutter **281/281** + analyze 0 · Python 63 OK. Bằng chứng: `.plan/next4/M1-site-result.md` + `result66.txt` (phần M1-SITE). Còn lại: commit chờ duyệt · checklist máy thật 28 case cần APK.
- [x] **[2026-09-19] DSH END-TO-END (`.plan/dsh_end_to_end.md`) — ✅ ĐÃ COMMIT `b61df0a` (backend) + `868be04` (Flutter client) + `f2dfddb` (docs), đã push**: bảng task `.plan/dsh_e2e_tasks.md`, toàn bộ bằng chứng **`result57.txt`** (§1–§13). **Backend**: `src/dsh-gateway.mjs` (mới) + route `POST /dsh/ask` — pre-screen WRITE ở cổng (NLP down ⇒ fail-closed), guard chống patch thiếu `COPILOT_DSH_CONTEXT` (LỖ HỔNG THẬT, tìm bằng cách chạy thật chứ không đọc code), session TTL, concurrency (metering tách khỏi limit — bug thật qua test), error map có copy tiếng Việt; `copilot-server.mjs` export `normalizeText` (tái dùng, không viết lại). **23 test**. **Flutter**: `_ModeBar` + `SegmentedButton` (default "Chat thường", KHÔNG auto, KHÔNG persist) → `dshAsk()` (`copilot_api_client.dart`, `dshTimeout` 210s > `DSH_TIMEOUT_MS` 180s của gateway), trạng thái đang phân tích, câu từ chối của gateway hiện NGUYÊN VĂN, không card/không `/execute`. **8 test + falsify battery A–F** (B/C phải tháo TOÀN BỘ 9 lớp `ref.mounted` mới đỏ — bài học result47 §6). **Real E2E XANH**: `/dsh/ask` → dsh runtime thật → router → **mac-custom (LLM thật, KHÔNG dùng quota Gemini)** → `copilot_ask` → NLP → **ERPNext THẬT** ⇒ **171.800đ/4 chứng từ** khớp CHÍNH XÁC ground truth lấy live; audit `attempts=['mac-custom'] status=200` ×3 (1 câu hỏi = 3 round-trip); D4 real WRITE BLOCK `DSH_WRITE_BLOCKED` trong **0.0105s** (chứng minh không spawn) + audit `gateway_refused`; D5 `/ask` thường ⇒ entry dsh 43 → 43 (0 mới). **DRIFT môi trường**: `oc/big-pickle` của mac-custom đã bị Mac khai tử (403 model-not-found) → đổi sang `gemini/gemini-3.6-flash` (verify live + `tool_calls` thật) ở **2 router config + patch dsh + default classifier**; docs `setup-test.md`/`features.md` cập nhật + 2 hàng troubleshooting mới. **BUG THẬT sửa (có sẵn từ Phase 3, KHÔNG phải của DSH)**: ghi `state` sau khi provider auto-dispose ⇒ `UnmountedRefException` (user rời màn hình giữa lúc chờ) — thêm 9 guard `ref.mounted` + test hồi quy cho **cả** đường normal. Suite mốc: **Python 62 · Node 290 · Flutter 150 · analyze 0**.
- [x] **[2026-09-19] COMMIT + PUSH + CI APK ✅ — 3 commit tách bạch theo quyết định user (P6 UX trước, TTS lên trên)**: `f55d557` feat(p6-ux) (8 file) · `1401f16` feat(tts) (12 file, 2 file mới) · `8d2055c` docs (11 file). Push `bbca7ca..8d2055c`. **CI run #20** [`35414524990`](https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35414524990) `8d2055c` **success**, artifact `erpn-chat-debug-apk` **84.213.013 bytes** (hết hạn 2026-12-17). **Cách tách đã verify chứ không đoán**: tạm dựng cây P6-only → `flutter test` = **120/120** khớp đúng con số đã ghi ở result55 §11 ⇒ chứng minh đúng trạng thái đã verify; sau khi giải nén snapshot → `flutter pub get` → **142/142** · analyze 0 · **0 dòng THÊM** chức năng P6 UX trong commit TTS (16 hit `voiceAutoSend` đã đọc từng dòng: chỉ context/xoá/comment) · secret scan 3 commit SẠCH · ⚠️ **APK chưa được grep binary** (tải artifact cần token — shell không có `GH_TOKEN`/`GITHUB_TOKEN`) ⇒ bằng chứng APK chứa TTS là **gián tiếp** (ancestor `1401f16` ⊂ `8d2055c` + size **+43.308 bytes**); chi tiết `result56.txt` §11.
- [ ] **[2026-09-19] TTS "ĐỌC CÂU TRẢ LỜI" (mobile client, theo `.plan/next2/tts-implementation-plan.md`) — ✅ ĐÃ COMMIT `1401f16` (xem mục trên; phần dưới giữ làm bằng chứng thiết kế)**: `flutter_tts 4.2.5` (on-device, `vi-VN`, rate 0.55) · setting `ttsEnabled` theo ĐÚNG khuôn `AppSettingsService` (`settings_tts_enabled_v1`, fail-safe mọi nhánh hỏng = OFF) + SwitchListTile "Đọc câu trả lời" trong Settings (**không dialog** — không nằm trong đường tiền) + ghi chú gói tiếng Việt · `<queries>` `TTS_SERVICE` trong `AndroidManifest.xml` (Android 11+ ẩn engine ⇒ `speak()` im lặng) · **`TtsService` interface (mới)** chỉ có `speak`/`stop` — KHÔNG method nào confirm/execute ⇒ không tồn tại đường TTS → ghi · hook **DUY NHẤT** tại `ChatController._append()` (không ở `build()`/`attachRejection()`); `pickEntity` đi qua `send()` nên vẫn được đọc (câu trả lời thật sự mới) · chỉ đọc `answer` + `proposal.summary`, sanitize gỡ markdown + **mọi token dạng UUID** · `speak()` no-op IM LẶNG khi máy không có gói tiếng Việt · try/catch quanh `speak` ⇒ TTS không bao giờ làm hỏng kết quả chat · **+21 test ⇒ Flutter 141** (120→141) · analyze 0 · Python 62 · Node 267 · **falsify A–F** (A ttsEnabled gate · B uuid stripper · C rò JSON máy · D try/catch · E auto-confirm nhét vào card · F nuốt proposal) — **C và E lần đầu XANH OAN ⇒ lộ 2 oracle RỖNG của chính tôi** (đã sửa, xem dưới) · **2 lỗi tự gây đã sửa**: (1) test "không có request /execute" gắn recorder vào Dio RIÊNG của `copilotApiClientProvider` trong khi card POST qua `dioProvider` ⇒ confirm thật không thể quan sát → override `dioProvider`; (2) khẳng định "không có UUID" bị chính sanitizer che ⇒ thêm khẳng định HÌNH DẠNG nguồn (`{`, `}`, `schema`, `params`, `risk_display`) · **[review vòng 2 — `result56.txt` §9, 2 lỗi THẬT đã sửa]** (F1) `await speak()` (đúng snippet plan §4) ⇒ engine TTS treo làm `send()` không bao giờ return ⇒ **stuck UI** (ô nhập không xoá); CHỨNG MINH bằng fake `Completer` không bao giờ complete ⇒ `TimeoutException` đỏ thật → sửa `unawaited(_maybeSpeak(turn))` + try/catch **bao trùm cả cổng** (lệch plan §4 CÓ Ý THỨC) · (F2) oracle DỄ VỠ: `textContaining('vẫn phải bấm Xác nhận khi thu tiền')` dùng chung đuôi câu với switch auto-send ⇒ đo thật khi bật cả 2 = **2 matches** → neo vào câu chỉ TTS có · (F3) 2 test bắt buộc của plan (#3 `build()` không đọc, #4 `attachRejection()` không đọc) **chưa từng falsify** → đã falsify G/H, đỏ đúng chỗ · **falsify A–H = 8/8** · **Flutter 142** (141→142) · analyze 0 · **[TRỞ NGẠI — xem `result56.txt` §10]** yêu cầu "commit TTS riêng" **KHÔNG tách được ở mức file**: `voiceAutoSend` (P6 UX) = 0 trong HEAD ⇒ 4 file trộn (nặng: `settings_screen.dart` 9/9, `settings_test.dart` 22/21), nghẽn ở `final ok = okGateway && okSubmit && okVoice && okTts;` (một hunk, TTS phụ thuộc `okVoice`) ⇒ commit chỉ-file-sạch sẽ **không build được**; **chưa stage/commit gì**, chờ user chọn (a) commit P6 UX trước rồi TTS lên trên, hay (b) 1 commit gộp có ghi rõ.
- [ ] **[2026-09-18] SETTINGS "TỰ GỬI SAU KHI NÓI XONG" (P6 UX, mobile client) — ⏳ CHỜ DUYỆT COMMIT**: switch mới trong Settings, **mặc định OFF** (giữ hành vi P6: đọc → xem/sửa → bấm Gửi). Bật ON ⇒ sau kết quả STT **final**, nếu ô nhập có text (trim ≠ rỗng) và không đang loading thì gọi **cùng** `onSend()` (POST `/ask`) — không path riêng, **không** auto-confirm WRITE, **không** đụng `/execute`; partial và transcript rỗng **không** trigger (rỗng còn **không** đụng ô nhập). Key versioned `settings_voice_auto_send_v1`, fail-safe mọi nhánh hỏng = OFF. Bật ON **không cần dialog** (khác F7-2 — không nằm trong đường tiền) nhưng copy ghi rõ "vẫn phải bấm Xác nhận khi thu tiền". **+11 test** · **falsify 3 guard** (B lần đầu KHÔNG đỏ vì có 2 lớp check trùng — đã gỡ lớp không thể chạm) · **Flutter 118** (107→118) · analyze 0 · 3 lỗi tự gây đã sửa (guard trùng · test cũ đỏ vì `SettingsScreen` là `ListView` LAZY không build phần tử dưới màn hình ⇒ cho viewport test cao 2400px · `textContaining` mơ hồ khi có 2 switch) · bằng chứng `result55.txt` §10 · **[review vòng 3 — `result55.txt` §11]**: gỡ **guard chết thứ 3** trong `_maybeAutoSend` (nay chỉ còn ĐÚNG 1 điều kiện `voiceAutoSend`; falsify C xanh toàn bộ ⇒ chứng minh không thể chạm; lớp chặn gửi chồng thật là `ChatController.send` khi `isLoading`) · sửa **2 doc sai** (R5 `_InputBar` còn ghi "no auto-send"; R6 `localeVerified` còn hứa "soft note") · sửa **báo sai kết quả lưu** (R8 `ok = okGateway && okSubmit && okVoice` — trước đó switch có thể hiện ON mà KHÔNG lưu được) · **+2 test ⇒ Flutter 120** · analyze 0 · ❓ **câu hỏi mở**: `pauseFor: 3s` (Android có thể ép 1–3s) ⇒ khi switch BẬT, ngập ngừng giữa câu = gửi câu nửa vời — đã ghi `human.md` §1, **chưa tự đổi hành vi**, chờ user chọn (a/b/c).
- [x] **[2026-09-18] BUGFIX P6 ĐÃ COMMIT + PUSH + APK CI ✅** — `77e2b57` (fix, 4 file code) · `bbca7ca` (docs) → `change/flutter-chat-mvp`; **APK run `35367603981` success** (artifact `erpn-chat-debug-apk` 84.169.705 bytes) và **xác minh binary**: `grep -c` trong `kernel_blob.bin` của APK = câu mới 2 lần, **câu SAI cũ 0 lần**, `vi_VN` 3 lần (⇒ APK không phải build cũ). Còn lại: cài máy thật + smoke mic (`human.md` §2). Chi tiết `result55.txt` §8.
- [x] **[2026-09-18] BUGFIX P6 — cảnh báo sai "Máy không có bộ nhận dạng tiếng Việt" (mobile client)**: user báo trên máy thật "Gboard hiểu tiếng Việt nhưng app báo máy không có bộ nhận dạng tiếng Việt". Nguyên nhân gốc (doc plugin `locales()`, nguyên văn): *danh sách này CHỈ phủ recognizer ON-DEVICE, có thể thiếu ngôn ngữ mà recognizer ONLINE vẫn nhận* — code cũ chỉ lấy `localeId` từ `locales()` ⇒ không thấy `vi` thì trả `null` ⇒ (a) UI kết luận "máy không có tiếng Việt" (sai bản chất), (b) `listen(localeId: null)` để OS tự chọn locale. Sửa 6 điểm: luôn xin `vi_VN` (`fallbackLocaleId`, kể cả list rỗng) · tách tín hiệu `localeVerified` (list ≠ năng lực) · platform từ chối locale đã xin ⇒ **retry 1 lần** với mặc định máy · `error_language_not_supported`/`_unavailable` bỏ locale ép nhưng **không** tắt service · UI **không hiện gì về locale** (UX follow-up cùng ngày: bỏ luôn gợi ý nhẹ — tín hiệu user không hành động được thì đừng hiển thị; cảnh báo THẬT denied/unavailable/error_language_* vẫn giữ, có test) · nới copy `unavailable`. **+6 test** (service 6→11: list thiếu `vi` · list rỗng · có `vi` ⇒ không retry · từ chối ⇒ `[vi_VN, null]` + `listening` · đã xác nhận ⇒ lỗi thật vẫn `unavailable` · refusal ngôn ngữ ⇒ service sống; widget 15→16: hint nhẹ + vẫn nghe + vẫn không gửi) · **falsify 4 guard A–D** (D = tái tạo ĐÚNG code cũ ⇒ đỏ đúng ca bug) · **Flutter 107** (101→107) · analyze 0 · Python 62 · Node 267 · **4 lỗi tự gây đã sửa** (falsify sai vị trí · trùng tên field/getter trong fake · `textContaining` phân biệt hoa/thường · `dart format` tall-style reformat cả file ngoài phạm vi → phải revert churn) · bằng chứng `result55.txt` + `.plan/phases2/p6-result.md` mục "Bugfix P6". Còn lại: cài APK + smoke mục 7 (máy thật).
- [x] **[2026-09-18] P6 (phases2) — VOICE / STT ✅ ĐÃ COMMIT `9b54d35` (đã push)**: gate 150 câu audio ĐÃ BỎ (user 2026-09-18) ⇒ dùng **STT của OS** (`speech_to_text` 7.5.0), không tự host model, không corpus riêng · luồng đúng `plan2_final` §20: mic → STT → text vào **CHÍNH ô nhập editable** → user sửa → Gửi → `POST /ask`; **không auto-Send**, interface `SpeechService` không có method nào gửi/execute ⇒ **không tồn tại đường voice → WRITE** · `AndroidManifest.xml`: `RECORD_AUDIO` + `<queries>` `android.speech.RecognitionService` (repo không có target iOS ⇒ keys `Info.plist` ghi lại trong result) · locale `vi_VN` + cảnh báo fallback; dictation giữa lúc đang gõ không xoá chữ đã viết; partial **thay** không nối; `dispose()` đóng mic · **21 test** (15 widget mock STT + 6 unit trên subclass plugin thật) · **+ falsify 8 guard** · **self-review tìm 4 lỗi thật đã sửa** (kết quả STT muộn ghi đè ô nhập đã gửi — plugin doc xác nhận "stopping a listen session will cause a final result to be sent" ⇒ guard UI + session token ở service · thông báo che "Đang nghe…" · double-tap mở 2 phiên · `cancel()` không thực thi hợp đồng) · **Flutter 101** (80→101) · analyze 0 · bằng chứng `.plan/phases2/p6-result.md` (bị gitignore). Còn lại: smoke mic máy thật (human) — **APK CI đã build + xác minh** trong mục bugfix ở trên.
- [x] **[2026-09-18] P8 (phases2) — MULTI-USER / RBAC / COMPANY SCOPE ✅ ĐÃ COMMIT `90401f0` (đã push, user duyệt trong chat)** (vùng phân quyền + tiền): **`src/authorization.mjs` (mới)** — boundary server-side đọc từ `capabilities.json`, không hardcode capability nào trong logic (grep = 0) · **2 chế độ**: `multi_user` (`COPILOT_USERS` JSON tường minh; user lạ degrade read-only, không crash; JSON hỏng ⇒ NÉM lỗi thay vì im lặng "mọi người được phép") và `single_tenant` mặc định (giữ hành vi cũ — **đo thật**: gỡ miễn trừ ⇒ **33 test đỏ**, 267→234) · company **server-first** (principal → `COPILOT_COMPANY` → request) + allow-list; multi-user thiếu company ⇒ `COMPANY_SCOPE_REQUIRED` (**map về copy P2** `AUTHORIZATION_DENIED` để user luôn có chữ) · **wire 4 cửa**: `/ask` chặn **trước** `route.factory()` (không proposal, 0 ERPNext read) · `/execute` bước 1b **trước** idempotency (403, **không tiêu `command_id`**) · `/jobs` lọc theo actor (+`hidden` counter) · `/execute/cancel` chỉ chủ lệnh hoặc người có quyền (record cũ không actor ⇒ fail-closed) · job replay mang `user_id` gốc ⇒ phân quyền **tính lại tại thời điểm ghi** · audit `user_id`+`company` vào record idempotency + job; `logEvent` `/execute`+`/cancel` dùng `principal.user_id` (**một khái niệm "ai" duy nhất** cho rate-limit/audit/job report) · **17 test mới** (`test/p8-authorization.test.mjs`) · **+ falsify 13 guard** (A–M, đỏ đúng chỗ, khôi phục diff sạch) · **self-review tìm 5 lỗi thật đã sửa**: rò `/jobs` chéo user · cancel không kiểm ai · `COMPANY_SCOPE_REQUIRED` không có copy · audit ghi sai người (credential ≠ principal) · allow-list là nhánh chưa từng được test (falsify 14/14 xanh) · **Node 267** (250→267) · Python 62 · Flutter 101 · analyze 0 · bằng chứng `.plan/phases2/p8-result.md` + `docs commit 2775d2f`. Còn lại: `COPILOT_USERS`/`COPILOT_COMPANY` thật cho deployment + user ERPNext thứ 2 = human gate.
- [x] **[2026-09-18] Phases2 P0–P8 ĐÓNG — toàn bộ đã commit + push**: P0 `b4acdb1` · P1 `ee93f13` · P2 `33ff725` · P3 `c38e4ea` · P4 `d7e9ba9` · P5 `6318eca` · P6 `9b54d35` · P7 `3e6240a` · P8 `90401f0` · P10-slice `7cb2798`/`21d77ff`/`d6295ab`. Suite tham chiếu: **Python 62 · Node 267 · Flutter 101 · analyze 0**.
- [x] **[2026-09-18] ĐỒNG BỘ `.project/` theo `ai-rules.md` (mục 1: kiến thức tĩnh cập nhật sau phase lớn)**: `architecture.md` (sơ đồ gateway 4 route + 7 lớp an toàn của lệnh ghi + module map P0–P8 + data flow có voice/picker/proposal) · `overview.md` (tóm tắt 1 câu mới + stack `speech_to_text`/tên app "Nghiệp Vụ AI" + trạng thái rút gọn P0–P8) · `patterns.md` (+8 pattern mới: Safety Gateway single door · contract-driven authz · fail-closed refusal có copy · human-approved learning · voice là input modality · snapshot frozen at ask-time · nhiều lớp độc lập · phases2 supersede) · `integrations.md` (CI trigger paths — backend/docs không trigger; LLM Router upstreams; authz env config) · `modules/chat.md` + `modules/copilot-backend.md` (endpoints `/execute` `/cancel` `/jobs`, files mới speech_service/proposal_card/entity_picker/settings, storage settings keys) · `state-routing.md` (route `/settings` + provider map `appSettingsService`/`speechService` + ChatState/proposal/command_id) · `design-system.md` (+4 widgets, luật banner/uncertainty copy) · `openspec.md` + `README.md` (trạng thái + định hướng). KHÔNG nhân bản progress vào `.project/` (luật ai-rules §1) — chỉ pointer + kiến thức tĩnh.
- [x] **[2026-09-18] Đợt trước (cùng ngày)**: F7 policy (a) `3b41313` (bảo trì không phải một lần thử) · F7-2 submit switch `eb4ba34` (setting "Cho phép nộp phiếu thu thật", frozen vào snapshot lúc hỏi; submit lỗi giữa chừng ⇒ PARTIAL không FAILED) · golden gaps k18/m15 `c5db7cc` ("một triệu hai" = 1.200.000; "Con Linh" resolve "Linh"; Golden runner 0 miss ⇒ **gate P9 mở**) · docs `46faf37` (result54 evidence). Chi tiết `result53.txt` + `result54.txt`.
- [x] **[2026-09-18] LESSONS_LEARNED.md + skill `erpn-verify-first` cập nhật sau P8** (5 bài học mới): một khái niệm "ai" duy nhất khi thêm identity · nhánh chết = test đi nhánh anh em (falsify từng nhánh, đếm pass/fail) · mã refusal mới phải đối chiếu bảng copy UI · "đo thật" = một lần chạy suite ghi con số (33 đỏ thay cho ước lượng) · đóng phase phân quyền phải duyệt từ danh sách ROUTE (`req.method ===`) chứ không chỉ call site của hàm gate.

- [x] **[2026-09-20] B4 — PURCHASE_ORDER.CREATE → ✅ DUYỆT ĐẠT (WRITE #4)**: §0 đo thật (PO tồn tại + 5 đơn; thiếu `custom_ai_action_id` → 417; site CÓ giá mua `Standard Buying` 295.000 vs bán 305.000; **misroute đo được: `đặt mua …` và `đặt nhà cung cấp …` đều thành ĐƠN BÁN**). Đã làm: contract PO (`draft_only`, `price_side: buying`, taxonomy `PO_*`) + stub `purchase_receipt.create` + nhóm `purchase_order_write` đứng TRƯỚC nhóm bán (deny-list `mua|nhập|nhận` thêm vào nhóm bán) + `purchase-order-write.mjs` (tái dùng toolkit SO; giá MUA; `schedule_date` schema-derived) + gateway executor + `PO_DUPLICATE_ACTION→409` ngay từ đầu + party-theo-route (`resolveSupplier`, một tên trùng 2 danh sách không chảy nhầm) + Flutter card + golden 13 ca mới (8 PO, 3 PR-stub, 2 âm). **3 bug tự mắc tự sửa** (golden r32; notIf dead-code khi thiếu startsWith — dead code NGAY TRONG lần thêm của mình; harness che guard giá). Bằng chứng: Node **404/402** (2 fail dsh-env từ B0) · B4 **20/20** · golden **7/7** zero-regress (231 ca) · Python **62 OK** · analyze **0** · card Flutter **41 pass** (P2b cũ) · **falsify 9/9** đỏ đúng, restore byte-identical. Chi tiết + gap: `.plan/phases3/B4-result.md`; migration 4 doctype (G1) **chờ lệnh riêng của user** (duyệt B4 kèm quyết định: chưa chạy, script giữ --dry-run). Hàng chờ commit: B1 → B2 → B3 → B4.

- [x] **[2026-09-20] C0 — OCR FOUNDATION (Trụ C, input layer) → ✅ ĐÃ DUYỆT** (user: "Đạt — duyệt" + "Chuẩn bị C1 — hỏi duyệt plugin native trước"): user chốt qua ask_user **server-side qua LLM router** (mac-custom self-host 0 quota; KHÔNG thêm plugin native). Đã làm: `src/ocr/ocr-provider.mjs` (interface `OcrProvider` + `assertOcrResult` + `assertOcrInput` (allowlist mime + trần 8MB) + `prepareOcrText` wrap untrusted TRƯỚC classifier/LLM + `getOcrProvider` env-switch: thiếu→mock, sai→hard error + `ocrLogLine` metadata-only) · `providers/mock-ocr.mjs` (CI, deterministic) · `providers/router-vision.mjs` (MVP: ảnh → OpenAI-shaped vision → router; fail-closed, không log ảnh) · `ocr_policy` khai trong contract + `validateOcrPolicy` fail-closed + accessor `ocrPolicy()` · `test/c0-ocr-foundation.test.mjs` **15/15** · falsify harness **TRONG REPO** `scripts/falsify/c0-ocr.mjs` **9/9 RED** (bài học từ review: harness ở /tmp đã mục nát). **2 lỗi tự tìm ở bước review tự sửa**: mime chưa allowlist, thiếu trần kích thước ảnh. Số chốt: Node **419/417** (2 fail dsh-env từ B0) · Python **62 OK** · Flutter **không đụng**. Chi tiết + giới hạn đo được: `.plan/phases3/C0-result.md`.
- [x] **[2026-09-20] C1 — CAMERA UI + FALLBACK (Trụ C) → ✅ ĐÃ DUYỆT** (user: "Đạt — duyệt" + "Chuẩn bị C2"): (vùng an toàn Ponytail: ảnh = dữ liệu không tin cậy + khó tái tạo để test)**: user chốt qua ask_user **`image_picker` (^1.2.3)** + **`POST /ocr` riêng**. §0 đo trước: `MAX_BODY = 1 000 000` (⇒ ảnh 3MB base64 ~4MB KHÔNG gửi được), chưa có route cho ảnh, client chỉ có `dio.post` JSON ⇒ không nới trần ẩu, **nén trên máy** (1600px/q80, retry 1024px/q60, trần 700KB) + mime lấy từ plugin → magic bytes → tên file, chỉ trong allowlist. **Server**: route `/ocr` (đi qua authorize sẵn có; bucket `read`; thứ tự throttle → **validate input TẠI ROUTE** → provider (fail loud) → seam C0) trả `{status,text,usable,confidence,min_confidence,instruction_pattern_found,provider,model,mock}` — **KHÔNG** trả `wrapped`; mock là provider mặc định ⇒ cờ `mock:true` để client/C2 biết đó là fixture; log 1 dòng metadata (không ảnh, không chữ). **App**: `data/ocr_models.dart` (parse **fail-closed**: `usable` chỉ true khi status==OK **và** cờ server đồng ý; status lạ ⇒ UNKNOWN ⇒ không dùng được dù payload nói `usable:true`) · `data/photo_picker_service.dart` (`PhotoPicker` interface + `SystemPhotoPicker`) · `CopilotApiClient.ocr()` + `AppConstants.ocrTimeout` 30s · `ocr_sheet.dart` (chữ **sửa được** + dòng tin cậy "Độ tin cậy 93% · tối thiểu 60% · nguồn" + cảnh báo chỉ-dẫn-bị-lọc + nhãn "bản đọc THỬ"; `LOW_CONFIDENCE/NO_TEXT/status lạ` ⇒ **chỉ còn [Nhập tay từ ảnh]**) · `chat_screen.dart` icon camera cạnh mic + `_openCamera()` (**không auto-send** kể cả khi `voiceAutoSend` bật — đó là setting dictation, có test riêng; không đường nào tới `/execute`). **Bằng chứng**: Node **425 (423 pass, 2 fail dsh-env từ B0)** (+6) · Flutter test **201 pass/1 fail** (P2b cũ = G3 B0) (+13) · analyze **0** · Python **62 OK** · **falsify C1 9/9 RED** — harness TRONG REPO `scripts/falsify/c1-ocr.mjs`, lần đầu **bắc qua 2 runtime** (5 guard Node + 4 guard Dart `flutter test`), restore byte-identical, không sót `.falsify-bak`. **5 lỗi tự mắc tự sửa** (constructor `_Recorder` không gán field ⇒ ca test im lặng chạy dữ liệu mặc định; `textContaining` phân biệt hoa/thường; kỳ vọng JSON 400 trong khi `MAX_BODY` reset socket — hành vi có sẵn mọi route; **lỗ thiết kế**: validate chỉ trong provider ⇒ mock trả "hóa đơn bịa" cho request `{}` ⇒ dời lên route + test; thiếu hằng timeout). OCR CLI không có trong env ⇒ review thủ công (và bước đó tìm ra lỗ thiết kế). Chi tiết: `.plan/phases3/C1-result.md`.

## Việc kế tiếp khi user sẵn sàng (thứ tự)

0. **C2 (ảnh → proposal)** — user đã chốt "Chuẩn bị C2": C2 mới là nơi chứng từ nhận dạng được đi vào CÙNG Safety Gateway (C1 dừng đúng ở "chữ → ô chat"); **vẫn phải hỏi duyệt trước khi tự động hoá bước nào trong đường ghi**.
1. **P9 (skill mới hàng loạt)** — gate ĐÃ MỞ (Golden 0 miss, P0–P3 acceptance đạt); CHỜ LỆNH user, không tự bắt đầu.
2. Human gates: `COPILOT_USERS`/`COPILOT_COMPANY` thật cho deployment + user ERPNext thứ 2 (P8) · build APK CI + smoke mic máy thật (P6) · smoke 2 bước trong `p8-result.md`.
3. Còn hoãn: saga §7 (chờ duyệt riêng) · P10 full (DR drill, dashboard, load test, rate-limit store phân tán) · thu audio → Phase 4 cũ (đã supersede bởi P6) · thought_signature live verify (cơ hội khi Gemini còn quota).
4. Sau khi APK chạy thật OK: sync + archive OpenSpec change `flutter-chat-mvp`, merge branch về master.
