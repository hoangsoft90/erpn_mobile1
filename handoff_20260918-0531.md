# Handoff — 2026-09-18 05:31 UTC (Phiên: đồng bộ docs sau vòng 3 + human.md + đổi tên app + push build APK)

> Đọc thêm: `result52.txt` (bằng chứng phiên này), `result51.txt` (P10 slice), `result50.txt` (P7),
> `next.md`, `checklist.md`, `working.md`, `human.md`, `handoff_20260917-result43.md` (phiên trước).

## 1. Mốc hiện tại

- **HEAD trước phiên = `d6295ab`** (`change/flutter-chat-mvp`, đồng bộ origin). Lõi **phases2 ĐÃ ĐÓNG**:
  P0 `b4acdb1` · P1 `ee93f13` · P2 `33ff725` · P3 `c38e4ea` (+`451cd8f`) · P4 `d7e9ba9` · P5 `6318eca`
  · P7 `3e6240a` · P10-slice `7cb2798`/`21d77ff`/`d6295ab`.
- **Suite chạy lại thật trong phiên này**: Python **60/60** · Node **241/241** · Flutter **69/69** · analyze **0**.
- **Phiên này làm**: (a) đồng bộ docs root + openspec với trạng thái thật; (b) tạo **`human.md`**;
  (c) **đổi tên app trên máy thành "Nghiệp Vụ AI"**; (d) commit + push để CI build APK.
- **KHÔNG đụng**: vùng tiền, ERPNext thật, idempotency-store, router/LLM config.

## 2. Việc phiên này đã làm (bằng chứng ở `result52.txt`)

1. **Quét marker trạng thái** (luật đã có trong skill nhưng chưa từng chạy) → tìm **4 marker mục nát**
   ở section phase CŨ: `next.md` P1 (đã `ee93f13`), `features.md` P3 (đã `c38e4ea`),
   `checklist.md` + `next.md` result43 (đã `c401b0f`/`f28869a`) ⇒ đã sửa hết.
2. **Đồng bộ docs**: `checklist.md` (P10 đủ 3 hash) · `next.md` (khối "Phases2 lõi đã ĐÓNG" + ô chờ
   quyết F7 + bước tiếp P8/P9) · `features.md` (4 bullet P4/P5/P7/P10-slice + trạng thái tổng mới) ·
   `openspec/.../tasks.md` §6 (6.8→6.16) · `faq.md` (**§6.8 giới hạn/429 không đốt `command_id`**,
   **§6.9 xếp hàng khi ERP sập**, **§6.10 bảo trì 503 + cảnh báo F7**, §6.2 phân biệt 2 nguồn 429).
3. **`human.md` (MỚI)**: chỉ chứa việc người thật còn phải làm — quyết F7 (a/b) · quyết SUBMIT phiếu thu ·
   thu 150 câu audio · test APK tại điểm bán · bật tunnel khi test · diễn tập kill-switch thật ·
   dán 3 giá trị GitHub Settings (không bắt buộc). Đã **xoá** mục "rotate key ERPNext" (đã xong).
4. **Đổi tên app**: `android:label` + `MaterialApp.title` + AppBar → **"Nghiệp Vụ AI"**
   (verify: `grep android:label` = `Nghiệp Vụ AI`; analyze 0; 69/69 test).
5. **Push để CI build APK**: workflow chỉ trigger khi có diff trong `apps/mobile/**` — đợt này có
   đúng diff đó (đổi tên app), nên push sẽ chạy build. Xem link run ở mục 4.

## 3. Đang chờ quyết định (chặn việc kỹ thuật)

- **F7 (vùng tiền, agent không tự sửa)**: lệnh đang QUEUED gặp kill switch bị đánh `FAILED` sau 1 lần
  và ngừng retry dù chưa từng thử ghi. Chọn **(a)** coi bảo trì là "không phải một lần thử" (giữ job chờ)
  hoặc **(b)** giữ fail-fast nhưng đổi trạng thái `BLOCKED_MAINTENANCE`.
  Bằng chứng: `docs/kill-switch-runbook.md` §4 · `result52.txt` §2.
- **SUBMIT phiếu thu**: phiếu hiện chỉ **NHÁP** (`docstatus 0`) — công nợ chỉ giảm khi người thật submit.

## 4. Việc CHỈ người thật (chi tiết ở `human.md`)

- Thu **150 câu audio** (`docs/audio-collection-script.md`) — đang chặn **P6 (voice/STT)**.
- Test APK **tại điểm bán thật** + xem icon/tên app trên máy (`docs/device-test-checklist.md`).
- Bật tunnel khi test app: `lt -s erpn8788 --port 8788` (Mac) + ERPNext ngrok sống.
- Diễn tập **kill-switch** trên gateway thật (`docs/kill-switch-runbook.md`).
- Dán 3 giá trị GitHub Settings (không bắt buộc — nhập trong ⚙️ Settings của app vẫn dùng được).

## 5. Bước kỹ thuật tiếp theo (khi user đủ điều kiện)

- **P8** (multi-user/RBAC — cần credential ≥2 user) · **P9** (write skill mới — cần acceptance trên
  ERPNext thật) · **P10 full** (DR drill, dashboard/log query, load test, rate-limit store phân tán).
- **KHÔNG mở**: P6 (thiếu audio), saga/REVERSAL (chỉ có plan, chưa duyệt), các phase cũ 04/08/10–15.
