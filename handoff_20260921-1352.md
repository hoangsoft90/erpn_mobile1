# Handoff — 2026-09-21 13:52 — P4-6 (Hôm nay | Hôm qua + delta) · Trụ D khép

**Phiên trước:** `handoff_20260921-0955.md` (P4-4 + P4-5) · **Deliverable phiên này:** `result-p4-6.txt`

## 1. Việc đã xong trong phiên này

**P4-6** — selector ngày cho **bán/thu** + **delta tuyệt đối** (`.plan/plan4_final.md` §6, §3.4).

| | |
|---|---|
| Server | **KHÔNG đổi** — route `/read/daily-summary` đã nhận `date` từ P4-2, client cũng đã có tham số (§0 xác nhận bằng đọc code) |
| Flutter | `SegmentedButton` Hôm nay (mặc định) / Hôm qua · delta `±Xđ` + định tính, **chỉ 2 chỉ số** (`sales_invoices.amount`, `receipts.total`), **không %**, chỉ hiện khi **đủ 2 ngày + cả hai `partial=false`** · công nợ **luôn hiện tại** + note UI khi xem Hôm qua · drill truyền ngày đang xem (giữ `null` cho Hôm nay ⇒ server quyết định) · **đọc Hôm qua lười** (chỉ fetch khi user chọn) |
| Files | `daily_summary_screen.dart` · `drill_models.dart` (`DrillIntent.date`) · `drill_list_screen.dart` (truyền `intent.date` — **bug thật**, xem §3) · `daily_summary_test.dart` |
| Bằng chứng | Flutter **248/248** (+6) · analyze **0** · Node **523 (521 pass, 2 fail dsh từ B0)** — không đổi · falsify **20/20 RED, 0 PROBLEM** (+3 ca) |
| Vòng thật (read-only) | 2 ngày đều **200 / `partial=false`** · **receivables 2 ngày GIỐNG HỆT** (488.935.625 / 4.700.000) ✅ §3.4 · delta ± tuyệt đối đúng · cross-check **ALL MATCH** |

**Trụ D (drawer) giờ đã khép:** P4-3 drawer + tóm tắt → P4-4 drill → P4-5 nháp app → P4-6 hôm qua/delta.

## 2. Trạng thái commit — ĐỌC KỸ TRƯỚC KHI LÀM GÌ

- Máy này **KHÔNG có git** (`fatal: not a git repository`). Hàng chờ commit: **P4-1 → P4-6**.
- Toàn bộ hàng chờ thuộc vùng **đọc SỐ TIỀN** ⇒ **AI không tự ký duyệt**; user phải duyệt trước khi commit.
- ⚠️ **Đừng push thô** khi repo có git: history không chung gốc + bản local thiếu file ⇒ push cả cây sẽ **XOÁ file trên branch duy nhất** (phải `--force`). Đã ghi ở handoff trước, nhắc lại.
- **APK debug: user đã chốt "để sau"** — không push, không `git init` trong phiên này.

## 3. Bài học phiên này (đã ghi LESSONS + skill `erpn-verify-first` +5 hàng)

1. **Kill harness falsify giữa chừng ⇒ mutation sót trên đĩa.** Ca P4-5 (`0đ` thay vì `—`) còn nguyên sau khi tôi Ctrl-C harness ⇒ lần chạy sau đỏ vì lý do vô hình. Đã khôi phục + thêm **restore-on-`SIGINT`/`SIGTERM`** vào `scripts/falsify/lib/harness.mjs`. *Nếu không phát hiện, code đã bị phá sẽ được commit.*
2. **Harness báo `PROBLEM: no test ran` = case của mình sai cấu hình**, không phải code sai (tôi khai sai `dartSuite`). Nó **không** cho RED giả — đúng thiết kế.
3. **Test yếu vẫn PASS sau khi bỏ guard**: ca drill-theo-ngày chỉ assert "có request" ⇒ phải assert **đúng ngày cụ thể**.
4. **Thêm field vào model ≠ nối vào call site**: `DrillIntent.date` có nhưng `_load()` không truyền ⇒ tính năng im lặng chết.
5. **Probe của tôi thiếu lọc company (lần thứ 3 trong dự án)** ⇒ lệch 5.000.000.000 vs 320.000; route đúng, probe sai (10 phiếu thu của `DEMO POC CO`, site có 3 công ty). **Nghi mình trước khi nghi server.**
6. **Bug thật về UX do test bắt**: màn re-mount vào cache tươi ⇒ `_yesterday` không bao giờ được nạp ⇒ delta biến mất im lặng. Sửa bằng đọc lười (đồng thời giữ 1 lượt ERP/lần mở).

## 4. Việc tiếp theo (đề xuất cho phiên sau)

| Ưu tiên | Việc | Ghi chú |
|---|---|---|
| 1 | **Commit hàng chờ P4-1→P4-6** khi máy có git | Cần **user duyệt** (vùng đọc SỐ TIỀN). Xem cảnh báo §2 về push |
| 2 | **B2/B3/B4 idempotency trên site THẬT** | `custom_ai_action_id` đã tạo xong 4/4 doctype ⇒ giờ mới chứng minh được thật; **cần user cho phép chạy ghi thật** — hiện vẫn chỉ đứng với mock |
| 3 | **APK debug** | User đã chốt "để sau"; checkout thiếu `.github/workflows`/Android SDK, `/home` ~1.5G |
| 4 | Trụ D còn gì? | Kiểm `.plan/plan4_final.md` xem còn mục nào chưa làm (P4-0→P4-6 đã xong) |

## 5. Cách chạy lại kiểm chứng

```bash
# Node (BẮT BUỘC qua npm test — có đặt COPILOT_MOCK_OK)
cd mcp-erpnext && npm test            # 523 · 521 pass · 2 fail (dsh từ B0)

# Flutter
cd apps/mobile && flutter analyze && flutter test    # 0 · 248/248

# Falsify (P4-4/5/6 dùng chung harness lib)
cd mcp-erpnext && node scripts/falsify/p44-read-drill.mjs   # 20/20 RED, 0 PROBLEM
# ⚠️ KHÔNG Ctrl-C giữa chừng (dù giờ đã có signal restore — vẫn nên để chạy hết)

# Python  (KHÔNG dùng pytest — máy không cài: "No module named pytest")
PYTHONPATH=src python3 -m unittest discover -s tests -q    # 62 OK
```

**Nguồn sự thật:** `next.md` + `checklist.md` · kiến trúc `.project/README.md` · bài học `LESSONS_LEARNED.md` · gotcha lặp lại `.agents/skills/erpn-verify-first/SKILL.md`.

> ⚠️ **Đính chính (P4-7):** lệnh Python ở trên bản đầu ghi `python3 -m pytest` — **sai**, máy không cài pytest.
> Lệnh đúng là `PYTHONPATH=src python3 -m unittest discover -s tests` (đã sửa tại chỗ). Xem P4-7 trong
> `LESSONS_LEARNED.md`.
