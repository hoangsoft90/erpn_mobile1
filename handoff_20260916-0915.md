# Handoff — 2026-09-16 09:15 (Phiên: result31 — commit gộp + nối router + cancel + xoá PE demo)

> Đọc thêm: `result31.txt` (bằng chứng đầy đủ), `next.md`, `checklist.md`,
> `handoff_20260916-0725.md` (phiên trước).

## 1. Mốc hiện tại

- **Commit `eea0411` ĐÃ PUSH** (`change/flutter-chat-mvp`): 37 files +3794/−124 —
  Stage B + fix money/identity + Phase 9 safety + docs result24-30 + faq +
  LESSONS_LEARNED + openspec. Message đúng dạng user duyệt (feat: phase 7/9
  payment write — …), body liệt kê 3 phần + tham chiếu result25-29.
- **5 việc đã duyệt — XONG HẾT** (result30 §7 → result31):
  1. ✅ Gộp 1 commit (user quyết thay tách 3) + push.
  2. ✅ **Nối payment_write vào `routeIntent()`** — điểm kỹ thuật cốt lõi của
     phiên. Cơ chế thật (verify bằng probe, không đoán): synonym mapper Phase 1
     biến "thu tiền/trả tiền/thanh toán" → **"payment" ở ĐẦU câu**, nên nhóm
     `payment_write` đặt ĐẦU ROUTES với anchor **`startsWith`** (so đầu câu).
     Bẫy đã gặp: so substring sẽ nuốt câu ĐỌC chứa "đã/chưa thanh toán" (synonym
     hoá thành "đã payment") — 2 test đọc từng FAIL trước khi thêm anchor.
     E2E thật "thu tiền cho chị Lan 500 ngàn" → proposal `create_payment_entry`
     /HIGH (amount 500.000 từ NLP, invoice nợ cũ nhất, có `created_at`); thiếu
     số tiền ⇒ đề xuất THU HẾT nợ (vẫn HIGH — không ghi ngầm). Widget test parse
     JSON verbatim từ E2E ⇒ **nút [Xác nhận] hiện thật trên Flutter**. Sửa chữ
     cũ "Phase 2 chỉ đọc" (grep 0 hit).
  3. ✅ **Route `/execute/cancel`** (giải zombie PENDING result29 §10-F2):
     `store.cancel()` chỉ từ PENDING; COMPLETED ⇒ 409 + result; PENDING ⇒
     `reconcilePaymentEntry()` trước — 0 chứng từ mới CANCELLED, thấy chứng từ
     ⇒ 409 + `erpnext_doc` (kèm test retry /execute cùng id → replay); thiếu
     reference ⇒ 409; ERPNext chết lúc đối soát ⇒ 503.
  4. ✅ **Xoá 2 PE demo `ACC-PAY-2026-00114/00115`**: đọc source trước → tool
     thật `erpnext_doc_delete` (draft docstatus 0, không cần cancel). Đường xoá
     KHÔNG được mở vào code sản phẩm — gọi qua JSON-RPC thô MỘT LẦN theo lệnh
     user. Verify độc lập: 2 PE GONE + hóa đơn gốc `ACC-SINV-2026-00047`
     outstanding 457.875 Unpaid GIỐNG HẾT trước xoá.
  5. ✅ faq.md đầu-file + §3.2/§3.3/§9/§8 khớp hành vi mới.
- **Suite: Python 60/60 · Node 118/118 · Flutter 29/29 · analyze 0.** Falsify
  từng fix: gỡ anchor → 15/16 FAIL; disable nhánh chặn cancel → 13/14 FAIL;
  phá `confirmable` → widget FAIL. Khôi phục đủ xanh, grep 0 mã tạm.
- Test bug thật trong phiên (2 lỗi test của tôi, không phải production):
  assertion `executable:true` sai thiết kế (HIGH cần confirm); fingerprint seed
  phải là `fingerprintProposal(PROPOSAL)` thật khi sau đó retry /execute;
  MOCK_ERP_FAIL_LIST phải delete env trước case sau.

## 2. CHỜ USER DUYỆT COMMIT (vùng tiền — KHÔNG tự commit)

9 file modified: `router.mjs` · `copilot-server.mjs` · `http-ask.mjs` ·
`idempotency.mjs` · `faq.md` · `router.test.mjs` · `copilot.test.mjs` ·
`http-execute.test.mjs` · `proposal_card_test.dart` (+ docs root nếu gộp chung).
Message đề xuất (result31 §7):
```
feat: phase 7b wire — "thu tiền..." builds a HIGH proposal; /execute/cancel
(reconcile-verified) for zombie PENDING intents
```
Không commit: `.gemini/` · `.opencode/` · `initp` (rác) · `.env` ·
`idempotency-store/` · `.plan/`.

## 3. Việc kế tiếp kỹ thuật (khi được giao)

- Flutter: card hiển thị lý do `PROPOSAL_STALE` chi tiết (problems[]) — nâng cao.
- Saga/undo-compensation Phase 9 nâng cao (undo KHÔNG phải `delete document`).
- `E2E_LLM_MODEL=real-gemini` — verify thought_signature live (thử cơ hội,
  không probe trước, chỉ báo khi kết quả khác 429/503).

## 4. Vẫn treo — KHÔNG phải việc agent

SUBMIT phiếu thu (user quyết riêng) · rotate key ERPNext · dán 3 giá trị GitHub
Settings · chọn endpoint lâu dài · thu audio 150 câu (`docs/audio-collection-script.md`)
· test APK thiết bị thật · review ai-rules.md.

## 5. Nguyên tắc giữ nguyên

Verification-before-completion (lệnh + output thật, đối chiếu audit quy UTC) ·
falsify từng fix bằng gỡ thật → FAIL đúng chỗ → khôi phục (bài học result27/29) ·
vùng tiền không tự commit/tự ký duyệt · đọc source trước khi đoán tool/API ·
không báo "xong" bằng lời.
