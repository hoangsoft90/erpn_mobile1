# Trạng thái change `flutter-chat-mvp` — 2026-09-21 (P4-7 review update)

> Change này chưa được archive vì 2 task còn mở là **việc của USER ngoài code**,
> không phải việc agent có thể tự làm. Ghi rõ tại đây để phiên sau không mở lại
> theo cách sai.

## Chưa xong (2 task — CHỜ USER)

- **3.4** — user dán `COPILOT_BASE_URL` / `COPILOT_AUTH_USER` (Variables) +
  `COPILOT_AUTH_PASSWORD` (Secret) vào GitHub Settings. Agent không thể tự làm:
  cần quyền admin repo GitHub + credentials thật (AGENTS.md: không tự cài/tự cấu
  hình dịch vụ; secret không đi qua chat).
- **4.3** — `/opsx:verify` → sync/archive khi APK chạy OK trên thiết bị thật.
  Điều kiện tiên quyết: endpoint cho thiết bị thật (Tailscale/mở port) + 3 giá
  trị GitHub ở 3.4 + cài thử APK. Đã bị chặn lần đo gần nhất bởi APK: user chốt
  **"để sau"** (handoff 2026-09-21) — checkout thiếu `.github/workflows`,
  thiếu Android SDK, `/home` ~1.5G.

## Ghi chú phạm vi

- plan4 (Trụ D — drawer `Tóm tắt ngày`) **không thuộc change này**: nó được kế
  hoạch riêng trong `.plan/plan4_final.md` và đã **khép** (`result-p4-done.txt`,
  12/12 case §7 có test).
- Archive chỉ nên diễn ra sau khi 3.4 + 4.3 có dữ kiện thật (CI xanh trên remote
  + APK chạy được trên máy thật) — không archive để "dọn file".
