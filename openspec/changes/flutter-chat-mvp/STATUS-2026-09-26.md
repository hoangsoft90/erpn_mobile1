# STATUS 2026-09-26 — change `flutter-chat-mvp`

> Cập nhật trạng thái change đang mở. **KHÔNG archive** chỉ để "dọn file": change này
> vẫn còn task **CHỜ USER** (bên dưới), nên phải nằm trong `openspec/changes/`.
> Bằng chứng chi tiết đợt này: `.plan/next5/*-result.md` (gitignored) + `result74.txt`
> (tóm tắt sống cùng repo) + `checklist.md` + `.plan/next6-audit1.md`.

## Đã xong trong đợt này

### 1. next5 — DRAWER READ ĐÓNG (D0→D5) — ✅ ĐÃ COMMIT `17d6f9b`

| Hạng mục | Trạng thái | Bằng chứng |
|---|---|---|
| 5/5 mục drawer bấm được (Công nợ · Nợ quá hạn/Top · HĐ chưa trả · Tồn kho nóng · Nháp hôm nay) | ✅ | `.plan/next5/D1-result.md` · `D1c` · `D2` · `D3` · `next5-drawer-done.md` (exit criteria §8 **10/10 ĐẠT**) |
| REAL-only + provenance dùng chung 3 màn tiền | ✅ | `.plan/next5/D0.5-result.md` (audit lần đầu FAIL → user chốt **(A) ẩn số** → sửa 11 file) |
| Footnote công nợ verbatim + số = GL raw (không trừ Draft PE) | ✅ | site có 13 nháp PE 162.092.570đ mà drawer = REST GL 461.505.625đ |
| D4 polish: refresh list ngắn · tiêu đề drill theo server · clear cache đổi server/user · badge fail-safe · timeout 7s | ✅ | `.plan/next5/D4-result.md` |
| Số đo cuối | ✅ | Node **820/818/2** (2 fail dsh từ B0) · Flutter **333/333** · analyze **0** · falsify drill **37 ca ALL RED** |
| Commit | ✅ `17d6f9b` (39 file, +4093/−169) | secret scan sạch |

### 2. P4-6 — Tóm tắt ngày "Hôm qua" hiện ĐÚNG số — ✅ ĐÃ COMMIT `ed79f77` [VÙNG SỐ TIỀN]

- Hướng user chốt **(A)**: khi xem "Hôm qua" → 2 thẻ (Hóa đơn đã xuất · Tiền khách trả)
  hiện **số của HÔM QUA**; **bỏ hẳn** dòng delta.
- `_DayNotRead`: hôm qua chưa đọc được ⇒ thẻ hiện tiêu đề + "Đang đọc…"/"Chưa đọc được…"
  + `Thử lại` — **fail-closed**, KHÔNG bao giờ rơi về số hôm nay. Drill đổi sang
  `d.meta.date` (ngày của server).
- Xoá `_deltaLine()` + keys `invoices-delta`/`receipts-delta`.
- Số đo: Flutter **333/333** · analyze **0** · falsify `p44-read-drill` **38/38 RED**
  (thay 3 ca delta bằng 3 ca guard-fix).

### 3. NEXT6 — audit phiên (Prompt-1, AUDIT-ONLY — KHÔNG sửa code)

- Deliverable: `.plan/next6-audit1.md` (277 dòng) — 11 gap ID (G1–G11) về
  session isolation: store key = `conversation_id` đơn độc (không principal) · không lock
  per-conversation · `sessionContext` global cross-user · `begin()` replay không kiểm user_id ·
  proposal không bind principal · 429 thiếu `Retry-After` · `DSH_MAX_SESSIONS` chưa dùng ·
  Flutter id không persist · thiếu endpoint clear · `/ask` log thiếu `conversation_id`.
- Baseline: `npm test` **820/818/2** · `flutter analyze` No issues · `flutter test` **333/333**.
- **Chưa tạo change `next6-session-isolation`** (đúng luật: audit-only thì chưa code ⇒ chưa cần change;
  sẽ tạo ở đầu Prompt-2 trước khi chạm source).

### 4. Bài học → skill (local-only, `.agents/` gitignored)

- `.agents/skills/erpn-verify-first/SKILL.md` (+5 dòng, file nay 519 dòng): `flutter analyze --no-pub`
  với `package_config.json` cũ ⇒ 1069 lỗi `undefined_class` giả · sửa code làm lệch anchor falsify
  ⇒ grep harness trước · đổi dữ liệu một thẻ ⇒ rà MỌI đường refresh/retry · mô tả phương án với
  scope phóng đại ⇒ phải hỏi lại · ca falsify fail-closed phải biến đổi DỮ LIỆU, không phải crash.

## Task còn lại — CHỜ USER (không phải việc agent tự làm)

1. **[USER]** **Push 2 commit** `17d6f9b` + `ed79f77` (branch `change/flutter-chat-mvp`) — chưa từng qua CI.
2. **[USER]** **Quyết M1/M2** phát hiện trong Code Review `ed79f77` (vùng SỐ TIỀN — AI không tự sửa):
   - M1: `onRefresh` (`daily_summary_screen.dart:265`) chỉ đọc lại HÔM NAY ⇒ đang xem "Hôm qua" thì
     kéo-refresh không làm mới ngày đang xem.
   - M2: `_BlockError.onRetry` (trong `_salesInvoices`/`_receipts` khi `d=_yesterday`) reload HÔM NAY,
     không phải ngày đang xem.
3. **[USER]** (còn treo từ 2026-09-22) Revoke token `ghp_…7aO` + dán token mới / dùng deploy key WRITE.
4. **[USER]** Duyệt **2 layout P5-4** + test tay Android thật (auto-send).
5. **[USER]** Duyệt commit đợt WRITE mới trước khi dùng ERPNext thật (migration `custom_ai_action_id`
   trên `Delivery Note`/`Purchase Receipt`).
6. **[USER]** Cho phép các vòng GHI THẬT còn treo (XML 2 lần P9-D) — như trước.

## Việc agent còn treo (không thuộc đợt này)

- `docs/plan4-mode-map.md` (P4-0) chưa tạo.
- B2/B3/B4 idempotency trên site ERPNext thật (cần user cho phép ghi thật).
- P4-3/P4-4/P4-5/P4-6 chưa tách `result*.txt` riêng (bằng chứng đang trong `.plan/`).
- NEXT6 gap G1–G11 chưa code (Prompt-2).
