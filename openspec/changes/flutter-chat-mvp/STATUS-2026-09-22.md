# STATUS 2026-09-22 — change `flutter-chat-mvp`

> Cập nhật trạng thái của change đang mở. **KHÔNG archive** chỉ để "dọn file": change này
> còn 2 task chờ USER (bên dưới) nên vẫn phải nằm trong `openspec/changes/`.
> Bằng chứng chi tiết: `.plan/result-p5-0.md` … `result-p5-4.md` (`.plan/` bị gitignore),
> tóm tắt sống cùng repo ở `result-p4-done.txt` (phụ lục 2026-09-22) và `checklist.md`.

## Đã xong trong đợt này (plan5 — vá 3 khe hở của `plan5_final`)

| Task | Trạng thái | Bằng chứng |
|---|---|---|
| P5-0 điều tra bug #2/#3 | ✅ | bug #2 và #3 **đã đóng ở tầng logic**; `/execute` timeout an toàn nhờ idempotency (không phải P0) |
| P5-1 | ✅ (không cần code) | tiền đề prompt sai; sửa bug thật: 5 test P4-6 hard-code ngày |
| P5-2 phân loại lỗi submit + ngày phiếu thu theo snapshot | ✅ | `submit_error_kind`; `posting_date` freeze lúc PROPOSE + `readPostingDate()` fail-closed trước `setReference()` |
| P5-3 badge NHÁP/ĐÃ NỘP + gợi ý + copy setting | ✅ | badge + `submit-hint`; glyph dòng kết quả theo cùng trạng thái; storage key không đổi |
| P5-4 voice-first (2 layout, Huỷ, cap 25s, VAD config) | ✅ | OFF giữ nguyên (26 test cũ xanh); ON có mic CTA + Huỷ; cap/VAD đọc từ `AppConstants` |
| P5-5 | ⏸ backlog | push-to-talk · noise meter · VAD theo entity · preview 500ms · draft aging |

**Bất biến còn nguyên sau mọi thay đổi:** voice chỉ là kênh nhập; tự gửi **chỉ** đi qua
`onSend()` → `/ask`; không đường nào tự xác nhận proposal hay chạm `/execute`;
`allowSubmitPayment` (TẮT mặc định) vẫn là cổng duy nhất cho submit.

## WRITE #5/#6 — P9 (`delivery.create`, `purchase_receipt.create`)

| Task | Trạng thái | Bằng chứng |
|---|---|---|
| P9-0 gate (GO/NO-GO) | ✅ **GO** (5 gap là việc A1/A2) | `.plan/result-p9-0.md` |
| P9-A1 `delivery.create` proposal path | ✅ | builder + policy + test (giữ cờ `stub` cho tới khi wire) — `.plan/result-p9-A1.md` |
| P9-A2 wire UX + `/execute` NHÁP | ✅ | route 7 lệnh / 9 câu hỏi không mở card · 1 phiếu `docstatus 0` · idempotent · **chaos retry không nhân đôi** · STALE/DRIFT ⇒ 0 phiếu | `.plan/result-p9-A2.md` |
| Review A1/A2 | ✅ **4 lỗ hổng thật đã sửa** | UOM nói-vs-dòng · dead clause · **draft-DN accumulation** (nay `openDraftCover`) · header cũ |
| P9-B `purchase_receipt.create` | ✅ | theo PO đã submit (`qty − received_qty − returned_qty`) hoặc direct supplier; route `"nhập hàng"` giữ câu HỎI về `stock.balance` — `.plan/result-p9-B.md` |
| Submit DN/PR | ❌ **không làm** (Go/No-Go riêng) | `writeDoctypes()` khai `submit:false`; executor verify đòi `docstatus === 0` |

**Số thật 2026-09-22 (cuối phiên):** Flutter **265/265** + `analyze` 0 · node `--test` **584**
(582 pass, 2 fail `dsh-gateway` **do môi trường DSH** từ B0) · `p9-delivery` **25/25** ·
`p9-receipt` **22/22** · falsify in-repo **10/10 ALIVE** + đột biến DN 4/4 · PR 6/6 RED
(restore byte-identical).

**Phát hành:** commit `53e1851` → CI `android-debug-apk` **success** (run `35688782230`,
artifact `erpn-chat-debug-apk` 81.4 MB).

## Task còn lại — CHỜ USER (không phải việc agent tự làm)

1. **[USER]** Revoke token `ghp_…7aO` (đã qua chat 2 lần + nằm trong `.chats/sess1.md`), rồi
   dán token mới vào `.env`; hoặc bật **deploy key WRITE** cho repo để bỏ hẳn PAT.
2. **[USER]** Duyệt **2 layout P5-4** (mô tả ở `.plan/result-p5-4.md` §6 — env này không có
   công cụ chụp màn hình) trước khi coi plan5 hoàn tất.
3. **[USER]** Test tay trên máy Android thật: auto-send có tự gửi không (phân biệt "chữ
   không hiện" = engine/quyền mic ≠ "chữ hiện mà không gửi" = OEM không bắn `final`).
4. **[USER]** Duyệt commit đợt 2026-09-20→22 (vùng **số lượng/kho + chứng từ mua** — AI không
   tự ký duyệt). Chưa dùng ERPNext thật cho 4 WRITE mới: cần cho phép rõ + **migration
   `custom_ai_action_id`** trên `Delivery Note`/`Purchase Receipt`.

## Việc agent còn treo (không thuộc plan5)

- `docs/plan4-mode-map.md` (P4-0) chưa tạo.
- B2/B3/B4 idempotency trên site ERPNext thật (cần user cho phép ghi thật).
- P4-3/P4-4/P4-5/P4-6 chưa tách `result*.txt` riêng (bằng chứng đang trong `.plan/`).
- Hạ tầng đã siết 2026-09-22: `.gitignore` chặn `.chats/.review/.gemini/.opencode/initp`;
  `.env` về `600`; thêm skill `erpn-deploy-github` (không chứa secret).
