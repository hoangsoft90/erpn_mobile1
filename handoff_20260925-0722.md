# handoff_20260925-0722.md — Bàn giao sau đợt next4 (A3 PDF + B + M1-site + P9-D + review A3)

## Đọc đầu phiên (theo thứ tự)

1. **`result70.txt`** — bằng chứng phiên này: số suite thật, output falsify/ smoke, 3 lỗ hổng A3 đã đo, bảng bài học → skill.
2. `checklist.md` (mục đầu file: **P9-D · A3-Flutter `.pdf` · A3 review vòng 2 · bài học skill · B-dedupe · A2-Flutter · A3 · M1-site**) — bảng ✅/❌ từng mốc.
3. `next.md` §"Trạng thái mới nhất (2026-09-25)" — 4 commit + việc sắp tới (1, 1b–1e).
4. `.plan/next4/A3-result.md` §9.1 + **§9.5** — review vòng 2 (H1/M1/M2 + 10 ca "không phải bug").
5. `.plan/next4/P9D-result.md` — P9-D, đọc §11 nếu cần vòng smoke sau commit.
6. `LESSONS_LEARNED.md` §"Đợt next4" — bài học đã lưu (kèm bảng chỉ rõ từng bài học nằm ở skill nào).
7. Nếu phải đọc code ERPNext: skill `.agents/skills/erpnext-rest-api-core` §6 (đã thêm 3 bullet đợt này).

## Đã xong trong phiên 2026-09-25

- **4 commit, CHƯA push** (branch `change/flutter-chat-mvp`): `7ca1409` (A3 PDF + B + M1-site, 28 file) ·
  `8a692f1` (Flutter kênh FILE `.xml`/`.pdf` + thẻ tạo khách + **fix 401** ở `ProposalCard`, 20 file) ·
  `47558ce` (docs + `result65/67/68/69`) · **`f82f656` (P9-D — commit RIÊNG vì vùng SỐ TIỀN, user duyệt riêng)**.
- Suit đo trong phiên: **Node 793** (791 pass, 2 fail `dsh-gateway` môi trường từ B0) · **Flutter 310/310** ·
  analyze 0 · falsify `p9c-payment-pay` **12/12 RED** (thêm 2 ca mới K/L) · subset A3+B+M1+A2 **56/56**.
- **Migration B đã VERIFY trên site thật**: `custom_business_doc_key` trên Purchase Order — idempotent, kiểm chứng
  độc lập bằng REST, probe chỉ-đọc ⇒ `field_unavailable:false` ⇒ gate đã mở (chi tiết `B-dedupe-result.md` §7).
- **P9-D smoke sau commit** (mock, hỏi 2 lần, KHÔNG submit): 2.500.000 → **1.500.000**, số nháp trong state vẫn 1.
- **Review A3 vòng 2**: H1/M1/M2 + 4 Low đã đo, **CHƯA sửa**.
- **Bài học → skill**: `erpnext-rest-api-core` §6 (+3) · `erpn-verify-first` (+5 dòng) ·
  **skill MỚI `.agents/skills/untrusted-file-parsing`**.
- Docs root đã đồng bộ: `checklist.md` · `features.md` · `next.md` · `faq.md` (§12 sửa + **§13/§14/§15 mới**) ·
  `LESSONS_LEARNED.md` · `openspec/changes/flutter-chat-mvp/tasks.md` (§6.26–6.29) · `.project/openspec.md`.

## Trạng thái một câu

**Toàn bộ đợt next4 (A3 PDF · B trùng tờ HĐ · M1-site · A2/A3 Flutter · P9-D) đã code+test+commit xong và migration
đã verify — nhưng 3 lỗ hổng A3 (H1 `invoice_no` rác, M1 trần inflate, M2 `readDate`) vẫn CHƯA sửa và đang chờ user quyết,
vì H1 chạm thẳng khoá chống trùng của B.**

## Việc phiên sau phải làm (thứ tự)

1. **HỎI/CHỜ USER: quyết A3 H1/M1/M2** (`.plan/next4/A3-result.md` §9.5 có đề xuất sửa cụ thể cho từng cái + test cần thêm).
   ĐỪNG tự sửa rồi tự commit: H1 ảnh hưởng định danh tài liệu **và** `business_doc_key` của B.
2. **UI-checkpoint** cho nút HĐĐT nhận `.pdf` (commit `8a692f1` đã vào, chưa chụp màn hình để user duyệt UI).
3. **2 vòng GHI THẬT cần user cho phép riêng** (đều chỉ tạo NHÁP, nhưng là ghi thật):
   (a) vòng XML 2 lần cùng file ⇒ kỳ vọng lần 1 = nháp, lần 2 = 409 `PO_DUPLICATE_DOC` + tên đơn cũ, đếm PO +0;
   (b) 1 nháp Payment Entry thật cho P9-D rồi hỏi lại (kỳ vọng số tụt đúng phần đã phủ + cảnh báo nêu tên nháp).
4. **Push + CI APK** khi user bảo "push" — 4 commit này chưa từng qua CI (chưa có APK mới).
5. Việc chưa làm của A3 (không chặn): falsify riêng · giải nén ObjStm (hiện từ chối theo tên) · PDF THẬT của NCC
   (fixture hiện tự dựng byte-by-byte) · kênh ẢNH chưa phát `source_document`.

## Cảnh báo cho phiên sau

- **Công cụ KHÔNG khả dụng trong phiên này**: OCR `open-code-review` · AgentMemory · `codebase-memory-mcp` ·
  `cocoindex-code` · **Simplenote MCP** (không có tool để gọi). Vì thế: (a) bước Code Review chạy dạng **fallback**
  (tự đọc diff + quét call-site), **không** có lớp phát hiện tự động NPE/XSS/SQLi; (b) **bản bài học cross-project
  chưa ghi được lên Simplenote** — nếu phiên sau có công cụ, hãy ghi bù (nội dung đã soạn sẵn trong
  `LESSONS_LEARNED.md` §"Đợt next4" nhóm C + mục "nháp `docstatus=0` không giảm GL").
- **Đừng sửa 4 test `p8`×2/`p10`×2 thành khác đi**: chúng đã được sửa một lần ĐÚNG (mỗi test dùng một khoản nợ riêng)
  khi P9-D vào; lý do đầy đủ ở comment trong test + `.plan/next4/P9D-result.md`.
- **`syncode`** (script gcloud của máy) và `checklist1.md`/`faq1.md`/`icon.png`/`query_customer.py` **cố ý KHÔNG commit** —
  đừng vô tình `git add .`.
- `working.md` mục P9-D vẫn ghi "CHỜ USER DUYỆT COMMIT" (đã lệch): P9-D **đã** commit `f82f656` — sửa khi có dịp.

## Suggested skills (cho phiên sau)

- **`erpn-verify-first`** — BẮT BUỘC trước khi viết code trong project này (đã thêm 5 dòng bẫy mới đợt này).
- **`untrusted-file-parsing`** — nếu phiên sau sửa A3 H1/M1/M2 hoặc viết bộ đọc file mới.
- **`erpnext-rest-api-core`** §6 — nếu chạm công nợ/nháp/Link field.
- **`flutter-test-discovery`** / **`dart-run-static-analysis`** — khi chạy suite Flutter (bài học: file test phải kết thúc `_test.dart`).
- **`openspec-archive-change`** / **`git-checkpoint`** — chỉ khi user yêu cầu archive/merge.
- **`handoff`**, **`simplenote-mcp`** — cần user gọi trực tiếp (agent không tự invoke được).

⚠️ **KHÔNG dùng**: `grill-me`, `grill-with-docs`, `tdd`, `diagnosing-bugs`, `to-prd`, `to-issues`, `triage` (đã bị AGENTS.md cấm).
