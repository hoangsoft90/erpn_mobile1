# Handoff — 2026-09-16 04:26 UTC (Phiên: Stage B review + fix idempotency khi khôi phục history)

> Đọc thêm: `result27.txt` (phiên này, bằng chứng đầy đủ), `result26.txt` (vòng review vùng tiền),
> `result25.txt` (ghi thật ERPNext + bug double-write), `next.md`, `checklist.md`, `working.md`.
> ⚠️ `.plan/phases/` đã lỗi thời ở nhiều chỗ (phase-03 ghi PWA; phase-05 còn scrub trong khi user
> đã ký KHÔNG scrub). Nguồn sự thật = file root. phase-09 vừa được viết lại (2026-09-16).

## 1. Mốc hiện tại

- **HEAD = `05dda38`** (đã push: `553d962` Phase 6 · `8ebfc0e` Phase 7 Stage A · `05dda38` docs).
  **Toàn bộ Stage B + các fix review CHƯA commit, CHƯA push** (vùng tiền — chờ user duyệt).
- **Phase 0–3 ✅ · Phase 5 ✅ (router + sign-off, KHÔNG scrub) · Phase 6 ✅**
- **Phase 7**: Stage A ✅ (commit `8ebfc0e`) · **Stage B kỹ thuật XONG + đã ghi THẬT** trên
  ERPNext demo (Mac qua ngrok) `ACC-PAY-2026-00114` 10.000đ — CHỜ DUYỆT COMMIT.
- Test xanh hiện tại: **Python 58/58 · Node 93/93 · Flutter 27/27 · flutter analyze 0 issue**.
- Trên ERPNext thật hiện có **2 Payment Entry NHÁP** (`ACC-PAY-2026-00114/00115`, cùng
  reference) — user sẽ tự dọn; nháp nên công nợ KHÔNG đổi.
- **Chưa có `submit`** trong scope đã ship (PE luôn tạo ở `docstatus 0`; `erpnext_doc_submit`
  bị cả cổng đọc lẫn cổng write chặn).

## 2. Phiên này đã làm gì

1. **Rà 3 điểm nghi vấn (item 3a)** — `result27.txt` §1:
   - readonly-guard vs callWriteTool: KHÔNG xung đột (create/submit/delete đều bị chặn ở đường đọc;
     write chỉ mở đúng `erpnext_doc_create` + doctype `Payment Entry`).
   - store idempotency KHÔNG ở /tmp: default `mcp-erpnext/idempotency-store` (gitignored,
     `ERPN_IDEM_DIR` không set trong `.env`).
   - 🔴 **Flutter commandId: rebuild OK, KHÔI PHỤC HISTORY KHÔNG** → đã fix (§3).
2. **Vá lỗ hổng idempotency khi khôi phục history** (money-zone): `toJson()` ghim
   `command_id`, `fromJson()` nạp lại vào `commandIdSeed`; khoá sống theo CARD qua cả restart.
3. **2 chaos test mới** cho crash-recovery (mock có fault hook
   `MOCK_ERP_FAIL_AFTER_WRITE` = "ERPNext đã ghi, response mất" và `MOCK_ERP_FAIL_LIST` =
   "ERPNext chết đúng lúc đối soát") + falsification 2 vòng.
4. **Skeleton Phase 9** (PLAN ONLY, không code): `.plan/phases/phase-09-proposal-state-machine.md`
   viết lại theo code thật (đã có gì / thiếu gì / state machine / 6 exit criteria).

## 3. Những gì KHÔNG được làm ở phiên này (giữ nguyên)

- ❌ Không tạo/submit/cancel/delete thêm document trên ERPNext thật (phiên này: 0 lệnh write).
- ❌ Không mở write skill sales/inventory (Phase 11) · ❌ không bắt đầu Phase 4 (chờ audio thật).
- ❌ Không tự thiết kế endpoint production/VPS (user đã chốt: Cloud Shell + Mac tunnel chỉ là
  workspace dev/demo; chuyển VPS làm SAU khi app xong).
- ❌ **Không tự commit vùng tiền** — Stage B + mọi fix đang chờ user duyệt.

## 4. Việc ưu tiên cho phiên sau

1. **Chờ user duyệt commit** (2 commit đề xuất ở `result27.txt` §7: (a) Stage B ghi thật,
   (b) fix khoá idempotency + chaos test). Duyệt ⇒ commit + push `change/flutter-chat-mvp`.
2. Sau khi commit: chỉ khi user yêu cầu mới chạy thêm ghi thật (mỗi lần ghi thật =
   command_id mới + verify đọc lại + đếm document theo reference).
3. Việc không chặn (khi user bảo tiếp): bắt đầu Phase 9 theo skeleton phase-09
   (expiry + re-validate + khoá theo `(customer, invoice)` + "không xoá document") — phase này
   **đổi hành vi vùng tiền** (từ "clamp im lặng" sang "lệch ⇒ 409"), nên vẫn phải chờ user duyệt.

## 5. Vẫn treo (KHÔNG phải việc agent)

- Dọn 2 phiếu nháp demo trên ERPNext + quyết có SUBMIT không.
- Rotate ERPNext key/secret · dán 3 giá trị GitHub Settings (COPILOT_BASE_URL / AUTH_USER /
  AUTH_PASSWORD) · thu audio 150 câu (`docs/audio-collection-script.md`, mở khoá Phase 4) ·
  test APK trên máy thật tại điểm bán · review `.project/ai-rules.md`.
- `thought_signature` với Gemini thật: vẫn chưa verify live (free tier cạn) — thử cơ hội,
  không probe trước, không đếm quota trước (bài học result19).

## 6. Nguyên tắc cứng (nhắc lại)

- Vùng tiền/số/phân quyền: **không tự commit, không tự ký duyệt**; AI chỉ đề xuất.
- Verification-before-completion: mọi claim cần lệnh + output THẬT (dán nguyên văn vào result).
- Mock phải TRUNG THỰC với server thật (bài học result25 §E.3: mock dedupe theo reference_no
  đã che đúng bug ghi trùng).
- Test mới ⇒ phải **falsify** (gỡ fix → test FAIL đúng chỗ); cẩn thận "falsify hụt" khi patch
  không chạm đúng code (đã xảy ra 2 lần, ghi ở `result26` §6 / `result27` §4).
- Không đoán tên tool/tham số ERPNext — đọc source trong `node_modules` trước (bài học
  result4/result21; `erpnext_create_payment_entry` KHÔNG tồn tại).
