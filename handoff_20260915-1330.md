# Handoff — 2026-09-15 13:30 (Phiên: fix credit-note + review vòng 2)

> Đọc thêm: `result21.txt` (bằng chứng đầy đủ), `next.md`, `checklist.md`,
> `handoff_20260915-0200.md` (phiên trước). Nguồn sự thật = các file root này
> (.plan/phases/ đã lỗi thời).

## 1. Mốc hiện tại

- **HEAD = `a379a71`** (đã duyệt + push): mac-custom upstream (LLM tự host trên Mac,
  không quota) làm chain dev hàng ngày + 2 config router + `dsh-e2e.patch.yml` + docs
  + `result19.txt` + `result20.txt` + `scripts/llm-router.mock.json`.
- **Đợt hiện tại CHƯA COMMIT (vùng tiền — chờ user duyệt)**: fix credit-note +
  review vòng 2. 10 file sửa + `result21.txt` mới.
- **Test xanh thật**: Python 58/58 · mcp-erpnext **50/50** (9.9s) · Flutter 14/14 ·
  openspec valid · secret scan CLEAN · `.env` vẫn ignored.

## 2. Việc đã làm phiên này

### 2.1 Fix credit-note (user đã duyệt hướng từ result20 §4)
`outstanding_amount > 0` → **`!== 0`** ở `mcp-erpnext/src/skills/customer.mjs`
(helper `listUnpaidInvoices`, dùng chung cho cả `getCustomerBalance` lẫn sales) và
`mcp-erpnext/src/skills/sales.mjs`. Mock thêm credit note `SINV-0004` (−320.000) để
có test hồi quy (`copilot.test.mjs`: CUST-00002 = 7.180.000đ/2).

Nhãn trung tính (user cho phép): "hóa đơn chưa trả" → "**chứng từ** chưa thanh toán",
thêm nhánh "hiện dư X …" khi `outstanding_vnd < 0`; nhánh liệt kê in số có dấu.

Kỳ vọng chuỗi ở 2 test Dart + `http-ask.test.mjs` cập nhật theo.

### 2.2 Verify THẬT (điểm quan trọng nhất)
Probe gọi thẳng `answerQuestion()` với **ERPNext thật** + NLP service thật, **không LLM**
(số tiền không phụ thuộc LLM):
```
Q: Khách làm tròn 2026-09-15-p1b-wf1-2 còn nợ bao nhiêu
   → ... còn nợ 457.875đ (1 chứng từ chưa thanh toán).      ✓ ground truth
Q: Khách smoke 2026-09-15-p1b-wf1-2 còn nợ bao nhiêu
   → ... còn nợ 171.800đ (4 chứng từ chưa thanh toán).      ✓ ground truth
                                          (trước fix: 269.000đ / 3)
```

### 2.3 Review vòng 2 — 3 lỗi THẬT (result21 §6)
- **(A) `node --test` chạy cả batch runner**: nó discover MỌI file trong `test/` ⇒
  `batch-accuracy.mjs` (18 câu) + `batch-groundtruth.mjs` (dump khách/hóa đơn THẬT)
  chạy như unit test. Trong shell đã `source .env` → bắn vào **ERPNext THẬT** + in tên
  khách/số tiền thật ra stdout. Fix: guard theo `process.env.NODE_TEST_CONTEXT`
  (**probe xác định: chạy trực tiếp = unset · dưới runner = `child-v8`**) + dòng skip
  in rõ. ⚠️ Guard ban đầu chỉ dùng `argv[1]` **SAI** — runner spawn từng file thành child
  nên `argv[1]` chính là file đó.
- **(B) `http-ask.test.mjs` treo cả suite >120s**: shell leak `ASK_USER/ASK_PASSWORD`
  (test chỉ strip `ERPNEXT_*`) → `resolveBindPolicy` throw trong **SETUP** → `try/finally`
  chưa vào nên child Python rò → suite không exit. Fix: strip cả `ASK_*` (hermetic loop +
  env của CLI spawn test) và đưa mọi setup vào trong `try`, finally `server?.close()`.
- **(C) Kỳ vọng eval cũ thành fail GIẢ**: 5 assertion `"(N hóa đơn"` không thể khớp sau khi
  đổi nhãn → tưởng accuracy tụt. Fix: `const DOC = /chứng từ|hóa đơn/` và chấm theo
  **số tiền + số lượng**, không chấm theo danh từ.

## 3. Đang chặn / chờ người thật

- ⚠️ **Tunnel Mac `llm9000.loca.lt` ĐÃ TẮT** — 3 lần probe đều **HTTP 503 "Tunnel
  Unavailable"** ⇒ E2E đầy đủ qua `mac-custom` chưa chạy được phiên này. Cần user bật lại
  `lt` trên Mac rồi chạy lại chuỗi dsh → router → mac-custom → MCP → NLP → ERPNext thật.
- **`thought_signature` vẫn CHƯA verify live** (từ result17/18/19) — chỉ có bằng chứng
  hermetic (router 19/19). Cần 1 session dsh thật.
- Treo khác: rotate key ERPNext · dán 3 giá trị GitHub Settings · chọn endpoint lâu dài ·
  thu audio 150 câu (`docs/audio-collection-script.md`) · test APK thiết bị thật ·
  review `.project/ai-rules.md` · Zen (để sau, giữ billing-blocked).

## 4. Việc tiếp theo (theo thứ tự)

1. **Chờ user duyệt commit đợt này** (vùng tiền — không tự commit). Đề xuất message:
   `fix: count credit notes in the receivable balance (net, not gross); make the unit
   suite hermetic and stop the test runner executing the real-ERPNext batch`
   → commit + push `change/flutter-chat-mvp`.
2. Khi user bật `lt`: chạy E2E đầy đủ qua `mac-custom`; nếu thuận tiện thử
   `E2E_LLM_MODEL=real-gemini` để verify `thought_signature` live (đúng 1 lần/ngày quota,
   không probe trước — luật result19).
3. Việc không bị chặn: giữ docs nhất quán; `.agents/skills/*` vẫn **gitignored** (chỉ sống
   trên VPS) — muốn theo repo thì user quyết (copy sang `docs/` hoặc bỏ ignore).

## 5. Nguyên tắc còn hiệu lực

- **Vùng tiền/số/phân quyền: KHÔNG tự commit, KHÔNG tự ký duyệt** — chờ user.
- **Verification-before-completion**: mọi claim cần lệnh + output thật; unit xanh ≠ chạy thật.
- **Không báo "xong" bằng lời.** Ponytail TẮT cho code chạm tiền/phân quyền.
- Sau mỗi phase lớn: cập nhật `resultNN.txt` + `checklist.md` + `features.md` + `next.md` +
  handoff mới; `working.md` theo dõi trạng thái.
