# Handoff — 2026-09-15 13:30 (cập nhật sau result22) — Phiên: fix credit-note → E2E xanh

> Đọc thêm: `result22.txt` (mới nhất — E2E mac-custom + review docs), `result21.txt`
> (fix credit-note + review vòng 2), `next.md`, `checklist.md`, `working.md`.
> Nguồn sự thật = các file root này (`.plan/phases/` đã lỗi thời: phase-03 còn PWA,
> phase-05 còn scrub trong khi user đã ký KHÔNG scrub).

## 1. Mốc hiện tại

- **HEAD = `6054458`** (đã duyệt + push): *fix: count credit notes in the receivable
  balance (net, not gross)* — 18 files. Trước đó: `a379a71` (mac-custom upstream),
  `8d9f04c` (mock E2E config), `be57052` (thought_signature gateway).
- **E2E đầy đủ qua `mac-custom` ĐÃ XANH** (result22 §4): dsh exit 0, trả
  **457.875đ / 1 chứng từ** và **171.800đ / 4 chứng từ** — khớp ground truth độc lập.
  Audit `audit-2026-09-15T13-46-29.jsonl`: **4/4 request 200, attempts=['mac-custom']**,
  `messages` 2 → 5 → 7 → 9 (⇒ ≥2 turn mang tool result; SỐ tool-call chính xác không
  chốt được — xem "mức bằng chứng" trong result22 §4).
- **Test xanh thật**: Python 58/58 · mcp-erpnext **50/50** (9.9s) · Flutter 14/14 ·
  openspec valid · secret scan CLEAN · `.env` vẫn ignored.
- **Đợt chưa commit**: docs của result22 (`result22.txt` mới + `checklist.md` +
  `next.md` + `working.md` + chính file handoff này) — **chờ user duyệt**.

## 2. Việc đã xong trong phiên

### 2.1 Fix credit-note (đã commit `6054458` — vùng tiền, user đã duyệt)
`outstanding_amount > 0` → **`!== 0`** ở `mcp-erpnext/src/skills/customer.mjs`
(helper `listUnpaidInvoices`, dùng chung cho `getCustomerBalance` + sales) và
`sales.mjs`. Mock thêm credit note `SINV-0004` (−320.000) → test hồi quy
(`copilot.test.mjs`: CUST-00002 = 7.180.000đ/2). Nhãn câu trả lời đổi thành
"**chứng từ** chưa thanh toán" + nhánh "hiện dư X" khi tổng âm.

### 2.2 E2E mac-custom xanh (result22) + real-gemini vẫn chặn
- Lần chạy đầu fail `408 (no body)` → đã **loại trừ "endpoint yếu" bằng probe 0-session**
  (payload 11.6KB → 200; có `tools` → 200; có `tools`+stream → 200/43 SSE chunk) ⇒
  flaky tunnel, chạy lại xanh.
- `E2E_LLM_MODEL=real-gemini` (1 lần, không probe trước): 502 `llm_router_all_failed
  (tried: gemini-openai)`; provider thật **5×429 + 2×503** ⇒ trần ngày free tier cạn.
  ⇒ **`thought_signature` VẪN CHƯA verify live** (lần thứ 4); bằng chứng duy nhất vẫn là
  hermetic (router 19/19).

### 2.3 Review docs đợt result22 — 3 lỗi thật của chính tôi (result22 §8)
- **(A) Overclaim** "2 tool-call, 2 turn replay" → chỉ chứng minh được "≥2 turn mang tool
  result"; transcript headless KHÔNG in khối tool-result. Đã sửa ở 4 file.
- **(B) Lệnh review của tôi sai**: `ls -t … | head -1` lấy file audit **mới nhất** (file
  gemini 7×502) thay vì file của phiên mac-custom → suýt tự bác bỏ claim ĐÚNG. Từ nay
  tham chiếu audit **bằng tên file**.
- **(C) 7 dòng trạng thái commit lỗi thời** trong working/checklist/next (result17→21 vẫn
  ghi "CHƯA commit/chờ duyệt" dù đã commit) — lỗi **lặp lại lần 2** ⇒ đã thành checklist
  trong skill: commit xong phải `grep "CHƯA commit|chờ duyệt"` trong CÙNG lượt.

## 3. Chờ người thật (agent KHÔNG làm được)

- **Dán 3 giá trị vào GitHub Settings** (token hiện tại chỉ-đọc, PUT 404): Variables
  `COPILOT_BASE_URL` + `COPILOT_AUTH_USER`; Secret `COPILOT_AUTH_PASSWORD`
  (= `ASK_USER`/`ASK_PASSWORD` trong `.env` — chỉ đọc tên, không in giá trị).
- **Chọn endpoint LÂU DÀI**: Tailscale (khuyến nghị) / hosting mở port 8788 / giữ tunnel.
  Tunnel `erpn8788.loca.lt` chỉ sống khi `lt` chạy trên Mac.
- **Rotate key ERPNext** (Hoàng làm trực tiếp trên server) → cập nhật `.env` qua SSH.
- **Thu audio 150 câu 3 miền** — kịch bản sẵn: `docs/audio-collection-script.md`
  (blocker Phase 4).
- Test APK trên thiết bị thật tại điểm bán · review `.project/ai-rules.md` ·
  quyết định `.agents/skills/*` có vào repo không (hiện **gitignored**, chỉ sống trên VPS).
- Zen: để sau, giữ billing-blocked (không xóa config).

## 4. Việc tiếp theo

1. **Chờ duyệt commit docs đợt result22** (không phải vùng tiền — code sản phẩm KHÔNG đổi
   trong đợt này). Đề xuất:
   `docs: record mac-custom E2E green (4/4 provider requests 200) and the real-gemini quota block (result22)`
2. **Khi quota Gemini reset (≥ 2026-09-16 07:00 UTC = 14:00 VN)**: chạy **đúng 1 session**
   `E2E_LLM_MODEL=real-gemini` để verify `thought_signature` live — **không probe trước**
   (luật result19: ① đếm request GỬI TỚI PROVIDER, không đếm 200; ② ngân sách 20/ngày ≈
   1 session dsh; ③ 429 đầu tiên = hết ngày, dừng mọi request; ④ việc verify quan trọng
   chạy vào request ĐẦU TIÊN của ngày quota).
3. Phase 4 (STT) vẫn bị chặn bởi audio; Phase 6+ chưa bắt đầu; tuyệt đối KHÔNG đụng
   `create_payment_entry`/write skill (Phase 7).

## 5. Nguyên tắc còn hiệu lực

- **Vùng tiền/số/phân quyền: KHÔNG tự commit, KHÔNG tự ký duyệt** — chờ user.
- **Verification-before-completion**: mọi claim cần lệnh + output thật; unit xanh ≠ chạy thật;
  câu claim phải đúng **mức bằng chứng** cho phép.
- **Không báo "xong" bằng lời.** Ponytail TẮT cho code chạm tiền/phân quyền.
- Sau thay đổi lớn: cập nhật result/checklist/features/next/working + handoff mới,
  và quét lại marker "CHƯA commit/chờ duyệt" trong cùng lượt.
