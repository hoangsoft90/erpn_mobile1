# handoff — 2026-09-21 09:55 · phiên P4-4 (drill-down) + review vòng 2 + P4-5 (nháp app)

Phiên này nối tiếp P4-3 (`handoff_20260921-0810.md`) trong cùng ngày. Deliverable:
**`result-p4-4.txt`** + **`result-p4-5.txt`**. Trạng thái: **kỹ thuật xong, chờ user duyệt commit**
(vùng đọc SỐ TIỀN ⇒ AI không tự ký duyệt). Hàng chờ commit nay: **P4-1 → P4-2 → P4-3 → P4-4 → P4-5**
(máy vẫn không có git).

> **Cập nhật cuối phiên (P4-5 + review vòng 2)**: (1) review P4-4 bắt **2 bug thật** — `clampUiLimit`
> `Number(null)=0` ⇒ absent limit bị clamp về min thay vì default; nháp app **không lọc company** cả
> block lẫn drill — đã sửa + test + falsify **17/17 RED**; (2) P4-5 — drill nháp hỏi `grand_total` cho
> cả 4 doctype ⇒ **vòng thật bộc lộ 503** (PE không có field đó — đo DocField meta) ⇒ field list **theo
> doctype** (`paid_amount` cho PE) + row COPY giá trị thật (không 0 bịa) + Flutter row nháp hiện "—" +
> **CẤM ghi/xoá/submit** được assert; (3) vòng thật: drill nháp **200, drill=block=0**, ALL MATCH;
> (4) docs ghi sai tên helper `resolveCompanyServerSide` đã sửa thành `resolveReadCompany`.

## 1. Trạng thái máy (kiểm lại đầu phiên sau)

- **Không có git** (`git rev-parse` → not a git repository) ⇒ không commit, OCR review không chạy
  (không diff ⇒ review mặc định), `detect_changes` không dùng được (grep + đọc file thay thế).
- **AgentMemory DOWN + MCP Simplenote không có trong tool phiên** ⇒ mọi bài học ghi hết vào
  `LESSONS_LEARNED.md` + skill `erpn-verify-first` (đã +2 hàng cho P4-4).
- **APK: user chốt "để sau"** — KHÔNG push thô (history không chung gốc + bản local thiếu file so với
  remote ⇒ push cả cây sẽ XÓA file trên branch duy nhất, cần `--force`). Chi tiết khảo sát:
  `checklist.md` mục P4-3.

## 2. Đã làm trong phiên (P4-4, plan4_final §4.4)

- **Server**:
  - `capabilities.json` — block `drill_screens`: **5 id cố định** (sales_orders_today ·
    invoices_today · receipts_today · overdue_customers · app_drafts_today), mỗi id gán
    **capability READ đúng dữ liệu nó đọc** = Nguồn quyền + Nguồn số.
  - `capability-contract.mjs` — validate fail-closed + accessor + clamp limit server-side
    (default 10, min 5, max 10).
  - `skills/ops-summary.mjs` — `readDayDrill`: **dùng lại đúng primitive của aggregate**
    (không extractor thứ hai ⇒ số dòng luôn khớp block).
  - `drill-views.mjs` — envelope GIỐNG A1 (items/total/truncated/generated_at) để Flutter tái dùng.
  - `http-ask.mjs` — route `GET|POST /read/drill`: auth non-loopback như /ask · bucket READ ·
    **KHÔNG classifier** (client gửi id, không text — §4.4 cấm inject free-text) · id lạ ⇒ 404 ·
    company resolve server-side qua helper dùng chung **`resolveReadCompany()`** (tách từ
    P4-2 — ranh giới tin cậy 1 nhà).
- **Flutter** (`features/ops/`): `drill_models.dart` · `CopilotApiClient.dailySummaryDrill` ·
  `drill_list_screen.dart` (Back/retry/truncated trung thực/**không affordance ghi**) · route
  `/drill` **fail-closed** khi thiếu intent · tap chỉ số trên màn Tóm tắt ngày qua `context.go`.
- **Test/falsify**: `test/p44-read-drill.test.mjs` **9/9** (có **guard chống drift**: tổng dòng
  drill = ĐÚNG số block aggregate trên cùng fixture) + authz tách file ·
  `test/drill_list_test.dart` **8/8** (tap drill **không gọi /execute** — assert log request ·
  không classifier · truncated · Back · fail-closed) · harness lib dùng chung
  `scripts/falsify/lib/harness.mjs` + `p44-read-drill.mjs` **12/12 RED**.

## 3. Số cuối phiên (chạy thật, 2026-09-21)

| Suite | Kết quả |
|---|---|
| Node (`npm test`) | **523 tests / 521 pass / 2 fail** — 2 fail là dsh-gateway từ B0 (máy không có dsh) |
| Flutter test | **242/242 PASS** |
| flutter analyze | **0 issue** |
| Falsify P4-4+P4-5 | **17/17 RED** (đỏ đúng tên test, restore byte-identical) |
| Vòng THẬT read-only | **ALL MATCH** — 5 drill ↔ block summary cùng thời điểm · drill nháp **200** (`total=0` đúng — site chưa có nháp trong ngày) |

## 4. Điều đáng nhớ nhất (chi tiết `result-p4-4.txt` §5 + LESSONS đợt P4-4 + skill +2 hàng)

1. **SQL COUNT ≠ danh sách đã lọc** — COUNT gộp dòng null ⇒ drill = summary +1; guard drift bắt ngay
   tại CI. Luật: đếm bằng **độ dài của chính danh sách đã lọc**.
2. **Guard chống drift là thứ đáng giá nhất của P4-4**: "tổng view con = số view cha trên cùng fixture"
   bắt được drift trước cả vòng thật.
3. **Test cũ để lại state trên đĩa dùng chung** (test chaos duy nhất không truyền `idemStore` ⇒ record
   PENDING sót trong `idempotency-store/`) ⇒ độc hại lần chạy sau, fail ổn định theo thứ tự — đã cô
   lập store; luật: test đỏ "không rõ vì sao" ⇒ kiểm state sót TRƯỚC khi nghi code mới.
4. **`Navigator.pushNamed` không đi qua GoRouter** — app dùng `context.go`; test điều hướng lộ ngay.

## 5. Việc tiếp theo (theo thứ tự)

1. **Duyệt commit** khi máy có git (hàng chờ: P4-1→P4-2→P4-3→P4-4→P4-5; footer các docs đã đánh dấu).
2. **P4-6** (optional, plan4 §8) — "hôm qua + delta" trên màn tóm tắt.
3. **B2/B3/B4 idempotency trên site thật** (cần chạy ghi thật — CHỜ user duyệt rõ; tới lúc đó
   correlation của B2/B3/B4 vẫn chỉ đứng với mock).
4. P10 full (DR drill, dashboard, load test, rate-limit store phân tán) — còn hoãn như cũ.

## 6. Đừng quên

- Suite dsh-gateway **2 test đỏ có chủ đích** trên máy không có dsh — không phải regression, đừng "sửa".
- Mock P4: fixture mode mô phỏng **"site thiếu Custom Field"** qua knob (parity với nhánh non-fixture,
  vá ở review vòng 2 của P4-3) — default mock = field **CÓ** (khớp trạng thái site nay).
- Vòng thật dùng `/tmp/p44-real-loop.mjs` (không commit) — regeneration rẻ nếu /tmp bị dọn.
