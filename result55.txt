========================================================================
RESULT 55 — BUGFIX P6: "MÁY KHÔNG CÓ BỘ NHẬN DẠNG TIẾNG VIỆT" LÀ SAI
Timestamp: 2026-09-18 (giờ shell)
Người báo: user (sau khi cài APK lên máy thật).
Phạm vi: CHỈ client voice — `apps/mobile/lib/features/chat/data/speech_service.dart`
+ `apps/mobile/lib/features/chat/presentation/screens/chat_screen.dart` + 2 file test.
KHÔNG đụng server/gateway/idempotency/router, KHÔNG ghi ERPNext thật.
Chưa commit (đụng mobile client → chờ user duyệt, như thông lệ).
========================================================================

== 1. HIỆN TƯỢNG (user, máy thật) ==

"Gboard voice hiểu tiếng Việt nhưng app hiện 'Máy không có bộ nhận dạng tiếng Việt'."

→ Không phải lỗi mỹ phẩm: cảnh báo SAI chặn đúng use case chính của P6 (nhập bằng
giọng nói tiếng Việt) — user thấy vậy thì không dám dùng mic dù máy nhận tốt.

== 2. NGUYÊN NHÂN GỐC (đọc doc plugin thật, không đoán) ==

`speech_service.dart` cũ:

    final locales = await _speech.locales();
    for (final locale in locales) {
      if (locale.localeId.toLowerCase().startsWith('vi')) return locale.localeId;
    }
    return null;                       // ← UI coi null = "không có tiếng Việt"

Doc của chính plugin (`~/.pub-cache/hosted/pub.dev/speech_to_text-7.5.0/lib/speech_to_text.dart`,
`locales()`), nguyên văn:

  "Android: The list of languages is based on the locales supported by the on device
   recognizer. This list may not be the complete list of languages available for online
   recognition. Unfortunately there is no way to get the list of languages supported by
   the online recognizer."

⇒ `locales()` chỉ phủ recognizer ON-DEVICE. Máy của user (Gboard/online) nhận tiếng Việt,
nhưng danh sách đó KHÔNG có `vi` ⇒ code cũ trả `null` ⇒ hai hệ quả sai cùng lúc:
  (a) UI nói "Máy không có bộ nhận dạng tiếng Việt" (claim về năng lực máy — sai bản chất);
  (b) `listen(localeId: null)` ⇒ OS tự chọn locale bất kỳ, app KHÔNG còn chủ động xin tiếng Việt.

Vết tích trong code cũ (đã xoá): `_pickVietnameseLocale()` trả `String?`;
`chat_screen.dart` dòng `_notice = _speech.localeId == null ? 'Máy không có bộ nhận dạng
tiếng Việt — hãy đọc kỹ lại câu chữ trước khi gửi.' : null;`

== 3. SỬA (6 điểm) ==

 1. `speech_service.dart` — `static const String fallbackLocaleId = 'vi_VN';`
    Không thấy `vi` trong `locales()` (kể cả list RỖNG) ⇒ vẫn xin `vi_VN`.
    (Android plugin đổi `_` → `-` trước `EXTRA_LANGUAGE` — đọc source Kotlin:
     `SpeechToTextPlugin.kt:203  localeId = localeId.replace('_','-')` ⇒ `vi_VN` → `vi-VN`.)
 2. Thêm `bool get localeVerified` — TÁCH "máy có liệt kê `vi` hay không" khỏi "dùng được
    hay không". UI chỉ được dùng nó cho gợi ý NHẸ, không bao giờ cho refusal.
 3. `listen()`: nếu platform TỪ CHỐI locale đã xin (throw `ListenFailedException`) và locale
    đó chưa được máy xác nhận ⇒ retry ĐÚNG 1 lần với locale mặc định; chỉ khi retry cũng
    hỏng mới `unavailable`. (Trước đây mọi throw ⇒ `unavailable` + tắt service.)
 4. `_handleError()`: `error_language_not_supported` / `error_language_unavailable` được
    xử lý TRƯỚC cờ `permanent` ⇒ bỏ locale đang ép (lần sau dùng mặc định máy) nhưng KHÔNG
    tắt service vĩnh viễn — đây là "ngôn ngữ chưa hỗ trợ", không phải "máy không có recognizer".
 5. `chat_screen.dart`: `_notice = _speech.localeVerified ? null : 'Máy không liệt kê tiếng Việt
    trong danh sách nhận dạng — vẫn thử nhận dạng tiếng Việt, nên đọc lại câu chữ trước khi gửi.'`
    ⇒ gỡ hẳn câu "Máy không có bộ nhận dạng tiếng Việt"; `_noticeIsError = false` (không đỏ).
 6. Nới copy trạng thái `unavailable` (dùng chung cho cả "máy không có recognizer" và
    "recognizer từ chối ngôn ngữ") → "Không dùng được nhận dạng giọng nói trên máy này. Bạn
    nhập bằng bàn phím như bình thường, hoặc thử lại nút micro."

== 4. TEST THẬT (lệnh + output nguyên văn) ==

$ cd apps/mobile && flutter test
  00:16 +106: .../chat_controller_test.dart: F7-2: the submit setting is read at ASK time...
  00:16 +107: All tests passed!

$ flutter analyze
  Analyzing mobile...
  No issues found! (ran in 4.5s)

$ PYTHONPATH=src python3 -m unittest discover -s tests      # (repo root)
  Ran 62 tests in 0.376s
  OK

$ cd mcp-erpnext && env -u ERPNEXT_URL -u ERPNEXT_API_KEY -u ERPNEXT_API_SECRET node --test
  ℹ pass 267   ℹ fail 0

Số test voice: `speech_service_test.dart` 6 → 11 · `voice_input_test.dart` 15 → 16
(tổng Flutter 101 → 107). Test mới, nguyên ý:
 - list KHÔNG có `vi` ⇒ `localeId == 'vi_VN'`, `localeVerified == false`, `localeAttempts == ['vi_VN']`
 - list RỖNG `[]` ⇒ y như trên (đúng ca máy Android thật)
 - có `vi` ⇒ dùng nguyên `vi_VN` + `localeVerified == true`
 - platform TỪ CHỐI `vi_VN` ⇒ `localeAttempts == ['vi_VN', null]` + status `listening` (KHÔNG unavailable)
 - locale ĐÃ xác nhận mà lỗi ⇒ `localeAttempts == ['vi_VN']` (không silent retry) + `unavailable`
 - refusal ngôn ngữ qua onError ⇒ status `unavailable`, `localeId == null`, lần nghe sau
   `localeAttempts == [null]` (service sống, không bị tắt vĩnh viễn)
 - widget: máy không liệt kê `vi` ⇒ hiện gợi ý nhẹ, câu cũ `findsNothing`, VẪN nghe, VẪN điền
   vào ô nhập và VẪN không gửi gì (`rec.paths` rỗng); máy có `vi` ⇒ không gợi ý

== 5. FALSIFY (gỡ → đỏ đúng chỗ → khôi phục; `git diff` sạch sau mỗi lần) ==

| | Gỡ gì | Test đỏ (nguyên tên) |
|---|---|---|
| A | `_localeId = fallbackLocaleId` → `null` | "a locale list WITHOUT vi still asks for vi_VN (bug P6)" · "an EMPTY locale list still asks for vi_VN (bug P6)" · "a platform that REJECTS the forced locale is retried with the default" |
| B | bỏ nhánh retry (`... || true`) | "a platform that REJECTS the forced locale is retried with the default" |
| C | xoá nhánh `error_language_not_supported` | "a language refusal drops the forced locale but keeps the mic usable" |
| D | UI quay lại ĐÚNG code cũ (`localeId == null` + câu cũ) | "device with no Vietnamese in the locale list → hint, not refusal" |

== 6. LỖI CỦA CHÍNH TÔI TRONG ĐỢT NÀY (đã ghi vào skill) ==

 6.1 **Falsify D lần đầu SAI VỊ TRÍ**: tôi thay `_notice = _speech.localeVerified ? null : '<câu mới>'`
     bằng `_notice = (_speech.localeId == null) ? null : '<câu mới>'` (đảo nhánh) ⇒ test đỏ là
     "a verified Vietnamese locale shows no hint at all" — tức đỏ ở ca KHÁC, KHÔNG phải ca bug của
     user. Làm lại đúng code cũ (câu cảnh báo cũ + điều kiện `localeId == null`) ⇒ đỏ đúng
     "hint, not refusal". Bài học: falsify phải TÁI TẠO ĐÚNG bản cũ; đỏ ≠ đỏ đúng chỗ.
 6.2 **Trùng tên field/getter trong fake test**: khai `bool localeVerified;` trong `_FakeSpeech`
     rồi thêm `@override bool get localeVerified` ⇒ 'localeVerified' is already declared
     (compile fail cả file test). Sửa: field `localeIsVerified`. (Dart không cho tham số named
     private `this._x` nên không thể "chỉ cần đổi thành _".)
 6.3 **`find.textContaining` phân biệt HOA/thường**: copy đổi "không" → "Không" ở đầu câu ⇒ 2 test
     đỏ dù logic đúng ⇒ mất 1 vòng sửa vô ích.
 6.4 **`errorListener` trùng field kế thừa của plugin** (analyze: `overridden_fields` +
     `annotate_overrides`) ⇒ đổi thành `lastErrorListener`.
 6.5 **`dart format` (Dart 3.13 tall style) reformat CẢ FILE**: repo viết theo style cũ —
     đo thật `dart format --set-exit-if-changed lib test` ⇒ **20/28 file non-conforming**.
     Chạy format trên 4 file đã sửa làm diff phình 272 → 459 dòng (churn ngoài phạm vi) ⇒ phải
     `git checkout` 2 file test + khôi phục 2 file lib từ backup rồi apply lại edit tay.
     Ba lần "Formatted 4 files (4 changed)" là dấu hiệu formatter không phải chuẩn của repo.

== 7. FILE + TRẠNG THÁI ==

M: `apps/mobile/lib/features/chat/data/speech_service.dart` (105 dòng đổi)
M: `apps/mobile/lib/features/chat/presentation/screens/chat_screen.dart` (19)
M: `apps/mobile/test/speech_service_test.dart` (148)
M: `apps/mobile/test/voice_input_test.dart` (51)
   → 4 files, +272/−51. Secret scan: không có key/token nào trong diff (không đụng `.env`).

Docs: `.plan/phases2/p6-result.md` (+ mục "Bugfix P6", sửa checklist máy thật mục 7, suite
101 → 107) · `working.md` · `next.md` · `checklist.md` · `.project/modules/chat.md` ·
`.agents/skills/erpn-verify-first/SKILL.md` (bài học, file gitignored) · `LESSONS_LEARNED.md`.

**ĐÃ COMMIT + PUSH** (user duyệt bằng lệnh "push code lên gh build apk", 2026-09-18):
`77e2b57` fix (4 file code, +272/−51) · `bbca7ca` docs (6 file, +195/−6) →
`5c5c99c..bbca7ca  change/flutter-chat-mvp -> change/flutter-chat-mvp`

== 8. APK CI — BUILD LẠI + XÁC MINH BINARY CHỨA BẢN FIX (không chỉ tin "run xanh") ==

$ git push origin change/flutter-chat-mvp
  5c5c99c..bbca7ca  change/flutter-chat-mvp -> change/flutter-chat-mvp

$ gh run list --limit 5
  queued    docs: record the P6 locale bugfix …   android-debug-apk   change/flutter-chat-mvp
            push   35367603981   2s   2026-09-18T16:17:37Z

$ gh run watch 35367603981 --exit-status --interval 20      # exit 0
$ gh run view 35367603981 --json conclusion,status,headSha,jobs
  conclusion=success status=completed head=bbca7ca jobs=Build debug APK:success

$ gh api /repos/hoangsoft90/erpn_mobile1/actions/runs/35367603981/artifacts
  name=erpn-chat-debug-apk  size=84169705 bytes  expired=false
  expires_at=2026-12-17T16:17:37Z

Xác minh APK THẬT sự chứa bản fix (tải artifact rồi grep binary, không suy từ status):

$ gh run download 35367603981 -n erpn-chat-debug-apk -D /tmp/apk-verify
  -rw-r--r-- 165061846  /tmp/apk-verify/app-debug.apk
$ unzip -o -q app-debug.apk -d extracted && grep -c <chuỗi> extracted/assets/flutter_assets/kernel_blob.bin
  "Máy không liệt kê tiếng Việt"            → 2
  "Máy không có bộ nhận dạng tiếng Việt"    → 0   ← câu SAI của bug đã biến mất khỏi binary
  "vi_VN"                                  → 3

⇒ APK từ CI đúng là bản có fix (câu cũ = 0 lần trong `kernel_blob.bin`), không phải build cũ.

Bước còn lại (người thật): cài APK rồi bấm 🎙 trên máy có Gboard tiếng Việt; nếu máy có logcat thì
dán dòng `EXTRA_LANGUAGE` / kết quả nhận dạng để đối chiếu.
Link tải artifact:
https://github.com/hoangsoft90/erpn_mobile1/actions/runs/35367603981/artifacts/erpn-chat-debug-apk

== 9. UX FOLLOW-UP CÙNG NGÀY — BỎ LUÔN GỢI Ý NHẸ (chưa commit) ==
(phần dưới giữ nguyên; tính năng mới "Tự gửi sau khi nói xong" ghi ở §10)

User báo sau khi dùng bản đã sửa: máy nhận tiếng Việt OK mà **vẫn** thấy dòng
"Máy không liệt kê tiếng Việt trong danh sách nhận dạng — vẫn thử…" ⇒ vẫn là tiếng ồn.

Yêu cầu + đã làm:
- **XOÁ notice khi chỉ vì `!localeVerified`** (`chat_screen.dart`): `_notice = null;` thay cho
  ternary theo `_speech.localeVerified` ⇒ grep `liệt kê tiếng Việt` trong `apps/mobile/lib/` = **0 hit**.
- **GIỮ cảnh báo thật**: `denied` / `unavailable` / `error_language_*` (đi qua `_onSpeechStatus`
  + `_deniedOrUnavailable`) — có test riêng cho ca "máy thiếu `vi` **và** bị thu hồi quyền" ⇒
  thông báo quyền micro **vẫn hiện**.
- **KHÔNG đổi** `fallbackLocaleId = 'vi_VN'`, không đổi logic `listen()`/retry/`_handleError`.
- `localeVerified` vẫn còn ở tầng service (nhánh retry + test dùng) nhưng **UI không đọc nữa**.

$ flutter test      → 107/107 · analyze → No issues found!
$ falsify E         → chèn lại notice `!localeVerified` ⇒ đỏ đúng
                      "locale list without vi → NO notice at all, mic just works" (khôi phục diff sạch)

Bài học: **tín hiệu user KHÔNG THỂ HÀNH ĐỘNG thì đừng hiển thị** — user không thể "thêm locale" cho
máy; câu gợi ý chỉ khiến họ tưởng app thiếu gì. (Đã ghi vào SKILL.md + LESSONS_LEARNED.md.)

**Human còn lại (không phải việc agent):** cài APK ở trên lên máy thật có Gboard tiếng Việt → smoke
mục 7 trong `.plan/phases2/p6-result.md` (đọc ra đúng chữ · KHÔNG hiện dòng nào về "tiếng Việt" ·
trên cùng máy, thu hồi quyền micro ⇒ **vẫn** phải hiện thông báo quyền · không tự gửi/tự nộp)

== 10. TÍNH NĂNG MỚI — SETTINGS "TỰ GỬI SAU KHI NÓI XONG" (chưa commit) ==

Yêu cầu user: switch trong Settings, **mặc định OFF**; khi ON thì sau kết quả STT **final**, nếu ô
nhập có text (trim ≠ rỗng) và không đang loading → gọi **cùng** đường Send (`POST /ask`), KHÔNG path
riêng, KHÔNG auto-confirm WRITE, KHÔNG đụng `/execute`, partial không trigger.

### Diff

| File | Nội dung |
|---|---|
| `core/constants/app_constants.dart` | key versioned `settings_voice_auto_send_v1` |
| `core/settings/app_settings_service.dart` | `voiceAutoSend` (default OFF, fail-safe: thiếu key/null prefs/JSON hỏng ⇒ false) + `saveVoiceAutoSend()` + `clear()` xoá luôn |
| `features/settings/.../settings_screen.dart` | mục **"Giọng nói"** + `SwitchListTile` "Tự gửi sau khi nói xong", copy đổi theo trạng thái (BẬT: "đọc xong là gửi câu hỏi ngay — **vẫn phải bấm Xác nhận khi thu tiền**"), lưu cùng nút Lưu |
| `features/chat/.../chat_screen.dart` | `_onSpeechResult`: final ⇒ `_maybeAutoSend()`; `_maybeAutoSend()` chỉ gọi `widget.onSend()` — cùng đường với nút Gửi |
| 3 file test | `voice_input_test.dart` +6, `settings_test.dart` +5 (service 3 + UI 2), `settings_navigation_test.dart` (viewport) |

Điểm chặn trong `_onSpeechResult` (một chỗ duy nhất cho mỗi luật):

    if (!_listening) return;                       // đã dừng/đã gửi ⇒ bỏ kết quả muộn (P6)
    if (transcript.trim().isEmpty) {                // không nghe được gì
      if (isFinal) setState(() => _listening = false);
      return;                                       // KHÔNG đụng ô nhập, KHÔNG gửi
    }
    ... ghi vào ô nhập ...
    if (!isFinal) return;                           // partial không bao giờ gửi
    setState(() => _listening = false);
    _maybeAutoSend();

    _maybeAutoSend(): if (!widget.enabled) return;                 // đang loading
                      if (field.trim().isEmpty) return;            // không có gì để gửi
                      if (!settings.voiceAutoSend) return;         // mặc định OFF
                      await widget.onSend();                       // CÙNG đường Send

### Test (lệnh + output thật)

$ flutter test   → 118/118 (107 → 118)   ·   flutter analyze → No issues found!

Test mới:
- service: `voiceAutoSend` default OFF (thiếu key / null prefs) · round-trip ON↔OFF (instance mới
  đọc lại được) · **storage hỏng/kiểu sai ⇒ false, không ném** · `clear()` xoá key
- UI Settings: switch có mặt + default OFF + **chưa ghi gì cho tới khi bấm Lưu** · bật ON **không cần
dialog** (khác F7-2 — nó không nằm trong đường tiền), copy "ĐANG BẬT" + "vẫn phải bấm Xác nhận",
Lưu ⇒ đọc lại bằng instance mới = true
- chat: OFF (mặc định) + final ⇒ **không** gửi, chữ còn nguyên trong ô · ON + final ⇒ **đúng 1**
request `/ask`, body text đúng, ô nhập được xoá · ON + partial ⇒ không gửi · ON + final nhưng
**transcript rỗng** ⇒ không gửi và **ô nhập không bị sửa** (không thêm space lạ) · ON + recognizer
từ chối ⇒ không gửi, vẫn hiện thông báo lỗi · ON + câu lệnh ghi ⇒ chỉ `/ask`, `paths` không chứa
execute

### Falsify (gỡ → đỏ đúng chỗ → khôi phục)

| | Gỡ gì | Test đỏ |
|---|---|---|
| A | check `voiceAutoSend` | 5 test (OFF-mặc-định + 4 test cũ "không được gửi") |
| B | block "transcript rỗng" ở caller | "auto-send ON: an empty final result sends nothing" |
| C | `if (!isFinal) return;` | "partial results never send" + 3 test P6 cũ |

⇒ **Falsify B lần đầu KHÔNG đỏ** vì tôi có 2 lớp check trùng: bản trong `_maybeAutoSend` là **code
không thể chạm tới** (caller đã chặn trước). Đã gỡ lớp trùng, giữ đúng 1 chỗ mỗi luật + gỡ luôn
tham số `transcript` không còn dùng. (Đúng bài học P8: nhánh chết ⇒ falsify không đỏ.)

### Lỗi tự gây trong đợt này

1. **Guard trùng/không thể chạm** (ở trên) — falsify phát hiện, đã gỡ.
2. **Thêm 1 mục vào ListView ⇒ test cũ đỏ vì ListView LAZY**: `SettingsScreen` là `ListView` nên
   phần tử dưới màn hình **không được BUILD**; `tester.ensureVisible(find.text('Lưu'))` cũ không
tìm thấy gì (`Bad state: No element`) ⇒ 2 test cũ + 1 test navigation đỏ dù code đúng. Sửa: cho
viewport test cao hơn (`tester.view.physicalSize = Size(1000, 2400)` + `addTearDown(view.reset)`)
thay vì đi scroll từng chỗ.
3. **Assertion copy mơ hồ**: `find.textContaining('TẮT')` / `('ĐANG BẬT')` giờ khớp 2 switch ⇒ phải
ghi rõ `'TẮT: xác nhận'` / `'ĐANG BẬT: đọc xong'`. (Cùng họ với bài học `textContaining` phân biệt
hoa/thường ở §6.3.)
4. Lint Dart: helper cục bộ không được bắt đầu bằng `_` → `scrollTo` (không phải `_scrollTo`).

== 11. REVIEW VÒNG 3 (2026-09-18) — 3 LỖI THẬT + 1 GUARD CHẾT THỨ 3 + 1 CÂU HỎI MỞ ==

Yêu cầu user: "review code những gì vừa làm". Phạm vi: 4 file code + 3 file test của đợt UX này.
Nguyên tắc: mỗi finding phải chỉ ra cơ chế cụ thể + falsify được, không phỏng đoán.

--- R5. Doc SAI trong chính class vừa sửa -------------------------------------

`_InputBar` docstring vẫn ghi: *"The mic has no auto-send and no route to `/execute`"* — SAI kể từ
khi thêm switch. Loại lỗi nguy hiểm vì người đọc sau sẽ tin doc chứ không đọc 3 lớp guard.
Sửa: "Auto-send exists ONLY as a user opt-in (default OFF) and reuses that same `onSend`".

--- R6. Doc SAI thứ 2 (tầng service) -----------------------------------------

`SpeechService.localeVerified` doc vẫn ghi *"the UI may only show a soft 'check the wording' note
— never a refusal"* — sau §9 thì UI **không hiển thị gì**. Ghi lại đúng vai trò còn lại: nó chỉ
được `listen()` đọc để quyết định cho retry locale một lần với locale mặc định.

--- R7. GUARD CHẾT thứ 3 trong CÙNG tính năng ---------------------------------

`_maybeAutoSend()` có 3 lớp check. Falsify từng lớp:

| Lớp | Gỡ ra | Kết quả |
|---|---|---|
| `!widget.enabled` (đang có request) | bỏ hẳn | 🟢 xanh toàn bộ 120 test ⇒ **không thể chạm** |
| check rỗng ô nhập | bỏ (đợt trước) | 🟢 xanh ⇒ đã gỡ |
| check rỗng transcript | bỏ (đợt trước) | 🟢 xanh ⇒ đã gỡ |

⇒ `_maybeAutoSend` nay **chỉ còn 1 điều kiện**: `voiceAutoSend`. Các luật còn lại đã có đúng
**một nhà**: session/`_listening` ở `_onSpeechResult` · "có gì để gửi" ở `_send` · "không gửi chồng"
ở `ChatController.send` (`if (state.isLoading) return false`) — lớp này mới là lớp THẬT SỰ chặn gửi
chồng, nên bản copy trong `_maybeAutoSend` không bao giờ đỏ được.

--- R8. BÁO SAI KẾT QUẢ LƯU (đúng họ lỗi user từng gặp với URL gateway) ------

`_save()` chỉ lấy `okGateway` để chọn thông báo:

    final okGateway = await settings.saveGateway(...);
    await settings.saveAllowSubmitPayment(_allowSubmit);   // ← bool bị bỏ
    await settings.saveVoiceAutoSend(_voiceAutoSend);      // ← bool bị bỏ
    ... Text(okGateway ? 'Đã lưu — ...' : 'Không lưu được ...')

⇒ storage ghi được string nhưng từ chối ghi bool (ghi một phần) ⇒ switch **hiện ON** mà **không hề
được lưu** ⇒ user tưởng đã bật, app đọc prefs = OFF ⇒ "bật mà không thấy tác dụng".
Sửa: `final ok = okGateway && okSubmit && okVoice;` (thông báo lỗi khi BẤT KỲ phần nào không ghi được).

--- Test thêm + falsify -----------------------------------------------------

    $ flutter test   → 120/120 (118 → 120)   ·   flutter analyze → No issues found!

2 test mới:
- `auto-send ON: a SECOND final result cannot send twice` — recognizer nhả nhiều final khi tắt;
  xác nhận 1 câu = 1 request và final muộn **không** ghi lại chữ vào ô đã gửi.
- `a switch that did not persist is reported, never called success` — `_FakePrefs(failBoolWrites: true)`
  (setString OK, setBool ném) ⇒ phải thấy "Không lưu được cài đặt", **không** được thấy "Đã lưu —".

| # | Gỡ gì | Kết quả |
|---|---|---|
| A | `ok = okGateway && okSubmit && okVoice` → `ok = okGateway` | 🔴 đúng test mới R8 |
| B | bỏ `if (!_listening) return;` | 🔴 **nhưng ở test CŨ**: "a late result after stop must not resurrect text" |
| C | bỏ `if (!widget.enabled) return;` | 🟢 xanh ⇒ guard chết, đã gỡ |

⇒ Ghi nhận TRUNG THỰC: falsify B đỏ, nhưng **không phải** ở test mới của tôi — nghĩa là lớp guard
đó đã được test cũ canh. Bài học: "đỏ" không tự chứng minh test MỚI có giá trị; phải hỏi "test của
MÌNH bắt được regression nào mà test khác không bắt được". Test mới vẫn giữ vì nó canh **đường
auto-send** (test cũ không chạy qua switch), nhưng không được gán cho nó công lao của lớp
`!_listening`.

--- ❓ CÂU HỎI MỞ (đã ghi `human.md` §1, chưa tự đổi hành vi) -----------------

`_startListening` đặt `pauseFor: 3s`. Doc của chính plugin:

> "`pauseFor` sets the maximum duration of a pause in speech with no words detected, after that it
> automatically stops the listen for you. On some systems, notably Android, there is a system
> imposed pause of from one to three seconds that cannot be overridden."

Với switch BẬT: người dùng **ngập ngừng giữa câu** ⇒ "final" tới ⇒ câu **nửa vời** được gửi và
phiên nghe kết thúc. Hành vi này đúng đặc tả ("final ⇒ gửi") nhưng có thể không phải ý user.
Ba lựa chọn (a/b/c) ở `human.md` §1 — cần user quyết, agent không tự đổi.

--- Dọn kèm: 2 dòng lạc ở cuối §10 -------------------------------------------

Đoạn "nếu máy có logcat thì dán dòng `EXTRA_LANGUAGE`... Link artifact" của §8 bị dán lẫn vào cuối
mục "Lỗi tự gây" của §10 (làm sai nghĩa cả hai mục). Đã trả về đúng §8.

--- Files (đợt review này) --------------------------------------------------

**Sửa:** `apps/mobile/lib/features/chat/presentation/screens/chat_screen.dart` (doc R5 + gỡ guard chết R7)
· `apps/mobile/lib/features/chat/data/speech_service.dart` (doc R6)
· `apps/mobile/lib/features/settings/presentation/screens/settings_screen.dart` (R8)
· `apps/mobile/test/settings_test.dart` (fake + 1 test R8) · `apps/mobile/test/voice_input_test.dart` (1 test R7)
· `result55.txt` · `.plan/phases2/p6-result.md` · `human.md`
