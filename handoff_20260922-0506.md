# Handoff — 2026-09-22 05:06 — plan5 (P5-0→P5-4) + push lần 2 + chặn 3 rò rỉ bảo mật

> Phiên trước: `handoff_20260921-1549.md`. Bằng chứng chi tiết từng phase nằm ở
> `.plan/result-p5-0.md` … `result-p5-4.md` (**`.plan/` bị gitignore ⇒ không lên GitHub**);
> bản tóm tắt sống cùng repo ở `result-p4-done.txt` (phụ lục 2026-09-22) và `checklist.md`.

## 1. Việc đã xong trong phiên này

| Phase | Kết quả | Deliverable |
|---|---|---|
| **P5-0** điều tra 3 bug | bug #2 (auto-send không tự gửi) và #3 (draft dù bật nộp thật) **đã đóng ở tầng logic**; `/execute` timeout **không** tự reconcile nhưng an toàn (`retry_same_command_id` + 4 lớp chống trùng) ⇒ **không phải P0** | `.plan/result-p5-0.md` |
| **P5-1** | **Không thực thi** — tiền đề prompt sai (file nó `Depends` vào kết luận ngược lại); trong lúc verify bắt **bug thật**: 5 test P4-6 hard-code ngày ⇒ đỏ khi lịch sang ngày mới dù không dòng code nào đổi | `.plan/result-p5-1.md` |
| **P5-2** | `submit_error_kind` (permission/period_locked/workflow/other) + `posting_date` đóng băng lúc PROPOSE. **Review tự bắt lỗ do chính P5-2 mở**: ngày do client gửi ở `/execute` không ai validate ⇒ thêm `readPostingDate()` fail-closed (dạng ngày · ngày có thật · cửa sổ ±1 ngày) đặt **trước `setReference()`** | `.plan/result-p5-2.md` §7/§8 |
| **P5-3** | badge NHÁP/ĐÃ NỘP + gợi ý theo loại lỗi + copy setting rõ OFF/ON (storage key giữ nguyên) + glyph dòng kết quả theo cùng trạng thái (⚠️ khi submit bị từ chối) | `.plan/result-p5-3.md` |
| **P5-4** | voice-first là **layout thứ hai**: OFF nguyên trạng; ON mic CTA cùng vùng input bar + ô nhập chữ vẫn dùng được + **Huỷ** (bỏ câu vừa nói, khác tap-to-dừng giữ transcript) + **cap 25s** + VAD 2.2s vào `AppConstants` | `.plan/result-p5-4.md` |
| **PUSH LẦN 2** | commit `53e1851` → **CI xanh** run `35688782230`, artifact `erpn-chat-debug-apk` **81.4 MB** | — |

## 2. Phát hiện chính — 3 rò rỉ chặn được trước khi lên GitHub (đọc kỹ)

Cây local chuẩn bị push chứa, **chưa từng** có trên remote (kiểm `git ls-tree` remote = 0):

1. `.chats/sess1.md` — **chứa token GitHub THẬT** (`ghp_…`).
2. `.review/chat1..7.md` + `.review/dsh_test.md` — **8 transcript chat thô**.
3. `.gemini/**` (192K: config CLI + skills) và `initp` (script tạo `.gitignore` — `AGENTS.md` ghi rõ cấm commit).

`.gitignore` dự án chặn `.agent/.agents/.plan/.draft/.edits/.tests/node_modules/build/...` nhưng **KHÔNG** chặn 4 thứ trên.
Đã chặn vĩnh viễn: thêm `.chats .review .gemini .opencode` + `initp` vào `.gitignore`, và làm lại **clone mới + merge** (không amend lên commit đã nhiễm — tree của commit nhiễm vẫn nằm trong history).
Phát hiện nhờ `git grep -l <giá trị token thật>` + liệt kê top-level dir — **grep pattern hẹp là không đủ**.

Kèm theo: **`.env` đang là `664`** (ai cũng đọc được: ERPNext key/secret, GH_TOKEN, GEMINI/ZEN/MAC LLM key, ASK_PASSWORD) trong khi rule dự án bắt `600` ⇒ đã `chmod 600`. `.env` đã có sẵn `GH_TOKEN=` + `GH_REPO_URL=` ⇒ bản token trùng tôi tạo ở `~/.config/erpn-gh/token` đã **xoá** (secrets chỉ 1 bản).

## 3. Số thật cuối phiên

```
Flutter full            : 263/263  · flutter analyze: 0 issue
node --test (mcp-erpnext): 537  (535 pass, 2 fail dsh — có từ B0)
falsify harness         : 10/10 ALL ALIVE (p52 9/9 RED)
mutation tay (UI guard) : 5/5 bắt đỏ đúng test, restore sha256 byte-identical
VOICE-TEST (voice_input): 34/34 (26 cũ nguyên vẹn + 8 mới của P5-4)
PUSH                    : 53e1851 · CI success · APK 81.4 MB
```

## 4. Bài học mới (đã ghi `LESSONS_LEARNED.md` + skill `erpn-verify-first`)

- **Trước push: soi DANH SÁCH thư mục sẽ push, grep pattern hẹp không đủ** (vụ `.chats`/`.review`/`.gemini`/`initp`).
- **3 bẫy git**: `git rm -r --cached a b c` **abort toàn bộ** khi 1 pathspec không khớp (im lặng nếu nuốt stderr) · `git merge FETCH_HEAD` trả *"Already up to date"* **SAI** cho commit không liên quan (dùng **tên ref**) · `.gitignore` **không** áp dụng cho file đã tracked (`git check-ignore` cũng không báo).
- **Chèn/dọn code Dart bằng str_replace**: oldString nuốt comment quyết định / dấu `}` đóng class ⇒ cấu trúc sập dây chuyền (P5-4).
- **\"Như thể X\" = mô phỏng ĐẦY ĐỦ hệ quả phụ của X** — cap 25s mô phỏng tap-to-stop ⇒ **không** auto-send (test kỳ vọng `/ask` của tôi đã đỏ ⇒ sửa expectation, không thêm auto-send).
- **UI guard chưa có harness ⇒ đột biến tay + `sha256sum -c`**.
- **`findsNothing` trên glyph dùng chung** = assert giòn (⚠️ của dòng-hàng rỗng không liên quan trạng thái submit).

## 5. Docs đã cập nhật

`checklist.md` (mục plan5 + 4 việc chờ user) · `features.md` (voice-first + badge vào "đã xong", P5-5 vào backlog) · `next.md` (trạng thái mới nhất + thứ tự chờ duyệt) · `faq.md` (**§11 — nói bằng giọng nói**: Huỷ vs tap-to-dừng · cap 25s · tự gửi **không** tự xác nhận · nhãn NHÁP nghĩa là gì · ngày phiếu thu) · `result-p4-done.txt` (phụ lục 2026-09-22) · **`openspec/changes/flutter-chat-mvp/STATUS-2026-09-22.md`** (giữ change mở, không archive) · `working.md` · `LESSONS_LEARNED.md` · skill `erpn-verify-first` · **skill mới `erpn-deploy-github`**.

## 6. Việc chờ USER (không phải việc agent)

1. **Revoke token `ghp_…7aO`** (đã qua chat 2 lần + nằm trong `.chats/sess1.md`) rồi dán token mới vào `.env`; hoặc bật **deploy key WRITE** cho repo `erpn_mobile1` để bỏ hẳn PAT.
2. **Duyệt 2 layout P5-4** (`.plan/result-p5-4.md` §6 — env này không có tool chụp màn hình).
3. **Test tay máy Android thật**: auto-send có tự gửi không — phân biệt *"chữ không hiện"* (tầng engine/quyền mic) với *"chữ hiện mà không tự gửi"* (OEM không bắn `final`).
4. **Còn treo từ plan4**: `docs/plan4-mode-map.md` (P4-0) chưa tạo · B2/B3/B4 idempotency trên site thật (cần user cho phép ghi thật) · P4-3..P4-6 chưa tách `result*.txt` riêng.

## 7. Lệnh chạy lại kiểm chứng

```bash
cd apps/mobile && flutter test && flutter analyze      # 263/263 · 0 issue
cd mcp-erpnext && npm test                             # 537 (2 fail dsh từ B0)
cd mcp-erpnext && node scripts/falsify/p52-payment-snapshot.mjs   # 9/9 RED
# push lần sau: xem skill .agents/skills/erpn-deploy-github/SKILL.md (đọc token từ .env)
```
