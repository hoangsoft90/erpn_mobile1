# Handoff — 2026-09-21 15:49 — Rà toàn bộ harness falsify (10/10) + sửa harness chết-im + docs

**Phiên trước:** `handoff_20260921-1423.md` (P4-7 đóng plan4 V1) · **Bằng chứng:** phụ lục `result-p4-done.txt`

## 1. Việc đã xong trong phiên này

**Rà TOÀN BỘ 10 harness falsify** (`mcp-erpnext/scripts/falsify/`) — chạy thật từng cái, tìm anchor trôi / ca yếu:

| Harness | Kết quả |
|---|---|
| c0-ocr · c1-ocr · execute-id-types · issue1-mock-optin · issue1-resolver | **ALL FALSIFIED / ALIVE** ✓ |
| **c2-ocr** | ⚠️ **CHẾT IM** — xem §2 ⇒ sau sửa: **ALL C2 GUARDS FALSIFIED (10/10)** |
| p4-ops-summary · p42 · p43 · p44 | **15/15 · 15/15 · 11/11 · 20/20** ✓ |
| Mutation sót | **0** (`find -name '*.falsify-bak'` rỗng; `if (false)`/`return []` = 0) |

**Đóng câu hỏi review còn treo** (sentinel 403): grep `node_modules/@casys/mcp-erpnext` (pin 3.0.4) —
client **throw `FrappeAPIError`** khi `!response.ok`, message `[FrappeClient] … failed: … (HTTP <status>)`.
Hình thái throw của sentinel ĐÚNG; thêm test thứ hai dùng **đúng text thật** ⇒ regex UI không chỉ khớp
wording mock ⇒ p4-ops **25/25**.

## 2. Phát hiện chính — c2-ocr chết-im (đọc kỹ)

- Harness spawn `node --test` **không truyền `COPILOT_MOCK_OK=1`** (mà `npm test` đặt) ⇒ 3 test route
  fail ngay **baseline** ⇒ harness dừng đúng quy tắc "baseline phải xanh" ⇒ **exit trước mọi case ⇒
  0/10 ca C2 được kiểm** mà không ai hay.
- Nếu nó chỉ nhìn `status !== 0` thay vì baseline-check, **10 guard C2 sẽ "được chứng minh" trên nền đỏ sẵn**.
- Sửa: `env: { ...process.env, COPILOT_MOCK_OK: "1" }` như `npm test` ⇒ chạy lại đủ ⇒ 10/10 RED.
- Diễn biến phụ: lần chạy đầu tôi bọc `timeout 240` ⇒ kill lúc baseline (không sót mutation vì chưa áp ca
  nào — kiểm bằng `find *.falsify-bak` rỗng + baseline Node xanh sau đó). **Không Ctrl-C/timeout ngắn bọc
  harness đa-runtime** (c2 cần ~9 phút vì Dart baseline + Dart per-case).

## 3. Số thật cuối phiên

| | |
|---|---|
| Node (`npm test`) | **525 — 523 pass, 2 fail (dsh từ B0)** |
| Flutter | **248/248** · analyze **0** (không đụng trong đợt này) |
| Python | **62 OK** — `PYTHONPATH=src python3 -m unittest discover -s tests -q` (**không dùng pytest**) |
| Falsify | **10/10 ALL ALIVE**, 0 PROBLEM |

## 4. Bài học mới (đã ghi LESSONS đợt "rà harness toàn repo" — 5 bullet + skill `erpn-verify-first` +2 hàng)

1. **Harness spawn runner phải kế thừa/set đúng env của lệnh test CHÍNH THỨC** (`package.json` scripts) —
   và sau khi đụng harness, phải nhìn thấy nó **chạy đầy đủ case**, không dừng ở "exit 0".
2. **Baseline-check là GUARD**: nó biến "harness chết vì env" thành PROBLEM tường minh; phải phân biệt
   **đỏ-vì-mutation** với **đỏ-sẵn**.
3. **Audit tĩnh (regex/parse) dễ ra 0-match mà exit 0** — khi audit "xanh" trong khi có bằng chứng ngược
   (2 anchor vừa trôi tuần trước) ⇒ **audit sai, bỏ audit, dùng bằng chứng động**.
4. **Mock sentinel phải bám contract client THẬT**: grep node_modules để biết throw-hay-data + ghép
   đúng hình thái message thật vào ≥1 test; đừng để test chứng minh mock đồng ý với chính nó.
5. **`timeout` bọc harness Flutter là bẫy** — chạy từng harness một với timeout tool đủ rộng.

## 5. Docs đã cập nhật

| File | Nội dung |
|---|---|
| `result-p4-done.txt` | **PHỤ LỤC** — rà harness toàn repo (bảng 10/10 + 2 phát hiện + số sau đợt rà) |
| `LESSONS_LEARNED.md` | Đợt rà harness (5 bullet) |
| `.agents/skills/erpn-verify-first/SKILL.md` | +2 hàng (env harness · sentinel contract thật) |
| `checklist.md` / `next.md` / `features.md` | Section rà harness + số 525/10-harness |
| `faq.md` | +1 dòng treo: APK máy thật (user chốt "để sau") |
| **openspec** `changes/flutter-chat-mvp/STATUS-2026-09-21.md` | Ghi rõ **2 task chờ USER** (GitHub Settings 3.4 · APK máy thật 4.3) — **không archive để "dọn file"**; archive chỉ khi có dữ kiện thật |
| `working.md` | Entry đợt rà harness |

## 6. Trạng thái & việc chờ user (không đổi)

- Hàng chờ commit: **P4-1→P4-6** + đợt sửa harness — vùng **SỐ TIỀN** ⇒ **AI không tự ký duyệt**; máy **không có git**.
- ⚠️ Khi repo có git: **đừng push thô** (history không chung gốc ⇒ push cả cây sẽ XOÁ file trên branch duy nhất).
- Treo: B2/B3/B4 idempotency site thật (cần user cho phép ghi thật) · APK (user chốt "để sau") ·
  GitHub Settings cho CI (3 giá trị — việc của user).

## 7. Lệnh chạy lại kiểm chứng

```bash
cd mcp-erpnext && npm test                       # 525 · 523 pass · 2 fail (dsh từ B0)
cd ../apps/mobile && flutter analyze && flutter test   # 0 · 248/248
cd .. && PYTHONPATH=src python3 -m unittest discover -s tests -q   # 62 OK (KHÔNG pytest)

# 10 harness — chạy TỪNG cái, timeout tool đủ rộng, KHÔNG Ctrl-C
cd mcp-erpnext
for h in execute-id-types issue1-mock-optin issue1-resolver c0-ocr p4-ops-summary; do
  node scripts/falsify/$h.mjs | tail -1
done
node scripts/falsify/p42-read-daily-summary.mjs | tail -1   # 15/15
node scripts/falsify/p43-drawer-summary.mjs | tail -1       # 11/11
node scripts/falsify/p44-read-drill.mjs | tail -1           # 20/20
node scripts/falsify/c1-ocr.mjs | tail -1                   # cần ~9 phút
node scripts/falsify/c2-ocr.mjs | tail -1                   # cần ~9 phút — giờ đã có COPILOT_MOCK_OK
```

**Nguồn sự thật:** `next.md` + `checklist.md` · `.project/README.md` · `LESSONS_LEARNED.md` ·
`.agents/skills/erpn-verify-first/SKILL.md` · `result-p4-*.txt` + `result-p4-done.txt` (phụ lục).
