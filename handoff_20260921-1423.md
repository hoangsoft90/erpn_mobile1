# Handoff — 2026-09-21 14:23 — P4-7: đóng plan4 V1 (§7 12/12) + FAQ cho người dùng

**Phiên trước:** `handoff_20260921-1352.md` (P4-6) · **Deliverable phiên này:** `result-p4-done.txt`

## 1. Việc đã xong

**P4-7** — đóng plan4 V1: chứng minh §7 đủ, rồi chốt docs cho người dùng đọc.

| | |
|---|---|
| §7 acceptance | **12/12 có test** (trước phiên: 11/12) — bảng chi tiết ở `checklist.md` section P4-7 và `result-p4-done.txt` §1 |
| Case thiếu | **§7.6 "thiếu quyền một doctype"** → thêm sentinel `__FORBIDDEN__` trong `src/mock-server.mjs` (mô phỏng Frappe PermissionError/403) + test khẳng định block **NULL chứ không 0** + ca falsify **O** |
| Suites | Node **524 (522 pass, 2 fail dsh từ B0)** · Flutter **248/248** · analyze **0** · Python **62** |
| Falsify | p4-ops **15/15** · p42 **15/15** · p44 **20/20** · p43 **11/11** — **0 PROBLEM** |
| Docs | `faq.md` **§10** (cách đọc màn Tóm tắt ngày) + sửa §4.3/§7 lỗi thời · `features.md` · `checklist.md` · `next.md` · `LESSONS_LEARNED.md` · skill `erpn-verify-first` (+2 hàng) · `working.md` |
| Không làm (đúng lệnh dừng) | không skill WRITE mới (P9) · không Redis · **không WRITE vào két** (§3.6 giữ READ) |

**plan4 V1 giờ KHÉP.** Việc còn lại **không phải code**.

## 2. Phát hiện thật (đọc trước khi sửa tiếp)

1. **2 anchor falsify bị TRÔI ⇒ ca không chạy nữa, mà dòng tóm tắt trông y hệt "đã pass".**
   Ca **L** trôi ở P4-5 (thêm `"company"` vào field list `appDrafts`), ca **F** trôi ở P4-4 (guard công ty
   chuyển vào `resolveReadCompany()`). Chỉ thấy được vì harness **in `anchor missing` + tính PROBLEM**.
   Đã sửa cả hai. ⇒ **Sau mọi refactor, chạy lại TOÀN BỘ harness**, không chỉ harness của tính năng đang làm.
2. **Case §7.6 tưởng đã bao phủ nhưng không**: trang rỗng ("không có hôm nay") ≠ bị từ chối ("không được xem").
   Gộp hai thứ = bịa 0 về tiền. Đây là loại case phải đi tìm **từng case một**, không suy ra từ "đã có test cho nhóm đó".
3. **⚠️ Lệnh Python trong handoff P4-6 SAI và đã được sửa.** Máy **không cài pytest**; lệnh đúng:
   `PYTHONPATH=src python3 -m unittest discover -s tests -q`. (Bài học: ghi **lệnh mình thực sự chạy**, và chạy lại nó trước khi ghi vào docs.)

## 3. Trạng thái commit — ĐỌC KỸ

- Máy này **KHÔNG có git**. Hàng chờ commit: **P4-1 → P4-6** (P4-7 chỉ đổi test/docs/harness, cùng hàng chờ).
- Vùng **đọc SỐ TIỀN** ⇒ **AI không tự ký duyệt**; user phải duyệt trước khi commit.
- ⚠️ Khi repo có git: **đừng push thô** — history không chung gốc + bản local thiếu file ⇒ push cả cây sẽ
  **XOÁ file trên branch duy nhất** (phải `--force`).
- **APK debug: user đã chốt "để sau"**.

## 4. Việc tiếp theo (đề xuất)

| Ưu tiên | Việc | Ghi chú |
|---|---|---|
| 1 | **Commit hàng chờ P4-1→P4-6** khi máy có git | Cần **user duyệt** (vùng SỐ TIỀN) |
| 2 | **B2/B3/B4 idempotency trên site THẬT** | `custom_ai_action_id` đã có 4/4 doctype ⇒ giờ mới chứng minh được; **cần user cho phép chạy ghi thật** |
| 3 | **APK debug** | Thiếu `.github/workflows` + Android SDK; `/home` ~1.5G |
| 4 | Nếu mở rộng plan4 | V1 đã khép — mọi thứ thêm (tuần/tháng, két WRITE, Redis) là **V2**, phải qua gate riêng |

## 5. Lệnh chạy lại kiểm chứng (đã chạy đúng như dưới)

```bash
# Node (BẮT BUỘC qua npm test — có đặt COPILOT_MOCK_OK)
cd mcp-erpnext && npm test                       # 524 · 522 pass · 2 fail (dsh từ B0)

# Flutter
cd apps/mobile && flutter analyze && flutter test # 0 issue · 248/248

# Python (KHÔNG dùng pytest — máy không cài)
cd ../.. && PYTHONPATH=src python3 -m unittest discover -s tests -q   # 62 OK

# Falsify — 4 harness, TẤT CẢ phải 0 PROBLEM (chạy hết, không chỉ cái mình đang làm)
cd mcp-erpnext
node scripts/falsify/p4-ops-summary.mjs          # 15/15
node scripts/falsify/p42-read-daily-summary.mjs  # 15/15
node scripts/falsify/p44-read-drill.mjs          # 20/20
node scripts/falsify/p43-drawer-summary.mjs      # 11/11
# ⚠️ Đừng Ctrl-C giữa chừng (harness đã có restore-on-signal, nhưng để chạy hết vẫn sạch hơn)
```

**Nguồn sự thật:** `next.md` + `checklist.md` · kiến trúc `.project/README.md` · bài học `LESSONS_LEARNED.md` ·
gotcha lặp lại `.agents/skills/erpn-verify-first/SKILL.md` · bằng chứng từng bước `result-p4-*.txt` + `result-p4-done.txt`.
