# Handoff — 2026-09-16 06:50 (Phiên: Phase 9 phần an toàn + sync docs)

> Đọc thêm: `result29.txt` (bằng chứng đầy đủ), `result28.txt`, `result27.txt`,
> `next.md`, `working.md`, `checklist.md`.

## 1. Mốc hiện tại

- **Phase 0–3 ✅ · Phase 5 ✅ (router + sign-off, KHÔNG scrub) · Phase 6 ✅**
- **Phase 7 Stage A ✅ commit `8ebfc0e` · Stage B KỸ THUẬT XONG, CHƯA COMMIT (vùng tiền)**
- **Phase 9 phần an toàn — XONG kỹ thuật phiên này (`result29.txt`), CHƯA COMMIT:**
  - TTL 10 phút (`proposal-freshness.mjs` `assertFresh`) → 409 `PROPOSAL_EXPIRED`
    TRƯỚC `store.begin()` ⇒ không chiếm command_id.
  - Re-validate `detectDrift()` (snapshot outstanding/invoice/customer vs dữ liệu sống)
    → 409 `PROPOSAL_STALE` + `problems[]`, KHÔNG ghi, KHÔNG clamp im lặng.
  - intentKey `(kind|customer|amount|invoice)` chặn ý định trùng đang PENDING
    (fingerprint khác số tiền ⇒ intent RIÊNG).
  - `created_at` ghim vào proposal (Node + Dart `toJson/fromJson`) — sống qua
    khôi phục history.
- Payment Entry hiện **chỉ tạo NHÁP (docstatus 0)** — submit CHƯA nằm trong scope ship;
  `erpnext_doc_submit` bị chặn ở cả đường đọc lẫn cổng write (verify bằng đọc code).
- Test: **Python 60/60 · Node 112/112 · Flutter 28/28 · analyze 0**.
- 2 phiếu nháp demo `ACC-PAY-2026-00114/00115` vẫn trên ERPNext — user tự dọn.
- Phase 4 bị chặn audio thật · Phase 8+ chưa bắt đầu.

## 2. Việc đã làm phiên này

1. Sửa test drift fail đầu phiên — nguyên nhân là **kỳ vọng test cũ** (đếm 1 problem
   trong khi detectDrift trả ĐÚNG 2 problems theo thiết kế fail-closed), không phải
   code production. Test nay khẳng định cả 2 codes — chặt hơn.
2. Hoàn thiện test: +4 Node freshness/drift, +3 HTTP (expired không chiếm store entry ·
   stale · intent PENDING trùng + intent khác tiền = intent riêng), +1 Dart
   (`created_at` sống qua restore). Falsify từng fix (gỡ → FAIL đúng chỗ → khôi phục).
3. Xác nhận wiring Phase 9 bằng grep file thật (line numbers ở result29 §3).
4. Sync docs: checklist.md · next.md (row phase 8/9 + mục "Chờ user duyệt commit" +
   "Khoảng trống Phase 7") · working.md · features.md.
5. `result29.txt` + handoff này. Secret scan CLEAN. Không ghi ERPNext thật.

## 3. Commit đang CHỜ USER DUYỆT (vùng tiền — KHÔNG tự commit)

Ba cụm độc lập (message đề xuất ở result29 §6):
- **(a)** Stage B + review vùng tiền (result25/26/27) — 10 file code/test + 3 result.
- **(b)** faq.md + 2 fix NLP/resolver (result28) — money.py, test_money.py,
  copilot-server.mjs, customer-resolve.test.mjs, faq.md.
- **(c)** Phase 9 an toàn (result29) — proposal-freshness.mjs, action-proposal.mjs,
  idempotency.mjs, payment-write.mjs, http-ask.mjs, chat_models.dart,
  proposal_card.dart, 3 test file, docs.
KHÔNG commit: `initp` (rác), `.gemini/`, `.opencode/`, `.plan/` (git-ignored).

## 4. Khoảng trống lớn chờ user quyết (result28 §3)

`buildPaymentProposal()` chỉ được test gọi ⇒ **không câu hỏi nào sinh đề xuất thu
tiền** trong luồng chat ⇒ nút [Xác nhận] chưa bao giờ hiện. Chọn: nối
`record_customer_payment` vào router (Phase 7b) hay gộp với Phase 9 UI.

## 5. Việc kế tiếp đề xuất (sau khi user duyệt)

1. Commit (a)(b)(c) + push `change/flutter-chat-mvp`.
2. Phase 9 nâng cao: saga/undo-compensation + UI hiển thị lý do `PROPOSAL_STALE`
   chi tiết trên card + persist snapshot ra store.
3. Phase 7b (nếu user chọn nối): router nhận intent "thu tiền X cho khách Y" →
   buildPaymentProposal → card HIGH → /execute.

## 6. Chờ user (không phải việc agent)

- Duyệt 3 commit · SUBMIT/xoá 2 phiếu nháp demo · khoảng trống Phase 7 (§4).
- thought_signature live Gemini (cơ hội, không ép, không probe trước).
- Zen (billing, để sau) · rotate key ERPNext (để sau) · dán 3 giá trị GitHub Settings.
- Thu audio 150 câu (docs/audio-collection-script.md) — mở khoá Phase 4.
- Test APK thiết bị thật · review .project/ai-rules.md · quyết .agents/skills/* vào repo (không).

## 7. Nguyên tắc giữ nguyên

- Vùng tiền/số/phân quyền: KHÔNG tự commit, KHÔNG tự ký duyệt.
- Verification-before-completion: mọi claim = lệnh + output thật.
- Mock trung thực với ERPNext (không dedupe giả che bug). Fail-closed.
- Không báo "xong" bằng lời. An toàn > tốc độ.
