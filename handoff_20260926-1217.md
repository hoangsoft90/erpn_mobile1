# handoff_20260926-1217.md — Bàn giao sau đợt NEXT6 Prompt-5 + Prompt-6 + PUSH + CI APK

## Đọc đầu phiên (theo thứ tự)

1. **`result75.txt`** — bằng chứng phiên này: số suite thật, transcript REAL E2E, output CI, bài học → skill.
2. `checklist.md` (mục đầu file: **PHIÊN 2026-09-26 (2) — PUSH + CI APK**) — bảng ✅/❌ từng mốc.
3. `next.md` §"Trạng thái mới nhất (2026-09-26, đợt PUSH)" — commit + việc sắp tới.
4. `.plan/next6-final-report.md` — regression gate NEXT6 (PASS 26/26 criterion) + `.plan/next6-result5.md` (Prompt-5) + `next6-smoke-pe.md` (Prompt-4).
5. `LESSONS_LEARNED.md` §"Đợt next6 (Prompt-5/6)" — bài học đã lưu.
6. Skill `.agents/skills/erpn-verify-first/SKILL.md` — 6 hàng mới cuối bảng "Đoán vs Thực tế".
7. Skill `.agents/skills/erpn-deploy-github/SKILL.md` — công thức push/CI (ĐÃ LỆCH path máy: xem dưới).

## Đã xong trong phiên 2026-09-26 (đợt này)

- **Prompt-5 (đã commit `0ef6334`)**: `dsh-gateway.mjs` 3 logEvent `dsh_ask` thêm correlation
  (`request_id`/`user_id`/`conversation_id`/`dsh_in_flight`/`outcome`/`latency_ms`); Flutter adopt
  `conversation_id` server cấp (`DshAnswer.conversationId` + `ConversationIdService.save()` + controller).
  OpenSpec change `next6-real-contract-observability` (validate valid). Node 19/19 · Flutter 17/17.
- **Prompt-6**: `.plan/next6-final-report.md` — PASS 26/26 criterion; focused 99/99 · full Node 847/845/2 ·
  analyze 0 · REAL E2E dry-run + fresh REST `ACC-PAY-2026-00749` docstatus=1.
- **PUSH**: force-push cây local (user cấm clone) → `change/flutter-chat-mvp`: `50c3aaf` (full snapshot) +
  `6bb4d76` (xoá probe). Repo `hoangsoft90/erpn_mobile1`.
- **CI APK**: run `36241021427` **SUCCESS**, artifact `erpn-chat-debug-apk` (~81,5 MB).
- **Fix blocker CI**: xoá `apps/mobile/test/_probe_review_test.dart` (probe tạm, findings đã phủ bởi
  `proposal_card_test.dart`; render ngoài ListView ⇒ overflow ~6400px ⇒ 3 fail GIẢ). Flutter 343/343.
- **Bài học → skill** `erpn-verify-first` (+6 hàng).
- Docs đồng bộ: `checklist.md` · `next.md` · `faq.md` · `features.md` · `LESSONS_LEARNED.md` · `result75.txt`.

## Trạng thái một câu

**NEXT6 (Prompt-2→6) + Prompt-5 đã code+test+commit+PUSH; CI đã build ra APK thành công — nhưng repo
Variables/Secrets ĐANG TRỐNG nên APK chưa nhúng endpoint thật (rơi về loopback mặc định).**

## Việc phiên sau phải làm (thứ tự)

1. **HỎI/CHỜ USER (vùng SỐ TIỀN — vẫn treo từ trước)**: quyết **M1/M2** refresh/retry "Hôm qua"
   (`daily_summary_screen.dart:265` + `_BlockError.onRetry` reload HÔM NAY thay vì ngày đang xem). AI KHÔNG tự sửa.
2. **Cấu hình APK thật** (nếu user muốn APK dùng được): thêm repo
   `vars: COPILOT_BASE_URL, COPILOT_AUTH_USER` + `secret: COPILOT_AUTH_PASSWORD` rồi chạy lại CI
   (dispatch `android-debug-apk.yml`) — APK hiện tại trỏ loopback.
3. **Nhắc user revoke token GitHub** đã từng lộ qua chat (`ghp_…`); token hiện dùng nằm trong `.env` (chmod 600).
4. **`erpn-deploy-github` skill ĐÃ LỆCH**: path cũ `/home/huyhoang_haiba/...` + deploy key `~/.ssh/erpn_mobile_deploy`
   **không còn tồn tại**; máy này là `/home/hoangsoft90/htdocs_apps/erpn_mobile1` và có `.git`. Cập nhật skill
   khi có dịp: (a) user chốt KHÔNG clone/merge ⇒ dùng **force-push**; (b) force-push với lịch sử không liên quan
   **KHÔNG** kích hoạt workflow path-filter ⇒ phải `workflow_dispatch`.
5. Nếu user muốn OpenSpec sạch: archive `next6-real-contract-observability` (user chưa duyệt) + cuối cùng
   archive `flutter-chat-mvp` sau khi APK chạy OK máy thật.

## Cảnh báo cho phiên sau

- **Công cụ KHÔNG khả dụng phiên này**: OCR `open-code-review` · AgentMemory · `codebase-memory-mcp` ·
  `cocoindex-code` · Simplenote MCP. Code Review chạy dạng FALLBACK (thủ công, tự đọc diff) — KHÔNG có lớp
  phát hiện tự động NPE/XSS/SQLi; bản bài học cross-project CHƯA ghi được lên Simplenote.
- **Repo local hiện có 3 commit + remote khớp**: `0ef6334` → `50c3aaf` → `6bb4d76`. Lần sau push là fast-forward
  bình thường; chỉ force khi user lại yêu cầu "không clone".
- **Đừng `git add .` mù**: `.gitignore` đã chặn `.env/.agents/.plan/.chats/.review/.gemini/.opencode/initp`;
  `checklist1.md`/`faq1.md`/`icon.png`/`query_customer.py` **cố ý KHÔNG commit** (theo handoff cũ).
- **CI Variables/Secrets = 0** — kiểm lại bằng API trước khi hứa APK có endpoint.
- `_probe_review_test.dart` đã XOÁ — đừng tạo lại probe render card ngoài ListView (overflow giả).

## Suggested skills (cho phiên sau)

- `erpn-verify-first` (bắt buộc, trước khi code + trước khi báo xong) · `erpn-deploy-github` (push/CI —
  LƯU Ý đã lệch path, xem cảnh báo trên) · `erpnext-rest-api-core` (nếu đọc ERPNext).
