# Handoff — 2026-09-19 10:05 UTC (Phiên: push đợt DSH runtime + CI build APK run #22 + verify binary + đồng bộ .project)

> Đọc thêm: `result60.txt` (bằng chứng phiên này), `result59.txt` (DSH runtime discovery),
> `result58.txt` (DSH mini-sprint), `result57.txt` (DSH end-to-end), `next.md`, `checklist.md`,
> `working.md`, `human.md`, `.project/README.md`, `handoff_20260918-0531.md` (phiên trước).

## 1. Mốc hiện tại

- **HEAD = `7476051`** (`change/flutter-chat-mvp`, đã push, cây làm việc sạch — chỉ còn rác local
  `.gemini/`, `.opencode/`, `.review/`, `initp` KHÔNG stage).
- **Ba commit của đợt**: `dc60a4e` (fix(dsh) resolver runtime — **vùng an toàn**, user đã duyệt)
  · `03338ae` (docs) · `7476051` (docs ghi run #22).
- **Suite chạy lại thật**: Python **62/62** · Node **313/313** · Router **19/19** · Flutter **150/150** · analyze **0**.
- **APK**: CI **run #22 `35435259669` success** (head `03338ae`, dispatch TAY).
  Verify binary thật: `app-debug.apk` **165.117.508 bytes**, `sha256 9da9a05f…0574f`,
  label `Nghiệp Vụ AI` (trong `AndroidManifest.xml`, string pool UTF-16LE), marker `Phân tích bằng AI` ×3 ·
  `Chat thường` ×2 · `/dsh/ask` ×4 · `dshAsk` ×3 · `ttsEnabled` ×14 · `voiceAutoSend` ×16.
- **KHÔNG đụng**: vùng tiền, ERPNext thật (không tạo/submit/cancel PE), idempotency-store, `.env`.

## 2. Việc phiên này đã làm (bằng chứng ở `result60.txt`)

1. **Phát hiện trước khi push**: workflow `android-debug-apk.yml` lọc `paths: apps/mobile/**`
   ⇒ đợt chỉ sửa backend/docs **không tự build**; phải `gh workflow run android-debug-apk.yml --ref change/flutter-chat-mvp`.
2. **Push + kích CI tay** → run #22 success; tải artifact, băm sha256, grep marker trong binary
   (bài học: grep trong zip NÉN là vô nghĩa; AXML là UTF-16LE ⇒ `iconv -f UTF-16LE | tr -d '\0'`).
3. **Đồng bộ `.project/`** (theo `.project/ai-rules.md`, KHÔNG nhân bản progress):
   `architecture.md` (route `/dsh/ask`+`/dsh/health`, module `dsh-gateway.mjs`, nhánh AI trong data flow)
   · `patterns.md` (+3: resolver in "nguồn thắng", một chỗ quyết định spawn, guard `ref.mounted`)
   · `integrations.md` (run #22, dispatch tay, cách verify APK, `DSH_MODE`, drift model 3 chỗ)
   · `modules/chat.md` (chế độ AI + 2 endpoint mới + số test mới)
   · `modules/copilot-backend.md` (dsh-gateway + endpoint + `npm run dsh:check`)
   · `state-routing.md` (mode selector = state cục bộ; sửa fence ```dart bị lặp)
   · `design-system.md` (**sửa đoạn bị lặp 2 lần** + quy ước `SegmentedButton`)
   · `openspec.md` / `overview.md` / `README.md` (HEAD `7476051`, suite mới, chế độ AI).
4. **Docs root**: gỡ marker **"CHỜ DUYỆT COMMIT"** đã lỗi thời của đợt DSH runtime ở `checklist.md`
   + `next.md` (đã commit từ phiên trước — đúng loại lỗi "marker mục nát" đã ghi trong skill);
   `faq.md` **§6.12** (push không build APK) + **§6.13** (chế độ AI cần gì) + 1 dòng §9;
   `openspec/.../tasks.md` **§6.17–6.20**.
5. **`result60.txt`** (mới) + handoff này + `working.md`.

## 3. BLOCKED_EXTERNAL (không phải lỗi code)

- `https://llm9000.loca.lt/v1/models` trả **503 Tunnel Unavailable** ⇒ không verify được DSH READ
  qua topology thật (cùng code đã PASS thật lúc 09:19 trong phiên trước: 171.800đ/4 chứng từ).
  **Người thật**: bật lại `lt` trên Mac.
- Chưa từng chạy `npm run dsh:check` **trên chính máy Mac** (agent không truy cập được) — kỳ vọng
  `source=npx-pinned` vì nhánh `npx` đứng trước `/tmp/dsh-run` trong chuỗi ưu tiên.

## 4. Việc CHỈ người thật (chi tiết ở `human.md`)

1. Cài **APK run #22** + smoke: mic · TTS · auto-send OFF/ON · **chế độ "Phân tích bằng AI"**
   (agent đã verify đường HTTP thật, **CHƯA** verify UI trên thiết bị).
2. Bật lại tunnel trên Mac (mở khoá BLOCKED_EXTERNAL ở mục 3) + `npm run dsh:check` trên Mac.
3. Quyết định còn treo: **SUBMIT** phiếu thu thật · `pauseFor: 3s` của auto-send (a/b/c) ·
   F7-2 (bật "Cho phép nộp phiếu thu thật") · rotate key ERPNext · audio 150 câu ·
   review `ai-rules.md` · duyệt app icon · saga §7 · P9 (skill mới — gate đã mở, chờ lệnh).

## 5. Ranh giới đã giữ trong phiên này

- Không tạo/submit/cancel Payment Entry nào trên ERPNext (kể cả demo).
- Không tự commit vùng tiền: commit `dc60a4e` chỉ được thực hiện **sau khi user duyệt rõ** trong chat.
- Không stage `.env` / `idempotency-store/` / `job-queue/` / `learning-log/` / `llm-router-audit/` / `.plan/`.
