========================================================================
RESULT 31 — COMMIT GỘP `eea0411` + ITEM 2 (NỐI ROUTER) + ITEM 3 (/EXECUTE/CANCEL)
            + ITEM 4 (XOÁ 2 PE DEMO) + ITEM 5 (FAQ)
Timestamp: 2026-09-16 08:26 (giờ shell)
Thứ tự thực hiện đúng result30 §7 theo chỉ đạo user 2026-09-16 (gộp 1 commit,
không tách 3; làm lần lượt item 2→3→4→5; item 4 dừng nếu không có tool thật).
========================================================================

== 1. COMMIT GỘP `eea0411` — ĐÃ COMMIT + PUSH ==
Lệnh + output thật:
  git add <37 file: code 3 cụm + result24-30 + handoff + faq + LESSONS_LEARNED
           + openspec tasks.md + docs root>
  git commit -m "feat: phase 7/9 payment write — reconcile-by-reference, ..." 
  → [change/flutter-chat-mvp eea0411] 37 files changed, 3794 insertions(+), 124 deletions(-)
  git push origin change/flutter-chat-mvp
  → 553d962..eea0411  change/flutter-chat-mvp -> change/flutter-chat-mvp
Secret scan trên staged diff: CLEAN (grep API_KEY=/API_SECRET=/ghp_/Bearer → 0 hit).
Không commit: .gemini/ · .opencode/ · initp (rác) · .env · idempotency-store (gitignored).
Body commit liệt kê đúng 3 phần (Stage B / fix money+identity / Phase 9 safety) +
tham chiếu result24-30.txt — đúng message user duyệt.

== 2. ITEM 2 — NỐI buildPaymentProposal() VÀO ROUTER (dây nút [Xác nhận]) ==
Đã làm trên 3 file: router.mjs · copilot-server.mjs · test (router + copilot E2E).

2.1. Cơ chế câu lệnh thu tiền (verify bằng probe thật, KHÔNG suy diễn):
  PYTHONPATH=src python3 -c "from vietnamese_nlp import normalize; ..."
    'thu tiền cho chị Lan 500 ngàn'    => 'payment cho chị Lan 500 ngàn' | amount 500000
    'thu tiền chị Lan 500 ngàn'        => 'payment chị Lan 500 ngàn'    | amount 500000
    'thanh toán cho Lan 1 triệu'       => 'payment cho Lan 1 triệu'     | amount 1000000
    'trả tiền cho chị Lan 500 ngàn'    => 'payment cho chị Lan 500 ngàn'| amount 500000
    'thu 500 ngàn tiền cám cho chị Lan'=> giữ nguyên (không match synonym "thu tiền")
  ⇒ synonym mapper Phase 1 biến ĐỘNG TỪ thành "payment"; câu LỆNH thu tiền
  BẮT ĐẦU bằng "payment" (anchor đầu câu).

2.2. Route mới (router.mjs):
  - group "payment_write" đặt ĐẦU ROUTES với `startsWith: true` — anchor
    SENTENCE START, không phải substring. Nguyên nhân thật: "payment" cũng
    xuất hiện GIỮA câu trong câu hỏi ĐỌC ("chưa thanh toán" → "...chưa payment";
    "đã thanh toán" → "đã payment") — substring sẽ nuốt sai các câu đó.
  - routeIntent() nhận option `startsWith` → so `raw.startsWith(kw)`.
  - factory payment_write: findCustomer (resolver chung, ID từ tool result) +
    listUnpaidInvoices (đọc có assertKnownId) + buildPaymentProposal.
  - KHÔNG yêu cầu số tiền để route: thiếu tiền ⇒ builder tạo đề xuất THU HẾT
    nợ — vẫn là thẻ HIGH phải bấm xác nhận, không bao giờ ghi ngầm.

2.3. Nhánh answerQuestion (copilot-server.mjs) cho payment_write:
  - resolveCustomer trước (khách sai/không rõ ⇒ trả reason, KHÔNG ghi).
  - buildPaymentProposal(skills, {customer, ambiguous, candidates}, {amount_vnd: nlp.amount})
  - Trả proposal create_payment_entry/HIGH + answer tiếng Việt dừng ở thẻ xác nhận.
  - Builder từ chối (khách 0 chứng từ, tên mơ hồ...) ⇒ trả `reason` + `error_code`
    (PAYMENT_NO_OPEN_INVOICE...), KHÔNG crash.
  - SỬA CHỮ CŨ: câu trả lời nhóm payment (lịch sử phiếu thu) bỏ "Ghi nhận phiếu
    thu mới là Phase 7 — Phase 2 chỉ đọc" ⇒ gợi ý đúng: 'Để ghi phiếu thu mới,
    hãy nói "thu tiền cho <tên khách> <số tiền>"'. grep xác nhận còn 0 hit.

2.4. Test + output thật:
  - Router unit (router.test.mjs +2 test): 8/8 case probe thật (payment_write ×3,
    sales chứa "chưa payment", payment chứa "đã payment", customer, inventory,
    sales thuần) — trước khi thêm anchor, 2 case đọc FAIL (bị payment_write nuốt);
    sau anchor 16/16 pass.
  - E2E MỚI (copilot.test.mjs): "thu tiền cho chị Lan 500 ngàn" qua pipeline thật
    (NLP service + router + copilot + mock ERPNext) → proposal action=
    create_payment_entry, risk HIGH, need_confirm true, entity Nguyễn Thị Lan,
    amount_vnd 500000 (từ NLP, không phải full debt), invoice SINV-0001 (nợ cũ
    nhất), có created_at (Phase 9 age gate), answer chứa "Xác nhận". 
    ⚠️ Lỗi test của tôi khi viết: assert `executable: true` — SAI với thiết kế
    (isExecutable(HIGH)=false vì HIGH cần confirm; execute đi qua /execute sau
    xác nhận). Đã sửa assertion + comment giải thích.
  - Widget test Flutter (proposal_card_test.dart +1): parse ĐÚNG JSON server trả
    về (verbatim shape từ E2E) qua ActionProposal.fromJson → find 'Xác nhận thu
    tiền' findsOneWidget + badge "🔴 Cần xác nhận" + tên chứng từ. = BẰNG CHỨNG
    nút [Xác nhận] THẬT SỰ hiện trên Flutter khi hỏi đúng câu.

2.5. FALSIFICATION item 2 (gỡ thật rồi khôi phục — bài học result27/29):
  - Router: disable anchor (startsWith_disabled → substring) → router.test 15/16
    FAIL đúng 1 test anchor; khôi phục → 16/16; grep 0 mã tạm còn lại.
  - Widget: phá `confirmable` trong proposal_card.dart (if(_confirming || true...))
    → file test FAIL (00:02 +11 -4); khôi phục → +15 All passed; analyze 0 issue.

== 3. ITEM 3 — ROUTE /execute/cancel (giải zombie PENDING result29 §10-F2) ==
Thiết kế đúng chỉ đạo: CHỈ huỷ PENDING SAU KHI reconcilePaymentEntry() (tái dùng,
không viết logic đối soát mới) xác nhận 0 chứng từ trên ERPNext.

3.1. idempotency.mjs: thêm store.cancel(commandId) — chỉ cho phép từ PENDING;
    COMPLETED/FAILED ⇒ IDEMPOTENCY_CANCEL_REFUSED (huỷ COMPLETED = che tiền đã ghi);
    ghi status CANCELLED + cancelled_ts, nhả intent lock.

3.2. http-ask.mjs: POST /execute/cancel (cùng auth/bind policy):
    - 400 command_id không phải UUID · 404 không có lệnh
    - COMPLETED ⇒ 409 + kèm result (tiền đã ghi — không huỷ qua API)
    - FAILED ⇒ 409 hướng dẫn tạo command_id mới
    - CANCELLED ⇒ 200 replay (idempotent)
    - PENDING thiếu reference_no ⇒ 409 (không đối soát được ⇒ không huỷ mù)
    - PENDING + reference: reconcile →
        found  ⇒ 409 KHÔNG huỷ + trả erpnext_doc (tiền có thật; retry /execute
                 cùng command_id sẽ replay/hoàn tất — đã test)
        lỗi đối soát ⇒ 503 (ERPNext chết — không đoán)
        0 chứng từ ⇒ store.cancel() + 200 "có thể tạo ý định mới"

3.3. Test (http-execute.test.mjs +3 test, 14/14 pass):
    ① PENDING + 0 chứng từ → 200 CANCELLED, status store = CANCELLED
    ② PENDING + ERPNext CÓ chứng từ (seed ledger mock) → 409 + erpnext_doc
      "PE-CANCEL-TEST" + store vẫn PENDING; sau đó retry /execute cùng id →
      200 replay:true reconciled:true (đường đúng thay vì huỷ)
    ③ COMPLETED/FAILED/404/thiếu reference/503-đối-soát-chết/idempotent-cancel-2-lần
    ⚠️ 2 lỗi test của tôi khi viết (đã sửa): (a) fingerprint trong seed phải là
    fingerprintProposal(PROPOSAL) thật nếu sau đó retry /execute — nếu không sẽ
    IDEMPOTENCY_FINGERPRINT_MISMATCH; (b) sau case 503 (MOCK_ERP_FAIL_LIST) phải
    delete env trước case cancel-idempotent, không thì case sau cũng 503.

3.4. FALSIFICATION item 3: disable nhánh `if (rec2.found)` (python patch
    `if (false && rec2.found)`) → 13/14 FAIL đúng test ② (cancel vẫn 200 dù
    ERPNext có chứng từ); khôi phục → 14/14; grep "FALSIFY-DISABLED" = 0.

== 4. ITEM 4 — XOÁ 2 PE DEMO (ACC-PAY-2026-00114/00115) ==
4.1. ĐỌC SOURCE TRƯỚC (bài học result4/25): grep node_modules/@casys/mcp-erpnext/
    mcp-erpnext.mjs → tool thật tồn tại:
      "erpnext_doc_delete"  {doctype, name} — destructiveHint, "Only Draft
                             documents can usually be deleted"
      "erpnext_doc_cancel"  {doctype, name} — cho document ĐÃ SUBMIT
    Hai PE demo đều docstatus 0 (NHÁP) ⇒ delete trực tiếp hợp lệ, không cần cancel.

4.2. Kiểm tra TRƯỚC khi xoá (chỉ đọc):
    ACC-PAY-2026-00114 → docstatus 0 | paid 10000 | reference a7fe0949-... | party "Khách làm tròn 2026-09-15-p1b-wf1-2"
    ACC-PAY-2026-00115 → docstatus 0 | paid 10000 | reference a7fe0949-... (cùng)
    Invoice gốc: ACC-SINV-2026-00047 | outstanding 457875 | Unpaid  (trước xoá)

4.3. Cách gọi: client.mjs CHỈ mở callTool (read whitelist) + callWriteTool
    (chỉ doc_create/PE). `erpnext_doc_delete` bị guard chặn ở CẢ 2 đường — đúng
    thiết kế (đường xoá KHÔNG được mở trong code). Vì user đã ra lệnh rõ ràng
    (item 4), tôi gọi tool qua KÊNH JSON-RPC thô của chính client (mô tả trong
    script, không sửa client.mjs, không mở lỗ hổng vĩnh viễn trong code):
      DELETE ACC-PAY-2026-00114 → OK "Payment Entry ... deleted successfully" deleted:true
      DELETE ACC-PAY-2026-00115 → OK "Payment Entry ... deleted successfully" deleted:true

4.4. VERIFY ĐỘC LẬP sau xoá:
    ACC-PAY-2026-00114 → GONE ✓ (TOOL_ERROR DoesNotExist)
    ACC-PAY-2026-00115 → GONE ✓ (TOOL_ERROR DoesNotExist)
    ACC-SINV-2026-00047 | outstanding: 457875 | status: Unpaid  (SAU xoá — GIỐNG HẾT TRƯỚC XOÁ)
    ⇒ Hóa đơn gốc KHÔNG đổi (đúng — phiếu NHÁP chưa tác động công nợ).

4.5. Dọn dẹp: xoá các file probe tạm /tmp (probe-cancel*.mjs, mock state, backup
    dùng cho falsify) — grep không còn.

== 5. ITEM 5 — FAQ CẬP NHẬT ==
- Đầu file "Ba hiểu nhầm nghiêm trọng nhất": dòng 2 đổi từ "HIỆN CHƯA có gì sinh
  ra nút [Xác nhận]" ⇒ mô tả đúng: nút chỉ hiện khi RA LỆNH ghi, câu đọc không bao giờ có.
- §3.2 viết lại: "thu tiền cho <khách> <số tiền>" → payment_write → đề xuất
  create_payment_entry/HIGH → thẻ xác nhận; câu đọc không bao giờ có nút.
- §3.3 viết lại: chữ "Phase 7 — Phase 2 chỉ đọc" đã xoá; câu trả lời mới gợi ý
  đúng cú pháp ghi tiền.
- §9 bảng: 2 hàng đầu đánh ✅ ĐÃ XONG 2026-09-16; thêm hàng route
  /execute/cancel (đường thoát cho đề xuất treo).
- §8 thêm lệnh tự kiểm 4 (curl /ask câu thu tiền + grep proposal) và 5
  (curl /execute/cancel).

== 6. SUITE CUỐI (lệnh + output thật, sau TẤT CẢ thay đổi) ==
  PYTHON  PYTHONPATH=src python3 -m unittest discover -s tests → Ran 60 tests — OK
  NODE    node --test (mcp-erpnext/) → ℹ tests 118 · pass 118 · fail 0
  FLUTTER flutter test → 00:04 +29: All tests passed!
          flutter analyze → No issues found! (ran in 6.1s)
  (So với result30: Node 112→118 (+3 cancel, +2 router, +1 E2E write-proposal),
   Flutter 28→29 (+1 widget JSON thật), Python giữ 60.)

== 7. GIT: 9 file mới/sửa CHƯA commit (vùng tiền — chờ user duyệt) ==
  M apps/mobile/test/proposal_card_test.dart   (+ widget test JSON thật)
  M faq.md                                     (item 5)
  M mcp-erpnext/src/copilot-server.mjs         (nhánh payment_write + sửa chữ cũ)
  M mcp-erpnext/src/http-ask.mjs               (route /execute/cancel)
  M mcp-erpnext/src/idempotency.mjs            (store.cancel)
  M mcp-erpnext/src/router.mjs                 (payment_write + startsWith anchor)
  M mcp-erpnext/test/copilot.test.mjs          (E2E write-proposal)
  M mcp-erpnext/test/http-execute.test.mjs     (+3 test cancel)
  M mcp-erpnext/test/router.test.mjs           (+2 test anchor)
  Còn ngoài repo (không commit): .gemini/ · .opencode/ · initp.
  Đề xuất message:
    feat: phase 7b wire — "thu tiền..." builds a HIGH proposal; /execute/cancel
    (reconcile-verified) for zombie PENDING intents
  + push change/flutter-chat-mvp.

== 8. RÀNG BUỘC ĐÃ GIỮ ==
Không tự ký duyệt vùng tiền (commit mới chờ duyệt) · tool xoá được đọc từ source
trước khi gọi · đường xoá KHÔNG được mở vào code sản phẩm (chỉ JSON-RPC thô có
kiểm soát, một lần, theo lệnh user) · verify sau xoá bằng đọc lại ERPNext ·
falsify từng fix bằng gỡ thật → FAIL đúng chỗ → khôi phục (grep 0 mã tạm) ·
SUBMIT phiếu thu: user để sau — KHÔNG submit trong phiên này.

== 11. ADDENDUM — REVIEW VÒNG 2 (2026-09-16, sau khi viết result31) ==
Review soi diff bằng git diff + probe trực tiếp answerQuestion() → tìm ra 2 vấn đề
thật trong chính đợt item 2/3 vừa làm, đã vá + test + falsify.

F1 — ANCHOR ĐẦU CÂU VẪN NUỐT CÂU ĐỌC LỊCH SỬ (mức: cao về hành vi, không nguy hiểm
     vì fail-safe):
  Bằng chứng probe (answerQuestion() chạy thật, NLP + mock ERPNext):
    'thanh toán gần nhất của chị Lan là bao nhiêu'
      → normalize: 'payment gần nhất của chị Lan là bao nhiêu'
      → routed: payment_write (SAI — là câu ĐỌC lịch sử phiếu thu)
  Nguyên nhân: synonym mapper biến cả câu ĐỌC bắt đầu bằng động từ thành câu bắt
  đầu bằng "payment"; anchor startsWith không phân biệt được câu lệnh ghi.
  Fix: `notIf` deny-list trên route payment_write (bao nhiêu/mấy/gần nhất/mới
  nhất/?) — câu hỏi lịch sử rơi xuống nhóm payment ĐỌC (substring "payment").
  Fail-safe cả 2 chiều: định tuyến nhầm write → chỉ hiện thẻ HIGH; nhầm read →
  chỉ hiện lịch sử. +1 test router (round 2, 6 assertion), falsify: disable gate
  → FAIL đúng chỗ (actual 'payment_write' / expected 'payment'), khôi phục 17/17.

F2 — store.cancel() NGOÀI try/catch TRONG ASYNC HANDLER → RỦI RO CRASH PROCESS:
  Race thật: một /execute đồng thời (double-tap) hoàn tất lệnh giữa lúc đọc status
  và lúc cancel() → cancel() throw IDEMPOTENCY_CANCEL_REFUSED → thoát khỏi async
  handler KHÔNG catch → unhandled rejection → Node ≥15 CRASH cả process.
  Bằng chứng cơ chế (Node v24.20.0 chạy thật):
    - http.createServer(async () => { throw ... }) + 1 request → exit code 1
    - grep unhandledRejection/uncaughtException trong http-ask.mjs/index.mjs → 0
    - cancel() trên record COMPLETED → throw đúng mã (node -e trực tiếp)
  Fix: bọc store.cancel() trong try/catch → 409 kèm status hiện tại thay vì crash.
  Falsify: disable try/catch + chạy suite → 14/14 vẫn pass (child-process cách ly
  che crash), nên chứng minh bằng 3 phép đo cơ chế trên thay vì suite.

F3 (nhỏ, đã sửa cùng lượt) — reason của payment_write trích `nlp.text` (đã chuẩn
  hoá, "payment cho...") gây hiểu nhầm; đổi sang `rawText` (nguyên văn người dùng
  gõ, "thu tiền cho...").

Đã kiểm và ĐÚNG thiết kế (không sửa):
- /execute: 400 action sai + 403 EXECUTABLE_ACTIONS là 2 lớp (thừa nhưng vô hại —
  defence in depth); money-shape gate CHẠY TRƯỚC store.begin() (không đốt
  command_id); assertFresh trước begin; drift check trước setReference.
- buildPaymentProposal: fail-closed đầy đủ (khách/chứng từ/amount/sai mode);
  clamp amount>outstanding chỉ là ĐỀ XUẤT (execute đọc lại nợ live + detectDrift
  chặn lệch) — đúng spec "không clamp im lặng ở đường ghi".
- /execute/cancel: thang 404/409/503 đúng; CANCELLED replay idempotent; thiếu
  reference_no ⇒ từ chối (không huỷ mù); close client trong finally.
- Widget test JSON verbatim → nút [Xác nhận] render thật (đã có từ item 2).

SUITE SAU REVIEW: Node 119/119 (+1 test round-2) · Python 60/60 (OK) ·
Flutter 29/29 · analyze 0. Probe E2E sau fix: 'thanh toán gần nhất của chị Lan
là bao nhiêu' → routed payment (đọc), proposal null ✅.

BÀI HỌC MỚI (đã thêm vào .agents/skills/erpn-verify-first):
- Falsify lần 1-2 hụt vì grep marker TAP ('not ok') trong khi node 24 mặc định
  in spec reporter ('✖' + ℹ summary) — biến thể của bài học "falsify hụt":
  marker của tool phải lấy từ output THẬT của chính nó, không từ trí nhớ.
- Cancel-race: phép ghi trạng thái terminal (cancel/complete/fail) trong async
  HTTP handler PHẢI nằm trong try/catch — store throw refused = crash process
  nếu không, vì không có global unhandledRejection handler (chủ đích fail-fast,
  nên từng handler phải tự bọc).
