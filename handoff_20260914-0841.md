# Handoff — 2026-09-14 08:41 (Phiên: result9 — accuracy thật + push GH + CI fix)

> Đọc thêm: `result9.txt` (bằng chứng đầy đủ — accuracy 3 vòng, 5 nhóm lỗi, CI log),
> `next.md`, `checklist.md`, `handoff_20260914-0427.md` (phiên trước).

## 1. Mốc hiện tại

- **Phase 2 đo accuracy THẬT** theo exit-criteria phase-02 (`mcp-erpnext/test/batch-accuracy.mjs`,
  18 câu tiếng Việt, ERPNext thật qua ngrok, expected từ ground-truth dump cùng ngày):
  **vòng 1 = 27.8% → vòng 2 = 61.1% → vòng 3 = 18/18 = 100%**. Test cuối: **40/40 node · 58/58 Python**
  (money 259/259 nguyên vẹn).
- **Push GH xong**: remote `origin = https://github.com/hoangsoft90/erpn_mobile1.git`
  (tự setup trong phiên này từ `.env` GH_REPO_URL/GH_TOKEN — repo trước đó KHÔNG có remote,
  khác mô tả prompt). Branch `change/flutter-chat-mvp` đã lên GitHub (3 commits:
  33f9dc0 → 0ac8e61 → 590b1b2).
- **CI run đầu FAILURE**: run 34823212314, step Analyze — 19 lỗi `chatControllerProvider/state/ref`
  undefined. Nguyên gốc (verify): `*.g.dart` bị `.gitignore` dòng 20 → không lên CI, local xanh
  nhờ file sinh sẵn trên disk. **Fix đã sửa trong workflow** (thêm step
  `dart run build_runner build --delete-conflicting-outputs` sau pub get) — **CHƯA commit**.
- **14 file modified + 6 untracked CHƯA commit** — chờ user duyệt (đợt fix chạm pipeline
  trả lời tiền + docs): router.mjs, copilot-server.mjs, mock-server.mjs, skills/payment.mjs,
  kinship.py, workflow yml, 4 test file, 2 batch script, next.md + 4 file memory + result8/9.
- **Rotation key ERPNext: CHƯA hiệu lực** — verify bằng lệnh: key CŨ (lộ trong chat) vẫn trong
  `.env` VÀ vẫn HTTP 200 phía server. User chốt: chạy tiếp với key cũ, rotation = chưa xong.

## 2. Quyết định mới trong phiên này

- **Ambiguous-fallback ≥ 2 từ** (copilot-server.mjs resolveCustomer): fragment 1 từ khớp
  nhiều khách → trả null + hỏi lại, KHÔNG chọn hộ (fail-safe tiền, bắt được nhờ b07 trả nhầm 457.875đ).
- **Kinship chỉ strip title ở cụm xưng hô ĐẦU câu** — title giữa câu là phần tên thật trong DB
  (`"Công trình nhà ông An"`, `"Anh Ba — xây nhà"` — có thật trong 26 khách).
- **Router: nhóm SPECIFIC (sales/payment/inventory) check TRƯỚC customer** (customer có từ
  khóa quá rộng: khách/nợ/còn lại); bỏ `"còn bao nhiêu"` khỏi inventory; `replaceAll "khách hàng"`
  trước match để không bị keyword `hàng` ăn.
- **E2E test phải hermetic**: strip `ERPNEXT_*` khi spawn (env leak từ shell persistent làm
  mock test chạm nhầm server THẬT).
- CI phải tự sinh codegen: build_runner step trong workflow.

## 3. Bài học verify-first mới (đã cập nhật `erpn-verify-first` skill)

- `pkill -f "pattern"` tự ăn mình khi pattern nằm trong cmdline chính shell đó → dùng `[.]` bracket
- NLP service nền chết giữa các tool-call block → khởi động + verify health + chạy batch
  TRONG CÙNG 1 block, không pkill không cần thiết
- HTTP 417: site ERPNext whitelist field — `currency` bị chặn trong `erpnext_payment_entry_list`
  → fallback `erpnext_doc_list` không field currency (verify REST 29 entries trước khi viết)
- Unit xanh ≠ chạy thật: 36/36 node PASS nhưng accuracy thật 27.8% — mock không phản ánh
  thứ tự route + field whitelist của site thật

## 4. Chờ user (theo thứ tự)

1. **Duyệt commit đợt fix result9** → commit (đề xuất message:
   `fix: phase 2 — real-server accuracy 27.8%→100% (router priority, name resolution, kinship, payment 417/party_type, inventory filter) + CI codegen step`)
   → push → GH Actions run 2 build APK → tải artifact `erpn-chat-debug-apk`.
2. **Rotate key ERPNext ĐÚNG cách trên server** (key cũ vẫn hợp lệ — result9 §1).
3. LLM gateway thật (OpenAI-compatible) → chỉ sửa `/tmp/dsh-home/settings.yaml`.
4. Sau APK chạy thật OK: sync + archive OpenSpec change `flutter-chat-mvp` → merge về master.

## 5. Lệnh chạy lại để xác nhận trạng thái (cho phiên sau)

```bash
cd /home/kythuat_hoangweb/htdocs_apps/erpn_mobile1
# Node suite
cd mcp-erpnext && node --test 2>&1 | tail -3          # kỳ vọng: pass 40
cd ..
# Python suite
PYTHONPATH=src python3 -m unittest discover -s tests  # kỳ vọng: OK, 58 tests
# Batch accuracy thật (cần NLP service sống ở :8787)
setsid env PYTHONPATH=src python3 -m nlp_service.server >/tmp/nlp.log 2>&1 &
sleep 1; curl -s -m 3 http://127.0.0.1:8787/health
set -a; source .env; set +a
node mcp-erpnext/test/batch-accuracy.mjs > /tmp/batch.json 2>/tmp/batch.err
python3 -c "import json; d=json.load(open('/tmp/batch.json')); print(d['accuracy_pct'], len(d['cases']))"
# GH Actions trạng thái
set -a; source .env; set +a
curl -s -H "Authorization: Bearer $GH_TOKEN" "https://api.github.com/repos/hoangsoft90/erpn_mobile1/actions/runs?per_page=3" | python3 -c "import json,sys; [print(r['id'],r['status'],r['conclusion'],r['head_sha'][:7]) for r in json.load(sys.stdin).get('workflow_runs',[])]"
```

## 6. Cạm bẫy môi trường (đừng lặp lại)

- Shell persistent giữa các block: env `ERPNEXT_*` leak sang block sau → test phải strip env
- `pkill -f` với pattern có trong chính lệnh → shell tự kill (dùng `pattern[.]` bracket)
- `git status` sạch ≠ CI sẽ xanh: `*.g.dart` gitignore → CI phải tự generate
- AgentMemory/MCP cocoindex/codebase-memory/OCR/Simplenote vẫn KHÔNG khả dụng trong env này
  → review thủ công, không tự cài lại
