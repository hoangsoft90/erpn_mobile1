# Handoff — 2026-09-16 09:44 (Phiên: result31 + review vòng 2 — dây [Xác nhận] thật + cancel route)

> Đọc thêm: `result31.txt` (§1-10 công việc chính, §11 review vòng 2), `next.md`,
> `checklist.md`, `handoff_20260916-0915.md` (phiên trước), `LESSONS_LEARNED.md`.

## 1. Mốc hiện tại

- **Commit `eea0411` ĐÃ PUSH** (`change/flutter-chat-mvp`, 37 files +3794/−124):
  Stage B + fix money/identity + Phase 9 safety + docs result24-30 + faq +
  LESSONS_LEARNED + openspec. 5 việc đã duyệt XONG.
- **Phiên result31 đã làm xong (chưa commit — 14 file chờ duyệt)**:
  1. Nối `buildPaymentProposal()` vào `routeIntent()`: nhóm `payment_write` ĐẦU
     ROUTES, anchor `startsWith` + **`notIf` deny-list**. Cơ chế thật: synonym
     mapper Phase 1 biến "thu tiền/trả tiền/thanh toán" → "payment" ĐẦU câu.
     E2E thật "thu tiền cho chị Lan 500 ngàn" → proposal `create_payment_entry`
     /HIGH + widget test JSON verbatim ⇒ nút [Xác nhận] hiện thật trên Flutter.
     Thiếu tiền ⇒ đề xuất THU HẾT nợ (vẫn HIGH). Chữ cũ "Phase 2 chỉ đọc" đã xoá.
  2. Route `/execute/cancel` (giải zombie PENDING): chỉ huỷ PENDING sau khi
     `reconcilePaymentEntry()` xác nhận 0 chứng từ; COMPLETED ⇒ 409 + result;
     ERPNext chết ⇒ 503; CANCELLED replay idempotent; thiếu reference ⇒ 409.
  3. Xoá 2 PE demo `ACC-PAY-2026-00114/00115` (tool thật `erpnext_doc_delete`
     đọc từ source; gọi JSON-RPC thô 1 lần theo lệnh user; verify hóa đơn gốc
     `ACC-SINV-2026-00047` outstanding 457.875 Unpaid không đổi).
  4. faq.md cập nhật (đầu-file + §3.1/3.2/3.3/§9/§8).

## 2. Review vòng 2 (result31 §11) — 2 lỗi thật + 1 nhỏ, ĐÃ VÁ + FALSIFY

- **F1 (hành vi)**: anchor `startsWith` vẫn nuốt câu ĐỌC lịch sử bắt đầu bằng
  động từ synonym — probe thật: `'thanh toán gần nhất của chị Lan là bao nhiêu'`
  → normalize `'payment gần nhất...'` → routed **payment_write** (SAI).
  Fix: `notIf` deny-list (bao nhiêu/mấy/gần nhất/mới nhất/?) → câu hỏi rơi về
  nhóm payment ĐỌC. +1 test round 2 (6 assertion); falsify đạt: gỡ gate → FAIL
  đúng assertion (`actual payment_write / expected payment`), khôi phục 17/17.
- **F2 (crash)**: `store.cancel()` NGOÀI try/catch trong async handler — race
  với `/execute` đồng thời (double-tap) hoàn tất lệnh giữa lúc đọc status và
  cancel ⇒ throw `IDEMPOTENCY_CANCEL_REFUSED` uncaught ⇒ **Node ≥15 crash cả
  process**. Suite unit KHÔNG bắt được (child-process cách ly che crash) — chứng
  minh bằng 3 phép đo cơ chế: async handler throw → exit 1; grep 0 global
  handler; cancel(COMPLETED) throw trực tiếp. Fix: bọc try/catch → 409 kèm status.
- **F3 (nhỏ)**: reason dùng `rawText` (nguyên văn "thu tiền cho...") thay vì
  `nlp.text` (đã chuẩn hoá "payment cho...").
- Suite sau review: **Node 119/119 (+1) · Python 60/60 · Flutter 29/29 · analyze 0**.

## 3. CHỜ USER DUYỆT COMMIT (vùng tiền — KHÔNG tự commit)

14 file: `router.mjs` · `router.test.mjs` · `copilot-server.mjs` · `http-ask.mjs`
· `idempotency.mjs` · `faq.md` · `copilot.test.mjs` · `http-execute.test.mjs` ·
`proposal_card_test.dart` · `checklist/working/next/features.md` · `result31.txt`.
Message đề xuất (result31 §7):
```
feat: phase 7b wire — "thu tiền..." builds a HIGH proposal; /execute/cancel
(reconcile-verified) for zombie PENDING intents
```
Không commit: `.gemini/` · `.opencode/` · `initp` (rác) · `.env` ·
`idempotency-store/` · `.plan/`.

## 4. Việc kế tiếp kỹ thuật (khi được giao, sau khi commit được duyệt)

- Flutter: card hiển thị lý do `PROPOSAL_STALE` chi tiết (problems[]) — nâng cao.
- Saga/undo-compensation Phase 9 nâng cao (undo KHÔNG phải `delete document`).
- `E2E_LLM_MODEL=real-gemini` — verify thought_signature live (thử cơ hội, không
  probe trước, chỉ báo khi kết quả khác 429/503).

## 5. Vẫn treo — KHÔNG phải việc agent

SUBMIT phiếu thu (user quyết riêng) · rotate key ERPNext · dán 3 giá trị GitHub
Settings · thu audio 150 câu (`docs/audio-collection-script.md`) · test APK
thiết bị thật · review `.project/ai-rules.md`.

## 6. Bài học mới phiên này (đã vào skill + LESSONS_LEARNED)

- **Falsify-hụt biến thể mới**: grep marker TAP `not ok` trong khi Node 24 in
  spec reporter `✖` + `ℹ summary` — marker phải lấy từ output thật của tool.
- **Cancel-race**: store-mutation trạng thái terminal trong async HTTP handler
  PHẢI bọc try/catch (không có global handler = fail-fast chủ đích); suite pass
  ≠ không crash — kiểm exit code process thật.
- Route 2 chiều (ghi vs đọc) cần deny-list tường minh, anchor một chiều là chưa đủ.
