# Handoff — 2026-09-14 10:23 (Phiên: endpoint security + APK installable + docs sync)

> Đọc thêm: `result10.txt` (CI run #2 + APK artifact) · `result11.txt` (bind policy +
> verify thật + firewall finding + audio script) · `result12.txt` (docs sync + ai-rules)
> · `next.md` · `checklist.md` · `handoff_20260914-0841.md` (phiên trước).

## 1. Mốc hiện tại

- **6 commits trên GitHub** (branch `change/flutter-chat-mvp`):
  `33f9dc0` (Phase 1) → `0ac8e61` (Phase 2 real) → `590b1b2` (Phase 3 Flutter) →
  `119edd4` (fix accuracy 27.8→100% + CI codegen) → `4c5bd26` (docs rewrite) →
  `2946206` (result10 + sign-off draft) → `c3d74c3` (endpoint security + audio script)
  → `c9bc64d` (result11). CI **run #2 + #3 đều SUCCESS**.
- **Endpoint /ask có bảo mật** (`http-ask.mjs`): non-loopback bind BẮT BUỘC basic auth
  (server tự từ chối khởi động nếu thiếu); public interface cần thêm `ASK_ALLOW_PUBLIC=1`;
  Flutter client gửi auth qua `--dart-define`. Service đang chạy trên VPS (0.0.0.0:8788,
  credentials trong `.env`: ASK_USER/ASK_PASSWORD — KHÔNG có trong docs/repo).
- **Verify thật:** 401 không auth → 200 có auth → POST /ask trả 269.000đ đúng qua
  ERPNext thật (interface 10.88.0.4).
- **2 finding môi trường (không phải lỗi code):** ① port 8788 bị cloud firewall hosting
  chặn từ internet (máy là container 10.88.0.4, public IP là NAT) ② GH token chỉ-đọc
  (GET variables 200, PUT 404).
- **Kịch bản thu âm 150 câu** sẵn sàng: `docs/audio-collection-script.md` (7 nhóm A–G
  có ground truth, tên khách/vật tư THẬT từ ERPNext test).
- **`.project/ai-rules.md` MỚI TẠO** — file user nhắc tới không tồn tại; nội dung tổng
  hợp từ AGENTS.md/operating_rules/result1→11; **CHỜ USER REVIEW** trước khi coi là
  nguồn quy tắc số 1.
- **Gate pháp lý Phase 5: ĐÓNG.** `SIGNOFF-phase5-pii.md` chờ ký. KHÔNG code LLM
  Router/PII scrubbing cho tới khi có chữ ký (user nhắc lại 2 lần — gate cố ý).

## 2. Test hiện tại (chuẩn so sánh cho phiên sau)

```
node --test (mcp-erpnext)  → 48/48 pass  (40 cũ + 8 test auth mới)
Python unittest            → 58/58 OK   (money 259/259 nguyên vẹn)
flutter analyze            → No issues found
flutter test               → 13/13 pass
```

## 3. Chờ user (thứ tự; KHÔNG phải việc agent)

1. **Dán 3 giá trị GitHub Settings** → CI build APK cài được:
   - Variables: `COPILOT_BASE_URL=http://35.194.130.120:8788` · `COPILOT_AUTH_USER`
     (giá trị `ASK_USER` trong `.env` — hiện `copilot`)
   - Secret: `COPILOT_AUTH_PASSWORD` (giá trị `ASK_PASSWORD` trong `.env`)
2. **Chọn đường endpoint** (3 lựa chọn, result11 §5): Tailscale login trên VPS
   (khuyến nghị — PII không đi internet) / yêu cầu hosting mở port 8788 / ngrok từ VPS
3. **Cài APK lên thiết bị thật + test tại điểm bán** (artifact CI hoặc
   `/home/kythuat_hoangweb/erpn-apk/`)
4. **Ký `SIGNOFF-phase5-pii.md`** → mở gate Phase 5
5. **Rotate key ERPNext** (Hoàng, trên server — key cũ vẫn hợp lệ)
6. **Thu audio 150 câu** theo `docs/audio-collection-script.md` (mở khóa Phase 4)
7. **Review `.project/ai-rules.md`** (mới tạo)

## 4. Việc agent làm kế tiếp (khi điều kiện mở)

- Sau user dán variables → re-run CI (workflow_dispatch hoặc push rỗng) → verify APK
  artifact chứa đúng dart-define
- Sau user chọn Tailscale: đổi bind sang IP 100.x, xóa ASK_ALLOW_PUBLIC khỏi `.env`,
  hướng dẫn cài Tailscale trên điện thoại
- Sau APK chạy thật OK: `/opsx:verify` → sync + archive OpenSpec `flutter-chat-mvp`
  → merge branch về master
- Sau chữ ký sign-off: MỚI bắt đầu Phase 5 theo phase-05 (PII scrub + LLM Router
  config-driven + audit log; 2–3 tuần)

## 5. Cạm bẫy cần nhớ (đã vào skill `erpn-verify-first`)

- Hairpin NAT: curl tới public IP từ chính VPS luôn 000 — verify public bằng request
  từ ngoài
- `curl -m` exit 28 đứt chuỗi `&&` — chuỗi diagnostic dùng `;`
- Token GH: probe write bằng 1 PUT vô hại trước khi lên kế hoạch dựa trên API write
- Pure function nhận env qua tham số → mọi nhánh dùng tham số đó (không lẫn process.env)
- `.env` có thể được sửa ngoài session (mtime mâu thuẫn) — luôn grep giá trị cũ để
  verify rotation/thay đổi
