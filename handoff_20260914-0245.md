# Handoff — 2026-09-14 02:45 (Phiên: ERPNext thật + dsh smoke + commit #1)

> Đọc thêm: `result6.txt` (bằng chứng đầy đủ), `next.md`, `checklist.md`,
> `handoff_20260914-0155.md` (phiên trước).

## 1. Mốc hiện tại

- **Commit #1 = `33f9dc0`** (root commit, 55 files +6390): Phase 1 NLP + Phase 2
  skill layer + cầu nối + copilot MCP server + target-switch. `.env` KHÔNG
  trong commit (verified `git check-ignore` trước staging; grep secrets chỉ
  có trong `.env`).
- **ERPNext THẬT đã nối và đã query ra dữ liệu thật**: `.env` (mode 600,
  git-ignored qua .gitignore:47) — URL ngrok
  `prevail-pantyhose-overvalue.ngrok-free.dev`. Server 3.0.4 thật trả
  **125 tools**; customers/invoices thật (đều là dữ liệu smoke test của user).
- **dsh thật (0.1.5-rc.1) chạy headless end-to-end**:
  dsh → mock LLM (OpenAI-compatible) → `mcp__erpn_copilot__copilot_ask` →
  HTTP normalize → REAL ERPNext → *"Khách smoke 2026-09-13-p1done còn nợ
  269.000đ (3 hóa đơn chưa trả)"* — khớp đúng 3 hóa đơn thật (90k+89k+90k;
  hóa đơn outstanding=0 bị loại đúng).
- **Test: 34/34 node --test** (30 cũ + 5 target-switch + 4 resolve trừ đi
  5 cũ được thay bằng customer-resolve: net 34) · Python 58/58 PASS ·
  money 259/259 = 100% (không đụng).
- Chưa commit (chờ chạy xong phiên): result6.txt, scripts/mock-llm.mjs,
  customer-resolve.test.mjs, sửa copilot-server.mjs (resolveCustomer),
  patch env pass-through, docs. Đề xuất commit #2: `feat: phase 2 — real
  ERPNext target switch + dsh smoke tooling (mock LLM, resolver guard)`.

## 2. Quyết định user trong phiên (2026-09-14)

1. dsh + LLM backend: **"smock tạm"** → dsh cài SCRATCH ở `/tmp/dsh-run`
   (`npm i @deepseek-ai/dsh`, không global, mất khi reboot — cài lại 1 lệnh);
   LLM = `scripts/mock-llm.mjs` (OpenAI-compatible, deterministic, không
   dependency). Sau này **swap gateway thật = chỉ sửa
   `/tmp/dsh-home/settings.yaml`** (baseURL + apiKeyEnv), không đụng code.
2. Commit: user duyệt "thấy ổn và ko cần thay đổi gì thì commit" → commit #1
   đã tạo đúng message/scope đã duyệt trước đó.
3. ERPNext credentials: user cung cấp trong chat → đã lưu `.env` +
   chmod 600. ⚠️ **Cần nhắc user ROTATE key** (đã đi qua plain chat).

## 3. Kiến thức kỹ thuật quan trọng (đọc kỹ trước khi đụng dsh/MCP)

- **dsh đăng ký MCP tools với namespace**: `mcp__erpn_copilot__copilot_ask`,
  KHÔNG phải tên trần. Mock LLM phải resolve tên từ mảng `tools` của chính
  request (đã làm: exact → suffix → first).
- **dsh inject nội dung workspace thành user-role `<system-reminder>`**:
  AGENTS.md của repo (43k chars!), runtime context, skill catalog — tất cả
  đều role=user SAU task thật. Bài học: lấy user message đầu tiên KHÔNG phải
  reminder làm task. (Cũng là bằng chứng AGENTS.md được dsh đọc.)
- **session-title-llm gọi LLM không có tools** mỗi session — mock phải trả
  text thường cho turn không-tools.
- **Config dsh**: `$DSH_HOME/settings.yaml` shape `llm-pi-ai.providers.<id>
  { apiKeyEnv, api: openai-completions, baseURL, models[] }` +
  `agent-default-model` (từ docs providers.md chính thức, đã curl lấy bytes).
  `dsh --profile headless "task"` = chạy 1 task in kết quả thoát; `dsh web
  --patch <file> --no-open` = Web UI. `DSH_TELEMETRY_MODE=DISABLED` hợp lệ
  (mặc định dsh gửi telemetry ra ngoài — không ship session logs).
- **Env contract của server 3.0.4 thật**: đọc LAZY 3 biến `ERPNEXT_*`, auth
  header `token key:secret` — xác nhận từ source bundle, không đoán.
- **resolveCustomer (mới)**: ưu tiên candidate khớp ĐÚNG 1 khách
  (longest-prefix-first); khi trùng tên ("Khách smoke ...-p1done/p1final/
  postfix" đều chứa "Khách smoke") câu trả lời phải mang cảnh báo, không
  im lặng lấy kết quả đầu — đây là lỗi có thể ra SAI TIỀN.

## 4. Cách chạy lại smoke (sau reboot)

```bash
cd /tmp/dsh-run && npm i @deepseek-ai/dsh        # nếu /tmp bị mất
cd ~/htdocs_apps/erpn_mobile1
set -a; . ./.env; set +a                          # ERPNEXT_* thật
export DSH_HOME=/tmp/dsh-home DSH_TELEMETRY_MODE=DISABLED MOCK_LLM_API_KEY=dummy
python3 -m nlp_service.server &                   # :8787
node scripts/mock-llm.mjs &                       # :8899
node /tmp/dsh-run/node_modules/@deepseek-ai/dsh/lib/bin.js \
  --profile headless --patch mcp-erpnext/dsh.cordis.patch.yml \
  "Khách smoke 2026-09-13-p1done còn nợ bao nhiêu"
# → "Khách smoke 2026-09-13-p1done còn nợ 269.000đ (3 hóa đơn chưa trả)."
```

## 5. Việc tiếp theo

1. **Commit #2** (sau khi user xem): mcp-erpnext (env-switch + resolver +
   patch env pass-through), mock-llm, customer-resolve.test, result6,
   docs. KHÔNG commit `.env` (git-ignored, đã check).
2. **Rotate ERPNext key/secret** — nhắc user.
3. **Swap mock LLM → gateway thật** khi user cấp (sửa settings.yaml thôi).
4. **dsh Web UI leg**: `dsh web --patch ... --no-open` đã sẵn sàng — cần
   browser/cổng truy cập từ phía user.
5. **Phase 3 Flutter** — giờ có thể gọi thẳng `nlp_service` HTTP; Flutter
   client track vẫn chưa có phase riêng (chặn phase-04/15).

## 6. Cố ý CHƯA làm

- KHÔNG commit `.env` hay đưa secret vào file nào được track.
- KHÔNG cài dsh global / không đụng npm -g.
- KHÔNG đổi message/cấu trúc commit #1 (đã duyệt).
- KHÔNG làm STT/entity resolution/write/Flutter (phase 3/4/6/7).
- KHÔNG bật telemetry dsh (mặc định của nó gửi ra ngoài).

## 7. Môi trường

AgentMemory down (retry xong, xuống theo rule) · MCP cocoindex-code/
codebase-memory-mcp không expose · OCR không chạy · Simplenote MCP không có
→ code review thủ công. Git identity vẫn là placeholder "Your Name
<you@example.com>" — KHÔNG tự sửa git config (rule); user có thể muốn sửa
và amend.
