# Handoff — 2026-09-14 01:55 (Phiên: dsh E2E wiring)

> File này tóm tắt phiên cho agent sau. Đọc thêm: `result5.txt` (bằng chứng đầy đủ),
> `next.md`, `checklist.md`, `handoff_20260913-1502.md` (phiên trước).

## 1. Mốc hiện tại

- **Đã xong trong phiên này:** cầu nối dsh ↔ Phase 1 ↔ Phase 2, chạy được end-to-end
  **không cần ERPNext credential, không cần LLM**:
  - `nlp_service/server.py` — HTTP bridge Python (`normalize()` trên 127.0.0.1, stdlib)
  - `mcp-erpnext/src/copilot-server.mjs` — MCP server chuẩn stdio, 1 tool `copilot_ask`
  - `mcp-erpnext/dsh.cordis.patch.yml` — file đăng ký cho dsh (format example chính thức)
  - `scripts/ask-copilot.mjs` — driver transcript JSON-RPC nguyên văn
- **Test:** 25/25 `node --test` PASS (6 E2E spawn thật 3 process) · Python 58/58 PASS · money 259/259 = 100%
- **Transcript user yêu cầu (nguyên văn, xem result5.txt §3):**
  `"chị Lan còn nợ bao nhiêu"` → `"Nguyễn Thị Lan còn nợ 2.500.000đ (1 hóa đơn chưa trả)."`
- **Repo vẫn 0 commit** — user duyệt commit RIÊNG, không chặn việc code.

## 2. Phát hiện quan trọng của phiên (đừng lặp lại sai)

1. **dsh CHƯA được cài trên máy này** (`which dsh` trống, không có npm global, không có
   ~/bin). User nói "đã cài" — không đúng máy này. KHÔNG giả định nó có; kiểm tra trước.
2. **Máy không có LLM backend nào:** không ollama/llamafile; DEEPSEEK/OPENAI/GEMINI/
   ANTHROPIC/OPENROUTER đều unset (chỉ kiểm tên biến, không in giá trị) → chân
   "gõ câu trong dsh Web UI" bị chặn bởi 2 thứ: cài dsh + cấp LLM backend.
3. **read_url làm mất indentation của YAML** — nếu tin kết quả read_url, patch file dsh
   sẽ SAI CẤP THỤT. Phải `curl | cat -A` để lấy byte chính xác từ example chính thức
   (`- insert:` → list → `{id, name, config}`).
4. **Hai lỗi thật do test E2E bắt được** (không phải đọc code): (a) route payment/sales
   rơi vào nhánh balance → sai shape; (b) factory `sales`/`payment` trong `router.mjs`
   không export `findCustomer` → "chị Lan đã trả..." die. Đã sửa ở router: findCustomer
   chia sẻ cho sales/payment (code của customer skill, không nhân bản).

## 3. Hợp đồng an toàn hiện có (KHÔNG được phá)

- `money.py` (code chạm tiền): guard `ambiguous_tail` · cờ `has_nonzero` · allowlist
  `NICKNAMES` · shape guard SĐT/năm/định danh/`củ cải`-`chai nước` · lookahead `(?!\d)`
  cho num/shorthand — mỗi cái ứng với 1 bug thật đã trả số tiền SAI im lặng.
- `readonly-guard.mjs`: whitelist 12 tool `erpnext_*` + verb guard + `readOnlyHint`
  cross-check + `assertKnownId` (AI không được bịa ID) + `markUntrusted`.
- `copilot-server.mjs`: không route / không tìm thấy khách → `answer: null` + reason
  (KHÔNG đoán); con số trong câu trả lời COPY từ dữ liệu ERPNext, không chỗ nào tự tính;
  Phase 2 chỉ đọc — câu trả lời payment luôn nhắc "ghi nhận phiếu thu là Phase 7".
- `nlp_service/server.py`: bind 127.0.0.1 CHỈ (khóa trong code), không leak traceback.

## 4. Cách chạy (để verify lại ngay)

```bash
python3 -m nlp_service.server &                                  # bridge :8787
node scripts/ask-copilot.mjs "chị Lan còn nợ bao nhiêu"          # → 2.500.000đ
cd mcp-erpnext && npm test                                       # 25/25 PASS
PYTHONPATH=src python3 -m unittest discover -s tests             # 58/58 PASS
# khi dsh đã cài:
dsh web --patch mcp-erpnext/dsh.cordis.patch.yml                 # tool: mcp__erpn_copilot__copilot_ask
```

## 5. Việc tiếp theo (thứ tự)

1. **Commit Phase 1 + Phase 2 (chờ user OK riêng, KHÔNG tự commit).** Kế hoạch: 1 commit
   `feat: phase 1 — Vietnamese NLP pipeline (money/kinship/synonyms/quantity)` — add
   `src/ tests/ pyproject.toml README.md .gitignore result*.txt checklist.md features.md
   next.md handoff_*.md AGENTS.md openspec/ mcp-erpnext/ nlp_service/ scripts/`
   (trừ `node_modules`); KHÔNG add `.plan/` (gitignore), `.gemini/`, `.opencode/`, `initp`.
2. **ERPNext thật:** cần user cung cấp `ERPNEXT_URL` + `ERPNEXT_API_KEY` + `ERPNEXT_API_SECRET`
   → chỉ đổi `serverScript` mà `createMcpClient` spawn, logic giữ nguyên.
3. **dsh Web UI leg:** user cài dsh (`npx @deepseek-ai/dsh web`) + cấp LLM backend →
   chạy với patch file trên, gõ câu thật, ghi thêm transcript vào result6.
4. **Phase 3 Flutter** — giờ có thể gọi thẳng `nlp_service` HTTP; nhưng Flutter client
   track vẫn chưa có phase riêng (khoảng trống lớn nhất, chặn phase-04/15).

## 6. Cố ý CHƯA làm (đừng "tiện tay" làm)

- KHÔNG tự cài dsh / KHÔNG tự đăng ký API key LLM nào (cần user quyết).
- KHÔNG nối ERPNext thật (chưa có credential) — không xóa mock.
- KHÔNG làm STT, entity resolution, write nào (Phase 4/6/7).
- KHÔNG port NLP sang Dart/JS (quyết định cầu nối HTTP đã khóa).
- KHÔNG thêm Flask/FastAPI dependency — bridge dùng stdlib đủ (Flask có cài nhưng không cần).

## 7. Môi trường (lặp lại để agent sau không ngạc nhiên)

AgentMemory down · MCP cocoindex-code / codebase-memory-mcp không expose · OCR không
chạy được · Simplenote MCP không có → Code review thủ công; `git status` 0 commit,
~24 untracked items; `.plan/` bị gitignore.
