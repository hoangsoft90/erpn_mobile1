# Handoff — 2026-09-21 01:15 UTC (Phiên: phases3 Trụ B/C — C2 camera→proposal + review vòng 2 + đồng bộ docs)

> Đọc thêm: `result62.txt` (bằng chứng phiên này) · `.plan/phases3/C2-result.md` +
> `B0/B1/B2/B3/B4/C0/C1-result.md` · `next.md` · `checklist.md` · `working.md` ·
> `LESSONS_LEARNED.md` · `.agents/skills/erpn-verify-first/SKILL.md` · `human.md` ·
> `.project/README.md` · `handoff_20260919-1005.md` (phiên trước).

## 1. Mốc hiện tại

- **Máy này KHÔNG có git** (`git rev-parse` → fatal "not a git repository") ⇒ toàn bộ đợt
  phases3 (B0→B4, C0→C2) **KỸ THUẬT XONG nhưng CHƯA COMMIT**. Khi có repo: hàng chờ commit
  là `B1→B2→B3→B4→C0→C1→C2` (mỗi phase đã test + falsify độc lập).
- **Trạng thái duyệt:** C0 ✅ ĐÃ DUYỆT · C1 ✅ ĐÃ DUYỆT · **C2 CHỜ DUYỆT** (vùng ghi + ranh
  giới tin cậy — AI không tự ký duyệt).
- **Suite chạy lại thật (sau C2 + vá):** Python **62 OK** · Node **440 (438 pass, 2 fail
  `dsh-gateway` cần runtime dsh — đỏ từ B0, gap môi trường)** · Flutter **211 pass / 1 fail
  = probe P2b cũ (`test/_probe_review_test.dart`, harness tạm)** · analyze **0** ·
  golden **7/7 (231 ca)** · golden `o01–o08` / `q01–q08` OK.
- **Falsify in-repo:** `scripts/falsify/c0-ocr.mjs` 9/9 · `c1-ocr.mjs` 9/9 · **`c2-ocr.mjs`
  10/10 RED** (5 guard Node + 4 guard Dart). B2/B3 để harness ở `/tmp` **đã mục nát** — đã ghi
  thành bài học.
- **Mọi đường ghi hiện MOCK-ONLY** — chưa ghi ERPNext thật. Chỉ chạy thật khi user ghi
  "cho phép ERPNext thật".

## 2. Việc phiên này đã làm (bằng chứng ở `result62.txt` + `C2-result.md`)

1. **§0 đo TRƯỚC khi code C2** (đường thật Phase 1 + `routeIntent`): mọi ảnh chụp
   (`HÓA ĐƠN`/`PHIẾU THU`/output mock C1) rơi vào đường **ĐỌC** — ảnh **không có động từ
   mệnh lệnh** ⇒ quyết định thiết kế: **kind do USER chọn** trên sheet, không để OCR đoán.
   User chốt qua ask_user: (a) user chọn loại chứng từ; (b) form điền sẵn sửa trước, card
   confirm sau.
2. **C2 code**: `ocr_policy.document_kinds` (contract validate fail-closed) ·
   `src/ocr/ocr-slots.mjs` (slots + provenance gate 4 lớp + `resolvePartyFromPhoto` chặt hơn
   chat) · `src/line-parse.mjs` (nhà chung PURE cho builder + OCR) · route `POST /ocr/slots`
   (không trả `proposal/action/command_id`) · Flutter `OcrSlotsForm` → gửi câu qua `/ask` =
   user's own turn → card confirm CŨ (không đường ghi thứ hai).
3. **Review vòng 2 tìm 1 LỖ THẬT đã vá**: `/ocr/slots` nhận `body.text` từ client **không bị
   chặn độ dài** (trần 5000 chỉ sống ở `/ocr` nơi sản xuất bản đọc; comment còn khẳng định sai
   "đã sanitize ở C0 seam") ⇒ client đã auth đẩy ~1 MB text vào NLP + resolver O(n²). Vá
   `assertSlotsText()` tại route → 400 `OCR_READ_TOO_LONG` (trần `ocr_policy.max_text_length`)
   + test 5001 ký tự + falsify guard.
4. **Đồng bộ docs (phiên này)**: `working.md` · `checklist.md` · `next.md` · `features.md`
   (mục Trụ B/C) · `faq.md` (§6.14 camera/OCR + §7) · `LESSONS_LEARNED.md` (nhóm 3/6/16 + mục
   theo phiên) · `openspec/changes/flutter-chat-mvp/tasks.md` §6.21 · skill `erpn-verify-first`
   · `result62.txt` · handoff này.

## 3. ⚠️ Đính chính quan trọng (đã sửa)

Bản `result62.txt` §7 TRƯỚC đó khai đã cập nhật `features.md` / `faq.md` /
`LESSONS_LEARNED.md` / `handoff_20260920-1739.md`. **Kiểm lại bằng grep = 0 hit** và file
handoff đó **không tồn tại** — phiên trước bị cắt giữa chừng nên claim chưa thành sự thật.
Đã: (a) thực hiện các edit đó trong phiên này; (b) sửa §7 thành trạng thái thật; (c) ghi thành
bài học **nhóm 6** ("claim docs đã update trong file result nhưng edit chưa vào file").

## 4. Việc tiếp theo (chờ user quyết)

1. **Duyệt C2** (vùng ghi + ranh giới tin cậy) → mới xét commit/handoff kế.
2. **Chạy thật (khi user cho phép)**: (a) verify 4 write trên site thật sau khi **migration
   `custom_ai_action_id` cho SO + Quotation** đã chạy (script `scripts/add-correlation-field.mjs`
   đã soạn, derive từ contract — **KHÔNG tự tạo field**); (b) C-verify camera trên ERPNext thật.
3. **Gap đã biết, chưa sửa**: (a) `test/_probe_review_test.dart` (P2b) — user chọn xoá/chuyển
   thành test thật; (b) 4 fail `dsh-gateway` — cần runtime dsh (`npm i` ở root, KHÔNG tự cài);
   (c) 3/4 cách nói `lên/tạo/chốt đơn` chưa tới `sales_order.create` (synonyms.py Phase 1 —
   việc của Phase 1, chờ user); (d) `Tấn→Bao` chưa có hệ số (gap DỮ LIỆU ERPNext).
