# handoff — 2026-09-21 08:10 · phiên P4-3 (drawer + màn `Tóm tắt ngày`)

Phiên này nối tiếp phiên P4-2 (`handoff_20260921-0709.md`) trong cùng ngày. Deliverable:
**`result-p4-3.txt`**. Trạng thái: **kỹ thuật xong, chờ user duyệt commit** (vùng đọc SỐ TIỀN ⇒ AI không tự ký duyệt).

> **Cập nhật cuối phiên (user đã quyết)**: (1) **đã XOÁ** probe P2b ⇒ suite Flutter **233/233**; (2) **đã TẠO `custom_ai_action_id` trên site THẬT** cho SO/QT/PO (4/4 doctype, verify độc lập 3 lớp + control âm + idempotent) ⇒ vòng lặp thật nay **`partial=false`**, cross-check REST **488.040.625 / 281 HĐ** khớp; (3) **APK: user chốt "để sau"** ⇒ không push/không `git init`. Chi tiết: `result-p4-3.txt` §7 + `LESSONS_LEARNED.md` **nhóm 26/27** + skill `erpn-verify-first` **+3 hàng**.

## 1. Máy này KHÔNG có git (kiểm lại đầu phiên sau)

`git rev-parse --is-inside-work-tree` → `fatal: not a git repository`. Hệ quả: **chưa commit được gì**
(P4-1, P4-2, P4-3 đều đang chờ), **OCR không chạy được** (không có diff ⇒ review mặc định),
`detect_changes` của codebase-memory-mcp không dùng được (dùng `grep` + đọc file thay thế).
Ngoài ra: **AgentMemory DOWN** (`curl http://localhost:3111/agentmemory/health` → `http=000`) và
**MCP Simplenote không có trong tool của phiên** ⇒ phiên này không tra/lưu memory ngoài repo
(nội dung đã ghi hết vào `LESSONS_LEARNED.md` + skill `erpn-verify-first`).

## 2. Đã làm trong phiên

- **Flutter** (25 widget/unit test mới): `lib/features/ops/`
  - `data/daily_summary_models.dart` — parse §4.3, **block nullable** (unread ≠ 0)
  - `data/daily_summary_cache.dart` — cache SharedPreferences + cửa sổ **45s** (`AppConstants.summaryCacheWindow`)
  - `presentation/screens/daily_summary_screen.dart` — đủ 5 trạng thái §6 + pull-to-refresh + footer
  - `presentation/widgets/app_drawer.dart` — drawer §2 (`Tóm tắt ngày` **đầu tiên + "Mặc định"**, mục chưa làm = "Sắp có (bước P4-4/P4-5)")
  - `presentation/widgets/summary_status_footer.dart` — REAL|MOCK + URL + giờ (dùng ở cả drawer và screen)
  - `CopilotApiClient.dailySummary({date})` · route `/summary` trong `app_router.dart` · `Scaffold.drawer` ở `chat_screen.dart` · provider `dailySummaryCacheProvider`
- **Server (mảnh nhỏ, bắt buộc bởi §2)**: `erpTargetLabel(env)` trong `copilot-server.mjs` → route
  truyền vào `getDailySummary` → `meta.erp_target`; **dòng log khởi động dùng chính helper đó**
  (⇒ log và footer không thể lệch nhau). Không ai nói ⇒ `null`.
- **Test/falsify**: `apps/mobile/test/daily_summary_test.dart` **25/25** ·
  `mcp-erpnext/scripts/falsify/p43-drawer-summary.mjs` **11/11 RED** (7 Dart + 3 Node).
- **Docs**: `result-p4-3.txt` · `checklist.md` · `next.md` · `working.md` · `LESSONS_LEARNED.md`
  (**nhóm 24 + 25** + mục Đợt P4-3) · skill `.agents/skills/erpn-verify-first/SKILL.md` (**+8 hàng**).

## 3. Điều đáng nhớ nhất (chi tiết ở `result-p4-3.txt` §2 + LESSONS nhóm 24/25)

1. **Không có tín hiệu REAL|MOCK nào tới client** ⇒ không thể in `source` (hardcode `"real"`) rồi gọi đó là
   "REAL". Cách sửa đúng: **một hàm** (`erpTargetLabel`) dùng chung cho log + response, `null` khi không biết,
   client **không tự suy** (URL gateway không nói copilot đọc ERP nào).
2. **"Không hiện 0 giả" phải cài ở tầng PARSE**: `_block()` trả `null` cho cả object `{}` — nếu không, server
   lỗi ⇒ màn in "Tổng thu 0đ" (một câu về tiền chưa từng ai nói).
3. **Full suite Flutter đỏ sẵn 1 test KHÔNG phải regression của phiên này**: `_probe_review_test.dart` P2b
   (probe tạm từ result40) giả định nút [Xác nhận] quay lại sau khi `ListView` recycle, nhưng fix **F2**
   (`AutomaticKeepAliveClientMixin`) đã làm điều đó không còn xảy ra. Đã quy trách nhiệm bằng **bằng chứng**
   (chạy cô lập + comment F2 trích số đo của chính probe + kiểm cây import + probe đối chứng) ⇒ **user chốt XOÁ**
   (finding đã fix ở F2 nên không convert thành test thật) ⇒ suite Flutter **233/233 xanh**.

   **Bài học mới của mục gỡ chốt (§7)**: probe tự viết gọi API **sai param** thì "đỏ là do mình" — `order_by`
   bị json-encode ⇒ Frappe 417, trong khi **`erpnext-rest-api-recipes` đã ghi gotcha này từ trước** (thiếu bước
   **đọc skill trước khi viết probe**); và một phép verify có thể "PASS" mà **vacuous** (`Quotation` không có
   chứng từ nào ⇒ đọc-doc không chạm cột) ⇒ phải bịt bằng **`WHERE` trên chính cột** (thiếu cột ⇒ 417
   `Unknown column`) + **control âm**.
4. **Falsify có 2 lần "xanh oan"** vì fixture để **nhánh khác cũng chặn** ⇒ fixture của ca falsify phải
   **cô lập đúng biến** mà guard kiểm; và state theo thời gian (cache) phải assert cả **trạng thái trung gian**
   (request đang bay, `settle:false` + server bị gate).

## 4. Chờ user quyết (đừng tự làm)

1. ~~**Xoá `apps/mobile/test/_probe_review_test.dart`?**~~ → **ĐÃ XOÁ** theo quyết định user ⇒ suite Flutter
   **233/233 xanh** (236 test → 233: bỏ 4 test của probe, giữ 25 test P4-3).
2. ~~**Tạo `custom_ai_action_id` trên SO/QT/PO thật?**~~ → **ĐÃ TẠO** (user duyệt bỏ `--dry-run`): 4/4 doctype
   `Data unique=1 search_index=1`, script **idempotent** (lần 2 = no-op), verify **độc lập 3 lớp** + control âm;
   `partial` **hết bật** trên site này và **không phải sửa code**. **Còn lại**: correlation/idempotency của
   B2/B3/B4 **giờ mới có thể** chứng minh trên site thật nhưng cần **chạy ghi thật** (tạo nháp rồi tạo lần 2)
   — **chưa được phép** ⇒ phải phát biểu đúng là **vẫn chỉ đứng với mock**.
3. **Duyệt commit** (khi máy có git) — P4-1 · P4-2 · P4-3 là vùng **đọc số tiền**.
4. ~~**APK debug qua CI rồi cài**~~ → **user chốt "ĐỂ SAU"** (push repo sẽ để GH Actions build) ⇒ phiên này
   **không push, không `git init`, không đụng repo**. Khảo sát giữ nguyên bên dưới để phiên sau **không push thô**:
   - **CI không build được chính code này**: checkout **không có `.git`** (không push được) + **không có
     `.github/workflows`** ⇒ `gh workflow run` chỉ build **trạng thái trên remote = KHÔNG có P4-3** ⇒ APK
     đó không có drawer/màn tóm tắt (coi như xong là sai).
   - **Build local không được**: `/usr/lib/android-sdk` chỉ có `platform-tools` (**thiếu `platforms/` +
     `build-tools/`**), `/home` còn **~1.5G** ⇒ cần cài SDK + dọn đĩa = **cài tool mới ⇒ chờ user duyệt**.
   - **Cài lên máy không làm được từ đây**: `flutter devices` ⇒ chỉ `Linux (desktop)`, **không phone/emulator**.
   - **Đo thêm (read-only `gh api`)**: remote `hoangsoft90/erpn_mobile1` **CÓ** workflow **`android-debug-apk.yml`**
     và chỉ **1 branch** `change/flutter-chat-mvp`; `gh` 2.99.0 + `GH_TOKEN` trong `.env` ⇒ đường (a)/(b) khả thi.
   - ⚠️ **Nhưng đừng push thô từ bản này**: không có `.git` ⇒ history **không chung gốc**, và bản local **thiếu
     file so với remote** (chính `.github/workflows` là ví dụ) ⇒ commit cả cây local rồi push sẽ **XÓA file
     trên branch duy nhất của họ** (phải `--force`) = **không hoàn tác**. Nếu đi đường (b): `git init` →
     `git fetch` → checkout **đúng tree remote** → **overlay chỉ file P4-1/P4-2/P4-3** → commit → push (không force).
   - Đường còn lại: (a) user push từ máy họ → tôi tải artifact + **grep verify binary** rồi đưa đường dẫn APK;
     (b) làm **theo đúng thứ tự an toàn ở trên** (một hành động **ghi vào repo dùng chung** ⇒ chỉ khi user duyệt rõ);
     (c) bỏ bước APK, chuyển P4-4.

## 5. Việc tiếp theo (đề xuất)

- **P4-4 — drill structured** (`.plan/plan4_final.md` §4.4): `drill.sales_orders_today`, `drill.invoices_today`,
  `drill.receipts_today`, `drill.overdue_customers`, `drill.app_drafts_today` — **tái dùng pattern A1**
  (`/read/list` + `ReadListScreen`, Back về drawer), **cấm** inject free-text → classifier, **cấm** `/execute`.
  Các mục "Sắp có (bước P4-4)" trong drawer là chỗ để nối vào (đổi thành `route` là xong).
- **P4-5 — nháp block chi tiết** (drawer "Nháp hôm nay").
- Nếu muốn test Flutter bằng mắt nhanh: `flutter test test/daily_summary_test.dart` (25 test, toàn bộ route
  thật) — không cần ERPNext, không cần mạng.

## 6. Điểm vào nhanh

| Cần gì | Ở đâu |
| --- | --- |
| Bằng chứng phiên này | `result-p4-3.txt` (§0 đo trước · §2 review 4 phát hiện · §3 số · §4 real loop · **§7 gỡ chốt: field thật + `partial=false` + APK để sau**) |
| Bài học | `LESSONS_LEARNED.md` **nhóm 24/25** + **nhóm 26/27** (probe sai param ⇒ đọc skill trước; verify VACUOUS) + mục **Đợt P4-3**; skill `.agents/skills/erpn-verify-first/SKILL.md` |
| Màn hình | `apps/mobile/lib/features/ops/presentation/screens/daily_summary_screen.dart` |
| Drawer | `apps/mobile/lib/features/ops/presentation/widgets/app_drawer.dart` |
| Test | `apps/mobile/test/daily_summary_test.dart` |
| Falsify | `mcp-erpnext/scripts/falsify/p43-drawer-summary.mjs` (`node scripts/falsify/p43-drawer-summary.mjs`) |
| Chạy tay (build/test Flutter) | `PATH=/google/flutter/bin:$PATH flutter test` trong `apps/mobile` (máy này flutter KHÔNG có trong PATH mặc định) |
| Chạy tay (Node) | `cd mcp-erpnext && npm test` (script đã set `COPILOT_MOCK_OK=1`) |
| Vòng lặp thật (read-only) | `/tmp/p43-real-loop.mjs` (nạp ERPNEXT_* từ `.env`, KHÔNG in secret) — dạng mẫu cho các lần sau |
