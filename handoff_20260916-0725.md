# Handoff — 2026-09-16 07:25 (Phiên: docs-sync + trạng thái giữa chừng của 5 việc đã duyệt)

> Đọc thêm: `result30.txt` (bằng chứng), `result29.txt` (3 commit §6 + review §10),
> `LESSONS_LEARNED.md` (chỉ mục nhóm lỗi), `next.md`, `working.md`, `checklist.md`.

## 1. Mốc hiện tại

- Phase 0–3 ✅ · Phase 5 ✅ · Phase 6 ✅ (`553d962`) · Phase 7 Stage A ✅ (`8ebfc0e`).
- **User ĐÃ DUYỆT (2026-09-16)**: ① commit 3 cụm a/b/c (result29 §6) ② nối
  `buildPaymentProposal()` vào router (dây nút [Xác nhận] thật) ③ route
  `/execute/cancel` ④ xoá 2 PE demo ACC-PAY-2026-00114/00115 ⑤ faq §3.2/§3.3/§9.
- **TẤT CẢ 5 VIỆC TRÊN CHƯA HOÀN THÀNH** — chỉ mới làm docs-sync + openspec +
  LESSONS_LEARNED. Chi tiết từng item ở result30 §4 và checklist.md.
- HEAD = `05dda38` (chưa push gì mới). 36 file modified/untracked = đúng nội dung
  3 cụm a/b/c + faq + docs + `initp` (rác, không commit).
- Suite xanh lần chạy gần nhất: Node 112/112 · Python 60/60 · Flutter 28/28 · analyze 0.
- Submit phiếu thu demo: user Để SAU — KHÔNG submit.
- Mới: `LESSONS_LEARNED.md` (chỉ mục 6 nhóm lỗi lặp, trỏ SKILL.md) · openspec
  tasks.md thêm mục §6 Phase 6–9 (validate OK) · 5.9 đã đánh [x] đúng thực tế.

## 2. Bắt đầu phiên sau: làm gì đầu tiên

**Tách 3 commit a/b/c (surgical split)** — phần khó nhất đã được nhận diện:
5 file dùng chung giữa (a) Stage B và (c) Phase 9: `http-ask.mjs`,
`skills/payment-write.mjs`, `idempotency.mjs`, `mock-server.mjs`,
`test/http-execute.test.mjs`. Cách làm:
1. `git stash` an toàn hoặc làm trên bản sao; tách bằng
   `git apply --cached --reverse` các hunk Phase 9 (freshness/intent/clash/
   created_at) trên 5 file dùng chung, giữ lại Stage B thuần.
2. Sau mỗi bước: `node --test` phải xanh ở trạng thái trung gian (Stage B
   không có Phase 9 code vẫn phải pass — các test Phase 9 nằm trong hunk bị
   tách). KHÔNG dùng `git checkout -p` mù.
3. Fallback nếu tách quá rủi ro: đề xuất user gộp 1 commit, body ghi rõ 3 phần.
4. Commit (b) độc lập: `src/vietnamese_nlp/money.py`, `tests/test_money.py`,
   `copilot-server.mjs`, `test/customer-resolve.test.mjs`, `faq.md`,
   `result28.txt` — KHÔNG đụng 5 file dùng chung.
5. Push `change/flutter-chat-mvp` sau khi cả 3 commit xong.

## 3. Tiếp theo (đã duyệt, làm sau commit)

1. **Item 2 — payment_write vào router**: `routeIntent()` thêm nhóm ghi để
   "thu tiền cho <khách> <số tiền>" → `buildPaymentProposal()` (risk HIGH,
   params.outstanding_vnd là BẮT BUỘC cho Phase 9 drift check). Sửa text câu
   trả lời còn ghi "Phase 7 — Phase 2 chỉ đọc" (grep `copilot-server.mjs`).
   Verify: /ask thật + widget test nút [Xác nhận] render với JSON thật.
   Sau đó cập nhật faq đầu-file + §3.2/§3.3/§9 (đánh dấu các chỗ ở result30 §3).
2. **Item 3 — /execute/cancel**: chỉ huỷ PENDING sau khi
   `reconcilePaymentEntry()` trả `found:false` (0 chứng từ); thấy chứng từ ⇒
   từ chối huỷ, trả về kết quả reconcile. Test + falsify như trước.
3. **Item 4 — xoá 2 PE demo**: ĐỌC SOURCE @casys/mcp-erpnext trước để tìm tool
   xoá/cancel thật (result4/25 — không đoán tên tool). Nếu không có tool nào:
   báo user xoá tay trên UI, KHÔNG tự chế. Sau xoá: verify outstanding hóa đơn
   gốc không đổi (phiếu chưa submit nên công nợ vốn không đổi — ghi bằng chứng).
4. Sau mỗi bước lớn: result31+ + checklist/next/working + handoff mới.

## 4. Bẫy đã biết của phiên sau (đọc trước khi code)

- `source .env` kéo theo `ASK_*` ⇒ bind policy từ chối khởi động (result21 §6B).
- Store idempotency có cache RAM: sửa file ngoài rồi thử lại KHÔNG có tác dụng
  cho tới khi restart server (result25).
- `/tmp` bị dọn: harness + audit phải nằm trong repo dir (result20/22).
- Không dedupe giả trong mock (ERPNext không unique reference_no) — result25.
- Fail-closed mọi nhánh đường tiền; falsify phải chứng minh file đổi thật
  (result26/27/29 §10) — xem `LESSONS_LEARNED.md`.

## 5. Vẫn treo — KHÔNG phải việc agent

Rotate key ERPNext (để sau) · dán 3 giá trị GitHub Settings · thu audio 150 câu
(blocker Phase 4) · test APK thiết bị thật · review ai-rules.md · quyết
.agents/skills/* vào repo (không) · SUBMIT phiếu thu demo (user để sau) ·
thought_signature live Gemini (cơ hội, không probe trước).

## 6. Nguyên tắc giữ nguyên

Vùng tiền/số/phân quyền: KHÔNG tự commit, KHÔNG tự ký duyệt ·
verification-before-completion (mọi claim = lệnh + output thật) ·
mock trung thực · fail-closed · không báo "xong" bằng lời · an toàn > tốc độ.
