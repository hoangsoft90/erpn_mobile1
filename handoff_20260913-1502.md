# handoff_20260913-1502.md

Handoff session **thứ 4** của repo `erpn_mobile1` (session 3: `handoff_20260913-1435.md`).

Session này: **re-verify 5 repo DSH** (2 lớp độc lập) · **phase-03 sửa PWA → Flutter** (đồng bộ 4 file) · **Phase 2 E2E hoàn chỉnh với mock server** (không cần credential).
Bằng chứng: **`result4.txt`** (phiên này) + `result3.txt` + `result2.txt` + `result1.txt`.

---

## Mốc hiện tại

- **Python NLP: 58/58 PASS · money 259/259 = 100%** (không đụng trong phiên này).
- **Phase 2 JS: 19/19 `npm test` PASS** — gồm 6 test E2E spawn process thật (correlation theo id, concurrent, isError, JSON-RPC error).
- **Repo VẪN 0 COMMIT** — user đã duyệt nguyên tắc commit gộp (session 3), chờ user gõ OK lần cuối.
- Smoke flow nghiệp vụ chạy thật: `route → findCustomer → getCustomerBalance` → 2.500.000 VND; ID bịa → `ID_REFUSED`.

## Chạy thử nhanh

```bash
PYTHONPATH=src python3 -m unittest discover -s tests     # 58 tests OK
PYTHONPATH=src python3 tests/test_dialect_set.py         # 259/259 = 100%
cd mcp-erpnext && npm test                               # 19 tests pass
```

## Phát hiện kỹ thuật quan trọng của phiên

**Skeleton đầu whitelist SAI TÊN TOOL.** Đọc source bundle `@casys/mcp-erpnext@3.0.4` trong node_modules (57.377 dòng): package expose **125 tool, tất cả prefix `erpnext_*`** (`erpnext_customer_list`, `erpnext_doc_get`…) — **không tồn tại** `get_list`/`get_doc`/`run_sql_query` như tôi viết ban đầu. Đã sửa whitelist (12 tool đọc thật) + thêm test chặn regression. **Bài học: đừng đoán tên tool — đọc source package trong node_modules.**

**Shape 3.0.4 thật (dùng làm mẫu mock):** `tools/call` → `{content:[{type:"text",text:"<json>"}], structuredContent?, isError?}`; `erpnext_customer_list` → `{doctype, count, data:[…]}`; `erpnext_customer_get` → `{data: doc}`; tool đọc có `annotations.readOnlyHint: true`.

## Re-verify 5 repo DSH (T1) — KẾT QUẢ: CÓ THẬT, giữ nguyên kết luận

2 lớp độc lập (GitHub API + fetch HTML trực tiếp), 2 repo control trả 404 ở cả 2 lớp. Cả 5 URL HTTP 200, metadata đầy đủ (★0–206, created 2026-08-14→18, pushed đến 2026-09-12). Chi tiết URL + số liệu: `result4.txt` §T1 và `checklist.md`. **Không đổi thành "chưa xác minh được"** vì URL mở được thật. Quyết định KHÔNG dùng vẫn giữ (lý do thẩm định an toàn).

## Quyết định đã chốt (KHÔNG được tự đổi)

| Quyết định | Chi tiết |
|---|---|
| **Commit gộp Phase 1: user đã duyệt** | Chờ user gõ OK lần cuối → 1 commit `feat: phase 1 — Vietnamese NLP pipeline (money/kinship/synonyms/quantity)` |
| **Phase 2 làm không cần credential** | Mock server đúng shape; khi có thật chỉ đổi endpoint |
| phase-03 = **Flutter** | Đã sửa file; tên file giữ nguyên để tham chiếu cũ không gãy |
| Cầu nối = **HTTP service nội bộ** · `"công nợ"` = `receivable` · trẹo/chai đã làm · bạc chờ mệnh giá | Các session trước |

## Kiến trúc Phase 2 hiện tại

```
utterance → routeIntent (keyword, KHÔNG embedding) → factory(group)(mcp, knownIds)
  → skill functions (customer/sales/payment/inventory)
    → client.callTool(TOOL, args)  ← assertReadOnly chặn write TRƯỚC server
      → JSON-RPC 2.0 stdio (correlation theo id) → mock-server.mjs (hôm nay)
                                                     hoặc @casys/mcp-erpnext 3.0.4 (khi có credential)
  → markUntrusted() bọc mọi kết quả · assertKnownId chặn ID bịa
```

**Khi có credential:** `createMcpClient({ serverScript: realServerScript() })` + env `ERPNEXT_URL/API_KEY/API_SECRET` — không viết lại logic.

## Việc tiếp theo (theo thứ tự)

1. **Commit Phase 1** — chờ user gõ OK. KHÔNG add `.plan/`, `.gemini/`, `.opencode/`, `initp`; add `mcp-erpnext/` (node_modules đã gitignore).
2. **User cung cấp ERPNEXT_URL + API key/secret** → nối thật, chạy exit criteria phase-02 (4 skill ≥95%).
3. **Định nghĩa Flutter client track** — khoảng trống lớn nhất (phase-03 đã Flutter hoá nhưng track chi tiết chưa có). Tham khảo: repo `201222-L/dsh-mobile-remote` là plugin dsh + Flutter App thật (đã verify).
4. Thu audio 3 miền (trước Phase 4) · `bạc` = ? (chờ mệnh giá).

## Lưu ý cho agent sau

- **Đừng báo "xong" bằng lời** — mọi claim cần lệnh + output thật.
- **Đừng đoán tên tool/API của package bên thứ ba** — đọc source trong node_modules (bài học lớn nhất session này).
- **Node 24 quirk:** `node --test` (tự dò) — đừng truyền `test/` làm đối số.
- **Env thiếu tool:** AgentMemory down · MCP `cocoindex-code`/`codebase-memory-mcp` không expose · OCR không chạy được → Code Review thủ công · Simplenote MCP không có.
- **Vùng tiền: AI KHÔNG tự ký duyệt** — commit chỉ chạy khi user gõ OK.
- Python suite phải luôn xanh: `PYTHONPATH=src python3 -m unittest discover -s tests` trước khi kết luận bất cứ việc gì liên quan `src/vietnamese_nlp`.

## Suggested skills

- `verification-before-completion` — trước mọi claim.
- `openspec-propose` + `openspec-apply-change` — khi nối ERPNext thật (thay đổi kiến trúc thật).
- `ponytail` — BẬT cho code thường; **TẮT** cho code chạm tiền/phân quyền.
- `flutter-coding` + `architecture` + `design-system` — khi định nghĩa Flutter track / vào Phase 3.
- Cấm: `grill-me`, `grill-with-docs`, `tdd`, `diagnosing-bugs`, `to-prd`, `to-issues`, `triage`.
