========================================================================
RESULT 48 — P4 (phases2): LEARNING LOOP (human-approved)
Timestamp: 2026-09-18 (giờ shell)
Phạm vi: JSONL signal log + cluster report + workflow + 1 vòng thử thật.
KHÔNG auto-write contract; KHÔNG đụng ERPNext thật; KHÔNG Phase khác.
========================================================================

== 1. DELIVERABLES (4/4) ==
1.1 Log structured ......... src/learning-log.mjs — JSONL append-only,
    never-throw (test §2 best-effort), text ≤500 chars, ts ISO-UTC,
    outcome/error_code/matched/group/capability/has_proposal.
    Dir: <repo>/learning-log/ (gitignored — test §5; KHÔNG /tmp, bài học
    result31). Env: LEARNING_LOG_DIR, LEARNING_LOG=off.
1.2 Cluster script ......... scripts/learning-cluster.mjs (npm run
    learning:cluster) — READ-ONLY (guard test mới §6); nhóm
    UNKNOWN/UNIMPLEMENTED/LOW_CONFIDENCE theo key chuẩn hoá; --min/--dir;
    exit 1 khi log rỗng; report kèm ví dụ nguyên văn + outcome summary.
1.3 Workflow ............... docs/learning-loop-workflow.md — 5 bước,
    quyết định là của người; probe chống "ăn cắp" trigger (result9);
    numstat nhỏ cho contract; golden case bắt buộc cho trigger mới.
1.4 Cấm auto-write ......... script không có API ghi (falsify F2: inject
    writeFileSync → guard FAIL); pipeline chỉ append JSONL.

== 2. WIRING ==
- answerQuestionLogged() bọc answerQuestion() — copilotAsk (MCP) và /ask
  (HTTP) đều qua đó ⇒ 1 điểm log, không sửa 12 return site.
- http-ask.mjs: gọi answerQuestionLogged thay answerQuestion.

== 3. VÒNG THỬ (exit criterion 2) ==
- Log thử 9 câu mô phỏng 1 tuần → cluster report đúng: 5
  known_intent_unimplemented / 3 answered / 1 unknown_intent + cụm [2x].
- BÀI HỌC: các câu "tồn kho cám heo…" tôi ghi vào log thử thực ra router
  ĐÃ hiểu (routeIntent → stock.balance) — probe lộ log thử sai, không
  phải router thiếu. Quét rộng tìm UNKNOWN thật: "doanh số …" (5 biến
  thể) → null dù sales.summary tồn tại (stub).
- Hành động theo workflow: + "doanh số","doanh so" vào keywords sales
  (capabilities.json numstat 1/1) + golden r24a/r24b chèn surgical
  (+3/0, sau 1 lần json.dump reformat 1847/223 → khôi phục HEAD làm
  lại — bài học result46 §6b áp dụng ngay tại chỗ).
- Regression gate: golden 7/7 · routeIntent('doanh số hôm nay thế nào')
  → sales group (matched "doanh số") thay vì UNKNOWN.

== 4. FALSIFY (bản sao /tmp, test MỚI NHẤT được copy sang) ==
F1 append throw (giả lập disk-full) → assertion "append must succeed"
   FAIL ✅; khôi phục → pass ✅ (never-throw + best-effort là luật).
F2 inject writeFileSync vào cluster script → guard READ-ONLY FAIL 1 ✅;
   bản thật PASS ✅.

== 5. LỖI TỰ PHÁT HIỆN + SỬA (đã vào SKILL.md/LESSONS_LEARNED) ==
- Test tự viết sai path .gitignore (ENOENT lộ ngay).
- Test tự viết dùng tên file JSONL riêng thay vì learningLogConfig().file
  → script đúng, test sai. Luật: test dùng đúng contract name.
- clusterKey: luật + ngoại lệ ("#" được giữ) → nhóm lệch; một luật duy
  nhất (drop mọi token 1 ký tự) sửa xong 3 câu "xóa khách hàng…" về 1 cụm.
- Assertion test sai hành vi chính mình viết (--min 2 vẫn giữ cụm 3x).
- json.dump reformat file golden → surgical insert theo anchor.

== 6. SUITE (lệnh + output thật) ==
Node     tests 204 · pass 204 · fail 0   (197 → 204, env -u ERPNEXT_*)
Python   OK (60 tests)
Flutter  +69: All tests passed!  ·  analyze: No issues found!

== 7. STAGE (chờ duyệt — contract/golden chạm policy) ==
8 file: src/learning-log.mjs (mới) · scripts/learning-cluster.mjs (mới)
· docs/learning-loop-workflow.md (mới) · test/p4-learning-loop.test.mjs
(mới) · src/copilot-server.mjs · src/http-ask.mjs · capabilities.json
(1 dòng) · golden-dataset.json (+3) · package.json (+1 script) ·
.gitignore (+4) · result48.txt.
learning-log/ runtime data KHÔNG commit (gitignored, test chốt).

== 8. REVIEW VÒNG 2 (sau khi user hỏi "viết xong đã test chưa?") ==
FINDING 1 (wiring): copilotAsk (MCP/dsh) vẫn gọi answerQuestion() trực tiếp
   — chỉ /ask HTTP được log; comment "ONE wrapper covers both" SAI.
   → FIX: copilotAsk gọi answerQuestionLogged; comment sửa đúng.
FINDING 2 (claim vượt bằng chứng): p4-result/result48 ghi "E2E 1 call /ask
   = 1 dòng JSONL (chạy trong suite)" nhưng KHÔNG có test nào như vậy.
   → FIX: thêm test E2E thật (test §7) — spawn NLP (PYTHONPATH=src, cwd
   repo root 2 mức) + copilot-server thật + LEARNING_LOG_DIR test-scoped;
   assert đúng 1 dòng JSONL, text/outcome/group đúng.
LỖI E2E lần đầu: nlp child thoát (1) — spawn thiếu PYTHONPATH=src +
REPO tính 3 mức thay vì 2. Probe trực tiếp python3 -m nlp_service.server
từ mcp-erpnext/ → ModuleNotFoundError lộ ngay nguyên nhân.
Suite sau review: Node 204/204 · Python 60 · Flutter 69 · analyze 0.
