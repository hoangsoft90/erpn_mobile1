# handoff_20260913-1359.md

Handoff session **thứ 2** của repo `erpn_mobile1` (session 1: `handoff_20260913-1332.md` — Phase 0 + Phase 1).

Session này: **đợt fix Phase 1** sau khi review `money.py`. Không bắt đầu phase mới.
Bằng chứng đầy đủ: **`result2.txt`** (session này) + `result1.txt` (session trước).
Trạng thái tóm tắt: `checklist.md` · `features.md` · `next.md`. **File này không lặp lại chi tiết — đọc 3 file đó.**

---

## Mốc hiện tại

- **Repo: VẪN 0 COMMIT.** User chốt "chưa commit gì cả" → commit gộp sau. 15 mục untracked.
- **Test: 55/55 PASS** · **money accuracy 247/247 = 100%** (corpus 234→247, negative 25→33).
- Đã fix **4 false positive** trong `money.py` + thêm **tiếng lóng `trẹo`/`chai` = triệu**.
- Đã thêm **`phase-15-monetization-ads.md`** vào roadmap.
- **Còn 1 BUG thật CHƯA fix** (xem mục dưới) + **1 hành vi đổi có chủ đích** cần biết.
- Chưa có UI, chưa nối ERPNext, chưa có STT, chưa có cầu nối Python↔Flutter.

## Chạy thử nhanh

```bash
PYTHONPATH=src python3 -m unittest discover -s tests   # 55 tests, OK
PYTHONPATH=src python3 tests/test_dialect_set.py       # 247/247 = 100% + challenge set
PYTHONPATH=src python3 -m vietnamese_nlp --pretty "Bác Hai trả 2tr5 tiền cám heo"
```

---

## Quyết định user chốt TRONG session này (agent sau KHÔNG được tự đổi)

| Quyết định | Chi tiết |
|---|---|
| **Chưa commit gì cả** | Để untracked, commit gộp sau. Repo vẫn 0 commit |
| **Cầu nối Python ↔ Flutter/dsh = HTTP service nội bộ (localhost)** | 1 nguồn logic duy nhất. **KHÔNG** port sang Dart (tránh 2 bản logic lệch số). Phải định hình trước Phase 5 |
| **`"công nợ"` → `receivable`** | Giữ nguyên, không sửa code, không tách 2 intent |
| **Monetization: CÓ làm** | → đã tạo `phase-15-monetization-ads.md` |
| **Tiếng lóng miền Nam: CÓ** | `trẹo`/`chai` đã làm; `bạc` chờ user xác nhận mệnh giá |

## BUG THẬT CHƯA FIX (quan trọng nhất session này)

**Hậu tố tiền tệ DÍNH LIỀN số không parse:**

| Input | Kết quả |
|---|---|
| `"anh Nam trả 2000 đ"` | 2000 ✅ |
| `"anh Nam trả 2000đ"` | **None** ❌ |
| `"anh Nam trả 5000vnd"` | **None** ❌ |
| `"anh Nam trả 5000k"` | 5000000 ✅ (`k` nằm trong `_SCALE_ALT` nên dính liền OK) |

**Nguyên nhân (đã verify bằng `_tokenize`):** regex `num` trong `_TOKEN_RE` có lookahead `(?!\w)`; `đ` là ký tự Unicode word → `"2000"` không match được làm num token → cả biểu thức không chạy. **Đây là bug CÓ TỪ TRƯỚC**, không do guard mới gây ra.

**Chưa fix vì:** phải sửa regex tokenizer = vùng TIỀN → cần user duyệt. Đã ghi `ch-012`/`ch-013`. Fail theo hướng **thiếu amount** (an toàn), nhưng tài liệu đang quảng cáo "hậu tố tiền tệ" là hỗ trợ → thực tế chỉ đúng khi có dấu cách.

## Hành vi ĐỔI CÓ CHỦ ĐÍCH (đừng "sửa" mà không hỏi)

`"anh Nam trả 2000"` → **TRƯỚC: 2000 · SAU: None.** Vì 2000 nằm trong dải năm 1900..2099, trần, không hậu tố → guard (b) loại. Đây là lựa chọn **fail-safe** theo quyết định "an toàn số tiền > độ phủ". Đường thoát: `"2000 đ"` hoặc `"hai nghìn"`. Đã ghi thành `neg-033` → nếu sau này muốn đổi, test sẽ đỏ (buộc quyết định lại). **Đây là trade-off, không phải bug.**

### Hợp đồng an toàn của `money.py` (CẬP NHẬT — đọc trước khi sửa, đây là code chạm TIỀN)

1. Amount chỉ sinh khi có **big scale word** HOẶC **≥4 chữ số** HOẶC **hậu tố tiền tệ**.
2. Từ số match **CÓ DẤU** — `năm` (5) ≠ `Nam` (tên). Không nhận input không dấu (cố ý, `ch-005`).
3. `NICKNAMES` là **allowlist tường minh** — `mươi` **KHÔNG** nằm trong đó, nếu thêm thì `"ba mươi nghìn"` sẽ hỏng.
4. `ambiguous_tail`: số trần sau một big-scale hoàn chỉnh ở cuối câu → **từ chối** (`"2 triệu 500"` → `None`).
5. `has_nonzero`: scale word **không** tự tạo ra số lượng (`"không triệu"` → `None`).
6. **(MỚI)** `_looks_like_identifier()` — 4 shape guard, chạy TRƯỚC `_is_valid`, mọi nhánh fail về "không amount":
   - SĐT: toàn số, bắt đầu `0`, dài 9..11
   - Năm: 4 chữ số trong 1900..2099, không scale, không hậu tố
   - Ngữ cảnh định danh: từ trước ∈ `_ID_CONTEXT_WORDS`
   - Scale mơ hồ dùng như vật thật: `_AMBIGUOUS_SCALE_NOUNS["củ"]` / `["chai"]`
7. **(MỚI)** `trẹo`/`chai` = 1.000.000 trong `BIG_SCALES`. **`bạc` KHÔNG được thêm** (không có mệnh giá cố định — thêm là bịa số tiền).
8. `SynonymMatch` **cố ý không có offset** (kinship stripping làm lệch offset). `MoneyMatch`/`QuantityMatch` có offset, trỏ vào `original`.

**Nếu sửa `money.py`: bắt buộc chạy lại `test_money.py` + `test_dialect_set.py` trước khi báo xong.**

---

## Việc tiếp theo (theo thứ tự)

1. **User quyết: có fix bug hậu tố dính liền (`ch-012`/`ch-013`) không?** Nếu có → sửa `_TOKEN_RE`, chạy lại full test, KHÔNG tự commit.
2. **Commit** — repo vẫn 0 commit. Đề xuất `feat: phase 1 — Vietnamese NLP pipeline` (gộp cả đợt fix) để có điểm rollback.
3. **Định nghĩa Flutter client track** — khoảng trống lớn nhất của roadmap. Chặn `phase-04` (STT) và `phase-15` (ads).
4. **Sửa `phase-03`** (đang PWA, client đã chốt Flutter).
5. **Phase 2** — MCP ERPNext read-only + skill layer (`phase-02-mcp-erpnext-readonly-skills.md`), pin `@casys/mcp-erpnext@3.0.4` + stdio. **Cần user cung cấp ERPNext URL/API key.**
6. **Thu 100–200 câu audio thật 3 miền** — điều kiện còn thiếu của Phase 1, bắt buộc trước Phase 4.

## Cố ý CHƯA làm (đúng phạm vi — đừng "tiện tay" làm luôn)

Không đụng dsh · STT · ERPNext · MCP server · entity resolution · không port sang Dart.

## Files chính session này

- **Sửa:** `src/vietnamese_nlp/money.py` · `tests/test_money.py` · `tests/data/dialect_cases.json` · `README.md` · `next.md` · `features.md` · `checklist.md` · `.plan/phases/production_roadmap.md`
- **Tạo:** `.plan/phases/phase-15-monetization-ads.md` · `result2.txt` · `handoff_20260913-1359.md`

## Lưu ý cho agent sau

- **Đừng báo "xong" bằng lời** — user rất nhạy với báo cáo sai. Mọi claim cần lệnh + output thật.
- **Con số 100% đừng tin tuyệt đối:** session này là bằng chứng sống — corpus 234/234 = 100% mà vẫn có **4 false positive + 1 bug**. Giá trị thật nằm ở **33 negative case + challenge set 11 case**.
- **Env này thiếu tool:** AgentMemory down · MCP `cocoindex-code`/`codebase-memory-mcp` không expose · OCR không chạy được · Simplenote MCP không có trong tool list. Code Review phải làm **thủ công** (chính cách tìm ra 4 bug).
- `pytest` không có trong env → dùng `unittest` (stdlib). Test vẫn tương thích pytest.
- **Bài học đáng ghi vào Simplenote (chưa ghi được vì MCP không có):** lookahead `(?!\w)` trong regex tokenizer chặn nhầm ký tự Unicode (`đ`) → hậu tố tiền tệ dính liền số bị mất. Rất dễ gặp ở project khác.
- **Vùng tiền: AI KHÔNG được tự ký duyệt** — mọi thay đổi chạm logic tiền phải user review trước khi commit.

## Suggested skills (cho session sau)

**Bắt buộc / nên dùng:**
- `verification-before-completion` — trước mọi claim "xong".
- `git-checkpoint` — repo 0 commit, cần baseline trước khi làm Phase 2.
- `openspec-propose` + `openspec-apply-change` — repo đã cấu hình OpenSpec; Phase 2 thay đổi kiến trúc nên có change riêng.
- `systematic-debugging` — khi sửa tokenizer / bug NLP; đừng sửa mò.
- `ponytail` — BẬT cho code thường; **TẮT** cho code chạm tiền/số/phân quyền (đang là `money.py`, Phase 5 PII, Phase 10 RBAC).

**Dùng khi cần:**
- `flutter-coding` + `architecture` + `design-system` — khi định nghĩa Flutter client track.
- `flutter-backend-api-mapping` — khi nối Flutter với HTTP service của `vietnamese_nlp`.
- `mcp-loop` + `ui-checkpoint` — sau khi sinh code Flutter, trước khi cho user duyệt UI.
- `schema-migration` — khi Data Model delta spec khác model hiện có (Phase 5–7).
- `release` / `gh-actions-expo-apk-build` — khi build APK (không tự build sau mỗi feature).

**Cấm dùng (theo `AGENTS.md`):** `grill-me`, `grill-with-docs`, `tdd`, `diagnosing-bugs`, `to-prd`, `to-issues`, `triage`.
`prototype`, `handoff`, `git-guardrails`, `improve-codebase-architecture`, `writing-great-skills` — **chỉ gọi khi user chủ động nhắc tên**.
