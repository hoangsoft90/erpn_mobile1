# Handoff — 2026-09-14 04:27 (Phiên: Phase 3 Flutter chat MVP)

> Đọc thêm: `result7.txt` (bằng chứng đầy đủ), `next.md`, `checklist.md`,
> `handoff_20260914-0245.md` (phiên trước). Đừng lấy kế hoạch từ
> `.plan/phases/` — root docs là nguồn sự thật (theo prompt user).

## 1. Mốc hiện tại

- **Branch `change/flutter-chat-mvp`** (từ commit `0ac8e61`), **CHƯA commit
  Phase 3** — chờ user duyệt UI-checkpoint (code hiển thị số tiền).
- OpenSpec change `flutter-chat-mvp`: **valid 4/4 artifacts**, tasks.md đã
  đánh dấu xong 1.1–3.1; còn 3.2 (README/next.md — đang làm), 4.1 (UI
  checkpoint), 4.2 (docs), 4.3 (verify + commit + archive).
- **Đã xây xong kỹ thuật:**
  - Task 1: `mcp-erpnext/src/http-ask.mjs` — HTTP POST /ask → answerQuestion()
  - Task 2: `apps/mobile/` — Flutter chat MVP đầy đủ (Riverpod codegen +
    GoRouter + Feature-First + design tokens; shared_preferences user duyệt)
  - Task 3: `.github/workflows/android-debug-apk.yml` (build APK trên GH
    Actions — KHÔNG build trên VPS, quyết định user)
- **Test xanh (lệnh thật):** Node 36/36 · Python 58/OK · Flutter analyze
  No issues · Flutter test 13/13.
- Flutter skills đã cài: 7 skill từ `flutter/agent-plugins` (user chọn bộ
  tối thiểu) → `.agents/skills/` + `skills-lock.json`.

## 2. Bug thật đã sửa trong phiên (chi tiết result7.txt §3)

1. ChatBubble chỉ vẽ question khi ok=true → answer không bao giờ hiện
   (bắt bằng debug widget test in toàn bộ Text). Sửa: 1 turn = cặp bubble.
2. ChatTurn lấy question từ server echo → đổi thành typedQuestion.
3. ChatTurn.error dead code → xóa.
4. ENOSPC tạm thời làm hỏng build assets → shader exception ảo trong
   flutter test; flutter clean là hết. (Bài học VPS.)

## 3. Việc kế tiếp (thứ tự)

1. **Cập nhật README + checklist/features/next** mục chạy Flutter + /ask
   (task 3.2) — nếu phiên trước chưa kịp làm.
2. **UI-checkpoint (task 4.1):** trình bày mô tả UI cho user duyệt; VPS
   không chạy được desktop (thiếu clang/cmake/ninja/libgtk-3-dev) —
   screenshot thật sẽ có sau khi GH Actions build APK.
3. User OK → **commit Phase 3** trên branch (KHÔNG commit .env; .plan/ +
   .gemini/ + .opencode/ + initp vẫn exclude như cũ).
4. User cấp **GitHub repo** → push → GH Actions build APK → user tải
   artifact `erpn-chat-debug-apk` cài máy thật.
5. Sau khi chạy thật trên máy: sync/archive OpenSpec change (task 4.3).
6. Còn treo: **rotate ERPNext API key/secret** (đã đi qua chat lần 2 nhắc).

## 4. Cách chạy (từ repo root)

```bash
# backend copilot (3 tiến trình, 3 terminal):
PYTHONPATH=src python3 -m nlp_service.server            # :8787 NLP bridge
# terminal 2 (mock, không cần ERPNext credential):
node mcp-erpnext/src/http-ask.mjs                        # :8788 /ask
# terminal 2 (THẬT — dùng .env):
set -a; source .env; set +a; node mcp-erpnext/src/http-ask.mjs

# Flutter client:
export PATH="/google/flutter/bin:$PATH"
cd apps/mobile && flutter run \
  --dart-define=COPILOT_BASE_URL=http://<LAN-IP>:8788
```

## 5. Nguyên tắc vẫn hiệu lực

- Code chạm tiền/hiển thị số tiền → KHÔNG tự commit, chờ user duyệt.
- Không báo "xong" bằng lời — lệnh + output thật (result7.txt).
- An toàn > tốc độ; fail-safe giữ nguyên (null answer vẫn hiển thị lý do,
  không bịa dữ liệu).
- Sau mỗi thay đổi lớn: cập nhật result/checklist/features/next/handoff.
