# handoff — P4-0 (2026-09-21)

## 1. Việc đã làm (đúng phạm vi prompt P4-0 — KHÔNG implement skill lớn)
- Đọc bắt buộc: `.plan/plan4_final.md` (§3.2/§10/§12) · `capabilities.json` · `plan2_final` READ path · AGENTS.md.
- Probe ERPNext thật **chỉ READ** (get_list): 6 Mode of Payment, 245 PE (173 submitted), 9/9 `Account.account_type`, 3 Company.
- Ghi: `docs/plan4-mode-map.md` (map + rule) · `checklist.md` (mục P4-0) · `result-p4-0.txt`.

## 2. Kết luận kỹ thuật
- **Két P4-1 KHÔNG bị chặn**: 3/3 company có `default_cash_account` (§10.3 plan4_final); DEMO POC CO + SANLOAN thiếu `default_bank_account`.
- **Cờ mới**: site này KHÔNG xếp được Cash|Bank bằng `MoP.type` — 53% PE submitted mode TRỐNG; "Chuyển khoản" bị `type=Cash` nhưng PE thật vào tài khoản Bank (ACB).
- Khuyến nghị (CHỜ DUYỆT): xếp theo `Account.account_type` của tài khoản tiền (`paid_to` Receive / `paid_from` Pay) → fallback `Company.default_*` → đếm riêng "không xếp được" (không 0 giả). Đo được: rule-1 đủ xếp 100% PE mode-trống.

## 3. Cần user quyết trước P4-1
1. Duyệt khuyến nghị `Account.account_type` (thay vì map theo mode như định hướng ban đầu).
2. Xác nhận 2 mode kiểu `Bank` chưa có PE (Cheque/Bank Draft/Credit Card) cứ xếp bank theo MoP.type.

## 4. Cấm vẫn đúng cho P4-1 (theo spec)
UI Flutter (P4-3 trở đi) · WRITE · Redis · skill WRITE mới. Map phải nằm config server/contract, Flutter không hardcode tên mode.

## 5. Trạng thái chung phiên
- Đã xong ngay trước P4-0: re-verify ERPNext-thật sau khi site bật trễ (result63 §10) — không số liệu giả, fix issue1 xác nhận đúng trên dữ liệu thật.
- Chờ duyệt commit (máy không có git): B1→B4, C0–C2, issue1 fix, và giờ thêm P4-0 (docs + checklist).
- Node 461 (459 pass, 2 fail dsh-env) · Python 62 OK — P4-0 không đụng code, không đổi suite.
