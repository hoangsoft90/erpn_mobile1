# Handoff — 2026-09-17 10:40 (Phiên: P1 implement + self-review + result45)

> Đọc thêm: `result45.txt` (bằng chứng vòng review này), `result44.txt` (P0 + review P0),
`next.md`, `checklist.md`, `working.md`, `LESSONS_LEARNED.md`.

## 1. Mốc hiện tại

- **P0 ĐÃ COMMIT + PUSH `b4acdb1`** (capability contract + Safety Gateway + kill switch +
  golden dataset) + docs/assets `0ce2893` (redact key cũ trong result10).
- **P1 (phases2) KỸ THUẬT XONG — CHỜ DUYỆT COMMIT (vùng tiền)**:
  - `src/entity-resolution.mjs` — 4 trạng thái theo policy trong `capabilities.json`
  - WRITE HIGH không auto-select fuzzy; AMBIGUOUS → **candidate picker Flutter**
    (`entity_picker.dart` + chat_bubble) → `/ask` nhận `entity_id`, server re-validate
    trên fresh ERPNext read (id chỉ là hint)
  - Immutable snapshot (`proposal_id`/`version`/`expires_at`) trong `action-proposal.mjs`
  - Codes tách: `PROPOSAL_EXPIRED` vs `PROPOSAL_VERSION_STALE`/`PROPOSAL_ENTITY_CHANGED`
    (`proposal-freshness.mjs` + banner Flutter phân biệt)
  - `src/execution-state.mjs` — `UNKNOWN_EXECUTION_STATE` → RECONCILING theo
    `custom_ai_action_id`
  - `src/business-dedup.mjs` — fingerprint warn + `dedup_ack` qua `/execute`
  - NLP down → chặn WRITE phụ thuộc amount (fail-closed)
- **Review vòng 2 (result45)**: fix harness Flutter `_bodyOf` (dio đưa Map NGUYÊN vào
  adapter — cast `as String` ném TypeError bị bọc thành "Không kết nối được máy chủ",
  capture rỗng); soi 2 điểm wiring server: `/execute/cancel` là lock-release CỐ ÊN
  (comment trong code), `entity_id` re-validate đúng — KHÔNG sửa gì server.
- **Suite: Python 60/60 · Node 172/172 · Flutter 67/67 · analyze 0.**
- Bài học mới vào skill (88 rows, 0 malformed): "test báo lỗi transport + capture rỗng ⇒
  nghi harness trước production; shape adapter nhận phụ thuộc cấu hình harness".

## 2. Việc kế tiếp (thứ tự)

1. **User duyệt commit P1** — message đề xuất:
   `feat: p1 entity execution resilience — 4-state resolver + candidate picker + immutable snapshot + PROPOSAL_VERSION_STALE/ENTITY_CHANGED + UNKNOWN→RECONCILING + business dedup + NLP-down write block`
   - Stage: 8 file src sửa + 3 src mới (entity-resolution/execution-state/business-dedup)
     + capabilities.json + entity_picker.dart + p1_picker_dedup_test.dart + 3 test Node sửa
     + 5 file Dart sửa + docs (checklist/next/working/features/faq/LESSONS/skill/result44-45).
   - KHÔNG stage: `.env`, idempotency-store, `.plan/`, `.gemini/`, `.opencode/`, `initp`.
   - Secret scan CLEAN trước commit (đã redact key cũ trong result10 từ commit 0ce2893).
2. Sau commit: ghi hash vào checklist/working + push `change/flutter-chat-mvp`.
3. P2 kế tiếp theo `.plan/phases2/` — chỉ khi user ra lệnh.

## 3. Gap đã biết (không phải việc phiên sau bắt buộc)

- Golden phơi 3 gap Phase 1 (k18 "Con Linh", m15 "một triệu hai", k36 "Bác sĩ Nam") —
  ghi trong features.md "Nợ kỹ thuật", hiện lại mỗi lần chạy Golden.
- Saga/compensation Phase 9 (plan §7) — chờ user duyệt riêng.
- Việc chỉ user làm được: GitHub Settings 3 giá trị, APK thiết bị thật, thu audio 150 câu.

## 4. Nguyên tắc giữ nguyên

- Vùng tiền/số/phân quyền: KHÔNG tự commit, KHÔNG tự ký duyệt — chờ user trong chat.
- verification-before-completion: mọi claim = lệnh + output thật; fix vùng tiền phải falsify
  (trên BẢN SAO /tmp, không trên cây sống — bài học result44).
- Không mở Phase 4/8/10–15 cũ, không `unknown → DSH`, không skill WRITE mới.
