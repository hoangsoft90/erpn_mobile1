# Handoff — 2026-09-21 02:01 UTC (Phiên: issue1 — tắt fallback-mock ngầm + verify ERPNext THẬT)

> Đọc thêm: `result63.txt` (bằng chứng phiên này) · `.plan/issue1.md` (vấn đề gốc) ·
> `.plan/issue1_fix1.md` (kế hoạch ĐÃ LÀM) · `.plan/issue1_fix.md` (bản AI khác — KHÔNG áp dụng) ·
> `checklist.md` · `next.md` · `working.md` · `LESSONS_LEARNED.md` (nhóm 17 + mục issue1) ·
> `.agents/skills/erpn-verify-first/SKILL.md` · `handoff_20260921-0115.md` (phiên trước).

## 1. Mốc hiện tại

- Máy này **KHÔNG có git** ⇒ issue1 + toàn bộ phases3 (B1→C2) **CHỜ COMMIT** khi repo có.
- **ERPNext THẬT ĐANG BẬT** (`prevail-pantyhose-overvalue.ngrok-free.dev`, HTTP 200, 0.39s) —
  verify read-only được từ máy này (kèm header `bypass-tunnel-reminder: 1`).
- Suite: **Node 449 (447 pass, 2 fail `dsh-gateway` do thiếu runtime dsh — đỏ từ B0)** ·
  **Python 62 OK** · Flutter không đụng phiên này.
- **issue1 fix KỸ THUẬT XONG — CHỜ DUYỆT** (vùng loại trừ an toàn).

## 2. Việc phiên này đã làm

1. **Đúng thứ tự `.plan/issue1_fix1.md`**: mục 6 (audit) → 6b (grep toàn repo · phân biệt
   thiếu-cấu-hình vs không-kết-nối-được · rà secret) → 2 (`pickServerScript`) → 3 (`package.json`)
   → 5 (test + falsify, gồm 5.4 tái hiện bug gốc).
2. **Fix**: cổng `COPILOT_MOCK_OK === "1"` ⇒ thiếu cấu hình **THROW `ERPNEXT_NOT_CONFIGURED`**
   (1 điểm chốt: mọi đường sống đều qua `pickServerScript()`). Giữ mock cho CI/suite.
3. **Khoá chống tái phát**: tripwire tĩnh mới (chỉ 3 file được chạm `MOCK_SERVER`) +
   `test/mock-optin.test.mjs` (E2E tái hiện bug + control) + `scripts/falsify/issue1-mock-optin.mjs`
   (**5/5 RED**, restore byte-identical).
4. **Verify ERPNext THẬT** → phát hiện **BUG MỚI** (xem §3).
5. Docs: `result63.txt` · `working.md` · `checklist.md` · `next.md` · `LESSONS_LEARNED.md`
   (nhóm 17 + mục issue1) · skill `erpn-verify-first` (+2 hàng) · handoff này.

## 3. ✅ BUG THỨ HAI — ĐÃ HỎI USER VÀ ĐÃ SỬA

> Xem `result63.txt` §9. Ban đầu ghi là "chờ quyết định"; user đã chốt **duyệt issue1 + thêm
> validate lúc BOOT cho `http-ask` + fix ngay bug resolver** ⇒ cả hai đã làm xong, test + falsify +
> đo lại trên ERPNext thật.

Trên ERPNext thật: `"Nguyễn Thị Lan"` **không tồn tại**, nhưng có `"Nguyễn Thị B"`.
`resolvePartyFromList()` (pha substring `includes`) lấy fragment `"Nguyễn Thị"` — user gõ dài hơn
(`"Nguyễn Thị Lan"`) — khớp **unique** `"Nguyễn Thị B"` ⇒ `ambiguous:false` ⇒

- **ĐỌC**: `"Nguyễn Thị Lan còn nợ bao nhiêu"` → **trả lời tự tin về SAI khách**, `error_code: none`.
- **GHI**: `"thu tiền cho Nguyễn Thị Lan 500 ngàn"` → **`ENTITY_PICK_REQUIRED`**, không proposal ⇒ **an toàn**.

**Đã sửa — rule 2b** (`resolvePartyFromList`): bỏ hit duy nhất khi **cả 3**: (a) tên ERPNext còn token
phía sau vị trí khớp; (b) token kế tiếp là name-like (không chữ số + không thuộc từ vựng ý định của
router, lấy từ `listRouting()`); (c) token đó không có trong tên row. Bỏ thì **chặn cả vị trí bắt đầu**.
Kèm **validate lúc BOOT cho `http-ask`** (thiếu cấu hình ⇒ exit 1, không bind cổng).

**Bằng chứng**: `entity-name-collision.test.mjs` 10 test · **đo ERPNext THẬT 11 câu trước/sau: chỉ 1
dòng đổi — đúng ca bug** (`Nguyễn Thị B` → `null | AMBIGUOUS_ENTITY`), 10 ca còn lại y nguyên ⇒
zero hồi quy · **falsify rule-2b 5/5 RED** + **mock-optin 6/6 RED** · Node **461 (459 pass, 2 fail
dsh-env)** · Python 62 OK. **3 hồi quy do bản đầu của rule 2b (chỉ full suite bắt được) đã sửa** —
group 18 trong `LESSONS_LEARNED.md`.

**Ghi nhận KHÔNG sửa (§9.3)**: nhiều tên nhiều từ thật vẫn MISSING/AMBIGUOUS (một phần do Phase 1
stripper ăn mất tên) — gap phủ sóng, đề xuất task riêng (Phase 6).

## 4. Việc tiếp theo

1. **Commit issue1** (đã duyệt) — gộp hay tách với hàng chờ phases3 (máy này không có git).
2. **Quyết gap phủ sóng entity resolution** (§9.3): tên nhiều từ thật vẫn MISSING/AMBIGUOUS —
   có thành task/OpenSpec change riêng (Phase 6) không.
3. **KHÔNG QUÊN**: `npm test` giờ chạy `COPILOT_MOCK_OK=1 node --test`. Chạy `node --test` trần
   sẽ khiến các suite mock **treo** (đã đo: exit 124) — dùng `npm test`.
4. **KHÔNG QUÊN**:   harness `scripts/falsify/*.mjs` tự sửa file rồi khôi phục — nếu bị ngắt giữa chừng, kiểm
   `find -name '*.falsify-bak'` + grep chuỗi mutation trước khi tin trạng thái repo (nhóm 17 trong
   `LESSONS_LEARNED.md`).

## 5. Re-verify sau khi user bật ERPNext site (cùng ngày — see result63 §10)

User nghi: các probe "ERPNext thật" có lỡ chạy lúc site CHƯA bật (bật trễ ~10 phút)? Kết luận
3 lớp, READ-ONLY: (1) dữ liệu thật nguyên vẹn khớp bản ghi (122 Customer; `%Lan%`=[];
`Nguyễn Thị B` duy nhất) ⇒ đối chiếu before/after hợp lệ; (2) chạy lại cả 2 probe khi site BẬT —
từng dòng khớp bảng §9.2; (3) host chết (không mock) ⇒ **fail-loud** (`isError:true` +
`TOOL_ERROR … HTTP 0`) ⇒ không có số liệu "thật-giả" nào lọt vào result. Bài học nhỏ (đã ghi
skill): probe đọc `structuredContent` phải kiểm `isError` trước — host chết làm probe in
`error_code: (none)` nhìn như pipeline trả rỗng im lặng.
