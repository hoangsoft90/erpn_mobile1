# Handoff — phiên 2026-09-21 04:46 (P4-1 `ops.daily_summary`, backend skill + test)

**Trạng thái:** KỸ THUẬT XONG · **ĐÃ DUYỆT (user)** — chờ máy có git để commit (vùng liên quan SỐ TIỀN ⇒ cần duyệt người, đã có).
**Bằng chứng đầy đủ:** `result-p4-1.txt` · **Máy này KHÔNG có git** ⇒ chưa commit được gì (như B1→C2, issue1).

---

## 1. Đã làm gì

Spec: `.plan/plan4_final.md` §3 · §4.3 (schema) · §7 (acceptance) · §10 (đã chốt) + `.plan/plan4_review3.md` §1.2 (fixture 20 chứng từ).
Phạm vi giữ đúng prompt: **CHỈ backend** — không Flutter, không route HTTP, không `/execute`, không Redis, không ghi ERPNext.

| File | Việc |
|---|---|
| `mcp-erpnext/capabilities.json` | + capability READ `ops.daily_summary` (`route_group: ops`, no confirm, `triggers: []` ⇒ **đường chat không đổi**) + khối `ops_policy` ghi lại toàn bộ §10 |
| `mcp-erpnext/src/skills/ops-summary.mjs` | MỚI — aggregate server-side 8 block đúng §4.3 (SO tách `submitted`/`draft`; SI/PE chỉ submitted, `grand_total`; chi chỉ PE Pay + footnote JE; két = GL trước 00:00 VN trên `default_cash_account`; Cash\|Bank theo `Account.account_type`) |
| `mcp-erpnext/src/mock-server.mjs` | nhánh fixture `MOCK_ERP_P4_FIXTURE` + hỗ trợ `>` (so sánh **số**) + **paging** như tool thật |
| `mcp-erpnext/src/router.mjs` | wire `ops` (opt-in import — không đụng `routeIntent`) |
| `test/p4-ops-summary.test.mjs` | MỚI — **23 test** (§7 case 1–8, 11–12 + partial + tripwire tĩnh + truncation) |
| `scripts/falsify/p4-ops-summary.mjs` | MỚI — **12 ca**, mỗi ca đỏ đúng chỗ, restore byte-identical |

## 2. §0 — 4 dữ kiện ĐO TRƯỚC khi code (đổi cả thiết kế)

1. Tool thật **đòi filter value là STRING** (số ⇒ `Property /filters/0/2 must be string`); **mock không validate**.
2. `count` = **số dòng TRẢ VỀ** (limit 3 → count 3) ⇒ **không** dùng được để phát hiện cắt trang.
3. `custom_ai_action_id` **chỉ tồn tại trên Payment Entry** (Custom Field meta: 0 row cho SO/SI/QT/PO).
4. `>` với chuỗi chạy OK · `Account`/company = 104 · `GL Entry` đọc được (két làm được thật).

## 3. Vòng review tự bắt 5 lỗi THẬT (OCR không chạy được — không có git)

1. `receivables` filter **số** ⇒ **cả nhánh chết trên site** → `"1"` + **tripwire tĩnh** khoá lớp lỗi.
2. `app_drafts` `catch {}` ⇒ **"0 nháp" giả** (§4.3 cấm) → block `null` + lỗi Frappe nguyên văn.
3. Có thể **cộng thiếu im lặng** khi trang bị cắt → `docList` xin `limit+1`, vượt ⇒ `ERP_TRUNCATED`.
4. `cashDrawer()` trả `cash_in_today/out/expected_closing` = **0 giả** → chỉ trả `{opening, account}`.
5. **Guard không falsifiable = trang trí** (`if (!so.ok…){}` rỗng; guard kiểu filter runtime) → xoá + chuyển thành tripwire tĩnh **strip comment**.

Dọn thêm: import `markUntrusted` thừa · `const today = date` · ternary lồng · `against_invoice` tính bằng trừ · indirection `rowOfCompanyStrict`.

## 4. Bằng chứng (đo trong phiên)

- Suite: Node **484 (482 pass, 2 fail = dsh-gateway, có từ B0)** · P4-1 **23/23** · **falsify 12/12 RED** · Python **62 OK** · Flutter không đụng.
- **Vòng lặp THẬT (read-only, ERPNext thật)**: 21/09 `receivables 551.910.625` **khớp chính xác** cộng độc lập qua REST (295 HĐ) · 20/09 thu 320.000, closing **243.622.620 = opening 21/09** · 19/09 thu 21.127.120 (`cash 19.627.120` / `bank 1.500.000`) chi 250.000, closing **243.302.620 = opening 20/09**.
- `app_drafts` → `null` + 1 lỗi nêu đúng message Frappe (site thiếu Custom Field) ⇒ `partial: true` (hành vi đúng, không bịa 0).

## 5. User ĐÃ DUYỆT 3 việc — và cả 3 ĐÃ XỬ LÝ trong cùng phiên

1. ✅ **ĐÃ SỬA — filter số cùng lớp ở B2/B3** (user: "Fix ngay"): `sales-order-write.mjs` + `quotation-write.mjs` `[["selling","=",1]]` ⇒ **đọc Item Price của CẢ HAI đường ghi chết trên site thật** mà 41 test mock vẫn xanh. Sửa `1`→`"1"`.
   - **Khoá toàn repo**: test mới **`test/filter-literal-types.test.mjs`** quét **mọi `.mjs` trong `src/`** (15 filter literal / 6 file), strip comment, cấm literal số, có **control `>=12`** để scan hỏng không "xanh vì tìm thấy 0". Tripwire **bỏ khỏi file test P4** ⇒ một nhà cho invariant.
   - **Falsify**: ca **K** + **M** trong `scripts/falsify/p4-ops-summary.mjs` (nay harness chạy **cả 2 file test**) ⇒ **13/13 RED**.
   - **Bằng chứng site thật**: `selling=1` → `TOOL_ERROR … must be string` · `selling="1"` → **27 dòng Item Price**. B2+B3 **41/41 pass**.
   - Ghi nhận: `scripts/falsify/` chỉ có c0/c1/c2/issue1/p4 — harness B2/B3 trước đây là **script tạm không nằm trong repo** ⇒ lớp lỗi này trước đó không ai canh.
2. ✅ **ĐÃ ĐO — `add-correlation-field.mjs --dry-run`** (chỉ đọc, user chọn bước này trước): **Payment Entry ĐÃ CÓ** field · **THIẾU: Purchase Order · Quotation · Sales Order** (khớp độc lập với probe `Custom Field` meta). ❓ **Còn chờ user quyết**: có tạo field trên ERPNext không (đây là GHI ⇒ AI không tự làm). Hệ quả nếu chưa tạo: `app_drafts` của P4-1 **phải** null (hiện tượng **tự hết khi có field, không cần sửa code**) và **idempotency/correlation của B2/B3/B4 hiện chỉ đứng với mock**.
3. ✅ **Duyệt P4-1** ⇒ mọi marker "CHỜ DUYỆT COMMIT" của P4-1 đã đổi thành "ĐÃ DUYỆT — chờ máy có git để commit" (`checklist.md`, `next.md`, file này).

**Commit** — máy này không có repo; khi có git thì commit theo lượt (P4-1 tách khỏi issue1).

## 5b. Review vòng 2 (theo yêu cầu "review code những gì vừa làm") — bắt 2 lỗ hổng của CHÍNH guard vừa viết

- **(a)** Dạng **array** `["docstatus","in",[0,1]]` **lọt** tripwire (nhóm value của regex dừng ở `]` đầu tiên) ⇒ vá: value bắt đầu `[` thì tách từng phần tử, nhận cả `+1`.
- **(b)** Ca falsify K/M đỏ nhưng **không chứng minh được tripwire là thứ bắt** (chạy cả suite → có thể đỏ vì lý do khác) ⇒ thêm tuỳ chọn **`only`**: K/M/N chỉ chạy **đúng file tripwire**.
- **Đã kiểm sạch**: tripwire **có được `npm test` chạy** (`grep` thấy `✔ static tripwire…`) — nếu không thì nó là trang trí; `src/` chỉ có `.mjs`; literal số ở `scripts/` chỉ trong **chuỗi mutation** của harness (cố ý); Flutter không tự gọi ERPNext; không import thừa; sau harness: **không mutation sót, không `.falsify-bak`**.
- **Sau khi vá: falsify 14/14 RED** (thêm ca **N**), tripwire+p4 **23/23**.
- **Sau khi sửa cả phát hiện ở §5b**: full suite **485 (483 pass, 2 fail dsh-env từ B0)**; falsify P4 **14/14** + falsify mới `execute-id-types.mjs` **3/3**; `http-execute` **18/18**; Python **62 OK**.

### ✅ Phát hiện MỚI — user duyệt "Sửa ngay (validate ở biên HTTP)" ⇒ ĐÃ SỬA

Chuỗi đã truy vết (không suy đoán): client gửi `proposal.action_id` (`http-ask.mjs:655`, **không kiểm kiểu**) → `safety-gateway.mjs:452` chuyển thẳng, **không đối chiếu** proposal đã lưu → dùng làm **giá trị filter động** (`sales-order-write.mjs:290`, `quotation-write.mjs:223`, `payment-write.mjs:253`).
Nếu client gửi **số** (`action_id: 123`): tool thật từ chối (`Property /filters/0/2 must be string`) ⇒ `correlation_field_unavailable: true` ⇒ `sales-order-write.mjs:448` **từ chối ghi** với mã **`SO_CORRELATION_FIELD_MISSING`** và câu "ERPNext chưa có field … chạy migration" ⇒ **buộc tội sai cho site**.
**Mức độ chính xác**: **fail-closed — KHÔNG có nguy cơ ghi trùng**; nhưng chặn oan lệnh hợp lệ + **chẩn đoán sai** (đi chạy migration không cần). Với payment: đơn có thể đã ghi mà báo "không xác minh được".
**Đã sửa**: helper `correlationIdProblem(body)` trong `src/http-ask.mjs` — `command_id`/`proposal.action_id` nếu có mặt phải là **chuỗi không rỗng**, ngược lại **400 `INVALID_CORRELATION_ID`**; gọi ngay sau parse body và **TRƯỚC rate-limiter** (request sai kiểu không được đốt `command_id`). Dùng lại pattern `isValidCommandId` mà `/execute/cancel` đã có từ trước (`/execute` thiếu = chỗ hở).
- **Test** (`test/http-execute.test.mjs`, +1): số ở `command_id` ⇒ 400 mã đó · số ở `action_id` ⇒ 400 · chuỗi rỗng ⇒ 400 **theo mã** · control (id chuỗi) đi tiếp · `store.status(12345) === null` (không đốt command_id).
- **Falsify**: harness MỚI `scripts/falsify/execute-id-types.mjs` — **3/3 RED** (bỏ check · chỉ kiểm command_id · nới mất nhánh chuỗi rỗng).
- **Kiểm tác động**: Flutter khai `command_id` là `String?` ⇒ client thật không gửi số.
- **Bài học phụ (đã ghi LESSONS nhóm 20 + skill)**: ca falsify **C không đỏ** vì test chỉ assert **status 400** — bỏ guard thì request vẫn 400 từ chỗ khác ⇒ phải assert **MÃ lỗi**.

## 6. Giới hạn phải nói rõ

- **OCR (open-code-review) không chạy được** (checkout không có git ⇒ không có diff) ⇒ đã làm **review mặc định** (đọc code + soát lớp lỗi phổ biến + đo site thật) và chính nó bắt 5 lỗi ở §3.
- `codebase-memory-mcp` (`detect_changes`) và `AgentMemory` **không có trong tool của phiên này** ⇒ chưa ghi được ADR/AgentMemory cho `ops_policy`; `ccc` **chưa init** (`~/.cocoindex_code/global_settings.yml` thiếu) ⇒ dùng `grep`/`rg`. **Không tự cài/khởi tạo** bất cứ thứ gì.

## 7. Việc tiếp theo (chưa bắt đầu)

- **P4-2**: Flutter drawer + drill-down (`ops` route READ có sẵn, UI chưa có) · **P4-3**: nút AI/dsh (nếu plan còn).
- Sau khi site có Custom Field: `app_drafts` sẽ tự hết partial mà **không cần sửa code** (không có nhánh hardcode theo site).
