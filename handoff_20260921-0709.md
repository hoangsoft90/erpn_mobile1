# handoff — 2026-09-21 07:09 · phiên P4-2 (route `/read/daily-summary`)

**Session vừa làm:** P4-2 — `GET|POST /read/daily-summary` (plan4_final §4.2/§4.3/§5/§7), nối tiếp P4-1 (skill `ops.daily_summary`).
**Deliverable:** `result-p4-2.txt` · **Trạng thái:** ✅ KỸ THUẬT XONG — **CHƯA COMMIT** (máy này **không có git**), **CHỜ USER DUYỆT** (vùng đọc SỐ TIỀN ⇒ AI không tự ký duyệt).

## 1. Máy này KHÔNG có git (kiểm lại đầu phiên sau)

`git rev-parse --is-inside-work-tree` → `fatal: not a git repository`. Hệ quả đã gặp ở phiên này:
- **OCR không chạy được** (không có diff) ⇒ mọi review trong phiên là **review mặc định** (đọc code + soát lớp lỗi phổ biến + đo trên site thật). Vẫn **bắt được 2 lỗi thật** — thiếu OCR không phải lý do bỏ review.
- Hàng chờ commit đang dài: **B1 → B2 → B3 → B4 → C0 → C1 → C2 → ISSUE1 → P4-1 → P4-2** (tất cả đã có result + falsify, chưa commit).

## 2. Đã làm trong phiên (ngắn gọn)

| Hạng mục | Nội dung |
|---|---|
| Route | `src/http-ask.mjs`: `GET|POST /read/daily-summary` — **không** qua NLP/classifier, `date` optional (body/query, body thắng), thiếu = **hôm nay tại tiệm** `Asia/Ho_Chi_Minh` |
| Company | **server-side**: pin config → `Global Defaults.default_company`; không có ⇒ 503 `COMPANY_UNRESOLVED`; client gửi company khác ⇒ 403 `COMPANY_SCOPE_MISMATCH` |
| Auth/limit | 401 (non-loopback, y như `/ask`) · `authorize` **trước mọi lần đọc ERP** (403) · bucket **READ** (429) |
| Partial | Object §4.3 trả **nguyên vẹn** (200 + `meta.partial` + block `null` + `errors`) |
| Mock | `erpnext_doc_get` nhánh `Global Defaults` + `MOCK_ERP_DEFAULT_COMPANY` (`=\"\"`) + `MOCK_ERP_FAIL_GLOBAL_DEFAULTS` |
| Test | `test/p42-read-daily-summary.test.mjs` **18** + `test/p42-read-daily-summary-authz.test.mjs` **1** (contract tạm đòi quyền ⇒ chứng minh được 403 **trước** khi đọc ERP) |
| Falsify | `scripts/falsify/p42-read-daily-summary.mjs` **14 ca** (xanh-trước / đỏ-đúng-tên-test / restore byte-identical) |
| Fix dùng chung | `src/client.mjs#close()` idempotent (phát hiện #3) — ảnh hưởng mọi route mở client ⇒ đã chạy lại full suite + vòng lặp thật |

**Số cuối (đo lại sau thay đổi cuối):** Node **505 (503 pass, 2 fail `dsh-gateway` có từ B0)** (+20 so với P4-1) · P4-2 **19/19** + `client.test.mjs` **9/9** · falsify **14/14 RED** · Python **62 OK** · Flutter không đụng.

## 3. Điều đáng nhớ nhất (chi tiết ở `result-p4-2.txt` §2/§5 + `LESSONS_LEARNED.md` nhóm 21–23)

1. **Test xanh vì ENV RÒ**: helper test khôi phục **danh sách cứng** key ⇒ `COPILOT_COMPANY` sót lại từ test trước ⇒ test "không pin company ⇒ 503" trả **200**. Sửa: snapshot **đúng key mình chạm**.
2. **`--test-name-pattern` khớp 0 test vẫn exit 0** (pattern chứa `?` = regex) ⇒ "xanh" giả; harness nay **escape + đếm test chạy ≥ 1 + chạy xanh TRƯỚC khi mutate**.
3. **`pickServerScript()`/`createMcpClient` ngoài `try` trong async handler ⇒ giết cả service** (unhandled rejection ⇒ Node thoát ⇒ request drawer giết luôn `/ask`). Đã đưa vào `try` + test "can never take the service down".
3b. **`client.close()` TREO VĨNH VIỄN khi child đã chết** (đo: `initialize()` reject sau 77ms nhưng `close()` không bao giờ settle) ⇒ mọi route gọi `close()` trong `finally` rò socket+child **mỗi request lỗi** (kể cả `/read/list`, `/ocr/slots`) → `close()` idempotent + test + falsify ca **N** (fix ở file dùng chung ⇒ đã chạy lại full suite + vòng lặp thật).
4. **"Company của session" phải ĐO đường đọc**: `Global Defaults` là **Single** (`doc_list` **500**, `doc_get` **OK**), `User.default_company` **417**; site **3 company** ⇒ không có default thì **TỪ CHỐI**, không lấy row đầu (đó là sổ tenant khác).

## 4. Chờ user quyết (đừng tự làm)

1. **Duyệt commit P4-1 + P4-2** (khi repo có git). Cả hai đều chạm SỐ TIỀN ⇒ AI không tự commit.
2. **Tạo `custom_ai_action_id` trên SO/QT/PO thật?** (hiện chỉ Payment Entry có). Chưa tạo ⇒ `app_drafts` của P4-2/P4-1 phải **null + partial** (route trả đúng như vậy trên site, xem §4 `result-p4-2.txt`), và correlation của B2/B3/B4 chỉ đứng với mock. Tạo field = **GHI vào ERPNext** ⇒ chờ user.
3. **AgentMemory đang DOWN** (`curl http://localhost:3111/agentmemory/health` → `http=000`); MCP Simplenote **không có** trong tool phiên này ⇒ phiên này **không lưu memory/lesson ra ngoài repo** được. Nếu cần: user tự chạy `npx @agentmemory/agentmemory` rồi bảo agent lưu lại (đừng tự khởi động lại service).

## 5. Việc tiếp theo (đề xuất)

- **P4-3** (plan4_final §8): Flutter **drawer shell + màn tóm tắt + stamp `generated_at` + pull-to-refresh** — gọi `GET /read/daily-summary`, hiển thị per-block lỗi khi `partial`, banner khi 401/503/offline, **không** hiển thị % so với hôm qua (chỉ số tuyệt đối / "đang tăng-giảm", §6).
- Sau đó **P4-4** drill structured (tái dùng khuôn list A1; **cấm** `/execute` từ drill) và **P4-5** block nháp app.
- Trước khi code tiếp: đọc `.plan/phases3/README.md` (cấm: auto-submit, bypass Safety, OCR authoritative id) + `checklist.md` (mục P4-2) + `result-p4-2.txt` §6.

## 6. Điểm vào nhanh

```
result-p4-2.txt                      ← toàn bộ bằng chứng + 2 lỗi review + vòng lặp thật
checklist.md   → mục "P4-2"          ← trạng thái từng hạng mục
next.md        → mục "P4-2"          ← roadmap P4-3 → P4-5
working.md     → dòng [2026-09-21] P4-2
LESSONS_LEARNED.md → nhóm 21/22/23 + "## Đợt P4-2"
.agents/skills/erpn-verify-first/SKILL.md ← 4 hàng mới (env rò · pattern 0-test · client ngoài try · đo đường đọc session)
mcp-erpnext/src/http-ask.mjs         ← route (grep "/read/daily-summary")
mcp-erpnext/scripts/falsify/p42-read-daily-summary.mjs ← 14 ca
mcp-erpnext/src/client.mjs   ← close() idempotent (fix dùng chung, có test riêng)
```
